# XGen Protocol — Project Roadmap
> **Status**: ACTIVE  
> Version: 3.27  
> Date: May 2026  
> **Last updated**: 2026-06-10  
> Language: English  
> Author: JozefN  
> Credits: Concept, philosophy, requirements, project direction: Jozef Nižnanský. Technical assistance and implementation support: AI-assisted development tools.  
> License: BSL 1.1 (converts to GPL upon project handover)  

---

## What this document is

The canonical coarse-grained view of where the XGen Protocol project has been, where it is now, and where it is going. One status per track, one line or one short paragraph per item, written so a reader can answer "where are we" without reading the project's full history.

**This document complements, does not replace.** Detailed progress lives in `JOURNAL.md` (contemporaneous record), settled architectural calls live in `DECISIONS.md` (numbered decisions), session-state operational guidance lives in `CLAUDE.md` (what Claude Code should read on the next session), specifications live in `docs/xgen_ch*.md` and `docs/xgen_appendix_*.md`. ROADMAP.md sits above all of these as the navigation map between them.

**This document mirrors reality, not aspiration.** When a milestone is descoped (M6 multiparty → the M9 multiparty track), it moves rather than disappears. When new work surfaces (Propagation Reliability Audit opened mid-project, Federation Event Propagation milestone added after the audit surfaced a gap), it lands here the moment it's recognised. The roadmap is not a plan-from-the-start that the project is executing; it is a living record of what the project now knows it needs.

**Detail is revealed empirically.** Future tracks are sketched briefly; their detail accumulates as they approach. The roadmap does not pretend to know what M9 will involve at the level of M5's specification when M5 was active. This is honest pacing: the unknown stays unknown until it has to be known.

---

## Status legend

| Symbol | State | Meaning |
|---|---|---|
| 🟢 | PLAY | Active work right now. The project is playing this track. |
| ✅ | DONE | Completed and shipped. Settled history. |
| 🟡 | PENDING | Designed or scoped, ready to start, no blockers. |
| ⏸️ | POSTPONED | Paused with a known resume condition. Will resume when condition is met. |
| ❌ | CANCELLED | Stopped. Will not ship. Different from deprecated — no replacement, just won't happen. |
| ⬛ | DEPRECATED | Superseded by named replacement. Named replacement always cited. |
| ⚫ | CLOSED | A finished arc or milestone folded into the historical record — settled and not superseded. Close in meaning to ✅ DONE; distinct from ⬛ DEPRECATED, which cites a named replacement. |

## Update discipline — mandatory

**This document MUST be updated whenever a milestone or phase reaches a state change** — without exception, without deferral, without "I'll do it next session." State changes that trigger an update include:

- A track moves from 🟡 PENDING to 🟢 PLAY (work starts)
- A track moves from 🟢 PLAY to ✅ DONE (work ships)
- A track moves from 🟢 PLAY to ⏸️ POSTPONED (work pauses with known resume condition)
- A track is added to the roadmap (newly recognised work surfaces)
- A track moves to ⬛ DEPRECATED with a named replacement
- A track moves to ❌ CANCELLED
- A phase within a multi-phase milestone closes (e.g. M6 Phase 0 → M6 Phase 1)
- An audit closes
- A design conversation locks decisions that change downstream scope
- A canonical document is created or supersedes another

The update happens **in the same commit** as the work that produced the state change — not in a follow-on commit, not in a separate housekeeping pass. If a milestone closes and ROADMAP.md still shows it as PLAY, the milestone has not actually closed. This is the same discipline that applies to CLAUDE.md and per-file `Status:` headers: the canonical record must reflect reality at every moment.

**This document and `CLAUDE.md` are updated together.** Two anchor points for the same reality. If one moves, the other moves. The same commit touches both. Drift between them is a discipline failure that must be corrected immediately.

Detail-level varies by state: settled work gets one line referencing JOURNAL/tag; active work gets a paragraph; near future gets a paragraph if soon, one line if not yet; far future stays brief. Detail accumulates as a track approaches and is reduced when the track settles (active paragraph → done one-liner).

---

## Visual structure — nested view

A structural at-a-glance of the project's milestone hierarchy. Past entries are collapsed to milestone-level depth; Present, Near future, parallel workstreams, open areas, the discipline cluster, and cross-cutting principles are shown at full nesting. The tree is a navigation aid — the prose sections below remain canonical for detail. If the tree and prose ever disagree, prose wins; the disagreement is itself a discipline failure that the next state-change commit must reconcile.

**Same-commit discipline applies to the tree, no exceptions.** When updating ROADMAP.md for any state change, the tree above MUST be updated in the same edit, even if the prose change feels small. The tree's value is at-a-glance correctness; if it's stale, it actively misleads rather than just being unhelpful. Drift between tree and prose is treated as the same shape of discipline failure as drift between ROADMAP.md and CLAUDE.md.

```
XGen Protocol
│
├── Federation Event Propagation milestone (✅ DONE 2026-05-25 at J-119; workspace 627 tests at close)
│   ├── ✅ Pass 2 design (10 F-items locked)
│   ├── ✅ Pass 3 design (canonical doc + runbook shipped)
│   ├── ✅ Phase 1 implementation (F-6 + F-7 wire shape)
│   ├── ✅ Phase 2 implementation (F-4 validation unification)
│   ├── ✅ Phase 3 implementation (F-1a tip exchange)
│   ├── ✅ Phase 4 implementation (F-1 federation push)
│   ├── ✅ Phase 5 implementation (F-1c per-peer record + reconnect)
│   ├── ✅ Phase 6 implementation (F-10 unknown-signer Identity)
│   ├── ✅ Phase 7 implementation (F-3 federation-relationship gate)
│   │   └── ✅ Phase 7 B3 amendment (predecessor-chain + step-11 closure)
│   ├── ✅ Phase 7.5 milestone (cold-start bootstrap)
│   │   ├── ✅ Phase 7.5 design (4 framework decisions)
│   │   └── ✅ Phase 7.5 implementation (5 commits, JOURNAL gap flagged)
│   ├── ✅ Phase 9 implementation (deployment integration tests; CLOSED 2026-05-25 at J-119 via the 3b arc J-110/J-111/J-112/J-118)
│   │   ├── ✅ Phase 9 survey (14 failure-mode catalogue + 12 scenarios)
│   │   ├── ✅ Phase 9 Commit 1 (G1 observability)
│   │   ├── ✅ Phase 9 Commit 2 (flake fixes)
│   │   ├── ✅ Phase 9 Commit 3a (harness + Scenario 1 regression witness with #[ignore])
│   │   ├── ✅ Phase 9 Commit 3b-2-equivalent (J-110) — Scenarios 5 + 6 + Phase 7.5 §5 extended skip-set + room_create narrowness regression (4 tests across 2 new modules; workspace 592 → 596)
│   │   ├── ✅ Phase 9 Commit 3b-3-pre (J-111) — harness extension (push-attempt counter + tracing-Layer composition + install_harness_subscriber per-test installer) + G2 trace-event retrofit (local_node_id field on apply_federation_push + 4 G2 federation-push trace events; workspace 596 → 599)
│   │   ├── ✅ Phase 9 Commit 3b-3 (J-112) — Compound C2 (F-5 anti-transitivity under push queue depth) test against extended harness (workspace 599 → 600)
│   │   ├── ✅ Phase 9 Commit 3b-4 runbook authoring (J-114) — runbook shipped at tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md Status: ACTIVE v1.0 (~57.5 KB, eight sections); D-078 promoted in same atom (Path B.i — "Production-grounded test enumeration" as protocol-test-layer sibling to D-077 meta-layer); three-instance threshold met (J-099 + J-109 + J-113)
│   │   ├── ✅ Phase 9 Commit 3b-4 (J-118) — NodeRuntime-level Scenario 4 (20 forgery tests under amended contract per J-113) + C5 + C7 + C9 + C10 (workspace 600 → 627, +27)
│   │   └── ✅ Phase 9 milestone close (J-119) — Federation Event Propagation umbrella milestone PLAY → DONE; M6 (new) + XGID Retrofit Pass 1 unblock simultaneously; per-phase shipped state in `docs/xgen_federation_propagation_design.md` §15 Implementation Complete
│   ├── ✅ Persistence-amendment sub-amendment milestone (sibling to bidirectional + topological-sort + Phase 7.5; opened 2026-05-23 per J-104; CLOSED 2026-05-24 per J-108)
│   │   ├── ✅ Audit phase (canonical doc shipped 2026-05-23 at tasks/PHASE_7_5_PERSISTENCE_AMENDMENT.md v1.0; COMPLETED v1.1 at design-close J-105; body §1–§11 stays authoritative as historical record of audit-at-lock-time per topo-sort + bidirectional precedents)
│   │   ├── ✅ Design phase (four Joe-locks at tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md v1.0 — Q1 (a).ii + (a).iii.β + candidate D-NNN flag; Q2 (a) return-vector; Q3 all-three drain helpers; Q4 (a) sentinel-tree in-scope; eleven sections sibling-in-shape to FEDERATION_TOPOSORT_DESIGN.md; **amended v1.1 → v1.2 at J-107 re-walk** with §3 Y-lock subsection recording (a).iii.β → (a).iii.α revert + bidirectional sustainability frame + §8 expanded scope of candidate D-NNN to cover five `ingest_event` silents + three drain helpers + M6 reject paths + B3 apply_event dependency)
│   │   ├── ✅ Implementation runbook authoring (shipped 2026-05-23 at tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md v1.0; ~95 KB, eight sections; sibling-in-shape to FEDERATION_TOPOSORT_IMPL.md v1.2; six runbook-structural Joe-locks at §1.1 + §2.3; two pre-draft code-trace findings shaped §4 + §4a narrow scope; J-106; **amended v1.0 → v1.1 at J-107 re-walk** with §4 amendments .1-.10 reflecting (a).iii.α revert + §4.9 package-scoped-verification correction text + new §7.8 discipline-notes sub-section naming D-077 origin)
│   │   ├── ✅ Track 1 re-walk canonical-record amendments (Y-lock revert (a).iii.β → (a).iii.α; D-077 promoted to DECISIONS.md as new principle bidirectional sustainability discipline at silent-discard / fallible-discard sites; **eight-file atomic commit per D-074 tenth instance** — expanded from seven when bridge-handoff folded in per anti-tempfile-deletion discipline; J-107; sibling-shape to topo-sort design-phase re-walk J-099/J-100 but directional-asymmetry-distinct — record-of-already-locked-decision vs decision-input)
│   │   ├── ✅ Implementation (✅ Commit 1 doc-pass `0ca29e6`; ✅ Commit 2 Q1 (a).iii.α binary-void signature + `tracing::error!` + (a).ii sort-on-replay defensive layer + verbatim code-comment block at runtime.rs:181 `f4f0e4e`; ✅ Commit 2a Q2+Q3 return-vector + `additional_persisted` aggregation through dispatch_event → process_inbound persist-loops + 5 new unit tests `c88fd73`; ✅ Commit 3 sentinel-tree refinement + verify 8/8 GREEN `a677244` — three within-Commit-3 audit gaps closed atomically per D-077 first worked instance: abort-fold + identity-registry-persist + space-event-store-persist; federation-registry-persist audited and confirmed safe via downstream production path)
│   │   └── ✅ Milestone close (Commit 4 at J-108 — twelve-file atomic per D-074 eleventh instance; collapses Phase 9 Commit 3b-1 into milestone close per Q4(a) lock; sentinel-tree four files crowned as activating integration regression locks; Scenario 3 transition FAIL → PASS verified at integration level; J-NNN freezes at runtime.rs:181 + design doc §15 + survey-findings M16 + design + impl runbook body markers; runbook v1.1 → v1.2 COMPLETED; ROADMAP v1.22 → v1.23; CLAUDE.md PLAY flip to Phase 9 Commit 3b-2-equivalent RESUMES; grep guardrail scope discipline codified at JOURNAL J-108 sub-section 8 as fifth project instance of surfacing-gap-becomes-codified-discipline pattern)
│   ├── ✅ Bidirectional federation_nodes phase (sibling to Phase 7.5; closed 2026-05-21 per J-096)
│   │   ├── ✅ Audit phase (canonical doc shipped 2026-05-21 at tasks/FEDERATION_BIDIRECTIONAL_NODES_AUDIT.md v1.0)
│   │   ├── ✅ Design phase (Q1 Reading (i) + Shape A + A.1 locked 2026-05-21; design task file at tasks/FEDERATION_BIDIRECTIONAL_NODES_DESIGN.md v1.0; D-075 promoted)
│   │   └── ✅ Implementation (4 commits: e975162 + a730eda + cbceb41 + f051039 + Commit 4 close; runbook at tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md v1.1 COMPLETED; 571 → 577 tests, +6 unit; J-096)
│   └── ✅ Topological-sort wire-order non-determinism phase (sibling to bidirectional federation_nodes; opened 2026-05-21 per J-096; CLOSED 2026-05-23 per J-101)
│       ├── ✅ Audit phase (canonical doc shipped 2026-05-22 at tasks/FEDERATION_TOPOSORT_AUDIT.md v1.0; amended v1.1 at Step 2 of design-phase re-walk — §11 added recording framing gap)
│       ├── ✅ Design phase (three locks at tasks/FEDERATION_TOPOSORT_DESIGN.md v1.0 — Q3.ii + Q2 middle + Q2.γ + Q1 Shape A v1; amended v1.1 at Step 2 of design-phase re-walk — §11 added recording Q4 causal-DAG-respecting order + Q1 supplement Path B; D-076 promoted to DECISIONS.md then amended in place at Step 2)
│       ├── ✅ Implementation runbook authoring (shipped 2026-05-22 at tasks/FEDERATION_TOPOSORT_IMPL.md v1.0; revised v1.0 → v1.1 at Step 3 J-100; COMPLETED v1.2 at milestone close J-101)
│       ├── ✅ Implementation — five-commit Clair-facing sequence shipped per revised runbook v1.1 (J-101 milestone close)
│       │   ├── ✅ Step 1 (J-098-session-close Joe-lock conversation) — Path B locked at event-construction layer; Path A + Path C rejected; D-076 amends in place
│       │   ├── ✅ Step 2 (canonical-record amendments + Rule 0 addition + JOURNAL J-099 — eight-file atomic commit per D-074)
│       │   ├── ✅ Step 3 (runbook v1.0 → v1.1 revision — shipped at J-100, five-file atomic commit per D-074; "Commit 2a" naming Joe-locked at Step 3 open)
│       │   ├── ✅ Commit 1 doc-pass (parents of 0543a86)
│       │   ├── ✅ Commit 2 determinism layer (0543a86) — Shape A v1 sort fix at fanout.rs:193 + sibling Site 1 sort + 4 unit tests including wire-order witness
│       │   ├── ✅ Commit 2a causality layer (4a6fd74) — Path B fix at build_room_create_event + validator companions unified per D-067 Option E across is_dag_root_type and validate_dag_structure + 17-site dag-test fixtures under Posture β; two layered B3-shape surfaces closed atomically
│       │   ├── ✅ Commit 3 Phase 9 Scenario 1 second #[ignore] lift (b370dc7) — doc-comment rewrite to 5-event chronology + 3-decision regression-lock framing; 8/8 green verification runs
│       │   └── ✅ Commit 4 milestone close per D-074 (J-101) — 8 files atomic; M15 catalogue row added; four J-NNN freezes (line 1140 retroactive J-096; line 1141 + M15 + phase9 doc-comment → J-101)
│       └── ✅ Phase 9 Commit 3b arc completed under the closed Federation Event Propagation milestone (3b-2-equivalent J-110 + 3b-3-pre J-111 + 3b-3 J-112 + 3b-4 J-118 + milestone close J-119; covered above under the Phase 9 implementation row)
│
├── XGID Adoption v1 milestone (✅ DONE 2026-05-20)
│   ├── ✅ Design walkthrough (Q1–Q6 locked, 2 sessions)
│   ├── ✅ Phase 1 canonical sources (8-artefact atomic commit, a5f3c8b)
│   ├── ✅ Phase 2 doc-tree sweep (classification table, 70e3e5a)
│   │   ├── ✅ Pre-walk Scope-A-vs-Scope-B Joe-lock (Scope B)
│   │   ├── ✅ Pre-walk SK appendix housekeeping (2 files → DEPRECATED)
│   │   └── ✅ 23-doc classification walk (6 groups A–F)
│   └── ✅ Implementation (Clair's 2 production commits + hygiene + close, J-095)
│       ├── ✅ Commit 1 (c95584a) — xgen-common XGID types + 5 invariance tests
│       ├── ✅ Commit 2 (24a255b) — SpaceLocalMetadata.introducer_node_id retype
│       ├── ✅ Hygiene commit (904441b) — workspace clippy under Rust 1.95.0 (NOT XGID code)
│       └── ✅ Milestone-close commit — JOURNAL J-095 + Ch4 pointer + cross-doc flips
│
├── ✅ XGID Retrofit Pass series — all 5 passes CLOSED (Pass 1 J-122 → Pass 5 J-148)
│   ├── ✅ Pass 1 — core data structures (CLOSED 2026-05-26 at J-122; seven atomic commits 403ef3f + 8a94dee + 75e81b4 + 774fe9d + 4895446 + 096162e + this Commit 6; plus J-121 hygiene atom 1dd909e; test count 489 at close per Path A workspace-broken intermediate; +5 invariance tests A–E)
│   │   ├── ✅ Commit 1 (403ef3f) — canonical-form module move (xgen-core → xgen-common; re-export shim preserved)
│   │   ├── ✅ Commit 2 (8a94dee) — convenience constructors on flavour wrappers (`from_event` / `from_space_create` / `from_room_create`; `from_assertion` deferred to Pass 2 with code-comment flag)
│   │   ├── ✅ Commit 3 (75e81b4) — xgen-common data-structure retypes + 5 invariance tests A–E
│   │   ├── ✅ Commit 4 (774fe9d) — xgen-core data-structure retypes (lib-clean per Path A Joe-lock; Borrow<str> additive API added at implementation kickoff over per-site explicit-wrap-with-comment churn)
│   │   ├── ✅ Commit 4a (4895446) — xgen-core test-fixture projection sweep (split from Commit 4 mid-implementation per Joe-lock when ~296 fixture errors surfaced)
│   │   ├── ✅ Commit 5 (096162e) — Appendix C + Appendix I retypes
│   │   ├── ✅ J-121 hygiene atom (1dd909e) — gate NodeXgid import to tests in exchange.rs (lib-level unused-import warning surfaced at Commit 6 verification gate; sibling-shape to J-095 hygiene 904441b)
│   │   └── ✅ Commit 6 milestone close (J-122) — runbook v2.0 → v2.1 COMPLETED + CLAUDE.md PLAY flip + this ROADMAP v1.32 → v1.33 + JOURNAL J-122 nine-section entry + HANDOFF flipped COMPLETED v1.1
│   ├── ✅ Pass 2 — xgen-core (code-only, zero doc work) — CLOSED 2026-05-27 at J-126; three-commit Clair-facing sequence (5892e9e + 22765a0 + 58b94a5) + this Commit 3 milestone-close commit; test count 491 at close per Path A workspace-broken intermediate (+2 vs J-122 baseline of 489); zero recurrences (first project milestone to ship with zero recurrences since the framework was named)
│   │   ├── ✅ Design phase (J-123; single principle locked at tasks/XGID_RETROFIT_PASS_2_DESIGN.md v1.0 — identifier slots retype, descriptive-string slots stay String, project at call-site via Borrow<str>; two architectural decisions Q2.8 partial HashMap retyping + Q5 validate_steps_8_13 deprecate-and-schedule per D-071 own-arc; ~31 KB lighter than trilogy per §1.2 precedent-departure self-defense)
│   │   ├── ✅ Implementation runbook (J-124; `tasks/XGID_RETROFIT_PASS_2_IMPL.md` ACTIVE v1.0 ~43 KB eight sections + §4a contingent; three-commit base + contingent Commit 2a shape; three Joe-lock checkpoints; runbook lighter than Pass 1 per Pass-internal-consistency framing)
│   │   ├── ✅ Commit 1 doc-pass (`5892e9e` at J-125; design Status ACTIVE → COMPLETED v1.0 → v1.1 + new §6.7 Shape α entry; ROADMAP v1.34 → v1.35; CLAUDE PLAY flip; JOURNAL chain entry; runbook header chain; pre-Clair audit clean across six dimensions; checkpoint #1 fires post-commit)
│   │   ├── ✅ Commit 2 (`22765a0`) — xgen-core algorithm-bearing retypes all five surfaces atomic (lib-clean per Path A; validate_event + ValidationOutcome at exchange.rs; NodeRuntime::dispatch_event + DispatchOutcome at runtime.rs; PendingBuffer arrival hooks at pending.rs; FederationRegistry + IdentityRegistry method APIs at registry.rs; accept_message; plus per-surface unit tests at runbook §4.7; deprecation attributes applied to validate_steps_8_13 + accept_event per design doc §4.2 Q5.b)
│   │   ├── ✅ Commit 2a (`58b94a5`) — xgen-core test-fixture projection sweep (Joe-lock checkpoint #3 split-trigger fired at 93 errors > ~50 threshold; nine xgen-core test modules updated across dag/pending.rs + node/runtime.rs + node/tests/phase9_* + identity/replication.rs + federation/{mod,registry}.rs + message/exchange.rs `#[allow(deprecated)]` test-mod attribute; sibling-shape to Pass 1 Commit 4a precedent)
│   │   ├── ✅ Commit 3 milestone close (J-126) — this commit; runbook v1.0 → v1.1 COMPLETED + design doc §6.7 J-NNN-placeholder freeze + CLAUDE PLAY flip + ROADMAP v1.35 → v1.36 + JOURNAL J-126 body entry
│   │   ├── ✅ (code) validate_event, ValidationOutcome
│   │   ├── ✅ (code) NodeRuntime::dispatch_event, DispatchOutcome
│   │   ├── ✅ (code) PendingBuffer arrival hooks
│   │   ├── ✅ (code) FederationRegistry / IdentityRegistry APIs
│   │   └── ✅ (code) accept_message signature
│   ├── ✅ Pass 3 design (J-127) — xgen-node + Appendix D scope; full seven-surface walk closed across two same-day sessions at v1.2; single governing principle inherited from Pass 2 unchanged; six architectural decisions locked at §4.1-§4.6 with §4.3 format-boundary preservation consolidated (wire OR persistence) at v1.2 sibling-shape to D-076 v1 → v1.1; three candidate D-NNNs (γ+δ+ε) flagged-not-promoted; layered-B3 confirmed null at full seven-surface scope (third Pass-arc instance, pattern durable)
│   ├── ✅ Pass 3 implementation runbook (J-128 v1.0; amended v1.0 → v1.1 at J-129 Track 1 canonical-record amendment — surfaces #1↔#2 + #5↔#6 ordering swapped at v1.0; `handle_federation_incoming` mis-located to federation_session.rs (production at xgen-node/src/app.rs:976); runbook now matches design doc §2 verbatim)
│   ├── ✅ Pass 3 J-130 drift-fix atom (silent gitignore-skip closure — J-129 atomic claim was five-file but git received four because `tasks/HANDOFF_TOPOSORT_RUNBOOK_AUTHORING.md` was explicitly gitignored at .gitignore:58; candidate D-NNN-η "claimed-atomic-file-count vs git-actually-shipped-file-count at canonical-record amendment commits" flagged-not-promoted per D-069; four-file atomic per D-074 (twenty-seventh instance); Pass 3 "Honest longer work" count increments to TWO — Sub-shape D gitignored-path silent-skip slip)
│   ├── ✅ Pass 3 Commit 1 doc-pass (J-131; this commit) — three-file atomic per D-074 (twenty-eighth instance) per Lock #3 per-commit cadence — but honest count is two files because post-J-129 strip-the-chain discipline + sibling-shape to J-123/J-124/J-125 chain-only doc-only milestone-event precedent means JOURNAL.md gets no edit (no body, no chain to update post-strip); ROADMAP v1.39 → v1.40 + visual tree Commit 1 row + Past entry + Present flip; CLAUDE PLAY flip "Commit 1 against amended v1.1" → "Commit 2 (Commit 1 doc-pass ✅)"; design doc + runbook untouched per Option C hybrid minimal (design doc COMPLETED v1.2 at J-127; runbook ACTIVE v1.1 at J-129); Joe-lock checkpoint #1 fires post-ship for three drift-detection points (ROADMAP version bump + visual tree row ✅; CLAUDE PLAY flip ✅; honest two-file-vs-three-file count discrepancy surfaced for Joe at checkpoint #1)
│   ├── ✅ Pass 3 J-132 Path-(iii) amend-in-place at checkpoint #1 resolution (runbook §3.2 third-file line rewritten "JOURNAL.md chain entry only" → "JOURNAL.md NOT amended post-strip"; §3.1 file-count corrected three → two; new §9.4 amendment-provenance; single-file atom per D-074 twenty-ninth instance; runbook v1.1 → v1.2)
│   ├── ✅ Pass 3 J-133 design doc §2 v1.2 → v1.3 Track 1 amendment at checkpoint #2 (two parameter-attribution drifts caught by Clair's D-078 verification: §2.3 Q3.6 non-existent parameter on `apply_federation_push`; §2.5 Q5.14 `OutboundMsg` mis-attribution — production at app.rs:1165 is `run_federation_session_post_handshake` parameter not OutboundMsg field; §2.5 sub-region enumeration omission of `run_federation_session_post_handshake` closed; candidate D-NNN-κ opens flagged-not-promoted at two instances; two-file atomic per D-074 thirtieth instance)
│   ├── ✅ Pass 3 J-134 design doc §2 v1.3 → v1.4 in-place rewrite-correction of J-133's own Q3.6 v1.3 + D-NNN-κ promoted to D-079 (Clair's J-134 atom-prep D-078 grep found `SpaceState.federation_nodes` is already `Vec<NodeXgid>` retyped at Pass 1 Commit 4 `774fe9d`; the J-133 Q3.6 v1.3 rewrite carried a wrong claim production doesn't honor; three-instance threshold met across two distinct catch-events — promotion to **D-079** "Design-doc Q-table grounded by symbol-definition grep"; canonical cautionary instance recorded — fix-author re-instantiates the discipline-failure being fixed; three-file atomic per D-074 thirty-first instance; "Honest longer work" Pass 3 count increments to TWO — recurrence shape)
│   ├── ✅ Pass 3 J-135 runbook v1.2 → v1.3 add T11 by name (D-078 application at test-enumeration layer; T11 `run_federation_session_post_handshake_spawned_task_owns_typed_captures` pins async-spawned forced-owned NodeXgid + Vec<SpaceXgid> capture-shape at app.rs:1152; Joe-lock checkpoint #2 follow-on after the J-133 Q5.14 v1.3 rewrite added the function to §2.5 sub-region 2; single-file atom per D-074 thirty-second instance)
│   ├── ✅ Pass 3 Commit 2 SHIPPED (J-136 `67fb48d`) — seven-surface retype atomic per D-074 thirty-third instance under Path 2 (Commit 2a split) per Joe-lock checkpoint #3; xgen-core/src/node/runtime.rs Surface #1 + #2 (six per-space HashMap keys → SpaceXgid + dispatch_event peer_node_id → Option<&NodeXgid>); xgen-node/src/federation_session.rs Surface #3 + J-134 Finding B annotation drop closing D-079; xgen-node/src/fanout.rs Surface #4 (ClientSenders IdentityXgid + FederationPeerSenders NodeXgid + FanoutRequest.new_joiner + event_space_id Option<SpaceXgid> + apply_fanout + collect_sync_history + compute_federation_delta + topological_sort_events HashSet<EventXgid>); xgen-node/src/app.rs Surface #5 (12 in-memory identifier slots + handle_federation_incoming T8 forced-owned + run_federation_session_post_handshake T11 per Q5.14 v1.3 13-param matrix + ConnectedClientInfo IdentityXgid + 4 persistence-format String per §4.3 + Q3-overload projection); xgen-node/src/reconnect.rs Surface #6 (three spawned functions forced-owned + AttemptCursor HashMap<NodeXgid, u32>); docs/xgen_appendix_d_en.md Surface #7 (four markdown table classification rows annotated typed-XGID-in-memory + String on-disk/wire per §4.3); xgen-{common,core,node} libs CLEAN; clippy --lib -D warnings CLEAN; cargo build --workspace deliberately broken at xgen-client only per Path A; **Joe-lock checkpoint #3: 638 test-fixture errors (xgen-core 160 + xgen-node 478) >> ~50 threshold → Path 2 (Commit 2a split) locked**; ten-file atomic; two discipline data points surfaced for JOURNAL body (Pass-arc pre-walk Pass 3 marker sparsity; Surface #4 "verification only" vs actual-lift discrepancy); "Honest longer work" count does NOT increment (within-milestone substantive event, stays at TWO)
│   ├── ✅ Pass 3 Commit 2a SHIPPED (J-137 `0cdf0ad`) — test-fixture sweep + 11 per-surface tests T1-T11 atomic per D-074 thirty-fourth instance; 638 test-fixture errors closed (xgen-core 160 + xgen-node 478) via parallel-subagent delegation under per-crate guard-rails (xgen-core 4 files / 0 deviations; xgen-node ~20 files / 6 minor deviations all honest-reported per Rule 1); T1-T11 added per §4.7 by name at runtime.rs (T1-T4) + federation_session.rs (T5) + fanout.rs (T6) + app.rs (T7+T8+T11) + reconnect.rs (T9+T10); 8/8 GREEN runs verification rigour per §5.3 + §4.9 (5 isolated + 3 workspace consecutive); test count stable at 589 = 34 xgen-common lib + 8 invariance + 453 xgen-core + 88 xgen-node lib + 6 precedence (+11 vs pre-sweep 578); both clippy gates clean (`--lib` + `--tests` -D warnings); `cargo build --workspace` deliberately broken at xgen-client only per Path A; pre-existing flakes did NOT fire; thirty-file atomic; runbook v1.4 → v1.5 + new §9.7 amendment-provenance recording parallel-subagent-sweep discipline data point; "Honest longer work" Pass 3 count stays at TWO inherited from J-129 + J-134
│   └── ✅ Pass 3 milestone CLOSED (J-138; this commit) — runbook ACTIVE → COMPLETED v1.5 → v1.6 + DoD checklist verified; design doc §6.1 J-NNN placeholder frozen retroactively to J-138 per J-108 codification + Pass 2 §6.7 freeze pattern + header chain entry; CLAUDE PLAY flip "Commit 2a ✅" → "Pass 3 milestone CLOSED at J-138; standby for next-milestone selection (Pass 4 + M6 (new) both ready)"; ROADMAP v1.42 → v1.43 + visual tree row 🟢 → ✅ + Past entry + Near future Pass 3 line removed; final test count 589 (+98 vs Pass 2 J-126 baseline of 491); 8/8 GREEN at J-137 milestone-bearing boundary + single workspace re-verification pass at this milestone-close boundary; layered-B3 audit answer: zero (third Pass-arc no-finding instance — three-instance chain durable); Pass 3 "Honest longer work" final count: TWO recurrences (J-129 + J-134, both prospective catches at canonical-record-amendment layer); four candidate D-NNNs (γ + δ + ε + format-boundary) status recorded at JOURNAL J-138 Sub-section 8 promotion-watch (none promoted per D-069); five-file atomic per D-074 (thirty-fifth instance + fourteenth milestone-close)
│   │   ├── ✅ (code) federation_session.rs handler identifier slots — Surface #3 at Commit 2
│   │   ├── ✅ (code) fanout.rs Client/FederationPeer Senders + FanoutRequest + event_space_id + apply_fanout + collect_sync_history + compute_federation_delta + topological_sort_events — Surface #4 at Commit 2
│   │   ├── ✅ (code) app.rs handler identifier slots + handle_federation_incoming (T8) + run_federation_session_post_handshake (T11) + ConnectedClientInfo — Surface #5 at Commit 2
│   │   ├── ✅ (code) reconnect.rs three spawned functions + AttemptCursor — Surface #6 at Commit 2
│   │   ├── ✅ (code) NodeRuntime six per-space HashMap keys + dispatch_event peer_node_id — Surfaces #1 + #2 at Commit 2
│   │   ├── ✅ (doc) Appendix D four markdown table classification rows annotated — Surface #7 at Commit 2
│   │   └── ✅ Per-surface tests T1-T11 + test-fixture sweep (638 errors) — Commit 2a (J-137)
│   ├── ✅ Pass 4 milestone CLOSED (J-146; this commit) — Commit 1 retype atomic (J-145 `3869d4c`, 8/8 GREEN, 637 tests) + Commit 2 close; layered-B3 null (four-instance chain); `cargo build --workspace` RESTORED at Pass 4 (Path A closed ahead of §2.9 Pass-5 expectation); "honest longer work" final FOUR; Pass 5 scope reduced 4→2 items; design phase had closed at J-140 (`tasks/XGID_RETROFIT_PASS_4_DESIGN.md` Status ACTIVE → COMPLETED v1.0 → v1.2 single-session full close — two-session split eligible per Pass 3 J-127 Sub-section 8 data point (c) but not exercised per "let us move ahead" mid-session pivot from Option II pause to Option I continue; honest framing recorded at §6.2 + §7.5; §3 governing principle locked inherited unchanged from Pass 2 + Pass 3 — four-instance Pass-arc inheritance; all five §4 anchors locked: §4.1 Surface #1 M5 Ops Layer composite (31 identifier retypes + 12 descriptive stays + 3 borderline locks for `home_node`/`node` NodeXgid + `source` String stay + Pass 1 additive-API extension Option β + serde-transparent wire-neutrality + honest recon corrections per Rule 5); §4.2 format-boundary preservation Option γ split (D-NNN-format-boundary promotion-watch STAYS OPEN at 3 structurally-distinct instances across 2 Pass-arc); §4.3 CLI arg parsing Option α (clap parse stays String at 16 identifier-shaped Args slots); §4.4 doc-vs-code Option γ hybrid split (runbook commit-sequence pre-framed at 8-9 commits); §4.5 D-NNN-ε promotion-watch CLOSED by honest framing per D-065 + D-079; §5 layered-B3 null at full eight-surface scope — four-instance Pass-arc no-finding chain established; §6 historical/future-pointer Shape α pointer-style fill + §6.2 two-session walk shape eligible-but-not-exercised at Pass 4 (bimodal Pass-arc precedent); §7 discipline notes five sub-sections; §7.6 candidate skipped per minimal-broadening discipline; four-file atomic per D-074 thirty-seventh instance)
│   │   ├── ✅ (design) full close at J-140 (v1.0 → v1.2; §3 + §4.1 + §4.2 + §4.3 + §4.4 + §4.5 + §5 + §6 + §7 all locked)
│   │   ├── ✅ (runbook) `tasks/XGID_RETROFIT_PASS_4_IMPL.md` authored at J-141 (Status ACTIVE v1.0; 8-9 commits Option B Joe-locked-by-recommendation; three Joe-lock checkpoints; +15 per-surface tests T1-T15 enumerated)
│   │   ├── ✅ (code) Commit 1 SHIPPED (J-145 `3869d4c`) — ONE consolidated xgen-client retype atomic (Pass-3 shape, RE-LOCKED at J-143): all seven surfaces + xgen-common §4.1.b additive-API + Surface #8 doc fragments + T1–T15; 8/8 GREEN (637 tests); checkpoints #2+#3 closed; Commit 1a did NOT fire (0 fixture errors). Surface #1 ops.rs = 49 String slots per §4.1.a [grep-corrected from 46 at J-142].
│   │   ├── ✅ (code) Surface #2 CLI Dispatcher (app.rs — clap Args stay String, project at dispatch arm; in Commit 1)
│   │   ├── ✅ (code) Surface #3 Batch Pipe Dispatch (batch.rs — get_dag_tips stays &str; in Commit 1)
│   │   ├── ✅ (code) Surface #4 Tauri Shell (desktop.rs — transparent consumption, 0 direct slots; in Commit 1)
│   │   ├── ✅ (code) Surface #5 Session State (session.rs ClientIdentity.identity_id→IdentityXgid; home_node stays String; ClientStateEvent no identifier slots; in Commit 1)
│   │   ├── ✅ (code) Surface #6 AI Resident (ai_service.rs AiPacingTracker key→SpaceXgid + ai_behavior.rs EventContext.ai_identity_id→&IdentityXgid; in Commit 1)
│   │   ├── ✅ (code) Surface #7 Pacing + Temperature (pacing.rs keys + temperature.rs; subject_id stays String per D-061; in Commit 1)
│   │   ├── ✅ (doc) Appendix F client-side sections — Surface #8 annotated; shipped in Pass 4 Commit 1 (J-145 `3869d4c`), Pass 4 CLOSED J-146
│   │   ├── ✅ (doc) xgen_aicontrol_implementation.md — Surface #8 annotated; shipped in Pass 4 Commit 1 (J-145 `3869d4c`), Pass 4 CLOSED J-146
│   │   └── ✅ (doc) Ch6 §6.15 client-side spec — Surface #8 annotated; shipped in Pass 4 Commit 1 (J-145 `3869d4c`), Pass 4 CLOSED J-146
│   └── ✅ Pass 5 — CLOSED J-148 (confirm-clean: Audit 1 = 1 trace fix F-1; Audit 2 clean; D-081 promoted)
│       ├── (code) test fixture builders
│       ├── (code) integration test helpers
│       ├── (code) trace event field types
│       ├── (code) log line formatters
│       ├── (code) debug/Display impls
│       ├── (doc) Appendix G log line convention
│       └── [at close: wire-format invariance promise promoted to cross-cutting]
│
├── M-series trunk
│   ├── ✅ M1 Binary Consolidation
│   ├── ✅ M2 Node Pipe Server
│   ├── ✅ M3 AI Operator Role
│   ├── ✅ M4 AI Client Binary
│   ├── ✅ M5 ops::* refactor
│   ├── ⬛ M6 (original) Multiparty baseline — DEPRECATED, replaced by M9
│   ├── ✅ M6 (new) Node admin write path — COMPLETE (all 7 verb categories A1–A7; 16 core verbs + 4 D-071 arcs + node-policy J-197)
│   │   ├── ✅ Phase 0 design (3 passes + Block 4; 33 verbs, Appendix K)
│   │   ├── ✅ Phase 1 (R1 `rooms`) — J-152; `members` deferred (no local data source), `federate` → Phase 7
│   │   ├── ✅ Phase 2 (J-153) — admin_ops/audit skeletons + EventAccepted wire + envelope event_id + rejection correlation (J-081 §5 gap closed)
│   │   ├── ⬛ Phase 3 (J-153) — COLLAPSED (reads bucketed into category phases per Block 4/Appendix K)
│   │   ├── ✅ Phase 4 (J-154) — A6 Logging & audit: 5 verbs + audit-write primitive + runtime log-level reload
│   │   ├── ✅ Phase 5 (J-155) — A5 Identity registry: 4 verbs; AdminContext → runtime-aware (P5); immediate revoke auth gate
│   │   ├── ✅ Phase 6 (A3 Bootstrap config) RESOLVED — not finished inline; reordered into its own D-071 bootstrap-client (A3) arc, CLOSED J-195 (framed send-path + `[bootstrap]` config/store + 5 verbs + `BOOT_71xx`); see the A3 ✅ line below. (J-156 deferral marker reconciled J-280.)
│   │   ├── ✅ Phase 7 (J-156) — A1 Federation mgmt HONEST-SUBSET: list + defederate (2 of 7); 5 verbs → D-071 federation-admin-control arc
│   │   ├── ✅ Backing audit (J-157, `tasks/M6_BACKING_AUDIT.md`) — A2 fully absent, A4 node-policy absent; ~14 ship / ~19 → 4 D-071 arcs; Phases 6+8 ship no verbs
│   │   ├── ✅ Phase 9 read subset (J-157) — A4 `space list-hosted` (backed); `audit-events` DEFERRED (§3.11.8 log unbuilt → protocol-audit-log arc); force-eject A4-D1 gated; node-policy arc
│   │   ├── ✅ Phase 10 (J-158) — A7 Plugin `list`+`status` (honest-thin: static compiled-in registry, no telemetry)
│   │   ├── ✅ Phase 9 (J-159) — A4 force-eject + node-unban (`membership.node_eject`/`node_unban`, Node-signed, wire 3043); M6 admin write-path COMPLETE at 16 verbs
│   │   ├── ✅ A4 force-eject Option B (J-160) — live fan-out + federation push (apply_fanout + apply_federation_push after persist; AdminContext sender maps threaded via the pipe); 16 verbs all live-propagating
│   │   ├── ✅ D-071 arc AUDIT phase (J-161) — backing audit of all 4 arcs (federation-admin-control · bootstrap-client · auth-module-registry · protocol-audit-log); all GAP-HIGH; `tasks/M6_*_AUDIT.md`
│   │   ├── ✅ D-071 arc design STUBS opened (J-162) — 4 PENDING design docs `tasks/M6_*_DESIGN.md` (decision scaffolds; arc-local FAC/BC/AMR/PAL-D# IDs); 4 audit cross-refs flipped + bumped v1.1
│   │   ├── ✅ M6 held doc work cleared (J-163) — §5.1/§6.A4 Option A→B amendment (admin-ops design v1.15) + M6_BACKING_AUDIT.md A4-row fix (v1.1)
│   │   ├── ✅ protocol-audit-log arc CLOSED (J-169) + doc-honesty reconciled (J-170) — writer (J-166, hook inside `persist_event`, Shape β, 8 live types/D-078) + reader `space audit-events` (J-167) + rebuild `space audit-rebuild` PAL-D3 (J-168) + close (J-169); §3.11.8 protocol log now built (`protocol_audit.rs`); 738 tests
│   │   ├── ✅ federation-admin-control 2a CLOSED (J-178) — Commits 1–5: state+migration · flag+queue · pause-point · accept/reject/initiate verbs · close; `accept`/`reject`/`initiate` SHIPPED; default-off invariant held; 760 tests
│   │   ├── ✅ federation-admin-control 2b (policy) — CLOSED (J-183); `set-policy`/`show-policy` + `policy_permits` enforcement at both sites, default-permit; whole federation-admin-control arc CLOSED (A1 7/7 ✅)
│   │   ├── ✅ auth-module-registry (A2) — CLOSED (J-185→J-189); `AuthModuleXgid` (D-083) + `AuthModuleRegistry` + 5 verbs (`list`/`register`/`revoke`/`set-tiers`/`test`); A2 5/5 ✅
│   │   ├── ✅ bootstrap-client (A3) — CLOSED (J-190→J-195); `[bootstrap]` config seed + `BootstrapRegistrationStore` + `bootstrap/signing.rs` + `bootstrap_client.rs` (framed send-path, NOT HTTP) + `bootstrap_keepalive.rs` (scheduler + re-advertise) + 5 verbs (`show`/`register`/`deregister`/`set-info`/`set-tiers`) + `BOOT_71xx`; A3 5/5 ✅; 831 tests
│   │   └── ✅ node-policy (5th/final D-071 deferral, 2 verbs — `set/show-node-policy`) — audit + design (NP-D1–D6 LOCKED) + runbook J-196; C1 store + verbs + threading SHIPPED J-197; C2 doc-close J-197 (Fork X, no checkpoint, no DECISIONS change)
│   ├── ✅ M7 family — DONE (`--aicontrol` v1 J-205 · events J-212 · completion J-223 · standalone J-226)
│   │   ├── ✅ M7 --aicontrol v1 — CLOSED (J-205, command-pipes-only) — shared `xgen-common::aicontrol` substrate + client (`ops::*`) + node (`admin_ops::*`) command pipes; adapter (D-065), `--batch` untouched (D-066); C1 J-201 · C2 J-202 · C4 J-204 · C6 close J-205; 898 tests. Events pipe (C3/C5) DEFERRED → **M7-events arc**.
│   │   ├── ✅ M7-events arc — CLOSED J-212 (client + node `.events` event-observation pipes). C1 `ClientSenders`→`Vec<(ConnId,Sender)>` retype (J-207) · C2 pure `Filter`/`parse`/`matches` substrate + EV-D4 v1.1 (J-208) · C3 node observer registry, Shape β process-global (J-209) · C4 node `.events` pipe `events_pipe.rs` — registry writer (J-210) · C5 client `.events` pipe — second same-identity WS riding the C1 retype, closing the J-203 collision (J-211) · C6 doc-only close (J-212). EV-D1–EV-D6 (arc-local, D-069); adapter over the retype (D-065); `--batch` untouched (D-066). **939**/0/1 (+41 across the arc); build + clippy clean. Deferred-on: AC-D4 token + AC-D6 idempotency (`--aicontrol` hardening arc), AC-D5 verbs (client-feature arc), client↔node `.events` integration test (✅ CLOSED J-229)
│   │   ├── ✅ M7-completion cluster — CLOSED at J-223 (six code commits; closes the `--aicontrol`-shaped remainder of M7; M7C-D1–D4 arc-local per D-069; 965/0/1). **Block A** ✅ client-feature (AC-D5 — `members` J-217 · `leave` J-218 · `create-dm-space` J-219 + node DM-init arm) → **Block B** ✅ hardening (AC-D4 token J-220 · AC-D6 idempotency J-221) → **Block C** ✅ `nodes`/`ordered_nodes` widening (J-222). Deferred to named homes: per-driver-identity control plane (M7C-D1/D2 end-state B) → privilege-model arc · plugin-write → temperature-plugin arc · pipelined handler → own arc · `migrate-start` → migration subsystem · live config reload → M7-standalone. **Explicitly OUT, each with a named home:** plugin-write verbs → temperature-plugin arc (gated on a 2nd plugin) · `CONCURRENT_COMMAND_NOT_ALLOWED` / pipelined handler → own arc (AC-D4 stays compatible) · `migrate-start` → migration subsystem · per-driver-identity control plane (M7C-D1/D2 end-state B) → privilege-model arc · live config reload → M7-standalone
│   │   └── ✅ M7-standalone — live config reload CLOSED (J-226) — `__RELOAD_CONFIG__` handler: re-read → all-or-nothing gate → diff/classify → apply `[logging].level` live → report; CP-1 dedicated `Arc<Mutex<NodeConfig>>` baseline; no write-back (F-R2); §2.6.3 corrected (M7S-D1/D6); 984/0/1 (+19). **M7 family DONE** (`--aicontrol` v1 · events · completion · standalone)
│   ├── ✅ M8 — state-resolution convergence CLOSED (J-241) [= Arc C, which borrowed the M8 number-slot; slot since vacated — M8 now = A/B metrics, see naming note below] — wired `derive_resolved` onto the node apply path + proved convergence (C1 in-process · C2 seam · C3 two-node); M9 not triggered; A/B metrics → M9
│   ├── ✅ Durable EventStore — EventStore trait + vanilla file backend + minimal durability floor CLOSED (J-228); D-080 implemented, engines = later module; C1 `bfe5cd8` + C2 `2eb3b0c`, 995/0/1
│   ├── ⚫ Storage-Engine / Plugin-Framework — CLOSED (J-232); compile-time plugin/module spine + first plugin (`xgen-store-sqlite`) behind J-228's `EventStore` trait; first `system·node` slot instance of the module-framework stance. C1–C5 + S shipped (1024/0/2 + 2 e2e); a selected engine is the live per-Space store + durability authority. Promoted D-085 (module framework) / D-086 (module identity) / D-087 (assurance enforcement); D-080 unchanged
│   ├── ✅ M8.6 — Federation stress (clock-injection seam + C4 spawn-leak gauge + connect-timeout + the 4 compounds C1/C4/C6/C8) — CLOSED J-294 (Commit 1+2a+2b; 1201/0/2; sensitivity witness recorded; two below-the-lock test-scope narrowings → phase9 suite)
│   │   └── ✅ INV-EXP — invite-expiry replay-gate fix CLOSED (J-298) — 3044/3045 admission gates run iff `origin == LocallySubmitted` (skip on `ReceivedViaFederation`), made correct on the drain path by per-entry origin in `PendingBuffer` (C1); 3044 clock → `self.clock.now_utc()` (D-090). C1 per-entry origin · C2 gate-guard+clock · C3 repro + 4 per-path + mixed-origin (6 tests) · C4 close; 1207/0/2 (+6); both sensitivity witnesses recorded (C1 batch-revert RED, C2 guard-revert RED); INV-D#/arc-local (D-069), no DECISIONS change (D-090 landed J-296)
│   ├── ✅ M8.7 — concurrent-commit resolution (R only) CLOSED J-302 (Phase-0 OPENED J-299; design Joe-LOCKED J-300; runbook + CC-D5 J-301; SHIPPED single commit J-302 — `mls.commit` conflict domain CC-D2 `(room,target_epoch)` + CC-D3 lexicographic tiebreak + CC-D5 `RoomState.mls_commit_tip` observable; convergence repro + sensitivity witness GREEN; 1212/0/2; CC-D# arc-local; S + home-DS serialization + loser-rebuild folded into the production openmls-client arc, L)
│   ├── ✅ M9 — build strategic multiparty test harness — CLOSED J-307 (xgen-mptest harness; both Round-0 smokes green; 5 findings → tasks/M9_findings.md)
│   │   ├── ✅ M9.1 — event timestamp-bound validation (F1/gap G6) — CLOSED J-311
│   │   └── ✅ M9.2 — harness-enablement seams (F2 fresh-peer initiate + F3 clock-advance + F4 raw-send; fenced test tooling) — CLOSED J-315
│   ├── 🟢 Multiparty tests — unnumbered; runs the M9 harness through escalating MP-R1 → MP-R2 → MP-R3; MP-R1 (deterministic floor) FIX PHASE — loop-to-green (D10); MP-F2 + MP-F3 shipped (J-324/J-326); MP-A-09 clean PASS; MP-F1a ✅ (J-328, facet-2/delivery); MP-F4 ✅ (J-331); MP-F1b (cross-node DM, iii) ✅ SHIPPED + CLOSED (J-333, Design Z: parties-set + identity-replicate hook, F-3 intact; MP-C-07 harness-green-with-boundary; invariant E → D-091) — remaining: 4 thin verbs → R1 rerun; production identity→home-node discovery routed
│   ├── 🟡 M10 — Auth Module reference set (Tier-1 ref module + one parameterized T2–T4 mock; UI-gate)
│   └── 🟡 M11 — `self` account (D-021 local-only synthetic identity; post-multiparty, identity-layer)
│
├── UI (clean-table rebuild — the post-M10 endpoint; built fresh after pre-UI work, Round-2 GO)
│   ├── ⬛ UI Phase 2 visual merge — DEPRECATED (J-284): element-modelling/visual-merge approach abandoned → superseded by the clean-table UI milestone
│   ├── 🟡 Clean-table UI milestone — the live UI build (replaces the deprecated visual-merge approach)
│   └── 🟡 Multi-device arc (R2-F09 — D3-gated; surfaces when the UI prototype exercises device add/remove)
│
├── Parallel workstreams
│   └── ⏸️ Slovak translation pass — POSTPONED (lowest priority; resume = English docs reach a stable end-state, or need arises)
│       └── [first concrete touchpoint: xgen_appendix_a_sk.md + _b_sk.md DEPRECATED 2026-05-20]
│
├── Open areas (deferred, not scheduled)
│   ├── ⏸️ Registry file encryption — identity/federation registries at rest; deferred (candidate storage/security module, rides the D-080/085 framework)
│   └── ⏸️ DPI resistance (D-023; Phase 3 — resume when Phase 3 opens)
│
├── Discipline / JOURNAL hygiene (✅ both follow-ups closed)
│   ├── ✅ JOURNAL Gap 1 — Phase 7.5 implementation retrospective entry (closed 2026-05-23 per J-103)
│   │   └── (commits 12cfe5a + aa2433f + 1be7189 + ecbbf19 + 8859093)
│   └── ✅ JOURNAL Gap 2 — XGID Adoption v1 design+Phase 1 retrospective entry (closed 2026-05-23 per J-102)
│       └── (commit a5f3c8b)
│
└── Cross-cutting principles (continuous, not milestone-shaped)
    ├── 🟢 D-069 — Design discipline (canonical-document rule)
    ├── ✅ D-070 — Two events of equal importance, opposite direction
    ├── ✅ D-071 — Audit-precedes-dependency discipline
    ├── ✅ D-072 — XGID Adoption v1
    ├── ✅ D-073 — Field-name-vs-type discipline
    ├── ✅ D-074 — Milestone-close commits MUST include JOURNAL.md
    ├── ✅ D-075 — state.federation_add is one party's act; federation_nodes is vantage-aware derived projection
    ├── ✅ D-076 — Wire-order determinism is a sender-side normative property for Node-to-Node federation
    ├── ✅ D-077 — Bidirectional sustainability discipline at silent-discard / fallible-discard sites (meta-layer above no-drift-surface discipline family; promoted J-107 persistence-amendment re-walk; backfilled to tree at J-114 atomic per α.a cascade-close)
    ├── ✅ D-078 — Production-grounded test enumeration (protocol-test-layer sibling to D-077 meta-layer; promoted J-114 Phase 9 Commit 3b-4 runbook-authoring atom; three-instance threshold met J-099 + J-109 + J-113)
    ├── 🟢 D-065 — Honest behaviour over polite behaviour
    └── 🟢 Honest longer work over fast shortcuts (not yet promoted to D-NNN)
```

### How to use this view

The tree is read top-down for navigation: scan to the cluster you care about, then drop into the prose Past/Present/Near future/Far future sections below for the detail. Status icons match the legend; nesting depth reflects logical containment, not strict commit order.

Three views fall out naturally:

- **What's settled?** Scan for ✅ marks. Most of Federation Event Propagation, all of XGID Adoption v1, M1–M5, D-070–D-073.
- **What's playing right now?** Nothing — **Joe selects the next arc.** Most recent close: **Arc H (E2E encryption / MLS, PG-05) CLOSED 🔷 INTERFACE-LOCKED at J-256 (2026-06-04; C1 `ee06168` J-255 + C2 `ffda2af` J-256 + close)** — the built-but-unwired Phase-2 epoch scheme operationalised onto the live `message.*` path + the `enc:` v2 **envelope** (per-message `CK` wrapped under the epoch key, a **D-088 amendment** giving crypto-shred its granularity) + the **content-blindness proof** (server-blindness real + proven on the wire) + KeyPackage lifecycle (5001/5002) + epoch-advance (commit-race fenced to D3). PG-05 closes interface-locked (real RFC 9420 = D3, cascade `D-088 → PG-05 → D3`); **the last Round-1 D-071 arc — all Round-1 arcs now closed.** Prior: **Arc I (GDPR erasure / right-to-be-forgotten, PG-02) CLOSED design-only at J-253 (2026-06-04; D-088)** — erasure *architecture* locked (crypto-shred content / orphan identity / monotonic tier-graded permission, protocol-binds-endpoints + Auth-Module-declares-interior); implementation deferred behind PG-05 (Arc H), so PG-02 stays OPEN as implementation. Prior: **Arc F — Space Migration subsystem (PG-11) CLOSED at J-251 (2026-06-04)** — C1 core completion (pure `transition()` + the `apply_space_migrate` cutover applier flipping `home_node` source→dest, AF-D1/D2) + C2 node driver (`migration_driver.rs` source+destination halves, 12-msg dispatch, EventStore bridge, retention AF-D5, `migration initiate` operator verb; two-node e2e flips `home_node` on **both** Nodes) + doc-only close (wire **6009 `migration_authority`** superseded the C2-guessed 6007). **Next-active = Joe selects the next arc.** The prior close is **Arc G — jurisdictional namespacing (PG-04) (J-249 open → J-250 close, 2026-06-04)** — C1 `SpaceState.jurisdiction` set-once at create (ch3 §3.7.3 + AppC) + C2 `FederationPolicy.allowed_jurisdictions` + `jurisdiction_permits` AND-composed with `policy_permits` at both federation sites (strict undeclared-denied) + `--allowed-jurisdiction` operator surface + ch3 MAY clause + close ch3 §3.6.7 central-aggregation MUST-NOT; honest dormant-but-correct, all AG-D# arc-local. Before it, **Arc E — primitive completion (J-245 open → J-248 close, 2026-06-04)** — PG-03 `TrustAssertion` (the third SignedPrimitive; struct + canonical sign/verify + the full §3.8.5 `validate_assertion` wired into registration; C1, J-246) + PG-08 `Thread` (`thread.*` events + `ThreadStatus` + `ThreadState`, rides M8 convergence; C2, J-247); all AE-D# arc-local. Before it, **Arc D — privilege-model / enforcement-hardening (J-242 open → J-244 close, 2026-06-03)** — PG-13 tier-gate on join + PG-12-min per-Room overrides (C1+C2, J-243), PG-10 reclassified NO-GAP. Before it, **M8 — state-resolution convergence (J-241)** — the built-but-unwired seven-layer `resolve()` wired onto the node apply path + convergence proved at three levels (C1 in-process · C2 runtime seam · C3 two-node), M9 not triggered. Before it, the **Storage-Engine / Plugin-Framework milestone (J-232, 2026-06-03)** — compile-time plugin/module spine + first plugin (`xgen-store-sqlite`) behind J-228's `EventStore` trait, a selected engine now the live per-Space store + durability authority (D-085/086/087; D-080 unchanged). Before it, **Durable EventStore at J-228 (2026-06-02)** — the `EventStore` trait + vanilla file backend + minimal durability floor, implementing D-080 (storage engines stay a later module behind the trait). Before it, the **M7 family** closed end-to-end (`--aicontrol` v1 J-205 · events J-212 · completion J-223 · standalone J-226) and the **M6 (new) admin write-path** completed (all seven verb categories + the four D-071 arcs + node-policy, J-197). **Next-active: Joe selects the next arc — all Round-1 D-071 arcs now closed; remaining work: PG-02 GDPR content-erasure *implementation* (gated on PG-05 real crypto = D3) · M9 (build strategic multiparty test harness) · M10 (Tier-1 Auth Module reference set — a UI-gate prerequisite, and the home of D-088's identity-orphan erasure half)**. D-065, D-067, D-069, D-074, D-078, and the "honest longer work over fast shortcuts" principle apply continuously.
- **What's the live frontier?** The **storage-engine / plugin-framework milestone closed at J-232** (spine + first plugin `xgen-store-sqlite` behind J-228's `EventStore` trait shipped; D-085/086/087 locked). **M8 (state-resolution convergence) CLOSED at J-241** (convergence wired onto the node apply path + proved C1/C2/C3; M9 not triggered). **Arc D (privilege-model / enforcement-hardening) CLOSED at J-244** (PG-13 + PG-12-min shipped C1+C2 J-243; PG-10 NO-GAP). **Arc E (primitive completion) CLOSED at J-248** (PG-03 `TrustAssertion` C1 J-246 + PG-08 `Thread` C2 J-247; full Role object model spun OUT to a later privilege-model arc, per-Room-type Thread behaviour → client/UI milestone). **Arc G (jurisdictional namespacing, PG-04) CLOSED at J-250** (J-249 open → C1+C2+close; `SpaceState.jurisdiction` set-once + `FederationPolicy.allowed_jurisdictions` AND-composed at both federation sites + ch3 MAY clause + §3.6.7 central-aggregation MUST-NOT; dormant-but-correct, all AG-D# arc-local). **Arc I (GDPR erasure, PG-02) CLOSED design-only at J-253** (erasure architecture locked via **D-088** — crypto-shred content / orphan identity / monotonic tier-graded permission; PG-02 stays OPEN as *implementation*, gated on PG-05/Arc H). **Arc F (Space Migration, PG-11) CLOSED at J-251** (C1 core completion + C2 node driver + doc-only close; node driver wired, two-node e2e flips `home_node` on both Nodes; wire 6009 added). **Arc H (E2E encryption, PG-05) CLOSED 🔷 INTERFACE-LOCKED at J-256** (J-254 open → C1 J-255 → C2 J-256 → close; the Phase-2 epoch scheme wired onto the live `message.*` path + the `enc:` v2 envelope [D-088 amendment, AH-D1] + content-blindness proof + KeyPackage lifecycle + epoch-advance; server-blindness real + proven, real RFC 9420 = D3). **Gap-audit register 10 done · 1 interface-locked (PG-05, Arc H) · 1 NO-GAP · 1 open (PG-02, gated on D3)** — **all Round-1 D-071 arcs now closed.** Remaining work (gap audit §4): **PG-02** GDPR content-erasure *impl* (gated on PG-05 real crypto = D3) · **M9** (build strategic multiparty test harness) · **M10** (Tier-1 Auth Module reference set — UI-gate), plus the heavier subsystem arcs named at each M7 close — privilege-model / control-plane-identity · temperature-plugin (gated on a 2nd plugin existing) · pipelined-handler · migration subsystem. Promoted to milestones (J-284): M8.6 federation stress (clock-injection seam + C1/C4/C6/C8, built+run in-milestone; NEXT-ACTIVE) · M8.7 D3 MLS operationalisation. Deprecated/postponed: UI Phase 2 visual merge ⬛ DEPRECATED (→ clean-table UI milestone) · Slovak translation ⏸️. Everything else is done, waiting on one of those, or further out. **Round 2 (UI-gate) executed at J-258 (2026-06-05): CONDITIONAL GO** — codebase coherent, only R2-F01 (client/node resolution divergence) touches UI correctness; next-active = Joe selects the fix ordering (R2-F01 fix-arc · doc housekeeping F02-F05/F07 · operator-terminology arc F06 · M10 · UI). See `tasks/ROUND_2_AUDIT.md`.

Milestone naming — stabilised 2026-06-05 (R2-F05). Two parallel designation systems; the multiparty test is deliberately unnumbered so it can shift without M-series renumbering.

```
M-series (numbered, operational)
|- M1-M7 .............. done
|- M8 ................. strong multiparty test / diagnostic          [CLOSED J-269]
|- M8.5 ............... finalization: INV + F-5 + S5 fixes  [CLOSED J-278; A·B·C done]
|- M8.6 ............... federation stress: clock seam + C1/C4/C6/C8  [CLOSED J-294]
|- INV-EXP ............. invite-expiry replay-gate fix (M8.6/C8 follow-on)  [CLOSED J-298]
|- M8.7 ............... D3 MLS operationalisation (openmls)          [NEXT-ACTIVE]
|- M9 ................. build strategic multiparty test harness      [pending]
|- M10 ................ Auth Module reference set (UI-gate)          [pending]
`- M11 ................ self account (D-021 local-only synthetic id)  [pending]

Gap-audit arcs A-I (PG-gap closures)
|- Arc A,B,D,E,F,G,H,I . closed
`- Arc C .............. state-resolution convergence (closed J-241)
                        borrowed the M8 slot; slot vacated -> M8 now = A/B metrics

Strategic milepoint (no M#, floats freely): Multiparty test
Gate (COMPLETE 2026-06-05): Round 2 -> GO -> R2-F01 (CLOSED J-264, A-pure)
             -> R2-F06 (CLOSED J-266, zero-rename); R2-F09 PULLED (multi-device, D3-gated)
Post-gate chain (locked 2026-06-05; M8.5 inserted J-269; M8.6/M8.7 inserted J-284; M11 inserted J-287): M8 (diagnostic, CLOSED)
             -> M8.5 (finalization: INV invitee-bootstrap + F-5 anti-transitivity + S5 rebind)
             -> M8.6 (federation stress: clock-injection seam + the four deferred compounds C1/C4/C6/C8, built + run in-milestone)
             -> INV-EXP (invite-expiry replay-gate fix; M8.6/C8 follow-on, before M8.7) [CLOSED J-298]
             -> M8.7 (D3 MLS operationalisation: real RFC 9420/openmls behind the Arc-H locked interface + concurrent-commit resolution) [Phase-0 OPENED J-299; runbook authored J-301; M8.7 = R only (concurrent-commit conflict domain); S+L = production openmls-client arc]
             -> M9 (build strategic multiparty test harness)
             -> Multiparty tests (strategic milestone, unnumbered) -> M10 (Auth Module ref set) -> M11 (self account, D-021) -> UI
```

Depth-asymmetry note: settled Past clusters (M-series M1–M5, completed Federation phases) show one line per milestone with no internal phase breakdown, mirroring the prose section's principle that detail accumulates as a track approaches and reduces when it settles. Live and Near-future clusters show full internal nesting because that detail is currently load-bearing for navigation.

---

## Past — settled

The arc the project has already traversed. Detail is intentionally compact; the canonical record of each item lives in the JOURNAL entries and tags cited.

### Phase 1 — minimal working implementation

✅ **Phase 1 complete** (April 2026, J-029, tag `v0.10.3`, 173 tests). Ten protocol layers shipped (crypto → wire format → DAG event store → WebSocket transport → Node identity → federation handshake → identity registration → Space/Room protocol → message exchange → smoke test). 17-step smoke test passes over real TCP against two live Node processes. The CLI surface (init, status, connections, spaces, peers, identity list, whoami) shipped on both binaries.

### Phase 2 — full protocol + crate split

✅ **Phase 2 protocol complete** (May 2026, J-058, 300 tests). Nine additional protocol layers shipped (layers 11–19, covering Auth Modules, advanced federation, state migration wire shapes, Bootstrap discovery, MLS wire shapes, audit-log facility). `smoke-ph2` runs 60/60 against two live Nodes. The xgen-core crate split (D-022, D-044) landed at J-045, establishing the dual-licence boundary (BSL 1.1 thin shells, GPL-2.0-or-later xgen-core library). One transport-layer bug (D-056, recv() routing collision between DAG Events and control messages on shared type-prefix strings) discovered and fixed during the live run.

✅ **Full integration stress test** (J-059, 6/6 PASS, 14.6s, 300 tests). Three-node topology (A:9080, B:9081, C:9082 + Bootstrap), all six scenarios pass. Two bugs found and fixed during live run (stack overflow in large async fn, B↔C federation recv hang). Comm record archived at `docs/tests/stress_complete_events.json`.

### Post-Phase-2 protocol additions

✅ **AI Identity, Pacing, and Temperature** (J-065, 387 tests, D-059/D-060/D-061). AI Identity extension to the registration pipeline (`is_ai` + `ai_capabilities` immutability, three error codes 3040/3041/3042, operator delegation EventTypes). Per-Space pacing rules with PacingManager and four edge-case handling. Temperature property foundation with reserved `meta_atts` keys, visibility filter, mute EventType with `auto_temperature` reason recognition, NoOpTemperaturePlugin reference implementation.

### M-series binary consolidation and refactor

✅ **M1 Binary Consolidation** (J-068–J-073, 391 tests). Four binaries collapsed to two (`xgen-node`, `xgen-client`), Tauri compiled into both per D-062, library-first dispatch per D-063, all 19 fundamental flags wired, Client `--service` headless resident operational.

✅ **M2 Node Pipe Server** (J-074, 391 tests). Node-side pipe surface with six flags real-implemented, four control commands (`--ping`/`--health`/`--stop`/`--reload-config`), read-only batch subset (7 verbs).

✅ **M3 AI Operator Role** (J-075, 411 tests). Per-(AI, Space) operator role distinct from member role, three-step fall-upward resolution algorithm, 3041 `ai_role_violation` enforcement, Client CLI `init --ai`/`ai delegate`/`ai revoke`/`ai status` subcommands, two-Node federation smoke verification.

✅ **M4 AI Client Binary** (J-077, 429 tests). AI Client as `xgen-client --ai-mode --service` resident mode, AiBehavior plugin trait with EchoPlugin reference implementation, AiPacingTracker for drop-on-throttle pacing, `__HEALTH__` extended with AI-mode lines, D-065 captures the recurring "honest behaviour over polite behaviour" principle.

✅ **M5 ops::* refactor** (J-078, 435 tests, 12 atomic commits, 17/17 smoke PASS, D-067). Every user-facing `xgen-client` verb (13 total) routes through single `xgen-client-lib::ops::<verb>` function. Three dispatchers (CLI arm, batch driver, pipe arm) became thin shims. The drift surface that produced F-003/F-004 in J-067 architecturally eliminated.

### Audits

✅ **CLI Flag Precedence Audit** (J-079, 463 tests, 5 atomic commits, D-068). What started as one named bug (`xgen-node --port`) surfaced five distinct violations. Two helpers (`xgen_common::precedence::resolve_setting<T>` and `resolve_log_level`) installed; every log-level resolution in the codebase now routes through one function. Drift surface eliminated architecturally.

✅ **J-080 carry-over pass** (468 tests, 3 atomic commits). Three of four J-079/M4 carry-overs closed (`--quiet` gating, init config schema-validity, short-lived CLI log path). Item 4 (`cmd_create_space` optimistic-ack UX) escalated to M6 design phase after investigation revealed it was not a Client-side UX bug but a missing protocol primitive — surfaced the accept-signal gap that drove M6 Pass 3 framework decisions.

✅ **Propagation Reliability Audit** (J-081, 468 tests, 1 atomic commit, no tests added — pure code-trace audit). Five-stage lifecycle walk; four of five sections surfaced drift between spec and code. Federation propagation (Stage 6) found to be architecturally absent in production. Validation asymmetry in `process_inbound` surfaced as separate correctness concern. `TransportMessage::Error` wire shape lacks `event_id` — refuting a multi-session-old assumption. Canonical document `docs/xgen_propagation_reliability.md` records full findings. The audit found what it was opened to find, and one substantial unexpected thing besides.

### M6 design phase

✅ **M6 Phase 0 — design phase closed 2026-05-18** (468 tests, 2 atomic commits during Phase 0, no code changes). Three-pass design phase: Pass 1 (Client `--batch` audit, `tasks/CLIENT_BATCH_AUDIT_M6.md`), Pass 2 (verb category sketches + Joe-lock proposals, `tasks/NODE_ADMIN_PASS2_PROPOSALS.md`, now DEPRECATED), Pass 3 (twelve framework decisions locked, canonical design doc shipped at `docs/xgen_node_admin_ops_design.md`). Key decisions: accept signal as first-class protocol primitive (`TransportMessage::EventAccepted` with G2 semantic), six Joe-lock items (connection authority, authorisation proof, live-reload bucket, audit shape and storage, failure semantics, verb naming convention), three discussion threads (phase ordering, missing categories, error format). D-070 candidate ("two events of equal importance, opposite direction") drafted in design doc §9, awaiting promotion. Block 4 (verb-by-verb walks) deferred to a future session, can run parallel to other work.

### Federation Event Propagation design phase — Pass 2

✅ **Federation Event Propagation Pass 2 closed 2026-05-18** (468 tests, single-session work, no code changes). Ten framework decisions Joe-locked in same-day conversation: F-1 hybrid push direction (push for steady state, pull for gap recovery) + F-1a tip exchange at handshake + F-1b drop-on-peer-down + F-1c Node-implementation per-peer record. F-2 long-lived continuous session + F-2 lifecycle boundaries + F-2a one WebSocket per pair bidirectional. F-3 event signature + federation relationship verification (two-check ingestion gate). F-4 unified validation core + per-event-type post-validation handlers + F-4a 30s HeldPending uniform + F-4b structural pre-checks before / semantic after. F-5 transitive federation locked-out v1 (v2 evolution path documented). F-6 `sync_complete` folded in (`SyncComplete { since, new_tip }`) + F-6a wire-shape details + F-6b 5s configurable safety-net (not protocol-fixed). F-7 response-size pagination folded in + F-7a 1000 events default (`[sync].batch_size`, not protocol-fixed). F-8 + F-9 documentation corrections deferred to Pass 3 same-commit. F-10 HeldPending extended for unknown-signer Identity case + F-10a same timeout policy as F-4a. Work shipped across `docs/xgen_federation_propagation_design.md` main doc (v0.6) plus three addenda (`_F7_addendum.md`, `_F8_F9_addendum.md`, `_F10_addendum.md`). Addenda exist because full-file rewrite per F-item became disproportionately expensive once the doc grew past ~70KB; Pass 3 consolidates them.

### Federation Event Propagation implementation — Phase 4

✅ **Federation Event Propagation implementation Phase 4 SHIPPED 2026-05-19** (J-085, 491 tests). F-1 federation event push + F-1b drop-on-peer-down + F-5 origin gating per design doc §4 + §4.5 + §8.5 + runbook §3.4 + §3.4.1. The "missing mechanism" verdict from the Propagation Reliability Audit (J-081 §2: Stage 6 federation propagation architecturally absent in production) is closed — federation push now exists as a production mechanism. Three implementation Joe-locks captured ahead of code in the §3.4.1 doc-pass commit: Q1 `EventOrigin` enum as runtime parameter (rejected wrapper struct and `#[serde(skip)]` on the wire-shape Event); Q2 `FederationPeerSenders` shape as `Arc<Mutex<HashMap<peer_node_id, Sender<OutboundMsg>>>>` mirroring `ClientSenders` (rejected co-location with shared_spaces — `SpaceState.federation_nodes` stays single source of truth); Q3 reuse of `process_inbound` with two-comment overload documentation (semantic overload of `identity_id` accepting Identity URI for clients OR Node URI for federation sessions, documented at function definition + federation call site). Plus R12-R15 Clair-latitude items: R12 registry lifecycle (register on handshake-ACTIVE, deregister on session exit), R13 try_send semantics (non-blocking, drop on channel-full per F-1b), R14 drop-on-peer-down log line, R15 origin attach at entry points. Forward-looking note in the EventOrigin doc comment: enum is extensible to future `ReceivedViaAdminInjection` (M6) and `ReceivedViaBackfill` (hypothetical replay tooling) variants. Structural changes: new `EventOrigin` enum in `xgen-core::node::runtime`; new `FederationPeerSenders` type in `xgen-node/src/fanout.rs`; new `apply_federation_push` function in `xgen-node/src/federation_session.rs` (sibling of `apply_fanout`, not a wrapper; F-5 guard at the top); `dispatch_event` + `process_inbound` signatures gain `origin` parameter; Phase 3's R1 plug-in point in `handle_federation_incoming` is now operational (out_tx registered on ACTIVE, outbound arm drains pushed events to the wire, inbound `Inbound::Event(_)` routes through `process_inbound`+`apply_fanout`+`apply_federation_push` with F-5 short-circuit, deregister on loop exit). Tests: 488 → 491 (+3 integration tests in new `federation_push_integration.rs`: `alice_post_propagates_to_bob_via_federation_push` covers the full push path end-to-end; `f5_anti_transitivity_received_via_federation_event_not_pushed` regression-locks the F-5 §8.5 guard via direct call; `f1b_drop_on_peer_down_no_panic` verifies graceful handling when peers are absent from the registry). Phase 5 (F-1c per-peer record + reconnect scheduling — `run_initiating` gains its first production caller in xgen-node/src/) is next-active. Two intermittent flakes disclosed in the JOURNAL per Rule 2: the pre-existing precedence env-var race (from J-079, ~10-20% workspace runs); the `reconnect_with_existing_tip_small_delta_delivered` Phase 3 test flake under increased Phase-4 parallelism (~10% workspace runs, 0% isolated). Both are flagged as known intermittent flakes; the fix path for both is the same shape (`#[serial_test::serial]` or controlled test parallelism).

### Federation Event Propagation implementation — Phase 3

✅ **Federation Event Propagation implementation Phase 3 SHIPPED 2026-05-19** (J-084, 488 tests). F-1a federation handshake reshape to bilateral tip exchange per design doc §4.4 + runbook `tasks/FEDERATION_PROPAGATION_COMPLETION.md` §3.3 + §3.3.1. The pre-F-1a flow (handshake → `space.join_request` → `state.federation_add` → history dump → `goodbye`-close) is replaced by F-1a tip exchange: bilateral `tips: BTreeMap<String, String>` on `Hello` + `Capabilities`, both sides stream per-Space delta via new `compute_federation_delta_for_space` helper + `SyncComplete` terminator, session stays open as the persistent F-2 push channel. Eight Joe-locks captured before code started (§3.3 Option 3 wire shape, §3.3.1 Locks 1-7: Option A full migration / a-i symmetry rule for `state.federation_add` / R1 unused outbound mpsc / R2 sibling delta helper / R3 informational `new_tip` semantic / R4 sorted-by-`space_id` ordering / R5 bilateral helper with initiator-side production caller in Phase 5); both lock-sets shipped in a §3.3.1 doc-pass commit ahead of the code commit per Pass-2-style ride-along discipline. New module `xgen-node/src/federation_session.rs` with `stream_federation_delta` orchestrator (sorted iteration, a-i symmetry, whole-batch SyncComplete). `handle_federation_incoming` refactored: drop JoinRequest receipt + drop inline state.federation_add build + drop goodbye + call `stream_federation_delta` + enter F-2 long-lived continuous session loop with R1-locked Phase-4-prep outbound mpsc. Seven `xgen-client` `run_initiating` call sites migrated to Option A full-migration shape (drop paired JoinRequest, add `BTreeMap::new()` tips, change recv-loop terminator from `Goodbye` to `SyncComplete` with `Goodbye`/`Closed` fallback for pre-F-1a peers). Tests: 480 → 488 (+8: 4 wire-layer in `xgen-core/src/federation/handshake.rs` covering tips-round-trip + tampered-tips + pre-F-1a wire-shape back-compat for both Hello and Capabilities; 4 integration in `xgen-node-lib` including new `federation_delta_integration.rs` file with brand-new + reconnect + pre-F-1a compat scenarios, plus `bilateral_tips_propagate_through_handshake` in existing `federation_integration.rs`). Phase 4 (federation event push, F-1 + F-1b + F-5) is next-active — the persistent F-2 session is now operational and Phase 4's push code plugs into the R1-prepared outbound mpsc.

### Federation Event Propagation implementation — Phase 2

✅ **Federation Event Propagation implementation Phase 2 SHIPPED 2026-05-18** (J-083, 480 tests). F-4 `process_inbound` validation pipeline unification per design doc §7. The pre-F-4 three-path asymmetry (audit J-081 §3.2: Path A messages → full 13-step pipeline; Path B `MembershipJoin` → direct ingest with no signature verification; Path C other state events → direct ingest with no signature verification) closes architecturally. New types: `ValidationOutcome` enum (`Validated` / `HeldPending(Vec<String>)` / `Rejected(ExchangeError)`) and `validate_event` function in `xgen-core/src/message/exchange.rs` (non-mutating, uniform structural and crypto checks, sub-check coverage table documented). New orchestrator: `DispatchOutcome` enum + `NodeRuntime::dispatch_event` + `drain_pending_uniform` in `xgen-core/src/node/runtime.rs` (F-4 §7.7 pipeline shape: structural pre-check → federation-relationship placeholder (Phase 7) → validation core → semantic pre-checks (AI role/capability/operator per §7.6) → ingest → drain). `process_inbound`'s three-arm event match collapsed to one `dispatch_event` call + `match outcome` block. HeldPending now applies to all event families with the same 30s timeout (F-4a — `PENDING_TIMEOUT_SECS = 30` in `xgen-core/src/dag/pending.rs` unchanged; pre-F-4 only `accept_message` populated the buffer). `ExchangeError` gains `Clone` (additive). `accept_message` preserved unchanged for smoke.rs test backward-compat — production traffic routes through `dispatch_event`. Tests: 476 → 480 (+4 in `xgen-node-lib::fanout::tests`: `f4_path_a_message_unknown_predecessor_held_pending_then_drains` regression-locks Path A; `f4_path_b_join_unknown_predecessor_held_pending_then_drains` closes audit Scenario-A non-message for Path B; `f4_path_c_state_unknown_predecessor_held_pending_then_drains` closes Scenario-A for Path C; `f4_rejects_bad_signature_on_membership_join` regression-locks the audit's HIGH-severity vulnerability vector). The audit's HIGH-severity precondition for Phase 4 (federation push) is now met. Coordination with M6 (new) Phase 2 preserved — rejection sites consistent across event families; M6 wires the wire-layer signal in its own milestone.

### Federation Event Propagation implementation — Phase 1

✅ **Federation Event Propagation implementation Phase 1 SHIPPED 2026-05-18** (J-082, 476 tests). F-6 (`transport.sync_complete`) + F-7 (response-size pagination) shipped together as one coordinated wire-protocol change per design doc §9 + §10. `TransportMessage::SyncRequest::limit: Option<u32>` and new `TransportMessage::SyncComplete { since, new_tip, continue_from }` variants with `#[serde(skip_serializing_if = "Option::is_none")]` for backward-compat. `collect_sync_history` signature changed to `(runtime, requester_id, since, limit) -> (Vec<Event>, Option<String>)` honouring `limit` and emitting `continue_from`. Four production callers migrated from quiet-time / hardcoded-deadline to SyncComplete-driven pagination loops with F-6b safety-net timeout: `xgen-client/src/batch.rs:get_dag_tips`, `xgen-client/src/ai_service.rs:224`, `xgen-client/src/ops.rs:721` (`history`), `xgen-client/src/ops.rs:939` (`ai_status`). Cross-Space behaviour Clair-locked as whole-batch (rationale at `xgen-node/src/fanout.rs:177-201`). `[sync]` config section on both binaries with `completion_timeout_seconds` (default 5) and `batch_size` (default 1000), `#[serde(default)]` so existing on-disk configs keep parsing. Tests: 468 → 476 (+7 unit in `xgen-node-lib::fanout` covering wire roundtrip and pagination semantics, +1 integration in `xgen-client/tests/sync_safety_net.rs` covering the safety-net error path). Federation Event Propagation implementation track moved from Near future to Present in this same commit.

### Federation Event Propagation design phase — Pass 3

✅ **Federation Event Propagation Pass 3 closed 2026-05-18** (468 tests, single same-day session following Pass 2, no code changes). Design phase closed; canonical document shipped; runbook handed off. Five deliverables in one coordinated commit: (1) canonical design doc consolidated to v1.0 ACTIVE at `docs/xgen_federation_propagation_design.md` — three Pass-2 addenda folded in as §10 (F-7), §11 (F-8), §12 (F-9), §13 (F-10), addendum files deleted, version bumped from v0.6, Status flipped from PENDING to ACTIVE; (2) all `[JOE-LOCK]` markers walked from "confirmed in Pass 2 conversation 2026-05-18; formal promotion at Pass 3" to final form `[JOE-LOCK: locked 2026-05-18 (Pass 2 conversation, Pass 3 promotion)]`; (3) F-8 corrections applied to `docs/xgen_ch4_implementation.md` §4.11.3 + §4.12.3 (forward-references to canonical design doc; located by content match against unique phrases rather than the audit's stale line numbers); (4) F-9 correction applied to `docs/xgen_node_admin_ops_design.md` §4.2 (Federation propagation Stage-6 sub-bullet now a forward-reference); (5) implementation runbook created at `tasks/FEDERATION_PROPAGATION_COMPLETION.md` (Status: ACTIVE, v1.0) — nine phases with hard-locked Phase 2 → Phase 4 ordering. Pass 2 task file (`tasks/FEDERATION_PROPAGATION_DESIGN.md`) and Pass 3 task file (`tasks/FEDERATION_PROPAGATION_PASS_3.md`) both flipped to COMPLETED in the same commit. Federation Event Propagation milestone block in CLAUDE.md flipped from 🟡 PENDING to 🟢 ACTIVE; implementation track in this document went 🟡 PENDING in Near future.

### Federation Event Propagation implementation — Phase 9 survey

✅ **Federation Event Propagation Phase 9 survey closed 2026-05-19** (J-090 deliverable shipped; finalised via Joe-lock in J-091, no code changes, 519 tests unchanged). Pre-implementation subsystem audit per `tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY.md` v2.0. Canonical deliverable at `tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY_FINDINGS.md` (Status flipped PENDING → COMPLETED v1.1 at J-091 close with all four §8 locks recorded inline). Survey recommended **12 Phase 9 scenarios** — 6 baseline (push smoke, anti-transitivity, drop-and-recover, validation-asymmetry regression, unknown-signer first-contact, federation-relationship rejection) + 6 compounds (C2 anti-transitivity at queue depth, C3 F-3 during F-1a recovery, C5 validation asymmetry under load, C7 pagination at boundary, C9 F-3 drain-time hazard, C10 identity-replicate hook under contention). Four compounds (C1, C4, C6, C8) deferred to follow-on federation-stress milestone (blocked on clock-injection seam or improbable bug shapes). Five structural gaps surfaced (G1 `xgen-node_state.json::peers` hard-coded to `vec![]`, G2 no stable trace events for F-1/F-3 paths, G3 no fan-out trace event — all three close as Phase 9 precondition per Q2 locked option a; G4 audit log for F-3 reject + G5 drop-peer affordance defer to M6 per Q2 locked option c). Flake-handling locked at Q3 option (c) — fix both pre-existing flakes (precedence env-var race + reconnect_with_existing_tip_small_delta_delivered) as precondition, escalate to underlying-race investigation only if Phase 9 implementation surfaces new flakes. 14-entry failure-mode catalogue (11 HIGH severity; 11 caught by Phase 9 set; M6, M8, M13 flagged for Client-Side Consequences Audit post-milestone). Survey discipline followed D-071: subsystem audit precedes dependent milestone phase, same pattern as J-081 → Federation design phase. Joe-lock + COMPLETED-flip + Phase 9 implementation task file authoring all happen in J-091, in the same commit as this entry.

### Federation Event Propagation implementation — Phase 7.5 design phase

✅ **Federation Event Propagation Phase 7.5 design phase closed 2026-05-19** (J-093 walkthrough, 519 tests unchanged, no code changes). Cold-start bootstrap design phase surfaced during Phase 9 Commit 3 harness setup when Scenario 1 ("two-Node push smoke") walked into failure-mode catalogue M5 ("F-3 brand-new federation bootstrap dead-locked") exactly where the Phase 9 survey predicted. Root cause: Phase 7's F-3 gate rejects `state.space_create` arriving via federation because `SpaceState.federation_nodes` lookup returns None when Space S doesn't exist locally yet; Phase 7's B1 skip rule only covers `state.federation_add`, not Space-create. Phase 9 stood down at Commit 3 boundary; J-092's Commits 1 + 2 (observability preconditions + flake fixes) stay shipped because both are pure infrastructure. Four framework decisions `[JOE-LOCK: locked 2026-05-19]` at `tasks/FEDERATION_PROPAGATION_PHASE_7_5_DESIGN.md` (Status: ACTIVE v1.0 — flips to COMPLETED in Commit 1 of the implementation runbook): **P7.5-A** narrow F-3 + F-4-step-1 skip for `state.space_create` and `state.dm_space_create` EventTypes (sibling to Phase 7 B1), with new `SpaceLocalMetadata` sibling structure in `xgen-common` carrying `introducer_node_id: Option<String>` field (name locked through any future XGID-typing pass) populated at federation Space-create ingestion for DoS-surface triage; **P7.5-B** third HeldPending trigger "missing federation relationship for (peer, space)" resolved by idempotent `state.federation_add` arrival hook, new error code `4007 federation_relationship_timeout`, combination semantics with F-4a (predecessor) and F-10 (Identity) via existing struct-variant Option fields, precedence ranking predecessor (4002) > federation-relationship (4007) > Identity (4006) because federation-relationship is the most upstream blocker; **P7.5-C** per-trigger timeout with predecessor + Identity staying at 30s and federation-relationship defaulting to 180s with new `[sync].federation_relationship_timeout_seconds` config field (brings F-10a's v2 evolution path forward to v1); **P7.5-D** new `pending_federation_relationship: usize` counter in `xgen-node_state.json` plus existing `f3_reject` trace event extended with disposition field (`rejected` vs `held_pending`) — trace event NOT renamed, introducer field NOT exposed in state file. The design phase added four substantive walkthrough findings beyond the draft: §5.3 DoS-surface paragraph with `SpaceLocalMetadata` sibling structure; §6.3 timeout precedence swap (4007 outranks 4006); §6.3 idempotent-hook clarification + two-stage cascade case; §7.3 timeout default raised from 120s to 180s. Implementation runbook authored same-day at `tasks/FEDERATION_PROPAGATION_PHASE_7_5_IMPL.md` (ACTIVE v1.0) with five-commit sequence: doc-pass, F-3 + F-4 step 1 skip + `SpaceLocalMetadata`, HeldPending third trigger + config + error code + observability, NodeRuntime-level integration tests (six scenarios), milestone close. Phase 9 remains PAUSED at Commit 3 boundary pending Phase 7.5 implementation closure + XGID concept work (sequenced between). Discipline note: the D-071 audit-precedes-dependent pattern extends one level finer to "design gaps surface during dependent work and close before the dependent work proceeds" — Phase 7.5 is the formal recognition of that pattern.

### Federation Event Propagation implementation — Phase 7.5 (Cold-Start Bootstrap)

✅ **Federation Event Propagation Phase 7.5 implementation SHIPPED 2026-05-20** (five commits `12cfe5a` + `aa2433f` + `1be7189` + `ecbbf19` + `8859093`, 519 → 556 tests, +37 across the milestone; **JOURNAL entry not written at milestone-close time** — the cross-doc "J-094" reference originally placed here pointed at an entry that was never authored, a Rule 4 discipline failure surfaced during XGID Adoption v1 Phase 2 close-out 2026-05-20 and confirmed via working-tree forensics; entry deferred to separate retrospective work per D-065 honest-provenance, will take next available J-number when written). Cold-start bootstrap closure shipped per `tasks/FEDERATION_PROPAGATION_PHASE_7_5_IMPL.md` (Status: COMPLETED v1.0 at milestone close). Five commits: (1) doc-pass adding canonical design doc §6.4.1 + §15 row, design task file flipped COMPLETED; (2) F-3 + F-4 step 1 skip for `state.space_create` + `state.dm_space_create` plus new `SpaceLocalMetadata` sibling structure in `xgen-common` with `introducer_node_id` field; (3) HeldPending third trigger "missing federation relationship for (peer, space)" + idempotent `state.federation_add` arrival hook + `drain_pending_by_federation_relationship` + new `[sync].federation_relationship_timeout_seconds` config field (180s default) + new error code `4007 federation_relationship_timeout` + `pending_federation_relationship` counter + `f3_reject` trace event extended with disposition field; (3.5) **Phase 7 B3 amendment** (`tasks/FEDERATION_PROPAGATION_PHASE_7_B3_AMENDMENT.md`, COMPLETED v1.0, commit `ecbbf19`) closing two latent Phase-7 gaps surfaced by Commit 4 integration tests: predecessor-chain deadlock + step-11 sender-membership rejection for `state.federation_add` arriving via federation channel; sibling-to-B1 framing (not P7.5-A extension) honest about attribution; skip set widened from step-9 + step-11 + step-13 to step-11 in full (both halves) after Q3-overload code trace established `IdentityRegistry::contains` is Identity-only while `verify_event_signature` works for Node URIs via pubkey-URI decoding; step-12 retained as authority anchor; (4) folded into Commit 3.5 — NodeRuntime-level integration tests written + green within the B3 implementation commit (six scenarios: cold-start end-to-end, mid-bootstrap drop/resume, F-10 + Phase-7.5 combination, two-stage cascade marked not-applicable post-B3 since federation_add no longer F-10-buffers via federation channel, timeout precedence at 4007 + 4002, negative regression); (5) milestone-internal close commit (this entry's commit). Architectural surfaces of note: federation-relationship arrival hook lifted from `xgen-node::app::process_inbound` into `dispatch_event` Step 7 so every caller exercises it under the runtime lock (mirror of Phase 6's Identity-hook architecture); `resolve_federation_relationship` gained `reindex_after_partial_release` helper preventing buffer-entry orphan when sibling drain-released events haven't ingested due to HashSet-order non-determinism (a bug shape that only surfaces with three HeldPending triggers, not two); `drain_timed_out` dropped implicit `.max(default_timeout)` so operator's configured federation timeout wins. Test count 519 → 556 across the milestone (+37: +5 B3 unit tests + 7 cold-start integration tests + 25 across Commits 2 + 3 dispatch / pending-buffer / config-field / counter / state-file roundtrip / SpaceLocalMetadata persistence / trace-event-disposition / etc.). Failure-mode catalogue M5 ("F-3 brand-new federation bootstrap dead-locked") structurally closed. Phase 9 stays PAUSED at Commit 3 boundary with XGID Adoption now the active blocker. The B3 mid-implementation Joe-lock walkthrough is recorded discipline-wise as J-081-shape: the D-071 audit-precedes-dependent pattern extending two levels finer ("design gaps surface during implementation testing and close before the implementation proceeds"), Phase 7.5's J-093-shape pattern recursing one more level into Commit-4 integration-test surface.

### XGID Adoption v1

✅ **XGID Adoption v1 design walkthrough closed + Phase 1 canonical sources commit shipped 2026-05-20** (single session, no test count change — documentation and design only). Walkthrough ran across two same-day sessions: session A opened Q1–Q4 partial and produced the mid-walkthrough design-phase restructure from five-upfront-deliverables to canonical-sources-first / doc-tree-sweep-second; session B closed Q4 final framing + Q5 + Q6 and drafted the Phase 1 canonical sources commit. **Six locked decisions Q1–Q6:** Q1 vocabulary scope (six XGID flavours — Event, Space, Room, TrustAssertion (hash-anchored family) + Node, Identity (principal family); session_id and trust_assertion_id explicitly placed as sub-axes within existing flavours, not new flavours; wire-envelope correlation handles, error codes, config keys, file paths, bootstrap URIs explicitly NOT XGIDs); Q2 type-system shape (layered newtype — base `Xgid(String)` plus six flavour wrappers each `Deref<Target = Xgid>`, all serde-transparent as plain strings; `XgidLike` trait sparingly used; principal flavours carry parse-fallible `pubkey() -> Result<VerifyingKey, _>` at v1, future-tightening to infallible deferred); Q3 adoption discipline (Shape γ + ASAP — staged retrofit, five passes immediately into Near future; principle wording locked verbatim into D-072 + Appendix J §J.11 + Ch3 §3.0.6); Q4 wire-format invariance (three-document placement + five-invariance scope + both wire crossings explicitly named including AI control / batch JSONL as named second wire crossing; §J.9 ships with two worked rejected-proposal examples (Cases #5 "use in-memory handle type as wire type" and #2 "shorten URI grammar for compactness") + reserved third slot; v1 pointers in `xgen_aicontrol_implementation.md` and Appendix F per Q4(b) lock); Q5 immutability framing (Option C layered — Ch3 §3.0.2 declarative sentence + Appendix J §J.4 construction-derived explanation + §J.10 "rename a Space" worked example demonstrating the property is structural, not policy); Q6 field-name-vs-type discipline canonicalisation (Option A — D-073 in DECISIONS.md as canonical home for the principle, one-sentence echo in Appendix J's introduction, Phase 7.5 §5.6 retained as historical originating-precedent record). **Phase 1 canonical sources commit shipped same session** — eight artefacts in one atomic commit: DECISIONS.md D-072 + D-073, `docs/xgen_appendix_j_en.md` (twelve sections), `docs/xgen_ch3_specification.md` §3.0 (six subsections, inserted as new §3.0 before §3.1 Wire Format), `tasks/XGID_ADOPTION_IMPL.md` (two-commit Clair-facing runbook with five required invariance test names pinned: `xgid_serializes_as_plain_string`, `xgid_deserializes_from_plain_string`, `flavour_wrapper_is_serde_transparent`, `event_xgid_roundtrip_through_event_canonical_form`, `node_xgid_roundtrip_through_handshake_message`), one-line normative pointers in `docs/xgen_aicontrol_implementation.md` and `docs/xgen_appendix_f_en.md` ("XGID discipline applies; full annotation pending Retrofit Pass 4"), `tasks/XGID_ADOPTION_DESIGN.md` (retrospective design walkthrough record — Status: COMPLETED from creation with explicit provenance disclosure per D-065 honest-behaviour principle, the document captures the path the walkthrough took and the Phase 2 sweep flag for Ch6 §6.15 Scope-A-vs-Scope-B classification), CLAUDE.md PLAY block refreshed. Mid-session restructure recorded in the design task file as a worked instance of "honest longer work over fast shortcuts" — the original five-upfront-deliverable shape kept finding affected docs piecemeal during Q4 walkthrough, restructure to canonical-sources-first plus dedicated Phase 2 doc-tree sweep absorbed the piecemeal problem by deferring per-doc classification until the canonical sources existed and a uniform classification rule could be applied. Phase 9 stays PAUSED at Commit 3 boundary; XGID Adoption v1 milestone closes when Clair ships the two-commit implementation, then Phase 9 resumes with integration test code using XGID types from start.

✅ **Topological-sort wire-order non-determinism design phase closed 2026-05-22** (`tasks/FEDERATION_TOPOSORT_DESIGN.md` Status: ACTIVE v1.0 ~33 KB; flips to COMPLETED in Commit 1 of the implementation runbook — sibling-in-shape to the bidirectional `federation_nodes` design-task-file lifecycle). Three Joe-locks: **Q3.ii canonical wire ordering required** — wire-order determinism is a sender-side normative property for Node-to-Node federation; two senders with identical Space history MUST produce byte-identical federation deltas modulo signature-bearing fields that vary by author and time. Q3.i (per-receiver determinism sufficient) considered and rejected as failing to capture the contract reality: federation message senders are protocol-output producers, not implementation-detail-bearers; D-067's no-drift-surface discipline at the wire-format layer requires senders to converge byte-identically. **Q2 middle + Q2.γ** — D-067 audit scope is middle (fix the primitive's contract once at `topological_sort_events:193` as canonical-regardless-of-input semantics; the primitive's contract becomes canonical at the source rather than each caller). Q2.γ forward-binding: Q3.ii scoped to Node-to-Node federation today, with explicit forward-binding to Node-to-Client sender output where analogous — `collect_sync_history` and `apply_fanout` history-push flagged for future scheduling rather than handled in this phase's implementation scope. **Q1 Shape A v1 + sibling Site 1 fix** — event_id lexicographic sort at `topological_sort_events` primitive plus sibling sort at `compute_federation_delta_for_space:321` before passing to the primitive. Pass-1-neutral via v1 `&str` sort plus code-comment block at both sites flagging Pass 3 retype to `EventXgid`. Shape A v2 (typed `EventXgid` sort coupled to Pass 1) considered and rejected: couples to Pass 1 unnecessarily for v1; Pass 3 retype-marker comment closes the same loop with less coupling cost. Shape C (canonical-event-bytes sort) considered and rejected: pays signature-cost-per-tie-break without contract benefit beyond duplicate-event_id edge case (and event_id is itself a hash of canonical bytes, so distinct event_id implies distinct canonical bytes for hash-anchored events). Shape D.1 (BTreeMap container change at `EventStore`) considered and rejected: pays subsystem-wide migration cost with no incremental contract benefit beyond fixing the primitive's contract. Shape D.1 + Shape A considered and rejected: pays both costs. Shape B (timestamp sort) + Shape D.2 (IndexMap container change) disqualified earlier at Q3 lock (both fail Q3.ii's byte-identical-across-senders requirement; timestamp values vary by sender clock; IndexMap insertion-order leaks sender-side history ordering). **D-076 promoted to DECISIONS.md** as the protocol-design principle the three Q-locks instantiate: "Wire-order determinism is a sender-side normative property for Node-to-Node federation. Two senders with identical Space history MUST produce byte-identical federation deltas (modulo signature-bearing fields that vary by author and time). Wire ordering is part of the protocol's contract, not implementation latitude. Forward-bound by Q2.γ to Node-to-Client sender output where analogous and should be reviewed when scheduling allows." D-076 is the first D-NNN to lock a wire-format-normative property explicitly. The binding it creates: future event-design Joe-locks must include "does this event's serialisation produce canonical wire ordering across senders" as a design-phase question. **D-067 + D-070 + D-075 + D-076 now form the four-decision no-drift-surface discipline family** — each locks a no-drift-surface property at a different protocol layer (D-067 code-organisation: single source of truth per logical responsibility; D-070 transport: outcome correlation pair on `TransportMessage`; D-075 event-model: single-party-assertion semantics + vantage-aware applier; D-076 wire-format: byte-identical sender output across senders); together they form a coherent discipline expressed across four layers; future contributors reading any of the four find pointers to the other three; the family operates as a single principle. **Inline-lock pattern (third recurrence).** All three audit-phase questions were locked inline in the audit doc at design-phase opening rather than carried as open questions for the design walkthrough to deliberate. The design task file expounds the already-locked decisions rather than walking them afresh. Pattern is now durable across three audit closures (bidirectional Q2 inline + Q1 design-phase-walked; this phase Q3 + Q2 + Q1 all inline). Pattern's discipline cost: Joe carries the lock decisions earlier. Pattern's benefit: design task file ships in a fresh session with cleaner exposition because the deliberation phase doesn't double as the artefact-authoring phase. **Split-session discipline (bidirectional precedent).** Design task file and implementation runbook kept as separate artefacts in separate sessions. Design task file's audience: future Chat Claude + Joe reading "why did we lock this." Runbook's audience: Clair reading "what do I ship in what order." Both benefit from different headspace. Three Joe-locks landed in the arc; pacing discipline matters at this point. **"Honest longer work over fast shortcuts" — third recurrence within this milestone.** Phase 7.5 was the first; bidirectional was the second; this is the third. Each recurrence: dependent work surfaced a load-bearing gap; the gap closed properly (audit → design → impl → close) rather than via a workaround. The pattern's cost is real (each recurrence delays Federation Event Propagation milestone closure by approximately one session-arc); the pattern's benefit is also real (each recurrence is a bug fixed before it ships to production). Implementation runbook authoring is next-active for Chat Claude + Joe in a fresh session; deliverable is `tasks/FEDERATION_TOPOSORT_IMPL.md` sibling-in-shape to `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` with four atomic commits expected (doc-pass; primitive + sibling fix + unit tests; Phase 9 Scenario 1 lift; milestone close). Audit doc Status flipped ACTIVE → COMPLETED v1.0 at this commit per the bidirectional precedent (audit doc's role as input to design-phase deliberation ends as the design task file lands the canonical record of the locked design; the audit doc's three inline locks are preserved as authoritative historical record of the design at lock-time). After implementation milestone closes: Phase 9 Commit 3b resumes; Federation Event Propagation milestone flips PLAY → DONE; M6 (new) implementation + Pass 1 of XGID Retrofit unblock simultaneously.

✅ **Topological-sort wire-order non-determinism implementation runbook shipped 2026-05-22** (`tasks/FEDERATION_TOPOSORT_IMPL.md` Status: ACTIVE v1.0, ~93 KB, eight sections; flips to COMPLETED v1.1 in Commit 4 of Clair's implementation arc — sibling-in-shape to `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` v1.0 → v1.1 lifecycle). Sibling-in-shape to `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` (COMPLETED v1.1) with one structural addition — **§7 discipline notes (six paragraphs)**, present in this runbook because (a) trilogy-internal consistency outranks one-step-earlier-precedent consistency when they conflict (topological-sort phase's audit and design docs both ship discipline-notes sections; impl runbook being the only document in the trilogy without one would itself be drift), (b) the bidirectional precedent's absence of §7 was absence-of-need at second sibling-recurrence rather than deliberate design (at second recurrence there is no pattern to call out; at third the pattern is durable enough to name), (c) D-076's family-completion as fourth member of no-drift-surface discipline family needs runbook-visible pointer rather than only after-the-fact JOURNAL coverage. §7.1 carries the precedent-departure defence so a future audit asking "why does this runbook have §7 when its sibling doesn't?" finds the answer self-defended inside the section. **Eight sections covered:** §1 framing + reading order + latitude + pre-existing flakes carry-forward; §2 sequence overview with four-commit table + files-touched + three Joe-lock checkpoints + what-this-milestone-CANNOT-close with dependency-chain diagram; §3 Commit 1 doc-pass with §6.4.3 content sketch + §15 row headline blockquote ~470 words; §4 Commit 2 primitive fix at `:193` with pre/post code + verbatim code-comment block + sibling Site 1 fix at `:321` with pre/post code + four seed tests + two optional tests + seven anti-drift guardrails; §5 Commit 3 Phase 9 Scenario 1 second `#[ignore]` lift with 8-green-runs verification minimum + `#[serial_test::serial]` asymmetry framing + exact doc-comment rewrite text + diagnostic playbook; §6 Commit 4 milestone close with six files atomic per D-074 + catalogue M15 row exact phrasing + JOURNAL J-NNN entry shape with seven sub-sections + CLAUDE.md PLAY block flip detail + ROADMAP.md flips detail; §7 discipline notes (six paragraphs covering precedent departure framing + sibling-in-shape third recurrence + inline-lock pattern third recurrence + split-session discipline + four-decision no-drift-surface discipline family with full table + "honest longer work over fast shortcuts" fourth recurrence within milestone scope); §8 cross-references (audit + design trilogy + DECISIONS.md eight entries + JOURNAL nine entries including J-098 expected at this commit + code surfaces five entries + Appendix J reference + operational state references). **Three Joe-lock checkpoints recorded for Clair:** post-Commit-1 if doc-pass surfaces drift in the canonical design doc; pre-Commit-2 unit-test list proposal (three-to-five final list locked by Joe); post-Commit-2 / pre-Commit-3 primitive shape locked. **Four-commit Clair-facing sequence recorded** with files-touched + DoD checklists + anti-drift guardrails per commit. **Test-count discipline recorded** with the wire-order-determinism witness (`compute_federation_delta_byte_identical_across_two_senders`) as the load-bearing fourth unit test — structural sibling to bidirectional's `apply_federation_add_two_vantages_mirror`. **What this milestone CANNOT close** framed at four sites (§2.4 + §6.5 + §6.6 + §6.7 final DoD): Commit 4 closes the topological-sort milestone only; Phase 9 Commit 3b unblocks → resumes (Scenarios 2 + 3 + compound scenarios C2/C3/C5/C7/C9/C10, ~5-7 atomic commits in their own sequence); Phase 9 milestone stays PLAY; Federation Event Propagation milestone stays PLAY; M6 (new) + XGID Retrofit Pass 1 stay PENDING. **J-NNN placeholders at three sites** that freeze together at Commit 4 (canonical-design-doc §15 row + doc-comment in `phase9_two_node_smoke.rs` + catalogue M15 row in `tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY_FINDINGS.md`) — all three reference the same milestone-close J-number, NOT J-098 which is this runbook-landing J-number. Inline-lock pattern third recurrence: three Joe-locks from design phase are treated as already-decided rather than re-walked; Clair reading the runbook sees implementation steps, not litigation steps. Split-session discipline: runbook is its own session-arc (audit session + design session + this runbook-authoring session + Clair's four-commit arc = four cleanly-separated session-arcs across the phase). **J-098 shipped across two commits per honest provenance**: original runbook-landing commit shipped `tasks/FEDERATION_TOPOSORT_IMPL.md` only; companion-updates housekeeping atom (CLAUDE.md + ROADMAP.md + JOURNAL.md) landed separately because Chat Claude drafted the three companion-file edits as chat prose without writing them to disk turn-by-turn, missing D-074 same-commit-atomicity at the original commit; slip framed explicitly in JOURNAL J-098 per D-065 honest-behaviour-over-polite-behaviour. Discipline lesson recorded: when drafting multi-file commits, write each file edit to disk via `Filesystem:edit_file` before moving to the next file's draft, not as prose-then-batch — prose-then-batch defers tool calls past the point where confirmation requests trigger, breaking the implicit assumption that drafted content has landed. Clair-facing entry point: `CLAUDE.md` PLAY block → `tasks/FEDERATION_TOPOSORT_IMPL.md` §1 reading order → design task file + DECISIONS.md D-076 + audit doc + canonical design doc → back to runbook §3 onward for per-commit work.

✅ **Topological-sort design-phase re-walk Step 2 SHIPPED 2026-05-22** (J-099; eight-file atomic commit per D-074: audit doc §11 amendment (v1.0 → v1.1) + design doc §11 amendment (v1.0 → v1.1) + DECISIONS.md D-076 in-place amendment + JOURNAL J-099 + CLAUDE.md Rule 0 addition + PLAY block flip + ROADMAP.md v1.14 → v1.15 + Step-2 HANDOFF Status flip + new Step-3 HANDOFF; no code changes, documentation only; 577 + 1 ignored unchanged across this work). Procedural shape is **Shape 2** per Joe-lock at J-098 session close — targeted patch via in-place amendments to canonical-record documents rather than full audit→design→impl re-walk. **The framing gap.** Clair's Commit 3 verification (post-J-098) confirmed that Commit 2's Shape A v1 sort fix satisfies D-076 v1's stated contract (byte-identical wire output across senders) but does NOT close Phase 9 Scenario 1: the canonical wire order is deterministic-across-senders but ~50% of nonce rolls produce a wire order where `state.room_create` precedes `state.space_create`. Concretely, `build_room_create_event` at `xgen-core/src/space/state.rs:797` constructs the event with `prev_events: vec![]` despite the function's own doc-comment claiming `space_id` is "the event_id of the parent state.space_create"; both `state.space_create` and `state.room_create` end up as DAG roots tied at the top of the topological sort; lex-by-event_id picks a canonical order but the chosen order is not causal-DAG-respecting; the receiver's `dispatch_event` Step 1 still rejects `state.room_create` with "space not found" and the bootstrap cascade still produces 2 Accepted / 2 Rejected / 101 HeldPending. The unstated assumption in Q3's framing (Q3 asked "is determinism normative?" but did not ask "what semantic property must the canonical order satisfy?") was that any deterministic canonical order would also be a causally-correct order; that assumption was false for events the event-DAG layer treats as roots despite a higher-layer parent-child relationship. **Path B locked as the fix shape.** Modify `build_room_create_event` to set `prev_events: vec![space_id.to_string()]` so the event-DAG honestly reflects the protocol-level parent-child relationship; `state.room_create` becomes a non-root event whose predecessor is `state.space_create`; the topological sort places it after the parent regardless of tie-break logic. **Commit 2's Shape A v1 sort fix stays useful** at the determinism layer beneath the causality layer (not reverted; safety net for events that legitimately tie at the DAG layer such as true sibling roots with no protocol-level ordering constraint). The two fixes layer cleanly: causality first (Path B at DAG-construction layer), determinism second (Commit 2 sort at tie-break layer); neither sufficient alone. **Path B scope narrow by Joe-lock.** `build_room_create_event` only; sibling event constructors (`state.federation_add`, `membership.*`, `message.*`, etc.) NOT audited this milestone for similar `prev_events` lies; that audit may surface later as own audit-precedes-dependent-design arc per D-071 if dependent work surfaces need. Rejected alternatives at re-walk: Path A (EventType-priority sort at topo primitive — wrong layer; encodes protocol-level relationships in sort policy where they should live in `prev_events`; would need extension for every new event family with semantic ordering constraints); Path C (broader sibling-constructor audit — own future phase if dependent work surfaces need; broadening to Path C would require Shape 1 (full re-walk) which Joe explicitly rejected at J-098 session close). **D-076 amended in place** — NOT split into D-076 + D-077. The two properties (byte-identical-across-senders determinism + causal-DAG-respecting order) cannot vary independently: a causal-but-non-deterministic wire format is broken (loses cross-Node debugging + MLS coupling benefits); a deterministic-but-non-causal wire format is broken (exactly what this milestone surfaced). Splitting would also have broken the no-drift-surface discipline family's one-per-layer shape (D-067 + D-070 + D-075 + D-076 across code-organisation + transport + event-model + wire-format). The amendment lives in a new subsection between the original "Decision" subsection (v1 prose stays authoritative) and the existing "Originating incident" subsection. **CLAUDE.md Rule 0 added** as fourth member of MANDATORY Behaviour rules: mandatory session-open reading sequence (CLAUDE.md PLAY block → latest JOURNAL entry → ACTIVE HANDOFF notes in `tasks/` → then whatever document Joe pointed at); holds regardless of how the session is opened; narrow pointer is treated as "expand to context, then read X." Rule 0 originated from the post-J-098 session-open failure where Chat Claude read the runbook in isolation when the runbook was two commits stale, missing a Joe-lock entirely. Sibling-shape to how D-070 / D-075 / D-076 v1 originated from earlier surface moments — discipline failures that surface are the project's mechanism for converting tacit expectations into explicit rules. Rule 0's surface is meta-level (session-open discipline) rather than protocol-level. **Five-step session arc:** Step 1 (J-098-session-close Joe-lock conversation) ✅ closed; Step 2 (this entry's atomic commit) ✅ closed; Step 3 (runbook v1.0 → v1.1 revision per `tasks/HANDOFF_TOPOSORT_RUNBOOK_REVISION.md` ACTIVE v1.0) 🟡 next-active; Clair's Commit-2-amendment + Commit 3 + Commit 4 🟡 awaiting Step 3 close; topological-sort milestone close 🟡 awaiting Clair. Clair stays stood down until Step 3 closes; her Commit 3 working tree (`xgen-node/src/tests/phase9_two_node_smoke.rs`, uncommitted) remains as sentinel per Joe-lock. **"Honest longer work over fast shortcuts" — fifth recurrence within Federation Event Propagation milestone scope** (Phase 7.5 first; bidirectional second; topological-sort design close J-097 third; topological-sort runbook landing J-098 fourth; topological-sort design-phase re-walk Step 2 fifth). Discipline notes: D-074 fifth application (JOURNAL.md in Step-2 atomic commit's changed-files list per established convention); D-065 honest-behaviour discipline applied throughout (amendments framed as honest extensions of v1.0 content, not as if v1.0 was always correct); D-076 family-completion preserved (D-076's amendment extends within the wire-format layer; no D-077 sibling; the four-decision family stays at four-per-layer shape).

### Persistence-amendment sub-amendment milestone

✅ **Persistence-amendment sub-amendment implementation milestone CLOSED 2026-05-24 at J-108** (twelve-file atomic commit per D-074 eleventh instance). Five-commit Clair-facing sequence + re-walk Track 1 shipped: Commit 1 `0ca29e6` doc-pass + Commit 2 `f4f0e4e` Q1 ingest-path + Commit 2a `c88fd73` Q2+Q3 dispatch+persist + Commit 3 `a677244` sentinel-tree refinement + verify (8/8 GREEN; 5 isolated + 3 workspace) + re-walk Track 1 `b9a30da` at J-107 (D-077 promoted) + this Commit 4 milestone close. **Phase 9 Commit 3b-1 collapsed into this milestone close per Q4(a) lock**; sentinel-tree four files at `xgen-node/src/tests/` (shipped atomic at Commit 3) crown as activating integration-level regression lock; Scenario 3 transition FAIL → PASS verifies the persistence fix end-to-end at integration level. **Five Joe-locks closed across milestone Q1→Q4 walk + re-walk Y-lock**: Q1 (a).iii.α (reverted from (a).iii.β at J-107 per Y-lock on cross-milestone Phase 7 B3 amendment dependency: B3's `state.federation_add` arriving via federation channel intentionally has missing predecessors, and B3 implicitly relied on the silent-discard at `xgen-core/src/node/runtime.rs:181` as a feature; Result-propagation would have broken B3 at the SpaceState mutation layer) + Q1 (a).ii defensive sort-on-replay at `replay_spaces_from_dir` retained; Q2 (a) return-vector (`DispatchOutcome::Accepted { new_joiner, additional_persisted: Vec<Event> }`); Q3 all-three drain helpers (`drain_pending_uniform` + `drain_pending_by_identity` + `drain_pending_by_federation_relationship`) return `Vec<Event>`; Q4 (a) sentinel-tree in-scope at milestone close (Commit 3b-1 collapses). **D-077 promoted at J-107 Track 1**: bidirectional sustainability discipline at silent-discard / conditional-mutation / fallible-discard sites — meta-layer above D-067 + D-070 + D-075 + D-076 v1.1 protocol-layer no-drift-surface family. **Layered-B3 second project-wide instance closes atomically** (drain-hook layer Q2+Q3 + runtime.rs:181 silent-discard layer Q1; sibling-shape to topo-sort Commit 2a J-101 first instance; two instances is not yet durable pattern — three would be). **Candidate D-NNN "ingest path invariant encoding under bidirectional sustainability discipline"** stays flagged-not-promoted at design doc §8 with scope expanded at J-107. **Grep guardrail scope discipline codified at JOURNAL J-108 sub-section 8 as fifth project instance of surfacing-gap-becomes-codified-discipline pattern**: rule's scope is freeze-site sources (canonical code/spec/test docs hosting J-NNN placeholders), NOT narrative prose in milestone-event documents (CLAUDE.md + JOURNAL.md entries) which use J-NNN as historical pointer at authoring time. Verification command updated form: `grep -rn 'J-NNN' . --include='*.rs' --include='docs/*.md' --include='tasks/*.md'` MUST return ZERO post-Commit-4; unconstrained grep returns non-zero by design (~30 narrative-prose hits expected). Flag for runbook §6.7 + §6.8 amendment at next sibling milestone. Sibling-shape lineage: D-070 + D-075 + D-076 v1 → v1.1 + Rule 0 + D-077 + this guardrail discipline form the pattern (five instances make it durable). **Ten files in this atomic commit**: JOURNAL.md (J-108 entry, eight sub-sections) + CLAUDE.md (PLAY block flipped from "persistence-amendment runbook authoring shipped; Clair pickup at Commit 1" framing (re-walk Track 1 framing) to "Phase 9 Commit 3b-2-equivalent RESUMES" + header chain) + docs/ROADMAP.md v1.22 → v1.23 (this entry) + tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md Status ACTIVE → COMPLETED v1.2 + body J-NNN → J-108 freezes (~46 sites) + tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md header chain + body J-NNN → J-108 freezes (8 sites) + tasks/FEDERATION_PROPAGATION_PHASE_9.md header `Last updated` (Commit 3b-1 collapsed; RESUMES at Commit 3b-2-equivalent) + tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY_FINDINGS.md new M16 catalogue row + "Caught by Phase 9 set" count 12 of 15 → 13 of 16 + docs/xgen_federation_propagation_design.md §15 row J-NNN → J-108 freeze + Q1 a.iii.α body correction + xgen-core/src/node/runtime.rs three J-NNN code-comment freezes (lines 74, 216, 832) + xgen-node/src/app.rs one J-NNN code-comment freeze (line 2768). HANDOFFs NOT in Commit 4 (both COMPLETED on remote at J-107). Sentinel-tree files already shipped at Commit 3 with no J-NNN markers in their doc-comments (authored with the milestone reference, not a J-NNN placeholder). **Workspace test count at Commit 3 baseline**: 592 (xgen-core 431 / xgen-node 68 / xgen-common 24 / xgen-client 47 / integration buckets 7,6,5,2,1,1). Delta across milestone: +7 (+2 Commit 2 + +5 Commit 2a + +0 Commit 3 unit tests because §3 work is harness-refinement; sentinel tests at `phase9_three_node_anti_transitivity.rs` + `phase9_drop_and_recover.rs` are themselves the regression locks). No test changes at this Commit 4 per runbook §6.7 DoD. Pre-existing flakes (precedence env-var race; reconnect_with_existing_tip_small_delta_delivered) did NOT fire during 8/8 verification runs. **"Honest longer work over fast shortcuts" — count inherited at eighth from J-104, NOT incremented at this milestone close** (close-event-not-recurrence-event sibling-shape to topo-sort J-101's seventh-recurrence framing). **D-074 application count: eleventh instance** (J-095 first; J-096 second; J-097 third; J-098-across-two-commits fourth; J-099 fifth; J-100 sixth; J-101 seventh; J-105 eighth; J-106 ninth; J-107 tenth; this J-108 eleventh). **Phase 9 milestone stays PLAY** (RESUMES at Commit 3b-2-equivalent with Scenarios 2 + compound scenarios C2/C5/C7/C9/C10; C3 dropped per Phase 9 §3.0 v1.1; ~4-6 atomic commits in their own sequence). **Federation Event Propagation milestone stays PLAY**. **M6 (new) + XGID Retrofit Pass 1 stay PENDING**; dependency chain extended by one more node, depth unchanged in shape. **Sibling-in-shape fourth recurrence shape durable** — audit → design → runbook → implementation → milestone-close (Phase 7.5 first; bidirectional federation_nodes second; topological-sort third; persistence-amendment fourth). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + grep guardrail scope discipline (this commit's codification) discipline.

✅ **Track 1 re-walk canonical-record amendments shipped 2026-05-24 at J-107** (eight-file atomic commit per D-074 tenth instance — expanded from seven in original J-107 enumeration when bridge-handoff `tasks/HANDOFF_TRACK_1_SESSION_CLOSE_2026-05-24.md` folded into atomic per anti-tempfile-deletion-of-decision-records discipline). Y-lock cross-milestone Phase 7 B3 amendment dependency surfaced at Clair's Commit 2 forced revert of Q1 from (a).iii.β (`pub fn ingest_event(&mut self, event: Event) -> Result<(), GraphError>`) to (a).iii.α (binary-void signature + `tracing::error!` at the silent site + Q1(a).ii sort-on-replay defensive layer retained). B3 federation-bootstrap path (J-088, locked 2026-05-20) implicitly relied on `let _ = graph.add_event(...)` silent-discard as a feature — `state.federation_add` event lands in EventStore + mutates SpaceState.federation_nodes even though graph.add_event returns UnknownPrevEvent; Result-propagation would have broken B3 at SpaceState mutation layer. Five resolution options walked with Joe (α conditional-exempt rejected on principle; β refactor B3 out of ingest_event ~80 lines; γ accept regression maximally expensive; δ test-only public surface rejected as permanent escape hatch; ε tristate Result<IngestOutcome, GraphError> with Ok(DagSkipped) ~40-50 lines; ζ ship ε + flag broader audit); then **Option X** (apply bidirectional sustainability broadly 80-200 lines across 4-7 sites + 2-4 cascading session-arcs) vs **Option Y** (revert to (a).iii.α + name discipline + document broader audit as future-walk material — ~5-10 lines + docs). **Joe locked Y on error-loop-risk grounds.** **D-077 promoted** as new principle: bidirectional sustainability discipline at silent-discard / fallible-discard sites — sustainability question MUST be asked in both directions (forward-drift AND backward-coherence). D-077 sits at meta-layer above no-drift-surface discipline family (D-067 + D-070 + D-075 + D-076 v1.1 at protocol layers; D-077 + Rule 0 at meta layer). Origin pattern sibling-shape to D-076 v1 → v1.1 + Rule 0 + D-075: principle-stated → implementation surfaced gap → amendment makes the missing dimension explicit (four project instances make the pattern durable). **Track-1-while-Clair-active first project instance** — directional asymmetry from topo-sort J-099/J-100 Track-1-as-decision-input precedent (here Track 1 is record-of-already-locked-decision — Y-lock made in conversation with Clair at session close before Track 1 was authored; Track 1's role is to write the locked decision into canonical record, not surface a new decision for Clair to consume). **Candidate D-NNN "ingest path invariant encoding" expanded scope at this re-walk** to cover five `ingest_event` silents + three drain helpers + M6 reject paths + B3 apply_event dependency; promotion trigger Joe-lock OR dependent-work concrete drift per D-071 surface-driven application. **Four milestone-close freeze sites enumerated by file:line** in J-107 discipline-notes sub-section as actionable form of J-101 enumeration lesson: `xgen-core/src/node/runtime.rs:181` + `docs/xgen_federation_propagation_design.md` §15 row + `tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY_FINDINGS.md` catalogue M16 row + sentinel-tree doc-comments at `xgen-node/src/tests/phase9_*.rs` — all must freeze in Commit 4 with `grep -rn 'J-NNN' .` from project root returning ZERO matches after staging per runbook §6.7 + §6.8 anti-drift guardrail. Bridge-handoff retained as historical record of session-close mechanism (first project instance of Chat-Claude-mid-Track-1-session-close pattern; sibling-shape precedent for future recurrences). **"Honest longer work over fast shortcuts" — stays inherited at eighth per within-milestone Shape-2 amendment framing**, NOT incremented at this re-walk surface (sibling-shape to topo-sort re-walk Step 2 J-099 + Step 3 J-100 staying at fifth + sixth recurrence within Federation milestone scope, not opening seventh + eighth). **D-074 application count: tenth instance** (J-095 first; J-096 second; J-097 third; J-098-across-two-commits fourth; J-099 fifth; J-100 sixth; J-101 seventh; J-105 eighth; J-106 ninth; this J-107 tenth). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 (this commit's promotion) discipline.

✅ **Persistence-amendment Commit 3 shipped 2026-05-24 at `a677244`** (Phase 7.5 sentinel-tree refinement + verify per runbook §5; four-file atomic per Joe-lock checkpoint #5 fold-into-Commit-3 ruling). Five sub-tasks closed: §3.1 `SavedNodeState` six-field struct + `shutdown_keep_data` method at latitude; §3.2 `spawn_in_process_node_with_state` free function at latitude (no structural divergence per Joe-lock); §3.3 sentinel-tree doc-comment J-NNN literal token retained for Commit 4 freeze; §3.4 `mod.rs` flip enabling sentinel mods first-class; §3.5 verification rigour 5 isolated (cargo clean between each) + 3 workspace = **8/8 GREEN** runs minimum per runbook §5.3; pre-existing flakes (precedence env-var race; reconnect_with_existing_tip_small_delta_delivered) did not fire in any of the 8 runs. **Three within-Commit-3 audit gaps closed atomically per D-077 first worked instance** (D-077 promoted three commits back at Track 1 J-107; first surface application immediately after promotion): (1) **abort-fold** — sentinel docstring at `phase9_drop_and_recover.rs:17-19` specifies `shutdown_keep_data` must "abort in-flight connection tasks"; existing `shutdown` did NOT track per-connection JoinHandles; Joe-locked Option α refinement-fold; added `connection_handles: Arc<Mutex<Vec<JoinHandle<()>>>>` field on `InProcessNode` + symmetric `.abort()` in BOTH shutdown methods + push-to-tracker in both spawn helpers; (2) **identity-registry-persist** — sentinel docstring at `phase9_drop_and_recover.rs:20-23` promised "replays identity registry from disk"; harness `register_identity` mutated in-memory `runtime.identity_registry` but did NOT persist to `identities_path`; production sibling at `accept_registration` app.rs:1628 does persist via `save()`; Joe-locked Option (α-1) production-sibling symmetric application; added `rt.identity_registry.save(&identities_path)` after in-memory mutation; (3) **space-event-store-persist** — sentinel docstring same sentence promised "replays Space event stores from disk"; harness `submit_locally` + `ingest` missed `persist_event` calls; production sibling at `process_inbound:~1505` with `additional_persisted` aggregation per Commit 2a; added `persist_event` calls + `submit_locally` iterates Commit 2a additional_persisted loop. **Federation-registry-persist audited and confirmed safe** per D-077 forward-AND-backward sustainability discipline: production downstream path `attempt_reconnect` → `run_federation_session_post_handshake` → `reg.save(&federation_registry_path)` at app.rs:1295 on handshake-ACTIVE persists final on-disk state correctly; no harness change needed. **Cumulative within-milestone audit-gap count after Commit 3 prospective sweep: five** (sentinel-tree compile-blocker + test #4 infeasibility + B3 dependency + Commit 3's three gaps). Sentinel-tree `phase9_drop_and_recover` + `phase9_three_node_anti_transitivity` transitioned from "uncommitted verification-only toggle through Commits 2 + 2a" to **active integration regression locks committed at Commit 3**. xgen-node test count 66 → 68 (+2 active sentinel scenarios).

✅ **Persistence-amendment Commit 2a shipped 2026-05-23 at `c88fd73`** (Q2 + Q3 return-vector + `additional_persisted` aggregation per runbook §4a; four-file atomic). `DispatchOutcome::Accepted { new_joiner, additional_persisted: Vec<Event> }` plumbed through three drain helpers (`drain_pending_uniform`, `drain_pending_by_identity`, `drain_pending_by_federation_relationship`) → `dispatch_event` aggregation via concatenation → `xgen-node/src/app.rs::process_inbound` + `handle_identity_replicate_msg` persist-loops iterate `additional_persisted` and call `persist_event` for each. All 8 sites locked at Joe-lock checkpoint #4 refined: 1 enum doc + 3 drain-helper docs + 2 dispatch_event inline + 2 xgen-node persist-loop inline. **Five new unit tests** green covering the return-vector mechanics. Two test-discovery loops surfaced + corrected before commit (test 3 needed two MembershipJoin events for Space + Room level; test 4 needed `build_message_text_event` for prev_events-before-signing per Step 8 canonical-hash check). Layer separation preserved: xgen-core stays I/O-free; pairs cleanly with Commit 2's Q1 Result chain.

✅ **Persistence-amendment Commit 2 shipped 2026-05-23 at `f4f0e4e`** (Q1 (a).iii.α binary-void signature + `tracing::error!` at silent site + Q1(a).ii sort-on-replay defensive layer + verbatim code-comment block at `xgen-core/src/node/runtime.rs:181` per runbook §4; two-file atomic). **Y-lock revert from (a).iii.β → (a).iii.α** under cross-milestone Phase 7 B3 amendment dependency: B3 federation-bootstrap path (J-088, locked 2026-05-20) implicitly relied on the silent-discard as a feature — federation_add event lands in EventStore + mutates SpaceState.federation_nodes even though graph.add_event returns UnknownPrevEvent. (a).iii.β `?` propagation would have broken B3 at SpaceState mutation layer. Joe locked Option Y on error-loop-risk grounds at session close in conversation with Clair before Track 1 was authored. Test count delta **+2** (tests 3 + 5 retained; tests 1+2 dropped on Result-shape revert — regression target evaporates under binary-void signature; test 4 dropped on Clair's structural-infeasibility trace — `validate_event` Step 9 and `graph.add_event` consult same `EventStore.contains()` in single-threaded flow, no interleaved mutation point for shape (a) mod-tests-internal field mutation locked at original checkpoint #2). **B3 regression-lock `b3_federation_add_via_federation_skips_step_9_predecessor` green isolated** under (a).iii.α per package-scoped Option C verification path. Workspace 8-green-run rigour deferred to Commit 3 per runbook §5.3 (sentinel-tree files reference `spawn_in_process_node_with_state` + `InProcessNode::shutdown_keep_data` not present until Commit 3 structural rework). Verbatim code-comment block at runtime.rs:181 carries milestone-level J-NNN placeholder freezing at Commit 4 milestone close per scope-vs-chronology framing (semantic scope is milestone-level reference, not Commit-2-level).

✅ **Persistence-amendment audit doc shipped 2026-05-23 at J-104** (`tasks/PHASE_7_5_PERSISTENCE_AMENDMENT.md` Status: ACTIVE v1.0 at J-104; COMPLETED v1.1 at design-phase close J-105; body §1–§11 stays authoritative as historical record of audit-at-lock-time per topo-sort + bidirectional precedents — NOT ARCHIVED, milestone-internal sub-amendment audit has different artifact lifecycle than J-081's project-wide subsystem audit). Sibling-in-shape to `tasks/FEDERATION_TOPOSORT_AUDIT.md` (COMPLETED v1.1) and `tasks/FEDERATION_BIDIRECTIONAL_NODES_AUDIT.md` (COMPLETED v1.0). **Audit doc surfaces:** (a) the drain-without-persist gap at the `xgen-core` ↔ `xgen-node` layer boundary — three drain hooks (`drain_pending_uniform` at `xgen-core/src/node/runtime.rs:670-682` Phase 4 / F-4a, `drain_pending_by_identity` line ~745 Phase 6 / F-10, `drain_pending_by_federation_relationship` line ~795 Phase 7.5 / F-3) re-dispatch released events INSIDE `dispatch_event` and silently drop the Accepted outcome via `let _ = self.dispatch_event(...)`; `xgen-node/src/app.rs::process_inbound` only persists the explicitly-passed event, never the drained ones; on Node restart `replay_spaces_from_dir` only sees the persisted events, and `graph.add_event` silently rejects events whose predecessors aren't in store; in-flight relationship state un-replayable; (b) the secondary layered silent-discard surface at `xgen-core/src/node/runtime.rs:181` `let _ = graph.add_event(&event, store);` serving two different masters at two different call sites; (c) the cascade trace through Scenario 3 implementation showing dispatch-time → persist-time → restart-time → replay-time gap propagation; (d) the originating architectural move at Phase 7.5 Commit 3.5 (J-103) hook lift from `process_inbound` into `dispatch_event` Step 7 — the lift was correct in shape (mirror of Phase 6 Identity hook architecture) but the persistence contract didn't follow; (e) four-question set carried open to design phase per D-069 audit-vs-design boundary discipline. Atomic single-file commit at J-104 (just JOURNAL.md) per J-098 atomic-single-file lesson; sentinel working tree retained uncommitted as verification artifact for the sub-amendment close — sibling-shape to how `phase9_two_node_smoke.rs` sat uncommitted during the topological-sort arc at J-099/J-100. Workspace test state honestly captured: 578 PASS + 1 FAIL = 579 attempted — Scenario 3's failure IS the production gap evidence per Rule 1, not suppressed.

✅ **Persistence-amendment implementation runbook shipped 2026-05-23 at J-106** (`tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` Status: ACTIVE v1.0, ~95 KB, eight sections — §1 framing + reading order + latitude + pre-existing flakes carry-forward + §2 sequence overview with five-commit table + files-touched roll-up + five Joe-lock checkpoints + what-this-milestone-CANNOT-close + §3 Commit 1 doc-pass + §4 Commit 2 Q1 ingest-path Result-handling + §4a Commit 2a Q2+Q3 dispatch+persist + §5 Commit 3 sentinel-tree refinement + verify + §6 Commit 4 milestone close + §7 discipline notes (§7.1 precedent-departure self-defense / §7.2 inline-lock pattern fourth recurrence / §7.3 sibling-in-shape fourth recurrence / §7.4 layered-B3 second project-wide instance / §7.5 candidate D-NNN flag visibility at three layers / §7.6 honest-longer-work count-inherited-at-eighth / §7.7 Commit-3b-1-collapse honest framing; §7.8 skipped per runbook-authoring §7 lock) + §8 cross-references; flips to COMPLETED v1.1 in Commit 4 of Clair's implementation arc per the runbook's §6 milestone-close DoD). Sibling-in-shape to `tasks/FEDERATION_TOPOSORT_IMPL.md` (COMPLETED v1.2, ~93 KB) — same eight-section shape; same five-commit Clair-facing sequence shape (1 doc / 2 Q1 / 2a Q2+Q3 / 3 sentinel+verify / 4 close); same §7 discipline-notes inclusion with precedent-departure self-defense at §7.1; landed cleanly inside ~80-100 KB target band. **Four-file atomic commit per D-074 ninth instance** (sibling-shape to topo-sort J-098 runbook-landing housekeeping atom shape, but landed atomic at first attempt rather than as fix-up housekeeping atom per the J-098 + J-099 + J-100 prose-then-batch family of slip-and-correct precedents now being avoided by writing each companion file to disk + verifying via `Filesystem:get_file_info` before the next): (1) `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` NEW ACTIVE v1.0; (2) JOURNAL J-106 entry; (3) CLAUDE.md PLAY block flipped from "persistence-amendment milestone design ✅; implementation runbook authoring next-active for Chat Claude + Joe" to "persistence-amendment runbook authoring shipped; Clair pickup at Commit 1" + header bump; (4) this ROADMAP.md v1.20 → v1.21. **Six runbook-structural Joe-locks** baked into runbook prose at §1.1 + §2.3: (1) Five-commit shape (1 doc-pass / 2 Q1 ingest-path / 2a Q2+Q3 dispatch+persist / 3 sentinel+verify / 4 close); (2) Five Joe-lock checkpoints (#1 post-Commit-1 doc-pass drift / #2 pre-Commit-2 unit-test list proposal / #3 post-Commit-2 / pre-Commit-2a primitive shape / #4 pre-Commit-2a verbatim code-comment block content with rungs-list bullet for ValidatedEvent wrapper + sealed traits + visitor pattern + formal verification rungs above (a).iii.β / #5 post-Commit-2a / pre-Commit-3 sentinel-tree refinement scope); (3) Verification rigour 5 isolated + 3 workspace = 8 green runs minimum at Commit 3, sibling-shape to topo-sort J-101 verification rigour; (4) Sentinel-tree refinement folded into Commit 3 with refinement-vs-rework distinction explicit at §5.2 (refinement = doc-comment changes, assertion-message rewording, helper renames, J-NNN freezes, test-data-structure shape updates; rework = scenario renames/splits/collapses, assertion-shape changes, new helper introductions — rework escalates to Checkpoint #5); (5) §15 row of canonical design doc lands in Commit 1 with J-NNN placeholder; placeholder freezes at Commit 4 to milestone-close J-number; (6) §7 discipline notes section with six sub-sections; §7.8 skipped per runbook-authoring §7 lock (Filesystem-tool meta-discipline doesn't belong in runbook prose; lesson stays at JOURNAL J-098 + J-099 + J-106 entries). **Two pre-draft code-trace findings shaped §4 + §4a narrow scope**: **Finding A — Q1 narrow scope**: pre-draft code-trace pass on `xgen-core/src/node/runtime.rs::ingest_event` (lines ~180-235) surfaced FIVE silent-discard sites total beyond the one design doc §3 named: line ~190 `event_id`-missing-return (unsigned event guard); line ~210 `let _ = graph.add_event(&event, store);` (THE site Q1 scope covers); line ~212 `let _ = store.insert(event.clone());` (duplicate-event idempotency guard); line ~221 `let _ = state.apply_event(&ev, &my_node_id);` (StateSpaceCreate replay loop, SpaceState mutation error swallow); line ~228 `let _ = state.apply_event(&event, &my_node_id);` (default branch, SpaceState mutation error swallow). The four out-of-scope sites belong to design doc §8 candidate D-NNN "ingest path invariant encoding" future-walk question, NOT this milestone's scope; runbook §4.1 records narrow scope explicitly and §4.10 anti-drift guardrail #1 reinforces it; sibling-shape to topo-sort runbook §4's narrow scope for `build_room_create_event` only (sibling event constructors NOT audited that milestone). **Finding B — Recursive drain Shape β2**: pre-draft trace of three drain helpers (`drain_pending_uniform` ~line 670; `drain_pending_by_identity` ~line 745; `drain_pending_by_federation_relationship` ~line 795) surfaced exact silent-Accepted-discard pattern design doc Q2 names. Two candidate shapes considered: Shape β1 (accumulator threaded through recursion — outer caller passes `&mut Vec<Event>` accumulator that inner levels mutate) and Shape β2 (each drain level returns its own `Vec<Event>`; outer concatenates). Joe-lock at β2 on five grounds at runbook §4a.4: (1) self-documenting signatures; (2) easier code-review and regression-trace; (3) bounded recursion depth (~5-15 events in typical cold-start bootstrap) makes Vec allocation cost negligible; (4) sibling-shape to existing `drain_pending_messages` pattern in runtime.rs; (5) avoids "outer caller forgets accumulator" footgun in β1. Initial event NOT in returned vector — stays with `process_inbound`'s existing persist site (no behaviour change for common case); `additional_persisted` contains ONLY drained events; `process_inbound` gains `for ev in additional_persisted { persist_event(ev) }` block immediately after existing `persist_event(event)` call; per-event persist-failure handling is `tracing::warn` + continue per D-065 honest-broadening at §4a.6. **Seven additional pre-draft code-trace findings surfaced inline at §4 + §4a**: (1) `ingest_event` has five silent-discard sites total; (2) only one production caller in xgen-core (`dispatch_event` at line ~559; test callers in `phase_7_5_tests` mod number 6+, Clair audits during Commit 2 to enumerate); (3) xgen-node has its own `ingest_event` caller via `replay_spaces_from_dir` at `xgen-node/src/app.rs:2628` — Commit 2 is therefore a multi-crate atomic commit (xgen-core signature change + xgen-node sort-on-replay logic + tests across both crates; sibling-shape to multi-crate Commits in topo-sort milestone); (4) `dispatch_event` already handles drain-helper outcomes via `let _ = self.dispatch_event(ev, origin, None);` at lines ~643/680/720 — exactly the silent-Accepted-discard pattern Q2 closes; (5) `process_inbound` at `xgen-node/src/app.rs:~1500` (precise line Clair confirms at Commit 2a) is the production caller that needs `additional_persisted` handling; sibling block needed in `handle_identity_replicate_msg` for the drained events from `drain_pending_by_identity`; (6) `GraphError` visibility check needed (likely `pub(crate)` or non-`pub` currently; needs `pub` to appear in `ingest_event`'s public signature; additive widening, doesn't break callers; sibling-shape to XGID Adoption v1's `canonical_event_bytes` carry-over); (7) `topological_sort` re-export from xgen-core::node::runtime needed for `replay_spaces_from_dir`'s sort-on-replay; `pub(crate)` would scope to xgen-core only, requiring a sibling sort in xgen-node — rejected per D-067 + D-076 no-drift-surface family (single source of truth per logical responsibility); re-export as `pub` is the locked shape; Clair verifies during Checkpoint #3. **Verbatim code-comment block locked at Joe-lock checkpoint #4** (runbook §4.3, four structural elements + rungs-list bullet): (1) Reference to candidate D-NNN "ingest path invariant encoding" flagged at JOURNAL J-105 + design doc §8 (future-walk readers find the rung-above question without re-walking the design phase); (2) The silent-discard-pattern-served-two-masters framing from design doc §3 (`graph.add_event` at this site serves two different masters: dispatch_event where validate_event already guarantees predecessor presence; replay_spaces_from_dir where on-disk ordering can produce miss); (3) The rung-above-(a).iii.β three-line bullet (ValidatedEvent wrapper — compiler-forced correct path via type-constructor discipline; Sealed traits + visitor pattern — new-caller shape constraint; Formal verification — machine-checked invariants; note that nothing is future-proof in absolute terms; each rung protects against one more class of drift); (4) Narrow-scope note ("this is the immediate fix; future work flagged at JOURNAL J-105 + design doc §8 candidate D-NNN 'ingest path invariant encoding'; do not broaden scope without Joe-lock at audit phase"). Only place in the runbook where the rungs-list bullet appears at the touch-site (visibility per §7.5 layer 2); Clair confirms verbatim at checkpoint #4 before Commit 2a ships. **One MCP tool-call discipline data point surfaced during the three-Filesystem-write sequence** (`Filesystem:write_file` for §1+§2+§3 → `Filesystem:edit_file` for §4+§4a+§5 → `Filesystem:edit_file` for §6+§7+§8): verified each file on disk via `Filesystem:get_file_info` between writes per J-098 + J-099 prose-then-batch discipline; second write's first attempt returned `Input validation error: path expected string, received undefined` because Chat Claude omitted the `path` parameter in the tool call schema; single-response recovery (retry with `path` parameter populated landed cleanly); distinct in shape from J-098's prose-then-batch slip family — single-tool-call schema error recovered within same response without needing a fix-up atom commit; recorded per D-065 honest framing; no persistent canonical-record impact. **Sibling-in-shape fourth recurrence count at this commit**: Phase 7.5 first (J-093/J-104); bidirectional federation_nodes second (J-096); topological-sort third (J-097/J-098/J-099/J-100/J-101); this milestone fourth (J-104/J-105/J-106/upcoming milestone-close). Four recurrences make the audit → design → runbook → implementation → milestone-close shape durable. **Inline-lock pattern fourth recurrence**: bidirectional (Q2 inline + Q1 walked); topo-sort design phase J-097 (Q3 + Q2 + Q1 all inline); topo-sort re-walk J-099/J-100 (Q4 + Q1 supplement inline); this milestone J-105 (Q1 + Q2 + Q3 + Q4 all inline); four recurrences across three sibling-recurrence depths makes inline-lock posture default for audit-design closures. **Layered-B3 recurrence count: still second project-wide instance** (no increment at this runbook-landing event; pattern increments at code-shipping events, not runbook-authoring events). **"Honest longer work over fast shortcuts" — count inherited at eighth from J-104, NOT incremented at this runbook-authoring close** (recurrences counted at milestone-events; this is a within-milestone runbook-landing event, not a new milestone-surface event). **D-074 application count at this commit: ninth instance** (J-095 first; J-096 second; J-097 third; J-098-across-two-commits fourth; J-099 fifth; J-100 sixth; J-101 seventh; J-105 eighth; J-106 ninth — runbook-landing housekeeping atom per topo-sort J-098 precedent shape but landed atomic at first attempt per honest framing improvement; grep-at-authoring-time discipline per topo-sort J-101 sub-section 4 precedent — Clair re-runs `grep -c "D-074" JOURNAL.md` at Commit 4 of this milestone's implementation arc). **What this commit does NOT close.** Phase 9 Commit 3b-1 stays PAUSED inside milestone scope (collapses into persistence-amendment milestone close at Commit 4 per Q4(a) lock — Phase 9 resumes at Commit 3b-2-equivalent after this milestone close, with Scenarios 2 + compounds C2/C3/C5/C7/C9/C10 remaining as Phase 9's ~5-7 atomic-commit sequence per Q4 Lock from J-091). Phase 9 milestone stays PLAY. Federation Event Propagation milestone stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING. Dependency chain unchanged in shape (this runbook-landing is within-milestone; depth unchanged). Clair's stand-down ends with this commit; she picks up at Commit 1 per runbook §3 in a future session-arc. Sentinel working tree (four files at `xgen-node/src/tests/`: phase9_harness.rs, phase9_three_node_anti_transitivity.rs, phase9_drop_and_recover.rs, mod.rs) remains uncommitted as verification artifact for milestone close per Q4(a) lock — do NOT `git restore`. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 honest-behaviour-over-polite-behaviour discipline.

✅ **Persistence-amendment design phase closed 2026-05-23 at J-105** (`tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md` NEW ACTIVE v1.0; eleven sections sibling-in-shape to `tasks/FEDERATION_TOPOSORT_DESIGN.md` (COMPLETED v1.0); five-file atomic commit per D-074 eighth instance: design doc NEW ACTIVE v1.0 + audit doc Status ACTIVE → COMPLETED v1.1 + JOURNAL J-105 + CLAUDE.md PLAY block flipped + ROADMAP.md v1.19 → v1.20). **Four Joe-locks recorded across the Q1→Q4 walkthrough:** **Q1 → (a).ii + (a).iii.β + candidate D-NNN flag** — silent-discard pattern at `xgen-core/src/node/runtime.rs:181` serves two different masters at two different call sites (`dispatch_event` line 668 where `validate_event` already guarantees predecessor presence so UnknownPrevEvent should never fire, and `replay_spaces_from_dir` at `xgen-node/src/app.rs:2628` where on-disk ordering can produce predecessor-missing cases); this is not a single load-bearing-by-design invariant; Q1(c) falls. Three sub-shapes for Q1(a) considered: (a).i propagate error; (a).ii sort-on-replay; (a).iii harden silent-discard. Joe-lock at (a).ii + (a).iii.β — sort-on-replay in `replay_spaces_from_dir` as defensive layer + `ingest_event` signature change to `pub fn ingest_event(&mut self, event: Event) -> Result<(), GraphError>` for compiler-forced caller handling. **Q2 → (a) return-vector** — `dispatch_event` returns `DispatchOutcome::Accepted { new_joiner, additional_persisted: Vec<Event> }`; drain helpers return drained Accepted events to dispatch_event; aggregation into vector; `process_inbound` iterates and persists each. Layer separation preserved (xgen-core stays I/O-free); pairs cleanly with Q1's Result chain; doesn't reverse J-103's Commit 3.5 lift; doesn't promote to D-NNN family member prematurely (Q2(c) callback would have been fifth no-drift-surface family member alongside D-067/D-070/D-075/D-076 but persistence concern is narrow to drain hooks; promoting at this milestone treats one-milestone-surface as project-wide invariant). Rejected: Q2(b) re-lift (would reverse J-103 Phase 7.5 Commit 3.5); Q2(c) persistence callback (over-built; promotes one-milestone-surface to project-wide invariant prematurely). **Q3 → all three drain helpers** — `drain_pending_uniform`, `drain_pending_by_identity`, `drain_pending_by_federation_relationship` all get the Q2(a) return-vector treatment. Same gap pattern at all three sites; closing one and leaving two open is shipping a known-incomplete fix. Sibling-shape to topo-sort Commit 2a where the primary fix surfaced two more encodings of the invariant; all three closed atomically. Q1's type system (Result-returning ingest_event) forces interaction with (i) and (ii) anyway — either we handle their Result honestly (touched them) or silently discard (reintroduces a Q1-shape gap one milestone later). **Q4 → (a) in-scope** — four sentinel-tree files (`phase9_harness.rs`, `phase9_three_node_anti_transitivity.rs`, `phase9_drop_and_recover.rs`, `mod.rs`) ship atomic at milestone close as activating regression lock. Three reasons: audit's own verification contract names Scenario 3 as the activating regression lock at audit §8 (sibling-shape to Scenario 1's role for D-075 + D-076 v1.1 at J-101); refinement-risk argument decisive (design phase didn't fully verify J-104 harness shape is correct for the locked fix; implementation-runbook authoring will walk the harness in detail; refinements likely); milestone-separation argument doesn't hold (the sub-amendment milestone exists *because* Commit 3b-1 surfaced the persistence gap; Commit 3b-1's verification artefact IS the milestone's activating regression lock). Consequence: **Commit 3b-1 collapses into the sub-amendment milestone close.** Phase 9 resumes at Commit 3b-2-equivalent (compounds C2/C3/C5/C7/C9/C10 remaining). **No re-walk fired during Q1→Q4 walk per Lock #2 discipline.** Sibling-shape opportunity to topo-sort design-phase re-walk J-099/J-100 was preserved but not exercised; Q1's Result-returning ingest_event became a clean constraint on Q2 rather than a destabilising factor. The sustainability question's mid-walk reframing of Q1 (a).iii.α → (a).iii.β was upstream-refinement within Q1, not downstream-contradiction across Q1→Q2; correctly handled inline rather than as a re-walk. **The sustainability question's reframing of Q1** is itself worth recording as a discipline data point: when asked "is this future-proof?", Chat Claude initially answered (a).iii.β; the question then surfaced the rungs above — ValidatedEvent wrapper, sealed traits + visitor pattern, formal verification — and forced honest framing that (a).iii.β is more sustainable than (a).iii.α but is not absolutely future-proof. Resolution: lock (a).iii.β as the *immediate* answer (smallest correct-today fix that meaningfully raises the floor) AND flag candidate D-NNN "ingest path invariant encoding" as the project-level open question for future walk per D-069 audit-vs-design boundary discipline. Sibling-shape to D-076's v1 → v1.1 progression at different scope. **Layered-B3 recurrence pattern — second project-wide instance** (first instance topo-sort Commit 2a at J-101 — two layered surfaces at graph.rs `is_dag_root_type` + exchange.rs `validate_dag_structure` closed atomically per D-067 Option E; second instance this milestone — primary fix at drain-hook layer surfaces secondary silent-error encoding at runtime.rs:181 `graph.add_event` UnknownPrevEvent silent-discard, both closed atomically inside Q1+Q2+Q3 locks). Two instances is not yet a durable pattern; three would be. **"Honest longer work over fast shortcuts" — count inherited at eighth, not incremented at design close** (recurrences counted at milestone-events, not design-events; this design close is *inside* the eighth recurrence's milestone scope). **D-074 application count: eighth instance** (J-095 first; J-096 second; J-097 third; J-098-across-two-commits fourth; J-099 fifth; J-100 sixth; J-101 seventh; this commit eighth). **Trilogy-internal consistency** (per J-098 §7.1 grounds): design doc inherits topo-sort design doc's eleven-section shape rather than bidirectional design doc's nine-section shape because the persistence-amendment trilogy has topo-sort as its closer sibling precedent. **Precedent-departure self-defense — none required**; one shape-variant: candidate D-NNN flag at §8 (no equivalent in topo-sort design doc, which locked D-076 directly at design close because the second invariant surfaced cleanly at design time). Sibling-shape rationale: this milestone's parallel surface (ingest-path invariant encoding question) is at different scope from D-076's wire-format invariant — broader, longer-term, project-philosophy-touching — and audit-vs-design discipline says don't lock at design close what hasn't been walked through its own audit-design-impl arc. **Implementation runbook authoring is next-active for Chat Claude + Joe** at `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` per topo-sort precedent J-098. Runbook handoff requirements specified in design doc §9. Clair stays stood down through milestone close; sentinel working tree (four files at `xgen-node/src/tests/`) remains uncommitted as verification artifact for the milestone close per Q4(a) lock. Per D-065 + D-067 + D-069 + D-071 + D-074 discipline.

✅ **Topological-sort wire-order non-determinism implementation milestone CLOSED 2026-05-23** (J-101; eight-file atomic commit per D-074: JOURNAL.md J-101 entry + CLAUDE.md PLAY block flipped from "Clair pickup at Commit 2a → Commit 3 → Commit 4 ←── HERE" to "Phase 9 Commit 3b ←── HERE" + header bump + docs/ROADMAP.md v1.16 → v1.17 (this entry) + tasks/FEDERATION_TOPOSORT_IMPL.md Status ACTIVE → COMPLETED v1.2 + tasks/FEDERATION_PROPAGATION_PHASE_9_SURVEY_FINDINGS.md new M15 catalogue row + tasks/FEDERATION_PROPAGATION_PHASE_9.md header Last updated (Commit 3b RESUMED) + docs/xgen_federation_propagation_design.md §15 two J-NNN freezes (line 1140 → J-096 retroactive; line 1141 → J-101) + xgen-node/src/tests/phase9_two_node_smoke.rs J-NNN → J-101 freeze in doc-comment per runbook §5.5). **Five-commit Clair-facing sequence shipped under amended D-076 v1.1**: Commit 1 doc-pass (parents of `0543a86`) + Commit 2 determinism layer (`0543a86`) — Shape A v1 event_id lex sort at `xgen-node/src/fanout.rs:193` + sibling Site 1 sort at `:321` + four unit tests including the wire-order-determinism witness `compute_federation_delta_byte_identical_across_two_senders` + Commit 2a causality layer (`4a6fd74`) — Path B fix at `xgen-core/src/space/state.rs:797` `build_room_create_event` sets `prev_events: vec![space_id.to_string()]` so the event-DAG honestly reflects the protocol-level parent-child relationship the function's own doc-comment already claims + new unit test `room_create_event_records_space_create_as_predecessor` + Commit 3 Phase 9 Scenario 1 second `#[ignore]` lift (`b370dc7`) — doc-comment rewrite to five-event chronology + three-decision regression-lock framing (D-075 + D-076 v1.1 determinism layer + D-076 v1.1 causality layer) per runbook §5.5; `#[serial_test::serial]` posture default-keep silent per §5.4; 5 isolated (cargo clean between each) + 3 workspace = 8 green runs minimum verification rigour per §5.3, all 8 passed + Commit 4 milestone close per D-074 (this commit). **Commit 2a expansion under Option E** — scoped to one file at runbook authoring (xgen-core/src/space/state.rs build_room_create_event); during implementation the initial Path B edit produced 13 xgen-core failures with `RootEventHasPrevEvents("state.room_create")` — a validator-layer encoding of the DAG-root invariant at xgen-core/src/dag/graph.rs::is_dag_root_type rejected the post-Path-B event shape. Narrowing `is_dag_root_type` (drop `StateRoomCreate`; raise visibility to `pub(crate)`) surfaced a second B3-shape gap: `validate_dag_structure` at xgen-core/src/message/exchange.rs:550 re-encoded the same DAG-root set inline via its own `matches!` block. Per D-067 no-drift-surface discipline, Option E unification: inline encoding replaced with delegated call to `is_dag_root_type`. Two sites collapsed into one canonical encoding. Plus Posture β dag-test fixture updates across 17 sites in 4 files (graph.rs 7 + mod.rs 7 + pending.rs 2 + store.rs 1) switching `EventType::StateRoomCreate` → `EventType::StateSpaceCreate` for codebase-wide invariant coherence. Commit 2a ships as six-file atomic per D-074. **Two layered B3-shape surfaces closed atomically within Commit 2a** — structurally novel pattern; the primary fix (Path B at construction layer) traversed two sibling-layer encodings of the same DAG-root invariant before codebase coherence. Bidirectional Commit 2.5 (J-096) closed one B3 surface; Commit 2a closed two layered surfaces (graph.rs then exchange.rs). Future audit work should look for layered B3 surfaces (Commit 2a's pattern), not just single ones. **Pre-existing flakes did not fire** in any of the 8 verification runs (precedence env-var race per D-068; reconnect_with_existing_tip_small_delta_delivered from Phase 3). **D-074 application count via grep at authoring time**: `grep -c "D-074" JOURNAL.md` returns 21 mentions; simple-counting milestone-close applications J-095 first, J-096 second, J-097 third, J-098-across-two-commits fourth, J-099 fifth, J-100 sixth, J-101 (this commit) seventh. **"Honest longer work over fast shortcuts" — seventh recurrence within Federation Event Propagation milestone scope** (Phase 7.5 + bidirectional + topological-sort design close J-097 + runbook landing J-098 + re-walk Step 2 J-099 + re-walk Step 3 J-100 + topological-sort implementation J-101 — the full audit → design → re-walk → implementation arc is itself the seventh instance). **D-071 fifth project-wide instance closes** at this commit. **Prose-then-batch atomicity-slip family — fourth recurrence acknowledged**: design doc §15 line 1140 retroactive J-096 freeze (bidirectional milestone close authoring intended this freeze but the placeholder did not land in J-096's Commit 4 commit; gap surfaced at Commit 4 authoring of THIS milestone; sibling-shape to J-094 + J-098 + J-099-eighth-file). **Latent test-comment-vs-actual-behaviour finding** (sidebar, non-blocking): xgen-client/src/app.rs:1551 + :4539 DM-second-room negative tests finally exercise the `DmSecondRoomNotAllowed` rule they advertised post-Commit-2a (pre-Commit-2a they were exercising `validate_dag_structure`'s root-event rejection first); "tests-finally-lock-what-they-claimed" pattern. **Topological-sort milestone flips PLAY → DONE; Phase 9 Commit 3b RESUMES** (Scenarios 2 + 3 + compound scenarios C2/C3/C5/C7/C9/C10 — Clair's next-active scope, ~5-7 atomic commits in their own sequence); Phase 9 milestone stays PLAY (umbrella in-flight until Phase 9's own milestone-close commit ships); Federation Event Propagation milestone stays PLAY (waiting on Phase 9); M6 (new) + XGID Retrofit Pass 1 stay PENDING (chain unchanged in shape — extended by this milestone's session-arcs, dependency depth unchanged). Per D-065 + D-067 + D-069 + D-071 + D-074 + D-075 + D-076 v1.1 discipline.

✅ **Topological-sort design-phase re-walk Step 3 SHIPPED 2026-05-22** (J-100; five-file atomic commit per D-074: revised runbook `tasks/FEDERATION_TOPOSORT_IMPL.md` v1.0 → v1.1 + `tasks/HANDOFF_TOPOSORT_RUNBOOK_REVISION.md` Status flipped ACTIVE → COMPLETED v1.1 + JOURNAL J-100 entry + CLAUDE.md PLAY block flip + this ROADMAP v1.15 → v1.16; no code changes; 577 + 1 ignored unchanged across this work). Substantive change: new **§4a Commit 2a — Path B fix at event-construction layer** section inserted between original §4 (Commit 2 — now preserved as determinism-layer historical record) and §5 (Commit 3 — `#[ignore]` lift), plus eleven local amendments across the document making the two-property contract under amended D-076 v1.1 visible end-to-end: §2.1 sequence table gains row 2a between row 2 and row 3 with status markers (✅ for shipped Commits 1+2, 🟡 for pending Commit 2a + Commit 3 + Commit 4); §2.2 files-touched roll-up gains `xgen-core/src/space/state.rs` as Commit 2a's file with the named edit at `:797` and the new unit test; §2.3 Joe-lock checkpoints incremented from three to four with checkpoint #3 extended to cover Commit 2a stability and new checkpoint #4 added with four locked structural elements for Commit 2a's verbatim code-comment block (D-076 v1.1 reference; Path B citation by design §11; doc-comment-was-already-correct framing; narrow-scope note); §4.0 SHIPPED-status preamble marking Commit 2 as shipped at `0543a86` and preserved as determinism-layer historical record (§4.1–§4.7 prose stays verbatim as historical record of design-at-Commit-2 time); §4.5 new Test 4 determinism-layer framing paragraph (Test 4 `compute_federation_delta_byte_identical_across_two_senders` is the determinism-layer regression lock; the causality-layer lock is `room_create_event_records_space_create_as_predecessor` at Commit 2a; together they cover D-076 v1.1's two-property contract at unit level); §5.5 doc-comment target text rewritten for five-event chronology (original stand-down + bidirectional Commit 3 lift + bidirectional Commit 4 re-stand-down + design-phase re-walk + Commit 2a landing + Commit 3 lift) + three-decision regression-lock framing (D-075 + D-076 v1.1 determinism layer + D-076 v1.1 causality layer); §6.3 catalogue M15 row Detection column rewritten with both-layers framing referencing amended D-076 v1.1 and citing both Commit 2 + Commit 2a as the joint closure; §6.4 JOURNAL entry shape gains Commit 2a sub-entry with scope-honesty paragraph requirement + D-074 count revised to grep-and-quote pattern at Commit 4 commit-authoring time (working baseline at J-100 is six instances) + honest-longer-work recurrence count revised to six; §6.5 + §6.6 + §6.7 update four-commit → five-commit shape across PLAY block flip description + ROADMAP tree + version numbers (v1.16 → v1.17 at Commit 4 rather than v1.14 → v1.15) + DoD checklist; §7.6 honest-longer-work expanded to enumerate six recurrences within Federation Event Propagation milestone scope; new §7.7 Path-B-vs-Path-A layer-distinction discipline note framing why Path B at event-construction was right over Path A at sort-layer refinement (future event-design Joe-locks should explicitly check that `prev_events` honestly reflects protocol-level parent-child relationships claimed in doc-comments); §8.1 + §8.4 + §8.6 cross-references updated for re-walk HANDOFFs + `build_room_create_event` code surface + amended D-076 v1.1 + Rule 0 reference. **"Commit 2a" naming Joe-locked at Step 3 open** rather than "Commit-2-amendment" used throughout the Step-3 HANDOFF body or "Commit 2.5" used at bidirectional in-flight sibling-half-step. Three grounds: (a) honest about sequence — the letter suffix says "after Commit 2, before Commit 3" structurally; "Commit-2-amendment" would suggest Commit 2 itself is being modified, which it isn't (Commit 2 already shipped at `0543a86` and isn't reverted); what's being amended is the intent of original Commit 2 under D-076 v1.1, not the commit; (b) table-friendly — "Commit 2a" fits cleanly in §2.1's table column without wrapping (the runbook is already dense; a verbose label compounds that); (c) precedent fit — bidirectional's "Commit 2.5" (J-096) used the decimal-sibling shape for a true in-flight sibling fix at the same layer (vantage-aware drain hook mirroring Commit 2's applier change at the same layer); this case is different: Commit 2a is a new fix at a different layer (event-construction, not sort tie-break), discovered after Commit 2 shipped, ordered after it because the test-lift in Commit 3 depends on it. Letter-suffix-insertion-at-different-layer rather than decimal-sibling-half-step-at-same-layer. HANDOFF body's "Commit-2-amendment" references are preserved as authoritative historical record of Step-2-bis-time naming; runbook ships with "Commit 2a" throughout. **Step-2-bis fix-up atom retrospective** included in J-100 as a single-sentence acknowledgement (sibling-shape to J-098's housekeeping atom precedent): `tasks/HANDOFF_TOPOSORT_RUNBOOK_REVISION.md` was named in J-099's eight-file enumeration as file #4 (NEW, ACTIVE v1.0) but did not land in the e0c5d36 Step 2 commit (`git show --stat e0c5d36` confirms seven files, not eight); slip surfaced at post-J-099 session-open verification when the entry-point file named in CLAUDE.md PLAY block could not be read from disk; sibling-shape to J-098's companion-files-deferred-as-chat-prose slip — same prose-then-batch root cause; authored as single-file Step-2-bis fix-up atom rather than rolled into Step 3's substantive commit because the Step-2 HANDOFF v1.1 Status update at e0c5d36 already claimed this file shipped at Step 2; honest framing per D-065 surfaces the gap explicitly rather than papering it over inside the downstream Step 3 commit; the fix-up atom's commit (single file at this J-100 entry's parent commit) has no JOURNAL entry of its own — the single-sentence acknowledgement inside J-100 is the canonical record. **Path B locked at J-098 session close** stays the substantive Clair-facing fix: modify `build_room_create_event` at `xgen-core/src/space/state.rs:797` to set `prev_events: vec![space_id.to_string()]`. Narrow scope by Joe-lock: `build_room_create_event` only; sibling event constructors (`state.federation_add`, `membership.*`, `message.*`) NOT audited this milestone, may surface later as own audit-precedes-dependent-design arc per D-071. **Five-commit Clair-facing sequence under amended D-076 v1.1:** Commit 1 ✅ (doc-pass, shipped at `0543a86`'s parents) + Commit 2 ✅ (determinism layer, shipped at `0543a86`) + Commit 2a 🟡 (causality layer, NEW for Clair) + Commit 3 🟡 (Phase 9 Scenario 1 second `#[ignore]` lift) + Commit 4 🟡 (milestone close per D-074). **Four Joe-lock checkpoints** (was three at v1.0): #1 post-Commit-1 doc-pass drift; #2 pre-Commit-2 unit-test list; #3 post-Commit-2 / pre-Commit-3 primitive shape locked (extended to include Commit 2a in the stability check); **#4 NEW pre-Commit-2a verbatim code-comment block content** with the four locked structural elements. **Clair's stand-down ends with this commit.** She picks up at Commit 2a per the revised runbook — NOT at Commit 3 yet. Her existing Commit 3 working tree (`xgen-node/src/tests/phase9_two_node_smoke.rs`, uncommitted) remains as sentinel through Step 3 close per Joe-lock; doc-comment text currently forward-looking (describes the fix as landed) which was false at Step 3 open but becomes true after Commit 2a lands; she picks up the uncommitted file as part of Commit 3 after Commit 2a ships. **Re-walk's procedural Shape 2 completes with this entry**: Step 1 (J-098 session close Joe-lock conversation) ✅ closed; Step 2 (J-099 canonical-record amendments) ✅ closed; Step 3 (this entry, runbook revision) ✅ closed; Clair's three-commit implementation arc (Commit 2a + Commit 3 + Commit 4) is the next-active work; she resumes per the revised runbook. **"Honest longer work over fast shortcuts" — sixth recurrence within Federation Event Propagation milestone scope** (Phase 7.5 first; bidirectional second; topological-sort design close J-097 third; runbook landing J-098 fourth; re-walk Step 2 J-099 fifth; re-walk Step 3 J-100 sixth). Each recurrence delays Federation Event Propagation milestone closure by approximately one session-arc and closes a real gap before it ships. Step 3's gap was framing-level rather than protocol-level ("the v1 runbook described a four-commit shape that's now wrong under amended D-076 v1.1"); the honest path was to revise the runbook to reflect the amended principle rather than let Clair ship from a stale runbook. **D-074 application count: sixth instance** (J-095 locked; J-096 first downstream application; J-097 second; J-098-across-two-commits third; J-099 fourth (Step 2 canonical-record amendments); J-100 fifth in the simple counting (this commit); five-vs-six framing depends on whether J-098's housekeeping atom counts as a separate application or as a single application spread across two commits; runbook's §6.4 sub-point 4 punts the exact count to Clair's grep-and-quote pattern at Commit 4 commit-authoring time). **What this milestone CANNOT close.** Step 3 closes the design-phase re-walk's documentation phase only. After Step 3: topological-sort milestone stays PLAY (Clair's three-commit implementation arc still pending); Phase 9 Scenario 1 stays `#[ignore]`-annotated until Commit 3 lifts it; Phase 9 Commit 3b stays paused inside milestone scope until Commit 4 ships; Federation Event Propagation milestone stays PLAY; M6 (new) + Pass 1 stay PENDING. Dependency chain unchanged in shape; Step 3 simply completes the documentation that Clair needs to ship Commit 2a + Commit 3 + Commit 4. Per D-065 + D-069 + D-071 + D-074 + D-076 v1.1 discipline.

✅ **Bidirectional federation_nodes implementation milestone CLOSED 2026-05-21** (J-096; four-commit sequence per `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` flipped Status: ACTIVE → COMPLETED v1.1 at milestone close; test count 571 → 577 across the milestone, +6 unit tests in Commit 2; Scenario 1 originally lifted in Commit 3 then re-stood-down in Commit 4 on a separate topological-sort finding, net wash on ignored count). **Commit 1 doc-pass (`e975162`)** — canonical design doc gained §6.4.2 sibling subsection (sibling to §6.4.1 Phase 7.5's F-3 framework subsection) + §15 Implementation Complete row; design task file Status flipped ACTIVE → COMPLETED v1.0; audit doc Status flipped ACTIVE → COMPLETED v1.0; no code change. **Commit 2 origin-aware applier + plumbing + six unit tests (`a730eda`)** — `apply_federation_add` in `xgen-core/src/space/state.rs:351-363` gained `my_node_id: &str` parameter with verbatim code-comment block citing D-075 design §3.3 ("which peer does this state.federation_add establish a relationship with, from my vantage?"); `SpaceState::apply_event` threaded `my_node_id`; both `NodeRuntime::ingest_event` call sites updated to pass `&self.node_id`; six unit tests covered the two vantage branches (`apply_federation_add_my_event_adds_content_node_id` sender-side, `apply_federation_add_peer_event_adds_sender` receiver-side), the mirror property (`apply_federation_add_two_vantages_mirror` — unit-level regression lock for D-075), the third-party observer case, the DM constraint preservation, and the missing-field rejection. **Commit 2.5 sibling vantage-aware drain hook (`cbceb41`, in-flight gap closure)** — drain-pair derivation at `NodeRuntime::dispatch_event` Step 7 in `xgen-core/src/node/runtime.rs:650-666` mirrors the applier's vantage logic verbatim; cross-references the sibling `apply_federation_add` site by `file:line` so a future contributor touching one will see the cross-link. In-flight discovery: Commit 3's first verification attempt failed because the drain hook keyed on `content.node_id` verbatim, exactly mirroring the applier-side bug Commit 2 fixed — the two sites ask the same question and must derive the same answer. **Commit 3 Phase 9 Scenario 1 resurrection (`f051039`)** — `#[ignore]` originally lifted; scenario passed in isolation and as part of full workspace; became activating regression lock at integration level. **Commit 4 milestone close (this entry's commit)** — six files: JOURNAL.md (J-096 entry written + header bumped), CLAUDE.md (PLAY block flipped from bidirectional implementation in flight to topological-sort phase OPENS; bidirectional implementation moved to ✅ DONE-IN-FLIGHT block; header bumped), `docs/ROADMAP.md` (Past gains this entry; Present updated for topological-sort PLAY + Phase 9 PAUSED reason updated; visual structure tree's bidirectional cluster all-✅ + new topological-sort cluster added as sibling + Phase 9 resume row reason updated; header version bumped 1.10 → 1.11), `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` (Status: ACTIVE → COMPLETED, Version 1.0 → 1.1), `tasks/FEDERATION_PROPAGATION_PHASE_9.md` (Last updated bumped acknowledging milestone closure and Scenario 1 re-stand-down), `xgen-node/src/tests/phase9_two_node_smoke.rs` (`#[ignore = "..."]` re-applied; doc comment rewritten to name the topological-sort finding with verbatim evidence and forward-reference placeholder `tasks/FEDERATION_TOPOSORT_AUDIT.md`). **Separate pre-existing finding surfaced during Commit 4 verification, NOT implicated by the bidirectional fix.** Wire-order non-determinism in `topological_sort_events` at `xgen-node/src/fanout.rs:193` fed by non-deterministic `HashMap.values()` iteration in `compute_federation_delta_for_space` at the same file ~:321. xgen-node-side diagnostic instrumentation in `process_inbound` confirmed 105 `dispatch_event` calls on B in **both** pass and fail runs of Scenario 1 — divergence is entirely upstream of the dispatcher, in delta serialisation order. In a failing run, `state.room_create` (DAG root with empty `prev_events`) reaches B's dispatcher before `state.space_create` (also DAG root); B's dispatch Step 1 rejects `room_create` with "space not found"; cascade produces 2 Accepted / 2 Rejected / 101 HeldPending against the passing-run baseline of 102 Accepted / 3 HeldPending. Both root events tie at the top of the topo sort and their wire order tracks HashMap iteration. Per Joe-direction this opens as its own phase per D-071, sibling-shape to the bidirectional `federation_nodes` audit → design → impl arc just closed (see Present section's 🟢 Topological-sort phase entry). Bidirectional fix verified-correct: six unit tests including `apply_federation_add_two_vantages_mirror` remain green and stand as the live D-075 regression lock while the integration-level scenario is re-ignored. Discipline notes: D-074 in second-instance application (JOURNAL.md in milestone-close commit's changed-files list); D-071 sibling-shape extension (third instance of audit-precedes-dependent pattern, after J-081 and the bidirectional arc); "honest longer work over fast shortcuts" applied at the close (full diagnostic walk + root-cause traced to source + separate phase opened per D-071, longer than a hand-wave workaround AND shorter than rediscovering the bug from production deployment); B3-shape gap question asked and answered (the drain hook in `dispatch_event` Step 7 added by Commit 2.5 has unit-level coverage via the applier tests' indirect verification; integration-level lock is the Scenario 1 itself, which gets it back when the topological-sort fix lands). Phase 9 Commit 3b stays paused inside milestone scope; topological-sort phase now the active blocker. M6 (new) blocking chain extended by one more node, unchanged in shape. Pass 1 of XGID Retrofit stays at Status: ACTIVE v2.0 unchanged (Shape A is Pass-1-neutral; the applier's `my_node_id: &str` parameter is the kind of XGID-typing widening Pass 3 will pick up naturally when the surrounding subsystem retrofit lands).

✅ **Bidirectional federation_nodes design phase closed 2026-05-21** (`tasks/FEDERATION_BIDIRECTIONAL_NODES_DESIGN.md` Status: ACTIVE v1.0 ~30 KB; flips to COMPLETED in Commit 1 of the implementation runbook — sibling-in-shape to Phase 7.5's design-task-file lifecycle). Three Joe-locks: **Q1 Reading (i)** — `state.federation_add` is one event recording one party's act ("A declares A federates with B for Space S"); `federation_nodes` is a derived projection with vantage-aware applier logic (event.sender if I am content.node_id, else content.node_id). Reading (ii) (two events per relationship, one per side) considered and rejected on precedent grounds: would have introduced the first event family in the registry whose semantic completeness requires two signed assertions of the same type, one per party; every other relationship-shaped event in the registry (membership.invite/join, state.dm_promote, state.space_create, state.room_create, state.ai_operator_delegate/revoke, membership.kick/ban/mute) follows the one-party-assertion + derived-projection pattern. **Shape A** — origin-aware applier; wire format unchanged; `apply_federation_add` gains `my_node_id: &str` parameter; logic picks the OTHER party from this Node's view; both branches needed (asserter + recipient apply same event from their own DAGs, arrive at symmetric `federation_nodes` through asymmetric branches). Shape D (extend wire format) considered and rejected: D.1 duplicates `sender` into content (worst-of-both-worlds), D.2 symmetric `{a_node, b_node}` secretly re-introduces Reading (ii) thinking through schema design; both pay Pass-1 cost. **Sub-option A.1** — re-derive on load; native fit confirmed against verified `SpaceState` non-persistence model. Verification check performed at design close: `xgen-core/src/node/runtime.rs` confirms `NodeRuntime::new()` initialises `spaces: HashMap::new()` (line 134); no persistence path exists for `SpaceState`; adjacent fields carry explicit "not persisted; rebuilt on restart" comments per Phase 2 simplification. A.1 is the existing model, not a deviation; the fix lands and self-heals on next Node start. **D-075 promoted to DECISIONS.md** as the protocol-design principle the locks instantiate — sibling-distinct from D-070 (D-070 is transport-layer acceptance-vs-rejection signal pair on `TransportMessage`; D-075 is event-model semantics at the protocol-event layer; the disciplines operate at different layers and address different questions). D-075 commits the protocol's general approach: every relationship-shaped event records one party's act; the resulting data object is a derived projection; vantage-aware appliers are the legitimate pattern when the event has a sender-vs-other-party asymmetry. Future events that genuinely cannot be modelled this way require an explicit future decision to depart from D-075, not a silent precedent break. Implementation runbook shipped same-day at `tasks/FEDERATION_BIDIRECTIONAL_NODES_IMPL.md` (Status: ACTIVE v1.0) with four-commit sequence: doc-pass, origin-aware applier + plumbing + unit tests (six suggested tests covering both vantage branches + mirror property + DM constraint + missing field; mirror-property test is the unit-level regression lock), Phase 9 Scenario 1 resurrection (lifts `#[ignore]`; becomes activating regression lock at integration level), milestone close (CLAUDE.md + ROADMAP.md + JOURNAL.md per D-074). Wire-format-neutral. Pass-1-neutral. Sibling-in-shape discipline pattern: audit (D-071) → design walkthrough (D-069) → locked principle (D-075) → implementation — the same four-step shape Phase 7.5 used. Phase 9 stays PAUSED at Commit 3a boundary; resumes from Commit 3b after implementation lands. M6 (new) blocking chain extended by one node, unchanged in shape.

✅ **Bidirectional federation_nodes audit doc shipped 2026-05-21** (`tasks/FEDERATION_BIDIRECTIONAL_NODES_AUDIT.md` Status: ACTIVE v1.0, ~44 KB — canonical record of the finding surfaced during Phase 9 Commit 3a Scenario 1 diagnostic run; sibling-in-shape to J-081 Propagation Reliability Audit and `tasks/FEDERATION_PROPAGATION_PHASE_7_5_DESIGN.md`). Per D-069 (canonical document) + D-071 (audit precedes dependent design) + D-074 (forward-binding) + "honest longer work over fast shortcut" discipline. Ten sections: provenance with the test-surface structural gaps that hid the bug through eight Federation Event Propagation milestone phases plus Phase 7.5 (cold_start_bootstrap_integration tests build bootstrap stream manually with `fed_add(content.node_id=peer_a_id)` not what production produces; federation_push_integration uses one-sided harness without real NodeRuntime on B; Phase 9 Scenario 1 first test to spawn two real NodeRuntime instances and run real bootstrap end-to-end through production code path); the mechanism code-verified at file:line references (sender side `xgen-node/src/federation_session.rs::stream_federation_delta` lines 75-175; builder `xgen-core/src/space/state.rs::build_federation_add_event` lines 910-933; receiver side `xgen-core/src/space/state.rs::apply_federation_add` lines 351-363; F-3 then-what in `xgen-core/src/node/runtime.rs::dispatch_event`); relationship to the design as locked including §6.4 Phase 7 Lock A1's anticipated-but-different failure mode (the design contemplated a race between two READS using different data sources, closed by mandating both reads use `SpaceState.federation_nodes`; this bug is a different shape — a semantic asymmetry between sender's view and receiver's view of the same WRITE produced by the same shared apply code); the unstated principle the bug reveals ("event semantic and event application semantic must be congruent across all ingestors" — `state.federation_add` is the only event in current Ch3 registry whose semantic is genuinely directional). Two structural questions for the design walkthrough — Q1 (one event with asymmetric interpretation, Reading (i), admits Shapes A+D; OR two events one per side, Reading (ii), admits Shapes B+C) and Q2 (signature carries enough info to derive receiver's value — code-verified yes: `build_federation_add_event` calls `sender_id(key)` where `key = node_keypair` = A's Node keypair, and Phase 4 Q3 overload allows `event.sender` to be a Node URI). Four candidate fix shapes with code-grounded cost/benefit + Pass-1 impact + summary table at §7.5: A origin-aware applier (smallest change; replay concern Q3 with sub-options A.1 persist origin / A.2 ReplayedFromDisk variant / A.3 reconstruct origin from event.sender == self_node_id heuristic); B reciprocal mint on ingest (no wire change; introduces "ingesting an event causes a new event to be authored" pattern that doesn't exist elsewhere; reciprocal propagates back to A on next pull — either special-case ignore or same bug mirrored); C two events at handshake (re-opens Phase 3 §3.3.1 Lock 2 a-i symmetry rule; prev_events sub-options C.1 reciprocal references A's federation_add / C.2 anchors to state.space_create); D schema extension (cleanest semantics; sub-options D.1 add `peer_node_id` field / D.2 symmetric `{a_node, b_node}`; ONLY shape with Pass-1 impact — one field row in Appendix C primitive schemas + Appendix I content-struct field tables; `event_content_struct_xgid_roundtrip` invariance test (Pass 1 sub-question 4) covers the new field). Phase-9 + milestone implications: Scenario 1 stays on disk as regression witness, `#[ignore]` lifts when fix lands; Commit 3b + compound scenarios C2/C3/C5/C7/C9/C10 paused inside milestone scope; deferred compounds C1/C4/C6/C8 unaffected (blocked on clock-injection seam independently); M6 (new) dependency chain extends by one node; Pass 1 stays at v2.0 (Shape D would add one row, others Pass-1-neutral). Discipline notes: worked instance of D-069 + D-071 + D-074 + "honest longer over fast shortcuts" — ship the regression witness + document the audit + walk the design phase + ship the fix is longer than workarounds and shorter than rediscovering the bug from a production deployment. Status: ACTIVE because the audit is the input to the next-active design phase walkthrough; flips COMPLETED when the design phase closes and locks a fix shape, at which point the locked design task file (sibling to `tasks/FEDERATION_PROPAGATION_PHASE_7_5_DESIGN.md`) becomes the new active artefact.

✅ **Federation Event Propagation Phase 9 Commit 3a shipped 2026-05-21** (harness + Scenario 1 regression witness + JOURNAL Commit 3a sub-entry under J-092; 571 passing 0 failing 1 ignored at close; Phase 9 milestone PAUSED at Commit 3a boundary pending bidirectional federation_nodes fix). Commit 3a closes the Phase 9 split-into-3a-plus-3b sequence (locked during pre-implementation walk per the multi-commit Phase 9 Q4 lock): harness module `xgen-node/src/tests/phase9_harness.rs` (~340 lines, exercised end-to-end during diagnostic, works correctly) plus Scenario 1 `xgen-node/src/tests/phase9_two_node_smoke.rs` (authored in full then `#[ignore]`-annotated with inline doc comment naming the finding). Six files changed: `xgen-node/Cargo.toml` (`tracing-test = "0.2"` dev-dep), `xgen-node/src/app.rs` (`pub(crate)` on `handle_connection` + `replay_spaces_from_dir`), `xgen-node/src/tests/mod.rs` (two new module declarations), the two test files, `JOURNAL.md` (J-092 umbrella gains Commit 3a sub-entry). **Finding surfaced during Scenario 1 diagnostic run:** `stream_federation_delta` emits `state.federation_add` with `content.node_id = peer_b_id` (semantics "A approves B"); both sides ingest the same event and apply `content.node_id` verbatim onto `federation_nodes`. Result: `A.federation_nodes = {B}` correct, `B.federation_nodes = {B}` incorrect because it should contain A so F-3 lets A's events through. F-3 on B HeldPends every post-bootstrap A→B push event indefinitely; 180s timeout fires; events discarded. No current code path emits the reciprocal `fed_add(node_id=A)`. Why no existing test caught this: cold_start tests build their bootstrap stream manually with `fed_add(node_id=peer_a_id)` which is not what production produces; federation_push_integration uses a one-sided harness; Phase 9 Scenario 1 is the first test to exercise the full bidirectional path with real dispatch on both sides. **Stand-down sibling to J-093 precedent.** Scenario 1 stays on disk as the regression witness; when the protocol fix lands, the `#[ignore]` lifts and the same scenario becomes the activating regression lock. Phase 9 Commit 3b (Scenarios 2 + 3) stays paused inside the same milestone scope until the protocol fix lands. Four candidate fix shapes captured in JOURNAL Commit 3a sub-entry for the design phase to weigh: (a) reciprocal a-i on receiver (receiving Node emits its own `fed_add` naming the sender); (b) post-handshake reciprocal hook (dedicated post-handshake step ensures both sides record each other); (c) `apply_federation_add` records sender too (one event documents both directions via sender field of the Event envelope); (d) hybrid combining elements. Each has wire-format implications, persistence implications, edge cases. Design walkthrough picks one with Joe-lock; Commit 3a does NOT pre-decide. Phase 9 milestone is doing its job — the 14-entry failure-mode catalogue at survey close named this category of finding as a HIGH-severity expected outcome of bidirectional deployment-level testing. Finding it before Pass 1 ships, before M6 goes active, before any downstream code depends on the broken behaviour is the cheapest possible moment.

✅ **XGID Retrofit Pass 1 runbook authoring closed 2026-05-21** (Chat Claude single-session authoring — skeleton through v2.0 — no test count change, documentation only). The Pass 1 implementation runbook now lives at `tasks/XGID_RETROFIT_PASS_1_IMPL.md` (Status: ACTIVE v2.0, ~65 KB) with all four sub-questions Joe-locked: **(1)** multi-commit sequence, six commits (canonical-form module move → convenience constructors → xgen-common data-structure retypes → xgen-core data-structure retypes → Appx C + Appx I retypes → milestone close); **(2)** `canonical_event_bytes` module move from `xgen-core/src/wire/canonical.rs` to `xgen-common/src/canonical.rs` (with xgen-core re-export shim) as isolated head commit of the sequence, structurally orthogonal to the retypes and unblocking commit 2's convenience constructors; **(3)** explicit `.as_str()` projection at cross-type-boundary call sites with code-comment discipline at non-trivial sites, NO `Deref<Target = str>` added on base `Xgid` (v1 precedent followed at scale — projection is visible in diff, comments act as TODO markers for Pass 2/3/4 widening); **(4)** five new invariance tests pinned by name (`event_struct_full_xgid_roundtrip_through_canonical_form`, `space_local_metadata_full_xgid_roundtrip`, `node_state_observability_xgid_roundtrip`, `event_content_struct_xgid_roundtrip`, `legacy_string_json_forward_compat_on_event`), file placement Clair-latitude; combined v1 + Pass 1 gives 10 invariance tests total. **Pass 1 title-vs-scope contradiction resolved during runbook authoring (Option B Joe-locked):** reconnaissance showed the foundational structures span both crates (`Event` + content structs + `SpaceLocalMetadata` + observability structs in xgen-common; `SpaceState`/`RoomState`/`FederationRegistry`/`IdentityRegistry`/`PendingBuffer` in xgen-core), so Pass 1 was renamed "xgen-common core types" → "core data structures (spans xgen-common + xgen-core)" in the same ROADMAP edit (v1.4 → v1.5). Three commits shipped against this work over the session: (a) skeleton + ROADMAP rename (v1.5), (b) sub-question locks + walkthrough population (runbook v2.0), (c) D-074 promotion + ROADMAP v1.6 (sibling work, not directly Pass 1 runbook but shipped same-day). One structural finding flagged in the runbook for Clair at commit 4 implementation kickoff: pure-scope Pass 1 leaves the workspace in a broken intermediate state at commit 4 boundary (xgen-node + xgen-client consume retyped xgen-core types); two paths documented — (a) hold pure scope and close the workspace at Pass 2, or (b) ship thin bridging-shim alternative at xgen-node/xgen-client call sites for workspace-green Pass 1 close. Clair surfaces to Joe at commit 4 for the call. **Pass 1 implementation remains Clair's pickup, blocked behind Federation Event Propagation milestone closure** (Phase 9 going DONE); when Phase 9 ships, Chat Claude flags the "🚦 Pass 1 ready for Clair handoff" signal in the closure commit's PLAY block.

✅ **XGID Adoption v1 implementation milestone CLOSED 2026-05-20** (J-095; Clair's two-commit plan shipped per `tasks/XGID_ADOPTION_IMPL.md` flipped to Status: COMPLETED v1.1; test count 556 → 571 across the milestone, +15: 10 in-module flavour tests + 5 wire-format invariance tests). **Commit 1 (`c95584a`)** ships `xgen-common/src/xgid/` module: base `Xgid(String)` newtype `#[serde(transparent)]` + six flavour wrappers (`EventXgid`, `SpaceXgid`, `RoomXgid`, `TrustAssertionXgid`, `NodeXgid`, `IdentityXgid`) each `Deref<Target = Xgid>` and serde-transparent + `XgidLike` trait + `XgidDecodeError` + hash-anchored `from_canonical_bytes(&[u8])` constructors (matching `xgen-core::crypto::hashing::hash_uri` byte-for-byte) + principal `from_pubkey(&VerifyingKey)` infallible + principal `pubkey() -> Result<VerifyingKey, _>` parse-fallible-at-v1. New `xgen-common` deps: `ed25519-dalek = "2"`, `sha2 = "0.10"`, `base64 = "0.21"`, `thiserror = "1"` (all matching xgen-core's pinned versions). Five required invariance tests pinned by name in `xgen-common/tests/xgid_invariance.rs`: `xgid_serializes_as_plain_string`, `xgid_deserializes_from_plain_string`, `flavour_wrapper_is_serde_transparent`, `event_xgid_roundtrip_through_event_canonical_form`, `node_xgid_roundtrip_through_handshake_message`. Plus 10 in-module flavour tests in `flavours.rs` (legacy-format URI byte-equal locks for both hash-anchored and principal flavours, decode rejection paths, Deref chain, into_xgid consumption, XgidLike trait unification). No production code outside xgen-common modified per Commit 1 DoD. **Commit 2 (`24a255b`)** retypes `SpaceLocalMetadata.introducer_node_id` from `Option<String>` to `Option<NodeXgid>` — the v1 inaugural production use of a typed XGID flavour, instantiating D-073's field-name-vs-type discipline at the use site. Three files: `xgen-common/src/space_local.rs` (struct + constructor + strengthened `serde_roundtrip_with_introducer` test acting as wire-format invariance witness including forward-compat from pre-XGID JSON shape — the per-call-site witness for Appendix J §J.5 invariance 2), `xgen-core/src/node/runtime.rs` (production caller wraps wire-authenticated peer ID via `NodeXgid::from_xgid(Xgid::new(peer.to_string()))` at type-boundary entry point, with code comment flagging Retrofit Pass 3 will widen `dispatch_event(peer_node_id: Option<&str>)` to `Option<&NodeXgid>` at which point the wrap collapses; two test assertions updated from `.as_deref()` to `.as_ref().map(|n| n.as_str())` for &str projection back to pre-retyped peer_id strings), `xgen-node/src/tests/cold_start_bootstrap_integration.rs` (same single-line read-side projection). Honest-broadening warning held throughout — six adjacent String-typed XGID fields presented as tempting parallel retypes (`dispatch_event(peer_node_id: Option<&str>)`, `peer_id: String` variables in F-3/F-4 dispatch graph, `space_id: String` in SpaceLocalMetadata and parallel sites) all deliberately left untouched, each belongs to its own Retrofit Pass where the surrounding subsystem retrofit lands. **Hygiene atom (`904441b`, `chore(workspace): clippy cleanups for new toolchain`)** shipped same-session as separate provenance-distinct sibling commit — workspace clippy gate flipped red → green under Rust 1.95.0; 26 files touched across all four crates; +191/-89 LOC. One behaviour-adjacent fix (`filter_map(|l| l.ok())` → `map_while(Result::ok)` on `std::io::Lines` in `xgen-node/src/pipe.rs` + `xgen-client/src/batch.rs`, closing a potential spin-forever loop on persistent read errors). Rest mechanical (lifetime elisions, `or_insert_with` → `or_default`, `vec!` → array literal, `map_or(false, …)` → `is_some_and(…)`, `assert_eq!(x, true)` → `assert!(x)`, `&[x.clone()]` → `std::slice::from_ref(&x)`, `if let Ok(_)` → `.is_ok()`, match-single-arm → `if let`, `.max(N).min(M)` → `.clamp(N, M)`) or `#[allow]` with per-case rationale comments (notable: `clippy::manual_clamp` on `clamp_temperature` because NaN handling is load-bearing — `f64::clamp` propagates NaN, spec requires NaN → 0.0; `clippy::result_large_err` on `HandshakeError` module because real fix is boxing the variant which belongs to a future error-type-size discipline pass; `clippy::wildcard_in_or_patterns` on the `VISIBILITY_MODERATOR | _` arm as documentary form per spec 3.7.13.3; `clippy::needless_range_loop` file-level in `xgen-client/src/app.rs` because integration-test loops index multiple parallel arrays by the same index). Test count unchanged at 556 (no behaviour regression). Hygiene commit's diff contains zero XGID code — honest provenance via separate atom so a future reader running `git log -- xgen-common` will see hygiene and XGID as distinct commits. **Milestone-close commit (this commit)** carries five files: JOURNAL.md (J-095 entry), CLAUDE.md (PLAY block flip + header bump), `docs/ROADMAP.md` (Past gains this entry, Present updated for next-active state, Near future loses the now-shipped XGID Adoption v1 implementation line, header bump), `tasks/XGID_ADOPTION_IMPL.md` (Status: ACTIVE → COMPLETED, Version 1.0 → 1.1), `docs/xgen_ch4_implementation.md` (one-line v1 follow-on pointer per A5 Joe-lock during Phase 2 sweep — Scope-B blockquote shape matching Phase 1 normative pointers in `docs/xgen_aicontrol_implementation.md` and `docs/xgen_appendix_f_en.md`, with header bump riding on top of Phase 8 doc-pass close annotation). **Carry-over to Retrofit Pass 1:** hash-anchored convenience constructors (`EventXgid::from_event`, `SpaceXgid::from_space_create`, `RoomXgid::from_room_create`, `TrustAssertionXgid::from_assertion`) deferred from Commit 1 because their implementation requires `canonical_event_bytes()` which lives in `xgen-core/src/wire/canonical.rs` and is not visible to `xgen-common` — module-level doc comment in `flavours.rs` flags this with full rationale; runbook's "where it is clean to do so" hedge applied; Pass 1 picks up by moving the canonical-form helpers from `xgen-core/src/wire/canonical.rs` to `xgen-common/src/canonical.rs` (with `xgen-core` re-exporting to preserve call sites) as part of the coordinated commit set, then adding the convenience constructors on the same flavour wrappers. Discipline notes (full detail in J-095): Rule 4 in action — milestone-close commit's changed-files list includes JOURNAL.md alongside the cross-doc updates (the candidate sibling principle flagged in J-094 being followed pre-emptively); D-069 canonical-document discipline as sustained pattern (the same coordinated-deliverable shape XGID Adoption v1 used at design close `a5f3c8b` eight artefacts atomically now used at implementation close); B3-shape gap question asked and answered (no gap — XGID is structurally narrower than Phase 7.5, all surfaces exercised by tests written in the same commits).

✅ **XGID Adoption v1 Phase 2 doc-tree sweep closed 2026-05-20** (single session, no test count change — documentation only). Phase 2 walked all 23 in-scope `docs/` files and produced the classification table at `tasks/XGID_DOC_SWEEP.md` (Status: COMPLETED v1.2). **Pre-walk Scope-A-vs-Scope-B Joe-lock: Scope B** — pointers belong in implementation-flavoured docs (consulted tactically without normative context); spec-flavoured docs inherit invariance through Ch3 §3.0 normatively without redundant pointers; design/audit/roadmap/journal/task files take no pointers. Audience analysis that locked Scope B: classification table is read by future Chat Claude sessions and Joe (Clair never reads it directly); pointer text inside docs is read by humans browsing documentation; no code is shaped by pointer presence/absence. Spec-readers are in spec-reading frame and consult Ch3 by structure; tactical readers benefit from pointer signal. Translation rule applied uniformly across six walk groups (A spec chapters, B English appendices, C design docs, D audit/historical, E implementation reference, F project navigation). **Verdict distribution across 23 in-scope docs:** 4 v1-already-shipped (Ch3, Appx F, Appx J, `xgen_aicontrol_implementation.md`); 1 v1-follow-on-pointer (Ch4, A5 Joe-lock during walk picked option (a) over deferring to a Pass); 2 Pass-1 docs (Appx C primitive schemas, Appx I data structures — with coordination flag pinned: Pass 1 code retype + both doc retypes must land in one coordinated commit set to prevent spec drift at Pass 1 close); 1 Pass-3 doc (Appx D storage/privacy field tables); 1 Pass-5 doc (Appx G log line convention); 14 no-update (Ch0, Ch1, Ch2, Ch5, Ch6, Appx A, Appx B, Appx E, Appx H, `xgen_federation_propagation_design.md`, `xgen_node_admin_ops_design.md`, `xgen_lifecycle_states.md`, `xgen_propagation_reliability.md`, ROADMAP.md). **Pass 2 has zero doc work** (xgen-core consumes types defined in xgen-common; surface documented across Appx C + Appx I, both retyping with Pass 1) — flagged for the Pass 2 runbook author. **Pass 4 has zero new doc rows but substantial per-section work** in two already-pointer-tagged docs (Appx F 890 lines + `xgen_aicontrol_implementation.md` 372 lines) — Pass 4's runbook should anticipate it is the heaviest doc-work pass despite no new classification-table rows. **Pre-walk housekeeping:** `docs/xgen_appendix_a_sk.md` and `docs/xgen_appendix_b_sk.md` flipped from ACTIVE to DEPRECATED with future-translation-pass rationale (`Last updated` bumped to 2026-05-20). Both excluded from the classification table per Joe's call. Future Slovak translation pass will retype from completed English docs as a single coordinated effort once English documentation reaches a stable end-state. Observations recorded in the classification table for future Retrofit Pass runbook authors. The Phase 2 deliverable is the classification table; per-doc edits land afterwards under each Retrofit Pass.

### XGID Retrofit Pass 5 milestone CLOSED at J-148 — whole five-pass XGID Retrofit arc CLOSED; 1 trace fix (F-1) + Debug/Display audit clean; D-081 promoted; D-080 collision caught

✅ **J-148 XGID Retrofit Pass 5 milestone CLOSED 2026-05-29** (this commit) = the entire five-pass arc CLOSED (Pass 1 J-122 → Pass 2 J-126 → Pass 3 J-138 → Pass 4 J-146 → Pass 5 J-148). Confirm-clean audit pass: Audit 1 (trace-field formatter, ~60 `tracing::` sites) = ONE fix F-1 (`app.rs:2288` Debug-formatted `Option<&IdentityXgid>` → `&str` projection); Audit 2 (Debug/Display impls on xgen-client public types) = clean. Commit A (one-line F-1 fix; `cargo build --workspace --all-targets` 0 errors, 637 lib GREEN, clippy clean) + Commit B (this close). **D-081 promoted** ("XGID typing is wire-format and persistence-format invariant") — numbered D-081 not D-080 (D-080 already taken by the Node-storage EventStore decision; collision caught at authoring per verify-before-asserting, sibling-shape to J-142). **Layered-B3 null — fifth Pass-arc no-finding instance** (J-122 + J-126 + J-138 + J-146 + J-148). **Five-file milestone-close atomic per D-074 forty-fifth instance + sixteenth milestone-close**: DECISIONS.md (D-081 top) + `tasks/XGID_RETROFIT_PASS_5_IMPL.md` ACTIVE → COMPLETED v1.1 + JOURNAL J-148 + CLAUDE PLAY flip (arc COMPLETE) + this ROADMAP v1.51 → v1.52 (visual tree Pass series + Pass 5 ✅; Near-future Pass 5 line CLOSED; cross-cutting gains D-081; this Past entry). §7.10 discipline-doc consolidation SKIPPED per Joe-lock. **Arc retrospective:** zero serialized bytes changed across all five passes (D-081); Path A opened Pass 1, closed Pass 4; "honest longer work" by pass = 1/0/2/4/1; D-073 fully realised in code across all four crates; XGID Adoption Q3 transitional clause retires. **What unblocks:** M6 (new) Node admin write path — ready for Joe's selection. Track 2 (Clair) stood down. Per Rule 0 + D-065 + D-069 + D-071 + D-074 + D-076 + D-081.

### XGID Retrofit Pass 5 scope-amendment Track-1 atomic at J-147 — design §2.9 + runbook §11.3/§11.4 reduced 4→2 deferred items; Path-A-closed-at-Pass-4 recorded canonically; ROADMAP Present refreshed

✅ **J-147 XGID Retrofit Pass 5 scope-amendment Track-1 atomic 2026-05-29** (this commit). Post-Pass-4-close canonical-record amendment (Chat Claude + Joe). Pass 4 over-delivered (J-145/J-146 — `cargo build --workspace --all-targets` restored 0 errors, Path A closed **at Pass 4**; xgen-client/tests fixture set closed at 0 errors), so Pass 5's deferred set reduces **4 → 2** (trace-field formatter audit + Debug/Display impl audit on xgen-client public types). **Five-file atomic per D-074 forty-fourth instance** (not a milestone-close; milestone-close tally stays fifteenth): design doc v1.6 → v1.7 (§2.9 amendment banner + item 1 SATISFIED-AT-PASS-4) + runbook v1.4 → v1.5 (§1.2 closure note + §11.3 build-broken bullet SUPERSEDED + §11.4 unblock 4 → 2) + JOURNAL J-147 + CLAUDE PLAY flip (Pass-4-closed → scope-amendment-shipped; standby for milestone selection) + this ROADMAP v1.50 → v1.51 (Present live entry refreshed + line-551 icon correction 🟢 → ⬛ on settled persistence-amendment entry + this Past entry). DECISIONS.md NOT amended (Rule-5 canonical-record correction; no new principle). **ROADMAP Present drift fix-up per project convention** — superseded entries kept in-place as demoted audit trail; live entry refreshed + the one mislabeled 🟢-on-settled icon corrected; deeper prune available as a separate atomic. **Next-active**: Joe selects next milestone (Pass 5 implementation + M6 (new) both ready). Per Rule 0 + D-065 + D-069 + D-071 + D-074.

### XGID Retrofit Pass 4 milestone CLOSED at J-146 — two-commit Clair-facing sequence (Commit 1 retype atomic J-145 + Commit 2 close); 637 tests; layered-B3 null (four-instance chain); cargo build --workspace restored; honest-longer-work FOUR

✅ **J-146 XGID Retrofit Pass 4 milestone CLOSED 2026-05-29** (this commit). Two Clair-facing commits on `main`: **Commit 1 `3869d4c`** (J-145 — consolidated fifteen-file xgen-client retype atomic: all seven surfaces + xgen-common §4.1.b additive-API + Surface #8 docs + T1–T15; 8/8 GREEN; checkpoints #2+#3 closed) + this **Commit 2** milestone close. Commit 1a did NOT fire (checkpoint #3 = **0 integration-test fixture errors** → absorbed; unprecedented vs Pass 1/2/3's 296/93/638 — xgen-client tests are behaviour-oriented, not XGID-fixture-oriented). **Test count: 637 lib** (61 client + 35 common + 453 core + 88 node) + integration GREEN; +15 per-surface tests T1–T15. **Layered-B3: null** — fourth Pass-arc no-finding instance (J-122 + J-126 + J-138 + J-146); chain durable. **`cargo build --workspace --all-targets` RESTORED (0 errors) — Path A CLOSED at Pass 4**, ahead of design doc §2.9 + runbook §11.3's Pass-5 expectation (honest finding per D-065); Pass 5 scope reduced from 4 deferred items to **2** (trace-field formatter audit + Debug/Display impl audit). **"Honest longer work over fast shortcuts" final count: FOUR** (J-142 count drift + J-143 commit-shape + J-144 classification drift + J-145 workspace-build-restored handling — all prospective catches at the canonical-record/verification layer). **Five-file milestone-close atomic per D-074 forty-third instance + fifteenth milestone-close**: runbook ACTIVE → COMPLETED v1.3 → v1.4 + DoD verified; design doc v1.5 → v1.6 + §6.1 J-NNN frozen to J-146 per J-108; JOURNAL J-146; CLAUDE PLAY flip; this ROADMAP entry + version v1.49 → v1.50. DECISIONS.md NOT amended. **Next-active: Pass 5 scope-amendment Track-1 atomic** (design doc §2.9 + runbook §11.3 4→2 items + §1.2 hygiene + Path-A-closed reframing), then Joe selects next milestone (Pass 5 implementation + M6 (new) both ready). Implementation arc: J-141 runbook → J-142 → J-143 → J-144 → J-145 Commit 1 → J-146 close.

### XGID Retrofit Pass 4 Commit 1 IN-FLIGHT: Surfaces #3/#5/#6/#7 classification locked at J-144 (checkpoint-#1-equivalent) — three drift findings + three classification calls corrected before the affected retypes

✅ **J-144 Surfaces #3/#5/#6/#7 classification lock 2026-05-29** (this commit). Resuming the in-flight Commit 1 (Surface #1 ops.rs 49-slot retype + mechanical projections + xgen-common §4.1.b additive-API already landed, `cargo build -p xgen-client --lib` clean), the remaining *declaration* retypes are Surfaces #3/#5/#6/#7 — which had **no Joe-approved slot table** at design close (only §2.x Initial Q-anchors; checkpoint #1 at J-142 verbatim-approved only §4.1.a/§4.3.0/§4.5.0). Per Lock 1 Trigger (a) + D-078, Clair grep-enumerated the actual slots and surfaced them for a checkpoint-#1-equivalent approval. The grep found **three drift findings** (`SessionState.home_node` is a `ws://` transport URL not a Node XGID → stays `String`; `lifecycle::ClientStateEvent` has zero identifier slots; `HealthState.operator_known` is a numeric count) + **three classification calls**, now locked at design doc **§4.6**: Q2 `subject_id` stays `String` (D-061 sentinel union `IdentityXgid` | `SUBJECT_ROOM`); Q3 `get_dag_tips(space_id)` keeps `&str` + `Borrow<str>` (Surface #2 Option α sibling); Q4 `EventContext.ai_identity_id → &IdentityXgid` (§3 governing principle). Plus ops.rs `home_node` keeps `NodeXgid` (no Surface #1 rework — it wraps a `ws://` URL via String→typed projection at the ops construction boundary, which is the §3 discipline; §4.1.a.iii reasoning corrected; flagged Pass-5 future-hygiene). **Six-file amendment atomic per D-074 forty-first instance**: design doc v1.4 → v1.5 (new §4.6 production-grounded classification + §4.1.a.iii reasoning correction + §2.x resolution pointers) + runbook v1.2 → v1.3 (§5/§7/§8/§9 Q-items corrected + T6/T10/T12/T15 renamed/reframed + §2.3 checkpoint #1 closure note + §14.5/§14.6) + JOURNAL J-144 + CLAUDE PLAY flip + this ROADMAP entry + version v1.48 → v1.49 + HANDOFF §3.1 updated. DECISIONS.md NOT amended. **Pass 4 "Honest longer work" count: TWO → THREE** — third prospective catch (after J-142 count drift + J-143 commit-shape), all before the affected production code. Honest data point for Pass 5: surfaces without a verbatim checkpoint-#1 classification table must be grep-enumerated and Joe-approved before retype; "Initial Q-anchor" prose is pre-walk shape-grounding, not authoritative — three of four surfaces' Q-anchors drifted from production here. Net Surfaces #3/#5/#6/#7 retype scope: #3 = 0 decl; #5 = 1; #6 = 2; #7 = 7. Next-active: resume Commit 1 against the §4.6 locks.

### XGID Retrofit Pass 4 Commit 1 PRE-CODE: commit shape RE-LOCKED at J-143 — per-surface 8-9 commits → one consolidated xgen-client retype atomic (Pass-3 shape)

✅ **J-143 commit-shape re-lock 2026-05-29**. At Commit 1 prep, `cargo build -p xgen-client --lib` reported **191 errors** — xgen-client is in the inherited Path A broken state (every surface consumes retyped Pass 1–3 upstream types). The J-141 per-surface commit sequence (Commit 1 = Surface #1 alone … Commit 7 = Surface #7) is **infeasible**: all seven surfaces share one crate (`xgen_client_lib`) that compiles as a unit, and Surfaces #2/#4/#6/#7 errors are independent of ops.rs, so no per-surface commit can leave the lib compiling and T1–T15 can't run until it does. Pass 3 avoided this by shipping all seven xgen-node surfaces in one atomic (Commit 2 `67fb48d`, ten-file). Joe re-locked the same shape for Pass 4: **Commit 1 = one consolidated xgen-client retype atomic** (all seven surfaces + xgen-common §4.1.b additive-API + all Surface #8 doc fragments + T1–T15; lib-clean + 8-GREEN verified there) → **Commit 1a** [contingent] test-fixture sweep → **Commit 2** milestone close (≈3 commits, matching Pass 3's 4). Doc-coupling principle preserved (fragments land inside Commit 1). **Five-file amendment atomic per D-074 fortieth instance**: runbook v1.1 → v1.2 (§2.1 commit table replaced + re-lock note + §1.2 + §2.3 + §14.4) + design doc v1.3 → v1.4 (§4.4.c 8-9-commit pre-frame marked SUPERSEDED) + JOURNAL J-143 + CLAUDE PLAY flip + this ROADMAP entry + version v1.47 → v1.48. DECISIONS.md NOT amended. **Pass 4 "Honest longer work" count: ONE → TWO** — second prospective catch (after J-142's count drift), both before any production code. Honest data point for Pass 5: a design-phase doc-vs-code commit-shape pre-frame must account for the consumer crate's single-crate-compilation constraint under Path A, not only doc-tree coupling.

### XGID Retrofit Pass 4 Commit 1 PRE-CODE: Joe-lock checkpoint #1 fired at J-142; §4.1.a slot count corrected 46 → 49 via Track 1 canonical-record amendment before any production code

✅ **J-142 Joe-lock checkpoint #1 fired 2026-05-29** (this commit). At Pass 4 Commit 1 session-open, Clair ran the LOAD-BEARING D-078 production-grounded verification of the three design-doc classification tables before touching code. `grep -cE '^\s+pub [a-z_]+: (String|Option<String>)' xgen-client/src/ops.rs` returned **49** String slots, not the **46** recorded at design close v1.2. Classification *substance* was 100% correct (every field → correct flavour); only the arithmetic drifted: `identity_id` ×4→×3; §4.1.a.i 31→33; §4.1.a.ii 12→11; §4.1.a.iii rows-vs-slots conflation 3→5; grand total 46→49 (37 retype + 12 stay). §4.3.0 (16 clap Args) + §4.5.0 (7 async-spawn @ desktop.rs L54/63/90 + ai_service.rs L554/575 + service.rs L183/202) verified CLEAN — all named contracts exist exactly as tabled. Joe locked **"amend design doc to 49, then code"** — Track 1 canonical-record amendment (sibling-shape J-129 Pass 3 runbook drift + J-133/J-134 design-doc amendments). **Five-file atomic per D-074 thirty-ninth instance + Lock #3 per-commit cadence**: design doc `tasks/XGID_RETROFIT_PASS_4_DESIGN.md` v1.2 → v1.3 (§4.1.0 + §4.1.a corrected; chain stripped per strict `Last updated`) + runbook `tasks/XGID_RETROFIT_PASS_4_IMPL.md` v1.0 → v1.1 (T1 renamed `..._46_slots_compile` → `..._49_slots_compile`; all live count refs corrected) + JOURNAL J-142 body entry + CLAUDE.md PLAY flip + this ROADMAP Past entry + version v1.46 → v1.47. DECISIONS.md NOT amended (Rule-5 arithmetic correction, no new principle). **Pass 4 "Honest longer work over fast shortcuts" count: 0 → ONE** — prospective catch at the design-doc-grounded verification layer; D-078 working exactly as designed, before a single production line or the misnamed `..._46_slots` test could ship. Discipline data point sharpened: recon estimates AND walk-time "corrected actuals" both drift; the only fully-authoritative slot count is `grep` against production at the implementation boundary. Checkpoint #1 now closes affirmatively; Commit 1 proceeds against the corrected 49-slot table.

### XGID Retrofit Pass 4 implementation runbook ✅ at J-141 (Status ACTIVE v1.0; 8-9 commits Option B Joe-locked-by-recommendation; Clair pickup at §3 Commit 1 next)

✅ **J-141 XGID Retrofit Pass 4 implementation runbook SHIPPED 2026-05-28** (this commit). Runbook `tasks/XGID_RETROFIT_PASS_4_IMPL.md` Status: ACTIVE v1.0 (~66 KB / 668 lines, fourteen sections — §1 framing + precedent-departure self-defense / §2 sequence overview with per-surface commit table + two split triggers + three Joe-lock checkpoints / §3-§9 per-surface Commits 1-7 (one per xgen-client surface, shared shape: scope + files + per-surface tests + verification + layered-B3 audit) / §10 Commit 7a [CONTINGENT] test-fixture sweep / §11 Commit 8 milestone close with DoD + verification rigour / §12 discipline notes six sub-sections / §13 cross-references / §14 footer authoring provenance). Sibling-in-shape to `tasks/XGID_RETROFIT_PASS_3_IMPL.md` COMPLETED v1.6 with structural extensions for Pass 4's per-surface-commit Option γ hybrid-split commit-sequence per design doc §4.4 lock at J-140. **Joe-lock at runbook authoring session (J-141): Option B locked-by-recommendation** for §2.1 commit-sequence shape — honest §4.4.4 application (no Commit 1 doc-pass; per-surface code+doc atomic × 7 + Commit 7a CONTINGENT + Commit 8 close = 8-9 commits expected total) over Option A (Pass-3-shape-mechanical retain zero-content Commit 1 doc-pass). The J-141 runbook-shipping commit IS the kickoff atomic per §4.4.b cross-surface fragments framing (ROADMAP + CLAUDE PLAY + JOURNAL bumps consolidate at milestone close, not as separate Commit 1). **Per-surface test target +15** (verbal lock at this authoring; Joe may adjust at checkpoint #1 approval): T1-T3 at Surface #1 (`ops_result_struct_field_retype_46_slots_compile` + `ops_result_struct_serde_transparent_wire_invariance` LOAD-BEARING wire-format invariance witness + `flavour_wrapper_is_empty_and_as_deref_additive_api_works`); T4-T5 at Surface #2 (`cli_args_clap_parse_stays_string_and_projects_typed_at_dispatch_arm` + `cli_format_path_typed_result_displays_via_display_impl`); T6-T7 at Surface #3 (`batch_get_dag_tips_space_xgid_at_dispatch_entry_boundary` + `batch_reply_json_serde_transparent_wire_invariance`); T8-T9 at Surface #4 (`tauri_command_return_serde_transparent_to_js_frontend` + `lifecycle_state_event_identifier_slots_retyped_descriptive_stays_string`); T10-T11 at Surface #5 (`client_identity_identifier_slots_retyped` + `session_state_on_disk_persistence_format_round_trip_string_at_boundary`); T12-T13 at Surface #6 (`ai_behavior_trait_method_signature_identifier_slots_typed` + `ai_pacing_tracker_per_space_xgid_key`); T14-T15 at Surface #7 (`pacing_per_space_sender_map_insert_retrieve_with_typed_key` + `temperature_event_payload_identifier_slots_retyped`). 14% more surfaces than Pass 3 (8 vs 7) + per-surface format-boundary witnesses lift count from Pass 3's T1-T11 to Pass 4's T1-T15. **Three Joe-lock checkpoints at §2.3** (remapped from Pass 3's pre-Commit-2 / post-Commit-1 / post-Commit-2 because Pass 4 has no Commit 1 doc-pass): **#1 pre-Commit-1 verbatim classification-table approval** (Clair extracts design doc §4.1.a 46-slot classification + §4.3.0 16 clap Args slots + §4.5.0 7 async-spawn sites verbatim; Joe approves before any production code lands; LOAD-BEARING D-078 application surface for Pass 4); **#2 post-Commit-1 first-surface drift check + wire-format invariance witness verification** (three drift-detection points: ops.rs Result struct retypes landed atomically with Appendix F doc fragment / Pass 1 additive-API extension shipped at xgen-common per §4.1.b Option β / T2 wire-format invariance witness passes); **#3 post-Commit-7 split-trigger decision per ~50-error threshold** (sibling-shape Pass 2 + Pass 3 pre-locked contingent-split posture durable cross-Pass discipline per JOURNAL J-138 Sub-section 2). **Two split triggers at §2.2** mirror Pass 3 §2.2 but adapted: Trigger (a) non-existent production contract per design doc §4.1.a + §4.3.0 + §4.5.0 verbatim tables (D-078 applies; Pass 3 J-129 + J-133 sibling-shape); Trigger (b) family-boundary size split if any Commit 1-7 exceeds ~600 lines diff. **§12 discipline notes six sub-sections** (lighter than Pass 3's nine because Pass 4 inherits more cross-Pass discipline carry-overs from JOURNAL J-138 Sub-section 2 without re-derivation): §12.1 precedent-departure self-defense at runbook layer; §12.2 Option B commit-sequence Joe-locked-by-recommendation at runbook authoring; §12.3 Pass 1 additive-API extension at xgen-common flavour wrappers (second instance load-bearing carry-over); §12.4 format-boundary preservation Option γ split (D-NNN-format-boundary stays OPEN); §12.5 D-NNN-ε CLOSED by honest framing (sibling-shape Pass-internal precedent); §12.6 layered-B3 expected null per four-instance Pass-arc no-finding chain durability. **§13 cross-references** anchor design doc + predecessor runbooks (Pass 1 v2.1 J-122 + Pass 2 v1.1 J-126 + Pass 3 v1.6 J-138) + trilogy precedent + ten cross-cutting principles + JOURNAL J-138 Sub-section 2 cross-Pass discipline carry-over enumeration. **Strict `Last updated` discipline applied per Joe instruction at J-141 turn**: runbook + ROADMAP + CLAUDE.md + JOURNAL.md headers carry ONLY `> **Last updated**: 2026-05-28` (no parenthetical chain; substantive amendment history lives in §14 footer authoring provenance + JOURNAL J-141 body). Memory entry saved (`feedback_last_updated_strict`) for future sessions. **Pass 4 "Honest longer work over fast shortcuts" count stays at zero** at this runbook authoring (J-139 design-open + J-140 design close + this J-141 runbook ship are within-milestone substantive events; close-event-not-recurrence-event framing inherited from J-128 Pass 3 runbook authoring + J-124 Pass 2 runbook authoring + J-126 / J-138 milestone-close framing). **D-074 application count: thirty-eighth instance** + Lock #3 per-commit cadence; not milestone-close so milestone-close tally — fourteenth at J-138 — does NOT increment. **Four-file atomic at this v1.0 runbook ship**: this runbook NEW v1.0 ACTIVE + ROADMAP v1.45 → v1.46 (visual tree Pass 4 design row ✅ + new runbook ✅ sub-bullet + Surface #1 🟢 next-active + this Past entry) + CLAUDE.md PLAY block flip "Pass 4 design CLOSED; runbook authoring next-active" → "Pass 4 implementation ACTIVE; Clair pickup at runbook §3 Commit 1" + JOURNAL J-141 body entry. DECISIONS.md NOT amended (no new principles locked at runbook authoring; D-NNN-format-boundary stays OPEN per design doc §4.2.3; D-NNN-ε closure inherited per design doc §4.5.3). **Honest data point recorded per D-065**: ROADMAP Present section drift from J-138/J-139/J-140 noted at J-140 Sub-section 12 as "candidate fix-up at next session" is NOT addressed in this commit per minimal-change discipline — J-141 scope is runbook-authoring atomic, not Present drift-fix. Candidate fix-up remains open for a separate atomic. **Next-active for Clair**: pickup at runbook §3 Commit 1 (Surface #1 M5 Ops Layer code+doc atomic). Read CLAUDE.md PLAY block + JOURNAL J-141 entry first per Rule 0, then runbook §1-§3 in order, then design doc `tasks/XGID_RETROFIT_PASS_4_DESIGN.md` v1.2 §4.1.a + §4.3.0 + §4.5.0 classification tables verbatim (Joe-lock checkpoint #1 requires verbatim classification-table approval before any production code touches). **Next-active for Chat Claude (standby)**: parallel-eligible items unchanged (M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6 ~35 verbs across 7 categories) if Joe selects parallel-track work. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079.

### XGID Retrofit Pass 4 design phase CLOSED at J-140 (single-session full close; design doc v1.0 → v1.2 ACTIVE → COMPLETED)

✅ **J-140 XGID Retrofit Pass 4 design phase CLOSED 2026-05-28** (this commit). Design doc `tasks/XGID_RETROFIT_PASS_4_DESIGN.md` Status: ACTIVE → COMPLETED v1.0 → v1.2 single-session full close (two-session split eligible per Pass 3 J-127 Sub-section 8 data point (c) but not exercised per "let us move ahead" mid-session pivot from Option II pause to Option I continue; honest framing recorded at §6.2 + §7.5). **§3 governing principle locked inherited unchanged from Pass 2 §3 + Pass 3 §3** — four-instance Pass-arc inheritance established (Pass 1 implicit at runbook; Pass 2 explicit at J-123; Pass 3 explicit at J-127; Pass 4 explicit at this J-140 lock). Sanity-check loop across seven xgen-client surfaces + Surface #8 doc-tree confirmed clean — no Pass-4-specific wrinkle at §3 layer; three wrinkle-candidates considered honestly per D-065 (Result-struct serde-transparent; async-spawn captures; Tauri IPC frontend) all deferred to §4 turf per Pass 3 §4.3 precedent (format-boundary is §4 application of §3 principle, not §3 amendment). **All five §4 anchors locked**: §4.1 Surface #1 M5 Ops Layer composite (§4.1.0 honest recon corrections per Rule 5 — 15 pub structs not 16, 46 String slots not 45, pre-guessed borderlines `since`/`tip_event_id`/`pubkey_uri` absent from ops.rs / §4.1.a 46-slot classification — 31 mechanical identifier retypes + 12 descriptive stays + 3 borderline locks: 2 NodeXgid retypes for `home_node` ×3 + `node` + 1 String stay for `source` operator-source enum-tag / §4.1.b Pass 1 additive-API extension Option β locked — inherent `.is_empty()` on flavour wrappers + Option `.as_deref()` per Pass 1 Commit 4 precedent over per-site rewrite / §4.1.c serde-transparent wire-neutrality confirmed at Surface #1 boundary); §4.2 format-boundary preservation Option γ split locked — A (Surface #1 stdout JSON) + B (Surface #3 named-pipe JSON) consolidate under Pass 3 wire-shape boundary class (no new count) + C (Surface #4 Tauri IPC) recognised as fresh boundary class at Pass 4; **D-NNN-format-boundary promotion-watch STAYS OPEN** at 3 structurally-distinct instances across 2 Pass-arc (Pass 4 boundary class is fresh-at-Pass-4 so D-077 multi-Pass-arc durability not yet met; fourth structurally-distinct instance at Pass 5 OR cross-milestone — M6/M7 admin write path or possible future gRPC/WebRTC/HTTP API surfaces — closes durability gap and promotes to D-080); §4.3 CLI arg parsing Option α locked — clap parse stays String at all 16 identifier-shaped Args slots (AiDelegate/AiRevoke/AiStatus/CreateRoom/Invite/Join/Send/HistoryArgs); project at dispatcher arm via Pass 1 wrapper constructor chain; FromStr explicitly deferred per D-071 — validated FromStr is the rung above per D-079 honest-framing precedent; §4.4 doc-vs-code commit-shape Option γ hybrid split locked — per-surface-coupled doc fragments ship atomic with their code surface commit (Appendix F §F.0.6 + xgen_aicontrol_implementation.md AI resident + Ch6 §6.15 pacing+temperature) + cross-surface or content-shape doc fragments consolidated in milestone-close commit per D-074; runbook commit-sequence pre-frame: 8-9 commits expected at next session-arc runbook authoring (heavier than Pass 3's 4 but lighter than trilogy's ~12 per Pass-internal-consistency framing); §4.5 async-spawned task captures sub-rule extension Option γ honest framing closure locked — 7 Pass 4 instances at Surface #4 + #6 (3 Tauri commands + 4 tokio::spawn) confirm ubiquity across structurally-different boundary classes; **D-NNN-ε promotion-watch CLOSED by honest framing** per D-065 (rule is Rust language idiom `'static` bound on tokio::spawn, not XGen-specific decision) + D-079 (record-at-canonical-document-not-D-NNN-if-rule-is-mechanical precedent); D-NNN slot preserved for actual XGen-specific decisions. **Cross-Pass discipline carry-over implication**: Pass 3 §4.5 J-127 + Pass 4 §4.5 this lock establish **two-instance pattern of honest-framing-resolution of promotion-watches at Pass-arc design close** — second instance establishes that promotion-watch close-by-honest-framing is a valid discipline action alongside promotion-by-honest-framing (Pass 3 J-134's D-079 promotion atom). **§5 layered-B3 expected answer LOCKED null at full eight-surface scope** — four-instance Pass-arc no-finding chain established (Pass 1 J-122 + Pass 2 J-126 + Pass 3 J-138 + Pass 4 J-140 all null); per-surface audit at design phase confirms no layered surfaces emerge per Rule 5 + D-065 honest framing; runbook §6.5 audit at Clair's implementation-boundary re-runs honestly per Rule 0 + Rule 5 honest-audit-not-honest-assumption discipline; Pass-arc layered-B3 expected-null is now load-bearing cross-Pass discipline carry-over per JOURNAL J-138 Sub-section 2 framing. **§6 historical/future-pointer entries filled in Shape α pointer-style** per Pass 3 §6.1 + Pass 2 §6.7 precedent: §6.1 Pass 4 design phase historical record (J-139 design-open + J-140 single-session full close pointer-style; implementation milestone-close J-NNN placeholder frozen at runbook close per J-108 codification); §6.2 two-session walk shape eligible-but-not-exercised at Pass 4 + Pass-internal-precedent inheritance recorded honestly. **§7 discipline notes consolidated five sub-sections** per Pass 3 §7 + Pass 2 §7 precedent shape: §7.1 honest recon corrections per Rule 5 + D-065 (single-digit drift between recon estimates and walk-time actuals — discipline data point for Pass 5 + future Pass-arc recon expectations); §7.2 format-boundary preservation Option γ split as honest-framing-resolution shape; §7.3 doc-vs-code commit-shape decision at design phase per D-069 + D-071 audit-precedes-dependent-design framing (runbook commit-sequence is downstream consequence of doc-tree coupling shape); §7.4 honest-framing-resolution of promotion-watches admits three shapes — promote (D-079 promotion atom precedent) / close-by-honest-framing (Pass 4 §4.5 D-NNN-ε) / hold-open-by-surface-diversity-threshold (Pass 4 §4.2 D-NNN-format-boundary); §7.5 two-session walk shape eligible-but-not-exercised at Pass 4 + revised discipline framing (bimodal Pass-arc precedent state — single-session at Pass 2 + Pass 4; two-session at Pass 3; future authors choose per same-session-capacity assessment without violating precedent). §7.6 candidate (Joe-locks-by-recommendation as inline-lock pattern fifth recurrence) skipped per minimal-broadening discipline (Pass 2 §7.2 already codifies). **Pass 4 "Honest longer work over fast shortcuts" count stays at zero** at this design close (J-139 design-open + this J-140 design close are within-milestone substantive events, not recurrence shape — sibling-shape to topo-sort J-098/J-099 within-milestone events; close-event-not-recurrence-event framing inherited from J-126 + J-138). **D-074 application count: thirty-seventh instance** + Lock #3 per-commit cadence; not milestone-close. **Four-file atomic at this design close**: design doc v1.0 → v1.2 + Status ACTIVE → COMPLETED + CLAUDE.md PLAY block flip "design phase ACTIVE" → "design phase CLOSED; runbook authoring next-active for Chat Claude + Joe in fresh session" + this ROADMAP v1.44 → v1.45 (visual tree row 🟢 → ✅ + this Past entry) + JOURNAL J-140 body entry. **Honest framing per D-065 on two-session split eligibility**: Pass 4 was eligible for two-session split per Pass 3 J-127 Sub-section 8 data point (c) but landed as single same-day session per "let us move ahead" continuation directive after §3 + §4 walk-and-lock first segment — mid-session pivot from Option II pause to Option I continue recorded honestly at §6.2 + §7.5. Discipline data point: precedent eligibility ≠ precedent exercise; bimodal Pass-arc precedent state recorded. **Next-active for Chat Claude + Joe**: Pass 4 implementation runbook authoring at `tasks/XGID_RETROFIT_PASS_4_IMPL.md` in fresh session per Pass 2 J-124 + Pass 3 J-128 design-then-runbook precedent. Runbook §3 commit-sequence inherits §4.4 Option γ hybrid-split pre-frame: per-surface code+doc atomic × 7 + Commit 2a test-fixture sweep contingent split (sibling-shape to Pass 3 §4a contingent-split posture pre-locked) + milestone-close commit → 8-9 commits expected total. Three Joe-lock checkpoints anticipated per Pass 3 precedent: #1 post-Commit-1 doc-pass drift; #2 pre-Commit-2 verbatim surface-list (Chat Claude extracts §4.1.a + §4.3.0 + §4.5.0 field-classification tables verbatim; Joe approves by name); #3 post-Commit-2 split-trigger per ~50-error threshold heuristic. **Next-active for Clair (standby)**: parallel-eligible items unchanged (M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079.

### XGID Retrofit Pass 4 design phase OPENED at J-139 (§1 framing + §2 surface enumeration at v1.0)

✅ **J-139 XGID Retrofit Pass 4 design phase OPENED 2026-05-28** (this commit). Design doc `tasks/XGID_RETROFIT_PASS_4_DESIGN.md` Status: ACTIVE v1.0 ships §1 framing + §1.2 precedent-positioning relative to Pass 3 + §1.3 NOT scope + §2 surface enumeration across seven xgen-client code subsystems + Surface #8 doc-tree sweep + §2.9 honest broadening + §2.10 pre-walk reconnaissance summary; §3-§7 deferred to subsequent walk-and-lock sessions per Pass 3 v1.0 → v1.1 → v1.2 precedent (Joe-lock at session open). **Pre-walk reconnaissance delivered via parallel Explore subagent** under "very thorough" search level + explicit no-file-modification guard-rail (sibling-shape to Pass 3 Commit 2a parallel-subagent-sweep discipline at runbook §9.7 but at design-phase open layer rather than test-fixture sweep). Subagent structured report covered xgen-client String identifier slots per file + Pass 4 forward-looking markers count + xgen-client build baseline state per Path A + test infrastructure + doc surfaces sized + subsystem ownership map. **Reconnaissance findings**: 192 xgen-client compilation errors per Path A across three structural categories (Result struct field type-mismatches + method availability on Xgid newtypes + HashMap key-value slot mismatches); zero `// Pass 4 widens` markers in workspace (three-instance sparsity chain at Pass-arc level durable per N+1 design discipline — Pass 1 → 33 markers, Pass 2 → 1 marker, Pass 3 → 0 markers); 42 String identifier slots across 7 subsystems (highest density ops.rs 16 pub Result struct types + ~45 slots + app.rs 42 occurrences); doc surfaces sized Appendix F 1193 lines + xgen_aicontrol_implementation.md 544 lines + Ch6 §6.15 lines 1326-1388+ (confirms Pass 4 heaviest doc-work pass per J-095 sweep classification). **Seven subsystem surfaces enumerated in dependency order**: Surface #1 M5 Ops Layer (foundational serde-transparent boundary; HIGHEST IMPACT) + Surface #2 CLI Dispatcher + Surface #3 Batch Pipe Dispatch + Surface #4 Tauri Shell + Surface #5 Session State (foundational consumed by all four prior) + Surface #6 AI Resident + Surface #7 Pacing + Temperature + Surface #8 doc-tree sweep. **Three precedent-positioning notes at §1.2**: Pass 4 is doc-heavy Pass; zero Pass 4 markers; Path A inherited break state three-instance durable. **Two D-NNN promotion-watches from J-138 Sub-section 8 advance at Pass 4**: D-NNN-format-boundary (third-instance threshold opens if client-side serialisation slot instantiates at Surface #1/#4/#8 walks) + D-NNN-ε async-spawned task captures (promotion-watch opens if structurally-different fifth instance fires at xgen-client async surfaces — Tauri commands, AI service spawns, batch dispatcher workers — at Surface #4/#6 walks). Both flagged-not-promoted at this design open per D-069 honest framing. **Pass 4 "Honest longer work over fast shortcuts" count starts fresh at zero** (J-126 + J-138 close-event-not-recurrence-event framing inherited; design-open is within-milestone substantive event starting milestone scope fresh). **D-074 application count: thirty-sixth instance** + Lock #3 per-commit cadence; not milestone-close. **Four-file atomic** at this design open: this design doc NEW v1.0 + CLAUDE.md PLAY block flip + this ROADMAP v1.43 → v1.44 + JOURNAL J-139 body entry. **Sibling-in-shape to Pass 2 J-123 + Pass 3 J-127 design opens** with two structural differences: parallel-subagent reconnaissance vs inline grep (discipline data point worth recording at §9.x amendment-provenance once Pass 4 design doc reaches v1.x with §7); seven heterogeneous-subsystem surfaces vs Pass 3's seven module-family-concentrated surfaces. **Next-active for Chat Claude + Joe**: subsequent walk-and-lock sessions for §3 governing principle (expected inherited from Pass 2 unchanged per Pass 1+2+3 chain durability) + §4 architectural decisions (Q-anchors per surface lock + two D-NNN promotion-watches resolve) + §5 layered-B3 expected answer (expected null per three-instance no-finding chain durable) + §6 historical/future-pointer + §7 discipline notes. After design close at v1.x COMPLETED, Pass 4 runbook authoring opens in fresh session per Pass 2 J-124 + Pass 3 J-128 precedent. **Next-active for Clair (standby)**: parallel-eligible items unchanged (M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079.

### XGID Retrofit Pass 3 milestone CLOSED at J-138

✅ **XGID Retrofit Pass 3 milestone CLOSED 2026-05-28 at J-138**. Four Clair-facing atomic commits on `main` per D-074 thirty-fifth instance + fourteenth milestone-close: Commit 1 `1be0249` doc-pass (J-131; Option C hybrid minimal; honest two-file atomic; honest count discrepancy resolved at J-132 Path-(iii) amend-in-place); Commit 2 `67fb48d` seven-surface retype atomic (J-136 ten-file; xgen-{common,core,node} libs CLEAN; Path 2 (Commit 2a split) locked at Joe-lock checkpoint #3 against 638 test-fixture errors >> ~50 threshold per runbook §5.1); Commit 2a `0cdf0ad` test-fixture sweep (J-137 thirty-file atomic; 638 errors closed via parallel-subagent delegation under per-crate guard-rails + 11 per-surface tests T1-T11 atomic per runbook §4.7; 8/8 GREEN verification at milestone-bearing boundary); this Commit 3 milestone-close commit. **Final test count: 589** (34 xgen-common lib + 8 invariance + 453 xgen-core + 88 xgen-node lib + 6 precedence); +98 net delta vs Pass 2 J-126 baseline of 491. Negative delta vs J-119's 627 stays expected per Path A inherited from Pass 1 + Pass 2 (xgen-client consumes retyped types; Pass 5 close restores). **Layered-B3 audit answer**: zero layered surfaces emerged (third Pass-arc no-finding instance — three-instance chain durable). **Pass 3 "Honest longer work over fast shortcuts" final count: TWO recurrences** at canonical-record-amendment layer (J-129 runbook v1.0 surface ordering drift + J-134 design doc §2 v1.3 → v1.4 in-place rewrite-correction; both prospective catches at Clair's session-open Rule-0 audits before any production code touched). Plus three honest data points inline without recurrence-increment (J-130 silent-gitignore-skip; J-131 honest count discrepancy; J-135 T11 prospective catch). **All three Joe-lock checkpoints fired as authored** at runbook §2.3: #1 post-Commit-1 doc-pass drift check at J-131 + resolution at J-132; #2 pre-Commit-2 verbatim surface list approval at J-133+J-134+J-135 triple-canonical-record-amendment arc; #3 post-Commit-2 split-trigger at 638 errors → Path 2 split locked. **Pre-locked contingent-split posture validated for second sibling milestone in a row** (Pass 2 fired at 93 errors → Pass 3 fired at 638 errors; same authoring discipline + ~7× scaling without re-derivation of decision protocol) — D-NNN-δ candidate promotion-watch advances at this milestone close. **Four candidate D-NNNs promotion-watch (none promoted per D-069)**: γ small-cardinality vs large-cardinality identifier-keyed maps (second instance at Pass 3 Surface #1) + δ pre-locked contingent-split posture (second validation instance) + ε async-spawned task captures force owned (four instances at one xgen-node module-family; promotion-watch opens at Pass 4 structurally-different fifth) + format-boundary preservation wire OR persistence (two conceptual instances; three-instance threshold opens at Pass 4 client-side serialisation slot). Full enumeration at JOURNAL J-138 Sub-section 8. **Verification at close**: 589 GREEN; both clippy gates clean (`--lib` + `--tests`, `-D warnings`); `cargo build --workspace` deliberately broken per Path A; grep `J-NNN` guardrail per J-108 codification returns ZERO at freeze-site sources post-staging; both pre-existing documented flakes did NOT fire across the 8 GREEN runs at J-137 milestone-bearing boundary nor at this milestone-close re-verification pass. **Cross-Pass discipline carry-overs (load-bearing for Pass 4 + Pass 5)**: Path A three-instance durability established; Borrow<str> additive API inherited at all HashMap lookup sites; layered-B3 expected-null structural fact; Pass-internal-consistency framing over trilogy-internal-consistency; pre-locked contingent-split posture as default. **Five-file atomic at milestone close**: runbook v1.5 → v1.6 ACTIVE → COMPLETED + new §9.8 amendment-provenance; design doc §6.1 J-NNN placeholder frozen to J-138 + header chain; JOURNAL J-138 body entry (eight sub-sections sibling-shape to J-122 + J-126) + J-136/J-137 self-reference freezes; CLAUDE.md PLAY block flip "Pass 3 implementation ACTIVE — Commit 2a ✅" → "Pass 3 milestone CLOSED at J-138; standby for next-milestone selection (Pass 4 + M6 (new) both ready)"; this ROADMAP v1.42 → v1.43 + visual tree Pass 3 row 🟢 → ✅ + this Past entry + Near future Pass 3 line removed. **What unblocks**: XGID Retrofit Pass 4 (xgen-client consumer-side retypes; runbook authoring is the next Chat Claude work-shape on the XGID retrofit track). M6 (new) Node admin write path stays unblocked-but-not-selected. Pass 4 + M6 (new) are both ready for selection; sequencing is Joe's call. **Track 1 (Clair): stood down** until Joe picks next-active. **Track 2 (Chat Claude): standby for next-milestone selection.** Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079 + grep guardrail scope discipline.

### XGID Retrofit Pass 3 Commit 2a SHIPPED (test-fixture sweep + 11 per-surface tests T1-T11 atomic; 8/8 GREEN verification; 589 tests stable)

✅ **J-137 XGID Retrofit Pass 3 Commit 2a SHIPPED 2026-05-28** — thirty-file atomic per D-074 thirty-fourth instance + Lock #3 per-commit cadence. Test-fixture projection sweep across xgen-core + xgen-node closes the 638-error count reported at Commit 2 lib-clean boundary (160 + 478 = 638; runbook §5.1 ~50 split threshold blown past → Path 2 locked at Joe-lock checkpoint #3 at Commit 2 ship). Sweep delegated to two parallel subagents (one per crate) under DO-NOT-CROSS-CRATE-BOUNDARY guard-rails + mechanical-projection-only-per-§5.2-verbatim-patterns instruction set. Both completed clean: xgen-core (4 files / 0 deviations); xgen-node (~20 files / 6 minor deviations all honest-reported per Rule 1 — two `idx` loop-variable shadowing renames cosmetic, one app.rs NodeXgid unused-import cleaned at integration, one federation_delta_integration replace_all collision recovered, one reconnect_integration mock-receiver wire-format projection intentional per §4.3, one smoke.rs `Vec<NodeXgid>::contains(&String)` rewritten to `.iter().any()` semantic-equivalent). Plus 11 per-surface tests T1-T11 added per runbook §4.7 by name + production anchors verified — T1-T4 at xgen-core/src/node/runtime.rs `mod persistence_amendment_commit_2a_tests` (Surface #1 three tests + Surface #2 dispatch_event); T5 at xgen-node/src/federation_session.rs new `mod tests` (Surface #3 typed slots); T6 at xgen-node/src/fanout.rs `mod tests` (Surface #4 topological_sort Pass 1 sentinel + EventXgid Ord delegation); T7 + T8 + T11 at xgen-node/src/app.rs `mod tests` (Surface #5 — persistence-format round-trip Q5.12 + handle_federation_incoming forced-owned Q5.2 + run_federation_session_post_handshake forced-owned Q5.14 v1.3 13-param matrix); T9 + T10 at xgen-node/src/reconnect.rs `mod tests` (Surface #6 — three spawned functions forced-owned + Arc<NodeXgid> shared reference). **Verification at Commit 2a milestone-bearing boundary**: 8/8 GREEN runs (5 isolated with `cargo clean -p xgen-common -p xgen-core -p xgen-node` between each + 3 consecutive workspace runs); test count stable at **589 = 34 xgen-common lib + 8 invariance + 453 xgen-core + 88 xgen-node lib + 6 precedence** across all 8 runs; both clippy gates clean (`--lib` + `--tests` -D warnings; six nits closed at integration time: `.get().is_some()` → `.contains_key()` in T1+T2, redundant closure `|e| event_id_str(e)` → `event_id_str` in agent-sweep fanout.rs, useless `vec![room_id.clone()]` → `[room_id.clone()]` in phase9_compound_c7); `cargo build --workspace` deliberately broken at xgen-client consumer sites only per Path A inherited from Pass 1 (192 errors all xgen-client; no regression at xgen-common + xgen-core + xgen-node); both pre-existing documented flakes did NOT fire across 8 GREEN runs (precedence env-var race; `reconnect_with_existing_tip_small_delta_delivered` — flakes stay documented in CLAUDE.md as known per Rule 2). **Parallel-subagent-sweep discipline data point** recorded at runbook §9.7 for future Pass-arc Commit-2a-shape runbook authors: when test-fixture sweep error count exceeds ~500, parallel-subagent delegation under per-crate guard-rails is a viable shape; discipline cost is explicit honest-deviation reporting at integration time (Rule 1) + per-crate independence verification (zero cross-crate file modifications via `git diff --stat`); pre-Commit-2a 8-GREEN verification catches integration-edge regressions. **Sibling-in-shape to Pass 2 Commit 2a `58b94a5`** (J-126 arc): test-fixture sweep + per-surface tests atomic, 8 GREEN runs, D-074 atomic-purpose-discipline preserved. Pass 3's thirty-file atomic substantially heavier than Pass 2's nine-file due to (a) 638 errors vs 93 (~6.9× heavier driven by Path A break inheritance + heavier Pass 3 surface count); (b) 11 per-surface tests vs 2 (driven by seven-vs-five surface count + structurally novel async-spawned-forced-owned + persistence-format boundary + HashMap-key retype atomic patterns per design doc §7.1 precedent-departure self-defense framing); (c) parallel-subagent delegation discipline data point recorded as Commit-2a-shape extension. **"Honest longer work" Pass 3 count stays at TWO** inherited from J-129 + J-134 (Commit 2a is within-milestone substantive event, not recurrence shape). **D-074 application count: thirty-fourth instance**; not milestone-close so milestone-close tally stays at thirteenth from J-126. **Next-active for Clair**: pickup at runbook §6 Commit 3 milestone close — Pass 3 PLAY → DONE; design doc §6.1 J-NNN freeze per J-108 codification; runbook ACTIVE → COMPLETED v1.5 → v1.6; ROADMAP visual tree 🟢 → ✅; CLAUDE PLAY flip "standby for next-milestone selection"; grep J-NNN guardrail = ZERO post-staging. **Next-active for Chat Claude (standby)**: parallel-eligible items unchanged. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079.

### XGID Retrofit Pass 3 Commit 2 SHIPPED (Path 2 split per Joe-lock checkpoint #3)

✅ **J-136 XGID Retrofit Pass 3 Commit 2 SHIPPED 2026-05-28** under Path 2 (Commit 2a split) per Joe-lock checkpoint #3. Ten-file atomic per D-074 (thirty-third instance) + Lock #3 per-commit cadence: five code files (xgen-core/src/node/runtime.rs + xgen-node/src/{federation_session,fanout,app,reconnect}.rs) + one doc file (docs/xgen_appendix_d_en.md Surface #7 four-row classification sweep) + four meta files (runbook v1.3 → v1.4 with new §9.6 amendment-provenance + this JOURNAL J-NNN body entry + ROADMAP v1.40 → v1.41 with this Past entry + visual tree row + CLAUDE.md PLAY flip). **Seven-surface retype atomic**: Surface #1 NodeRuntime six per-space HashMap keys → SpaceXgid (spaces / stores / graphs / pending / dm_proposals / space_local_metadata per Q1.1; internal helpers retyped per Q1.4; public API all_events + dag_tips retyped per Q1.5) + Surface #2 dispatch_event peer_node_id: Option<&str> → Option<&NodeXgid> per Q2.1 Joe-lock Q-B (borrowed boundary; D-075 fed_add_drain_pair retyped typed; drain_pending_by_identity + drain_pending_by_federation_relationship signatures retyped per Q2.5) + Surface #3 federation_session.rs handler identifier slots (stream_federation_delta &[SpaceXgid] + &NodeXgid + peer_tips stays BTreeMap<String, String> per §4.3 wire-format + apply_federation_push &NodeXgid per Q3.5; **J-134 Finding B / D-079 closure**: dropped Vec<String> annotation at federation_session.rs:248 — SpaceState.federation_nodes is already Vec<NodeXgid> post-Pass 1 Commit 4) + Surface #4 fanout.rs (ClientSenders IdentityXgid + FederationPeerSenders NodeXgid + FanoutRequest.new_joiner Option<IdentityXgid> + event_space_id Option<SpaceXgid> per Q4.1 Joe-lock Q-C forced-owned + apply_fanout &IdentityXgid + collect_sync_history &IdentityXgid with since/continue_from String per §4.3 wire-format + compute_federation_delta_for_space &SpaceXgid + Option<&EventXgid> + topological_sort_events HashSet<EventXgid>) + Surface #5 app.rs (12 in-memory identifier slots per Q5.1+Q5.3+Q5.6+Q5.15; 4 persistence-format String per Q5.8+Q5.9+Q5.10+Q5.11 §4.3; handle_federation_incoming T8 forced-owned per Q5.2 §4.2 v1.2 row 3 async-spawned-task-captures; run_federation_session_post_handshake T11 per Q5.14 v1.3 13-param matrix; ConnectedClientInfo.identity_id IdentityXgid per Q5.15; Q3-overload IdentityXgid projection at process_inbound + apply_fanout call sites for the identity-parameter-position slot per Phase 4 §3.4.1 Q3 lock) + Surface #6 reconnect.rs (three spawned functions forced-owned per Q6.1+Q6.2+Q6.3 + AttemptCursor HashMap<NodeXgid, u32> per Q6.4) + Surface #7 Appendix D (four markdown table classification rows annotated typed-XGID-in-memory + String on-disk + on-wire per §4.3). **Verification at Commit 2 boundary**: `cargo build -p xgen-common -p xgen-core -p xgen-node` CLEAN; `cargo clippy -p xgen-common -p xgen-core -p xgen-node --lib --all-features -- -D warnings` CLEAN; `cargo build --workspace` deliberately broken at xgen-client consumer sites per Path A inherited from Pass 1; `cargo test -p xgen-common -p xgen-core -p xgen-node --tests` **638 errors total (xgen-core 160 + xgen-node 478)** at Joe-lock checkpoint #3 against runbook §5.1 ~50 split threshold → **Path 2 (Commit 2a split) locked**. Full 8 GREEN runs per §4.9 fire at Commit 2a per §5.3 (deferred-GREEN framing for the contingent-split atomic-purpose-discipline). **WIP-branch lineage**: branch `wip/pass-3-commit-2-in-flight` carried two checkpoint commits (`728b834` Surfaces #1+#2+#3+#4 lib-clean + #5 partial + `2f647bf` Surfaces #5+#6 closed + xgen-node lib CLEAN); squashed at this Commit 2 ship per D-074 atomic discipline (single-commit per surface-set per the §4.10 Files-in-this-commit framing). **Two discipline data points surfaced for JOURNAL J-NNN body**: (a) Pass-1-pre-walk reconnaissance Pass 3 marker sparsity — only ONE `// Pass 3 widens` marker exists in production (xgen-core/src/node/runtime.rs:588) vs the 33 Pass 2 markers from J-125 audit; expected per Pass-arc framing (pre-walk designed for N+1 not N+2) but worth recording for Pass 4 + Pass 5 pre-walk discipline; (b) Surface #4 fanout.rs "verification only" framing vs actual-lift discrepancy — runbook §4.1+§4.2 framed Surface #4 as "verification only — likely 0 code changes" but actual baseline showed 9 errors at fanout.rs (Pass 1+2 propagation into xgen-node Path A inherited break state); substantive Surface #4 work landed to close propagation errors AND design Q4.1-Q4.7 retypes. **"Honest longer work over fast shortcuts" Pass 3 count stays at TWO** (Commit 2 ship is within-milestone substantive event not recurrence shape; count inherited from J-129 + J-134). **D-074 application count: thirty-third instance** Lock #3 per-commit cadence; not a milestone-close so milestone-close tally (thirteenth at J-126) does NOT increment. **Sibling-in-shape to Pass 2 Commit 2 `22765a0`** (eight-file atomic, same lib-only-verification framing; Pass 3's ten-file atomic is heavier by two files — reconnect.rs as distinct Surface #6 + Appendix D Surface #7 doc-tree sweep folded in per Q7.5 minimal-touch guidance). **Next-active for Clair**: pickup at runbook §5 Commit 2a (test-fixture projection sweep across xgen-core + xgen-node + 11 per-surface tests T1-T11 atomic + 8 GREEN runs per §5.3 + §4.9), then Commit 3 milestone close per §6. **Next-active for Chat Claude (standby)**: parallel-eligible items unchanged (M6 (new) Block 4 verb-by-verb walks). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + D-079.

### XGID Retrofit Pass 3 J-135 runbook v1.2 → v1.3 add T11 (D-078 application at test-enumeration layer)

✅ **J-135 Pass 3 runbook v1.2 → v1.3 SHIPPED 2026-05-28** — single-file amendment adding T11 (`run_federation_session_post_handshake_spawned_task_owns_typed_captures`) by name to runbook §4.7 Surface #5 per D-078 application at test-enumeration layer. Joe-lock-checkpoint-#2 follow-on after the J-133 Q5.14 v1.3 rewrite added `run_federation_session_post_handshake` to design doc §2.5 sub-region 2 "Top-level orchestrators" post the original +10 test enumeration authored at J-128. T11 pins the async-spawned forced-owned NodeXgid + Vec<SpaceXgid> capture-shape at app.rs:1152 across the spawn boundary (distinct sub-region from T8 which covers handle_federation_incoming at app.rs:976 wire-format-handler async-spawn pattern). Production anchor verified at J-135 author-time per D-078: app.rs:1151-1173 signature read with the three identifier-shaped slots (home_node_id at line 1161, peer_node_id at line 1165, peer_shared_spaces at line 1169) all intact in the Q5.14 v1.3 shape locked at J-133. **D-078 working as designed**: when a Pass-3-scope retype target enters at canonical-record amendment time, the test enumeration is verified against the new target before Commit 2 production code lands. Single-file atom per D-074 thirty-second instance. "Honest longer work" Pass 3 count does NOT increment — prospective-catch at test-enumeration layer pre-Commit-2; count stays at TWO inherited from J-129 + J-134. Sibling-shape to J-115/J-116 prospective-catch framing where catch happens before production code lands.

### XGID Retrofit Pass 3 J-134 design doc §2 v1.3 → v1.4 in-place rewrite-correction + D-NNN-κ promoted to D-079

✅ **J-134 Pass 3 design doc §2 v1.3 → v1.4 SHIPPED 2026-05-28** — in-place rewrite-correction of J-133's own Q3.6 v1.3 rewrite + D-NNN-κ promoted to **D-079** "Design-doc Q-table grounded by symbol-definition grep". Triggered when Clair's J-134 atom-prep D-078 grep against `SpaceState.federation_nodes` at xgen-core/src/space/state.rs:132 found the field IS already `Vec<NodeXgid>` retyped at Pass 1 Commit 4 `774fe9d` (J-122 close arc; confirmed via `git blame` + inline comment at state.rs:423). The J-133 Q3.6 v1.3 rewrite — authored by Clair from inference against the xgen-node-side federation_session.rs:248 local-variable annotation `Vec<String>` (a Pass-1-broken-by-Path-A intermediate-state compile error against the actual Vec<NodeXgid> source) — carried a wrong claim "retype lands at Q1.1 extension" that production already doesn't honor (the field IS already typed at Pass 1). Q3.6 v1.4 strikes the wrong sentence entirely; replaces with honest framing (inheritance from already-typed field; xgen-node call-site annotation fix at federation_session.rs:248 is Surface #3 inheritance-row work NOT a Surface #1 field-type extension). **Three-instance threshold met across two distinct catch-events** (J-133 Drifts #1+#2 in v1.0/v1.1/v1.2 original + J-134 Finding B in v1.3 rewrite) per sibling-shape to D-077/D-078 promotion-threshold framing → D-NNN-κ PROMOTED to D-079. **Canonical cautionary instance recorded** in D-079's narrative: fix-author re-instantiates the discipline-failure being fixed — the J-133 atom whose entire purpose was closing κ-drifts itself introduced a κ-drift because the amendment-author trusted a call-site annotation over a struct-def grep. The lesson: D-079 binds at every claim, not at the focal claim only; confidence in the surface being fixed displaces verification of adjacent claims. Three-file atomic per D-074 thirty-first instance (design doc + DECISIONS.md D-079 prepended + JOURNAL J-134 body). **"Honest longer work" Pass 3 count increments to TWO** — recurrence shape (J-133's wrong Q3.6 v1.3 reached canonical record on origin/main + needed honest fix at J-134; distinct from prospective-catch shape at J-115/J-116 + checkpoint #2 + J-135 that stopped before shipping).

### XGID Retrofit Pass 3 J-133 design doc §2 v1.2 → v1.3 Track 1 amendment at checkpoint #2

✅ **J-133 Pass 3 design doc §2 v1.2 → v1.3 SHIPPED 2026-05-28** — Track 1 canonical-record amendment at Joe-lock checkpoint #2 of Pass 3 Commit 2 prep. Triggered when Clair's D-078 verification of design doc §2 against production code surfaced two parameter-attribution drifts + one structural omission: **Drift #1** §2.3 Q3.6 claimed `apply_federation_push` parameter `peer_node_id: &str` — production at xgen-node/src/federation_session.rs:202-208 has 5 parameters (event, origin, runtime, federation_peer_senders, local_node_id) with NO peer_node_id parameter; destination peer flows internally as `peer_id` loop variable bound from federation_nodes snapshot at federation_session.rs:261. **Drift #2** §2.5 Q5.14 mis-attributed parameter to type — claimed `OutboundMsg.peer_node_id: String` (line 1165) as struct field — production: `OutboundMsg` is an ENUM at xgen-node/src/fanout.rs:31 with variants Event/HistoryBatch/SyncComplete and NO peer_node_id field; line 1165 in app.rs is a `peer_node_id: String` parameter on `pub(crate) async fn run_federation_session_post_handshake` (line 1152). **Drift #3** §2.5 "Three sub-regions" prose omitted `run_federation_session_post_handshake` from "Top-level orchestrators" alongside process_inbound + run_node. Both drifts share structural shape — parameter-attribution drift — opening candidate D-NNN-κ "design-doc Q-table-vs-production-code parameter-attribution discipline" flagged-not-promoted at two instances per D-069 (three-instance threshold wants instances across distinct catch-events; promotion-watch opens at Pass 4 design walk). Two-file atomic per D-074 thirtieth instance (design doc + JOURNAL J-133 body). "Honest longer work" Pass 3 count does NOT increment at J-133 — sibling-shape to J-115/J-116 prospective catches (checkpoint mechanism working as designed; catch happens before production code lands). **Subsequently incremented to TWO at J-134** when J-133's own Q3.6 v1.3 rewrite was found to carry a third κ-instance at J-134 atom prep.

### XGID Retrofit Pass 3 J-132 Path-(iii) amend-in-place at checkpoint #1 resolution

✅ **J-132 Pass 3 runbook v1.1 → v1.2 SHIPPED 2026-05-28** — Path-(iii) amend-in-place at checkpoint #1 resolution of the honest two-file-vs-three-file count discrepancy surfaced at J-131. §3.2 third-file line rewritten from "JOURNAL.md chain entry only" to "JOURNAL.md NOT amended post-strip" — post-J-129 strip-the-chain discipline per JOURNAL J-129 Sub-section 8 makes a chain-only entry a no-op + sibling-shape to J-123/J-124/J-125 chain-only doc-only milestone-event precedent suppresses body entry. §3.1 file-count corrected three → two. New §9.4 amendment-provenance sub-section recording v1.1 → v1.2 amendment + cross-forward note about §3.3 carry-forward inconsistency parked for future Pass-arc author consideration. Forward-looking doc-hygiene (preventing future Pass 4 + Pass 5 re-derivation of the same resolution). Single-file atom per D-074 twenty-ninth instance + Lock #3 per-commit cadence. CLAUDE PLAY does NOT flip (entry-point stays Commit 2). ROADMAP NOT touched. JOURNAL gets no entry (post-strip no-op — the very discipline being reconciled).

### XGID Retrofit Pass 3 Commit 1 doc-pass SHIPPED

✅ **J-131 XGID Retrofit Pass 3 Commit 1 doc-pass SHIPPED 2026-05-28** against amended v1.1 runbook (post-J-129 Track 1 canonical-record amendment) + cleared canonical record (post-J-130 silent-gitignore-skip drift-fix atom). Doc-pass minimal Option C hybrid per runbook §3.2 — three-file atomic per D-074 (twenty-eighth instance) + Lock #3 per-commit cadence PER RUNBOOK PROSE, but **honest count is two files (ROADMAP + CLAUDE.md)** because post-J-129 strip-the-chain discipline + sibling-shape to J-123/J-124/J-125 chain-only doc-only milestone-event precedent means JOURNAL.md gets no edit (the runbook's "JOURNAL chain entry only" framing was authored pre-J-129-strip; post-strip the chain doesn't exist to append to, and per Joe's lock the body is suppressed too). Surfaced at Joe-lock checkpoint #1 for resolution: the three-file-vs-two-file discrepancy is itself a discipline data point worth recording — sibling-shape to D-NNN-η's "claimed-atomic-file-count vs git-actually-shipped" surface, but at the prose-vs-strip-discipline-collision surface specifically rather than git-staging. Pass-3-internal precedent question: post-strip what counts as "JOURNAL chain entry only"? Surfaced at checkpoint #1; not Joe-locked at this atom. **Two-file atomic enumeration**: (1) `docs/ROADMAP.md` v1.39 → v1.40 + visual tree Pass 3 row gains J-131 Commit 1 ✅ sub-bullet + J-130 drift-fix sub-bullet (acknowledging J-130's prior atomic-shape correction) + line-156 Pass 3 implementation milestone-row flipped from "§3 Commit 1 next-active" → "§4 Commit 2 next-active; Commit 1 doc-pass ✅ at J-131" + Past entry (this paragraph) + Present section flipped + header date bump 2026-05-27 → 2026-05-28; (2) `CLAUDE.md` PLAY block flip from "Pass 3 implementation ACTIVE; runbook ✅ v1.1 at J-129; Clair pickup at runbook §3 Commit 1 against amended v1.1" → "Pass 3 implementation ACTIVE; Commit 1 doc-pass ✅ at J-131; Clair pickup at runbook §4 Commit 2 next" + previous J-129 PLAY block demoted to ⚫ historical-superseded + header date bump 2026-05-27 → 2026-05-28. **JOURNAL.md NOT amended at this atom** (sibling-shape to J-123/J-124/J-125 chain-only precedent under post-strip discipline). **Design doc + runbook NOT amended at this atom** per Option C hybrid minimal — design doc COMPLETED v1.2 at J-127; runbook ACTIVE v1.1 at J-129 + cleared at J-130. **Test count unchanged at 491** (no code touched at Commit 1; verification sanity-check only). **`grep -rn 'J-NNN' . --include='*.rs' --include='docs/*.md' --include='tasks/*.md'` returns ZERO matches post-staging** per J-108 codification (design doc §6.1 implementation-J-NNN placeholder freezes at Commit 3 milestone close, not at Commit 1). **No state transitions in this atom**: XGID Retrofit Pass 3 milestone stays PLAY (Commit 1 doc-pass is within-milestone). Pass 4 + Pass 5 + M6 (new) stay PENDING. **D-074 application count: twenty-eighth instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — thirteenth at J-126 — does NOT increment). **"Honest longer work over fast shortcuts" — Pass 3 count stays at TWO** at this Commit 1 doc-pass close (sibling-shape to topo-sort J-098 + persistence-amendment J-106 + Pass 1 + Pass 2 inherit-not-increment framing — Commit-1-doc-pass events are within-milestone, not milestone-events). **Sibling-in-shape to Pass 2 Commit 1 doc-pass at J-125** (the only direct precedent for an Option-C-hybrid-minimal Commit 1 against an already-COMPLETED design doc, though Pass 2 had a design-doc §6.7 entry edit making it five-file; Pass 3's design doc was already at v1.2 COMPLETED at J-127 with no §6.7-equivalent slot needed). **Joe-lock checkpoint #1 fires post-ship**: three drift-detection points to confirm (ROADMAP version bump v1.39 → v1.40 + visual tree row ✅; CLAUDE PLAY flip ✅; the honest two-file-vs-three-file count discrepancy surfaced for resolution). **Pre-Commit-2 Joe-lock checkpoint #2** fires next per runbook §2.3: Clair extracts the seven-surface Q-tables from design doc `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` v1.2 §2 verbatim and surfaces by name; Joe approves each surface before any production code lands (LOAD-BEARING D-078 application surface; Trigger (a) fires here if any named type or method does not exist in production — runbook v1.1 amended at J-129 should make checkpoint #2 pass cleanly absent further drift). **Next-active for Clair**: pickup at runbook §4 Commit 2 (seven-surface retype + per-surface tests atomic) post-checkpoint #1 + checkpoint #2 approvals. Read CLAUDE.md PLAY block + JOURNAL J-130 entry first per Rule 0, then runbook §4 in order, then design doc §2 verbatim. **Next-active for Chat Claude (standby)**: parallel-eligible items unchanged (M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 3 J-130 drift-fix atom for J-129 silent gitignore-skip

✅ **J-130 XGID Retrofit Pass 3 drift-fix atom SHIPPED 2026-05-28** (commit `6a7d126`). Four-file atomic per D-074 (twenty-seventh instance) + Lock #3 per-commit cadence — closes the silent gitignore-skip in J-129. **Trigger**: Clair's session-open Rule 0 pickup against the amended v1.1 runbook surfaced a fourth drift one rung up from J-129's three drifts — `git show --stat 697d603` reported 4 files changed but J-129 commit-message footer + JOURNAL J-129 Sub-section 6 + CLAUDE.md PLAY block all enumerate 5 files including `tasks/HANDOFF_TOPOSORT_RUNBOOK_AUTHORING.md`. `git check-ignore` confirmed explicit ignore at `.gitignore:58` — a one-off distinct from the seven sibling HANDOFF files which are all git-tracked. The HANDOFF file's on-disk content was at COMPLETED v1.1 with the J-129 chain entry (working-tree edit landed; git-staging silently skipped). **Path A locked over Path B (fold into Commit 1 — breaks Option C hybrid minimal lock) + Path C (amend J-129 canonical record in place — gitignore entry had no current purpose) + Path D (leave as drift — violates D-077 + D-078 anti-paper-over)**. **Candidate D-NNN-η "claimed-atomic-file-count vs git-actually-shipped-file-count discipline at canonical-record amendment commits" flagged-not-promoted per D-069** (one instance; three-instance threshold opens at next multi-file canonical-record amendment). Sibling-shape to D-077 + D-078 + D-NNN-ζ at the no-drift-surface discipline family, at the git-staging-layer surface specifically. Shared root-cause family across the four: prose claims something the implementing layer silently doesn't honor. **Four-file atomic enumeration**: (1) `.gitignore` line 58 entry removed; (2) `tasks/HANDOFF_TOPOSORT_RUNBOOK_AUTHORING.md` un-ignored via `git add` (content preserved as-is at COMPLETED v1.1 with the J-129 chain entry — sibling-shape to J-117 in-place drift cleanup framing); (3) `JOURNAL.md` J-130 body §-entry added + date bump 2026-05-27 → 2026-05-28; (4) `CLAUDE.md` date bump 2026-05-27 → 2026-05-28 only (per Joe-lock "single header chain entry only" + post-strip discipline; PLAY block does NOT flip). **DECISIONS.md NOT amended** (D-NNN-η stays flagged-not-promoted at one instance per D-069). **ROADMAP.md NOT amended at the J-130 atom** (within-milestone sub-amendment; sibling-shape to J-117 no-ROADMAP-touch framing) — this paragraph in the Past section is added retrospectively at J-131 Commit 1 atomic, not at J-130. **"Honest longer work over fast shortcuts" — Pass 3 count increments to TWO** at this J-130 drift-fix close. Sub-shape D (gitignored-path silent-skip slip) of prose-then-batch atomicity-slip family — structurally novel within the family (Sub-shape C git-staging slip would surface as untracked; Sub-shape D's failure is silent). **D-074 application count: twenty-seventh instance** + Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally stays thirteenth at J-126. **Strip-the-chain extension question** surfaced as discipline data point only at JOURNAL J-130 Sub-section 7 (does the discipline extend to HANDOFF files whose chain entries can't be archived through git history?) — not applied at this atom (would expand scope beyond drift-fix); candidate D-NNN-θ promotion-watch opens at next HANDOFF-file Status flip event. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 3 implementation runbook SHIPPED

✅ **J-128 XGID Retrofit Pass 3 implementation runbook SHIPPED 2026-05-27** at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` Status: ACTIVE v1.0 (~44 KB, nine sections — §1 framing + precedent-departure self-defense / §2 sequence overview + two-commit base + contingent Commit 2a + three Joe-lock checkpoints / §3 Commit 1 doc-pass minimal Option C hybrid / §4 Commit 2 seven-surface retype + per-surface tests / §5 Commit 2a contingent test-fixture sweep / §6 Commit 3 milestone close + DoD / §7 discipline notes nine sub-sections including §7.10 Pass 5 consolidation flag / §8 cross-references / §9 footer authoring provenance). **Four-file atomic commit per D-074 (twenty-fifth instance) + Lock #3 per-commit cadence**: (1) `tasks/XGID_RETROFIT_PASS_3_IMPL.md` NEW ACTIVE v1.0; (2) this ROADMAP v1.37 → v1.38 + visual tree Pass 3 row gains runbook ✅ + Past entry + Present flipped + header chain; (3) `CLAUDE.md` PLAY block flip from "Pass 3 design ✅ at J-127; runbook authoring next-active for Chat Claude + Joe" to "Pass 3 implementation ACTIVE — Clair pickup at runbook §3 Commit 1" + header chain; (4) `JOURNAL.md` J-128 entry + header chain. Runbook came in lighter at ~44 KB than the ~50-70 KB target estimated at design close — honest framing per D-065: §4.7 per-surface tests enumerated 10 tests in tighter shape than predicted; §4 + §5 narrative tighter than Pass 2 precedent. Pass-internal-consistency framing accepts the band. **Sibling-in-shape to Pass 2 runbook precedent** (`tasks/XGID_RETROFIT_PASS_2_IMPL.md` COMPLETED v1.1 at ~43 KB) with three structural extensions for Pass 3's seven-surface scope: (a) §4.7 per-surface tests heavier (+10 target vs Pass 2's +2); (b) §7 nine sub-sections (Pass 2 had eight; adds §7.6 format-boundary preservation unified + §7.10 Pass 5 consolidation flag as future-walk candidate per Joe's "optimize it in some time point" guidance — proposes consolidating §7 discipline-notes across all five Pass runbooks into a single `docs/XGID_RETROFIT_DISCIPLINE.md` reference doc at Pass 5 milestone close); (c) §5 Commit 2a [CONTINGENT] section elevated to own §5 (Pass 2 inline-referenced). **Three runbook-structural Joe-locks at session open**: (1) Option C hybrid minimal Commit 1 (no design-doc touch — Pass 3 design doc already at v1.2 COMPLETED at J-127; three-file atomic for Commit 1 vs Pass 2's five-file; the absent fourth drift-detection point at checkpoint #1 is absence-of-need per D-065); (2) per-surface test target +10 enumerated by name at §4.7 (1 for Surface #1 + 3 for Surface #2 + 1 for Surface #3 + 1 for Surface #4 + 2 for Surface #5 + 2 for Surface #6 + 0 for Surface #7 doc-only); (3) §7 nine sub-sections including §7.10 Pass 5 consolidation flag. **Two-commit base + contingent Commit 2a shape**: Commit 1 doc-pass minimal Option C hybrid (3 files: ROADMAP + CLAUDE PLAY + JOURNAL chain-only) + Commit 2 seven-surface retype + per-surface tests atomic (8-12 files) + Commit 2a [CONTINGENT] test-fixture sweep firing at Joe-lock checkpoint #3 if `cargo test -p xgen-common -p xgen-core -p xgen-node --tests` error count > ~50 + Commit 3 milestone close per D-074 (5-6 files). **Three Joe-lock checkpoints**: #1 post-Commit-1 doc-pass drift check (3 of 4 vs Pass 2's four); #2 pre-Commit-2 verbatim surface list approval (LOAD-BEARING D-078 application surface; Trigger (a) fires here if any named type or method does not exist in production); #3 post-Commit-2 split-trigger decision (~50 error threshold per Pass 2 + Pass 1 precedent). **Verification rigour 5 isolated + 3 workspace = 8 green runs minimum** at Commit 2 milestone-bearing boundary per §4.9; package-scoped to `xgen-common` + `xgen-core` + `xgen-node` per Path A inherited from Pass 1 (`cargo build --workspace` deliberately broken; Pass 5 close restores). **Three split triggers documented at §2.2** per Lock 1 enumeration: Trigger (a) non-existent production contract per design doc §2; Trigger (b) harness extension beyond design doc §4.6 fanout-already-covered scope; Trigger (c) family-boundary size split if Commit 2 alone exceeds ~600 lines OR any single surface exceeds ~400 lines. **No state transitions in this atom**: XGID Retrofit Pass 3 milestone stays PLAY (Pass 3 implementation has now begun; entry-point file flips from "runbook authoring next-active for Chat Claude + Joe" to "Clair pickup at runbook §3 Commit 1"). Pass 4 + Pass 5 + M6 (new) stay PENDING. **D-074 application count: twenty-fifth instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — thirteenth at J-126 — does NOT increment). **"Honest longer work over fast shortcuts" — Pass 3 count stays at zero** at this runbook-landing close (recurrences counted at milestone-events not runbook-authoring events; sibling-shape to topo-sort J-098 + persistence-amendment J-106 + Pass 1 + Pass 2 inherit-not-increment framing). **Sibling-in-shape seventh Chat-Claude-runbook-authoring recurrence**: topo-sort J-098 first; persistence-amendment J-106 second; Phase 9 Commit 3b-4 J-114 third; Pass 1 fourth (authored 2026-05-21); Pass 2 J-124 fifth; Phase 9 milestone-close completion of trilogy at J-119 sixth (related-but-distinct); Pass 3 J-128 seventh — runbook authoring pattern's durability across both audit-design-impl-trilogy milestones and XGID-Retrofit-Pass-arc milestones now extends to seven instances. **Pre-Clair audit posture deferred to Clair's pickup time** per Joe-lock — sibling-shape to J-120/J-125 six-dimension audit pattern (six-dimension audit pattern's third project-arc instance pending at Clair's pickup). **Next-active for Clair**: pickup at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` §3 Commit 1 (doc-pass minimal Option C hybrid). Read CLAUDE.md PLAY block + JOURNAL J-128 entry first per Rule 0, then runbook §1–§3 in order. **Next-active for Chat Claude**: standby until Clair's Commit 1 closes affirmatively at Joe-lock checkpoint #1; parallel-eligible items include M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6 (~35 verbs across 7 categories) if Joe selects that as parallel-track work. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 3 design phase CLOSED

✅ **J-127 XGID Retrofit Pass 3 design phase CLOSED 2026-05-27.** Four-file atomic commit per D-074 (twenty-fourth instance) + Lock #3 per-commit cadence: (1) `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` Status ACTIVE v1.1 → COMPLETED v1.2 + header chain + Surfaces #5-#7 Q-tables (19 + 11 + 8 = 38 new Q-rows) + §4.3 consolidation (wire OR persistence) + §4.2 rule-table extension (async-spawned-captures sub-rule as third row) + §5.5 layered-B3 confirmed null at full seven-surface scope + §6.1 historical-pointer Shape α fill + §7 discipline-notes five sub-sections; design doc lands at ~75 KB (heavier than v1.1 ~46 KB + heavier than Pass 2's ~31 KB + heavier than v1.1 §1.2's ~30-40 KB estimate — honest framing per D-065: §4.3 consolidation reasoning + §7 five sub-sections drove the size; Pass-internal-consistency framing accepts lighter-than-trilogy ~80-100 KB band, Pass 3 came in mid-band rather than at lighter end Pass 2 hit); (2) this ROADMAP v1.36 → v1.37 + visual tree Pass 3 Design row 🟡 → ✅ at J-127 + Past entry + Present flipped from "standby for next-milestone selection" to "Pass 3 design ✅; runbook authoring next-active" + header chain; (3) `CLAUDE.md` PLAY block flip from "XGID Retrofit Pass 2 milestone CLOSED at J-126; standby for next-milestone selection" to "XGID Retrofit Pass 3 design ✅ at J-127; runbook authoring next-active for Chat Claude + Joe" + header chain; (4) JOURNAL J-127 body entry (ten sub-sections per HANDOFF/precedent spec) + header chain. **DECISIONS.md NOT amended** at this design close — four-file shape rather than five-file. **Full seven-surface walk closed across two same-day design sessions** (morning #1-#4 at v1.1 + afternoon #5-#7 + §4.3 consolidation + §6 + §7 fills at v1.2): Surface #1 NodeRuntime six per-space HashMap keys at `xgen-core/src/node/runtime.rs` (Q-table 10 rows; Q1.1-Q1.10; deferred from Pass 2 per Pass 2 design doc §4.1 Q2.8.c); Surface #2 dispatch_event peer_node_id parameter retype to `Option<&NodeXgid>` borrowed boundary at runtime.rs (Q2.1-Q2.10); Surface #3 federation_session.rs handler identifier slots at `xgen-node/src/federation_session.rs` (Q3.1-Q3.10; wire-format vs in-memory split surfaced §4.3 wire-format boundary); Surface #4 fanout.rs handler identifier slots at `xgen-node/src/fanout.rs` (Q4.1-Q4.12; §4.4 forced-owned return shape rule recorded inline; §4.5 ClientSenders + FederationPeerSenders Pass 3 scope corrected from v1.0 framing); Surface #5 app.rs handler identifier slots at `xgen-node/src/app.rs` (19 Q-rows Q5.1-Q5.19; persistence-format boundary instantiation drove §4.3 v1.1 → v1.2 consolidation — same shape as wire-format boundary at structurally identical I/O byte-serialisation slots: space_file_name + persist_event filesystem path + load/save_space_local_metadata on-disk JSON HashMap + replay_spaces_from_dir disk-format iteration + IdentityReplicateMessage::Replicate destructured wire-message field); Surface #6 reconnect.rs scheduler identifiers at `xgen-node/src/reconnect.rs` (11 Q-rows Q6.1-Q6.11; cleanest pure inheritance from Surfaces #1-#4 + #5; six identifier slots retype owned per Pass 2 principle + §4.2 async-spawned-captures sub-rule); Surface #7 Appendix D doc retypes at `docs/xgen_appendix_d_en.md` (8 Q-rows Q7.1-Q7.8; minimal-to-zero substantive content edits — prose-not-schema document; 4 identifier-shaped hits all in markdown schema tables describing stored field semantics; §7.5 records the classification-vs-content-shape gap as discipline note candidate). **Single governing principle (§3) confirmed inherited from Pass 2 unchanged across the full seven-surface walk** — zero wrinkles. The format-boundary that emerged at §4.3 (consolidated at v1.2 to cover wire OR persistence) did NOT modify the principle itself — the principle's "&str projection via Borrow<str>" clause anticipates format-boundary slots stay String by construction; §4.3 makes this explicit as an architectural decision, not a principle amendment (sibling-shape to D-076 v1 → v1.1 amend-in-place where the second load-bearing property absorbed into the existing decision rather than splitting into D-076 + D-077). Three Pass-arc instances of inheritance-unchanged (Pass 1 implicit at runbook; Pass 2 explicit at J-123; Pass 3 explicit at this J-127) make the governing principle's stability durable at Pass-arc layer. **Six architectural decisions locked at §4.1-§4.6**: §4.1 six per-space HashMap keys retype shape (atomic: field types + helper signatures + public-API parameters; D-NNN-γ promotion candidate at 2 instances now: Pass 2 §4.1 Q2.8.c + Pass 3 §4.1 instantiation); §4.2 dispatch_event Option<&NodeXgid> borrowed boundary + sibling-shape rule table extended at v1.2 with async-spawned-task-captures sub-rule as third row (instantiated at Surface #5 handle_federation_incoming + Surface #6 reconnect.rs three spawned functions); §4.3 format-boundary preservation (wire OR persistence) **consolidated at v1.2** from v1.1's wire-only framing per sibling-shape to D-076 v1 → v1.1 amend-in-place reasoning; one principle, two layers, one decision-surface (D-NNN-δ promotion candidate at 2 instances); §4.4 forced-owned return shape rule (general rule recorded inline: a return type can be borrowed only if every branch can borrow from input at the same flavour; if any branch must construct a new value or change flavour, the return type is forced to owned; reusable for Pass 4 + Pass 5 walks); §4.5 ClientSenders + FederationPeerSenders Pass 3 scope (v1.0 framing implied Pass 4 deferral; v1.1 walk corrected to Pass 3 scope — xgen-node-internal types, never cross to xgen-client); §4.6 topo-sort &str slot at fanout.rs:193 already covered at Pass 1 Commit 3 (J-097 anticipated retype inherent in Pass 1's Option<EventXgid> retype; honest framing per D-065 recorded so the next sibling milestone author doesn't waste a walk session searching for a non-existent slot). **Three candidate D-NNNs flagged-not-promoted per D-069**: D-NNN-γ "small-cardinality vs large-cardinality identifier-keyed maps per-Pass call-site density" (2 instances; three-instance threshold opens at a future Pass-arc milestone if a sibling instance fires); D-NNN-δ "format-boundary preservation wire+persistence" (2 instances; three-instance threshold opens at Pass 4 if client-side serialisation-format slot — Tauri IPC, AI control protocol over HTTP, gRPC — instantiates); D-NNN-ε "async-spawned task captures force owned parameters" (3 instances at same xgen-node module-family surface; weaker durability evidence than three across structurally different surfaces per D-077 + D-078 surface-diversity framing; per D-065 honest framing the rule is a Rust language idiom (`'static` bound on `tokio::spawn` closures) not a XGen-specific call — promotion would record a language fact rather than a project decision; promotion-watch opens at Pass 4 surfacing structurally different fourth instance at xgen-client async surfaces — Tauri commands, AI service spawns, batch dispatcher workers). **Layered-B3 (§5.5) confirmed null at full seven-surface scope** — third Pass-arc instance after Pass 1 J-122 + Pass 2 J-126; pattern's durability at three instances now matches D-077/D-078 promotion-threshold framing. Pattern is now established at the Pass-arc layer: identifier-slot retype scopes do not surface layered-B3 because the projection mechanism (`Borrow<str>`) handles type-projection at boundaries uniformly without forcing secondary encodings of the same invariant. No layered surface emerged at any of the seven surfaces walked. **§6.1 historical-pointer filled in Shape α** (pointer-style sibling to Pass 2 §6.7). Records design close at J-127; full seven-surface walk closed across two design sessions; zero re-walks fired; single governing principle confirmed inherited from Pass 2 unchanged; six architectural decisions locked; three candidate D-NNNs flagged-not-promoted; layered-B3 confirmed null at full scope. **Implementation J-NNN milestone-close placeholder** to be frozen at runbook close per J-108 codification: §6.1 timestamp will be frozen retroactively at milestone-close commit per Pass 2 §6.7 pattern. **§7 discipline-notes consolidated with five sub-sections**: §7.1 Format-boundary preservation as unified architectural pattern (§4.3 v1.1 → v1.2 consolidation; sibling-shape to no-drift-surface discipline family at I/O-boundary layer; D-NNN-δ promotion-watch). §7.2 Async-spawned task captures force owned parameters (§4.2 v1.2 rule-table extension; D-NNN-ε flagged-not-promoted rationale; promotion-watch at Pass 4 for cross-crate fourth instance). §7.3 Forced-owned return shape rule (§4.4 general rule reusable for Pass 4 + Pass 5; Pass-internal-consistency rather than DECISIONS.md promotion). §7.4 xgen-node-internal type confusion at v1.0 framing data point for sibling milestone review (§4.5 records ClientSenders + FederationPeerSenders v1.0-vs-v1.1 correction). §7.5 Doc-tree sweep classification-vs-content-shape gap at Appendix D Surface #7 (§2.7 records J-095 Phase 2 doc-tree sweep classified Appendix D "in-scope for Pass 3" correctly in intent, but actual content shape — prose-not-schema, 4 hits in markdown tables — means Pass 3 substantive work at Appendix D is minimal at most; data point for sibling milestone runbook authors). **Two-session design walk shape acknowledged as Pass-internal precedent**: Pass 2 design close at J-123 fit single session; Pass 3 spanned two same-day sessions with foundational-first (#1-#4) + top-of-stack-second (#5-#7) split. Pattern flagged at J-127 body §8(c): future Pass-arc design walks with > 5 surfaces should consider two-session split as deliberate scaffolding rather than honest-longer-work recurrence. **Sibling-in-shape design-close fifth Pass-arc instance**: Pass 1 implicit-in-runbook (Pass 1 era lighter framing); Pass 2 explicit at J-123; Pass 3 explicit at this J-127 (third explicit Pass-arc instance); plus topo-sort (J-097) + persistence-amendment (J-105) at sibling-trilogy layer — five total design-phase-close events in the audit-design-impl arc family. Pattern durable at three explicit Pass-arc instances + two sibling-trilogy instances. **No state transitions in this atom**: XGID Retrofit Pass 3 milestone PLAY → DESIGN ✅ within-milestone; Pass 3 implementation has not yet started; M6 (new) + Pass 4 + Pass 5 stay PENDING. **D-074 application count: twenty-fourth instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — thirteenth at J-126 — does NOT increment). **"Honest longer work over fast shortcuts" — Pass 3 count stays at zero** at this design-close event (recurrences counted at milestone-events not design-events; design close inside Pass 3 milestone scope; Pass 2 final count was zero at J-126; Pass 1 final count was one at J-121). **Honest data point worth recording**: design doc came in at ~75 KB, heavier than v1.1 §1.2's ~30-40 KB estimate. Drivers: §4.3 consolidation reasoning + §7 five sub-sections + Surfaces #5/#6/#7 Q-tables fuller than anticipated. Per D-065 honest framing the original estimate was too aggressive given the §4.3 consolidation work + §7 five sub-sections that emerged at v1.2 walk. **Next-active for Chat Claude + Joe**: runbook authoring at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` in a fresh session per Pass 2 + trilogy precedent. **Target runbook shape**: three-commit base (1 doc-pass / 2 xgen-node + xgen-core HashMap-keys retype atomic / 3 milestone close) + contingent Commit 2a test-fixture sweep firing at Joe-lock checkpoint #3 if test-fixture error count > ~50 (sibling-shape to Pass 2's contingent-split posture pre-locked at design close per D-065 honest framing); three Joe-lock checkpoints (#1 post-Commit-1 drift; #2 pre-Commit-2 verbatim surface list — Chat Claude extracts the seven-surface Q-tables from design doc §2 verbatim and Joe approves by name before code lands; #3 post-Commit-2 split-trigger decision); runbook target ~50-70 KB likely (heavier than Pass 2's ~43 KB given Pass 3's seven-surface scope vs Pass 2's five). **Next-active for Clair**: stood down until runbook authoring closes. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 2 milestone CLOSED

✅ **J-126 XGID Retrofit Pass 2 milestone CLOSED 2026-05-27.** Three-commit Clair-facing sequence on main per `tasks/XGID_RETROFIT_PASS_2_IMPL.md` flipped Status: ACTIVE → COMPLETED v1.0 → v1.1 at milestone close. Commit 1 `5892e9e` doc-pass (J-125; design doc Status ACTIVE → COMPLETED v1.0 → v1.1 + new §6.7 Shape α entry; ROADMAP v1.34 → v1.35; CLAUDE PLAY flip; JOURNAL J-125 header chain entry; runbook header chain). Commit 2 `22765a0` xgen-core algorithm-bearing retypes (lib-clean per Path A inherited from Pass 1; all five surfaces atomic per design doc §2 locked Q-tables — validate_event + ValidationOutcome at exchange.rs; NodeRuntime::dispatch_event + DispatchOutcome at runtime.rs; PendingBuffer arrival hooks at pending.rs; FederationRegistry + IdentityRegistry method APIs at registry.rs; accept_message; plus per-surface unit tests at runbook §4.7; deprecation attributes applied to `validate_steps_8_13` + `accept_event` per design doc §4.2 Q5.b). Commit 2a `58b94a5` test-fixture projection sweep (Joe-lock checkpoint #3 split-trigger fired at 93 errors > ~50 threshold; nine xgen-core test modules updated — dag/pending.rs ~60 errors / node/runtime.rs 19 errors / node/tests/phase9_compound_c5_validation_under_load.rs 3 errors / node/tests/phase9_compound_c9_drain_time_hazard.rs 2 errors / node/tests/phase9_validation_asymmetry.rs 2 errors / identity/replication.rs 3 errors / federation/registry.rs 2 errors / federation/mod.rs 1 error / message/exchange.rs `#[allow(deprecated)]` test-mod attribute for accept_event calls; sibling-shape to Pass 1 Commit 4a `4895446` precedent; +137/-97 LOC across 9 files). This Commit 3 milestone-close commit completes the three-commit sequence. **Test count at close: 491** (xgen-common 34 lib + 8 invariance + xgen-core 449 = +2 vs J-122 baseline of 489 per per-surface tests from Commit 2). Negative delta vs J-119 baseline 627 stays expected per Path A — xgen-node + xgen-client consume retyped types and don't build at workspace level until Pass 3 + Pass 4 (Pass 5 close restores). **Layered-B3 audit answer**: zero, as expected at design close §5.5 (sibling-shape to Pass 1's J-122 finding — the projection mechanism Borrow<str> handled type-projection at boundaries without secondary encoding surfaces; the algorithm-bearing functions consume typed fields through Borrow<str> exactly the way Pass 1's validators did). **All three Joe-lock checkpoints closed affirmatively**: #1 post-Commit-1 drift check (four drift-detection points confirmed at session open after J-125 push — design doc Status flip ✅; ROADMAP version bump ✅; CLAUDE PLAY flip ✅; §6.7 entry ✅); #2 pre-Commit-2 verbatim surface-list approval (five surfaces enumerated from design doc §2 Q-tables, approved by name before any production code landed); #3 post-Commit-2 split-trigger decision (Clair reported 93 errors from `cargo test -p xgen-common -p xgen-core --tests` after lib retypes verified clean; 93 > ~50 threshold so Joe locked split per Pass 1 Commit 4a precedent). **"Honest longer work over fast shortcuts" — Pass 2 milestone-scope final count: zero recurrences.** **First project milestone to ship with zero recurrences since the framework was named** — Pass 1 closed with one (J-121 hygiene atom), Phase 7.5 with one, bidirectional with one, topo-sort with three (re-walk Steps 2/3 + design close), persistence-amendment with one, Federation Phase 9 3b arc with ten. The combination of factors at Pass 2: (a) design phase named layered-B3 as expected-null in advance per §5.5 + design doc §7.7's Pass-internal-consistency framing; (b) runbook pre-locked the contingent-split posture rather than mid-implementation Joe-lock; (c) Pass 1's Borrow<str> additive API meant projection was structurally cheap; (d) the runbook authoring phase (J-124) and Commit 1 doc-pass phase (J-125) both used Shape-α lighter framings that respected Pass-internal-consistency; (e) pre-Clair audit at J-125 confirmed clean across six dimensions before any code touched. **Verification at close**: `cargo test -p xgen-common -p xgen-core`: 491 tests pass (34 + 8 + 449, 0 failed, 0 ignored). 8/8 GREEN at Commit 2a milestone-bearing boundary (5 isolated runs with `cargo clean -p xgen-common -p xgen-core` between each + 3 consecutive workspace runs of the same `cargo test`). `cargo clippy -p xgen-common -p xgen-core --lib --all-features -- -D warnings`: clean. `cargo clippy -p xgen-common -p xgen-core --tests --all-features -- -D warnings`: clean (the `#[allow(deprecated)]` on the exchange.rs test mod is the only suppression in the milestone; documented inline with pointer to the D-071 future-removal arc — exercising `accept_event` before its own audit-design-impl removal arc is a known temporary state during Pass 2). `cargo build --workspace`: deliberately broken per Path A — Pass 3 + Pass 4 own xgen-node + xgen-client retypes; Pass 5 close restores. **J-NNN freeze guardrail (J-108 codification)**: `grep -rn 'J-NNN' . --include='*.rs' --include='docs/*.md' --include='tasks/*.md'` returns ZERO matches post-staging. No code-side J-NNN placeholders existed for this milestone close — Commit 2 used concrete J-125 references in deprecation comments + code-comment blocks (rather than J-NNN placeholders) because the design doc's Status flip to COMPLETED happened at J-125, providing a concrete J-anchor for "Pass 2" before milestone close. Design doc §6.7 placeholder frozen to J-126 at this commit. **D-074 application count: twenty-third instance + thirteenth milestone-close** (J-125 was twenty-second; not a milestone-close so the milestone-close tally stayed twelfth at J-122; this J-126 increments to twenty-third + thirteenth milestone-close per the sibling-shape J-122/J-109 dual-counter framing distinguishing "atomic-commit discipline across all multi-file atoms regardless of milestone-close framing" from "milestone-close commits specifically"). **Five-file atomic commit at this close per D-074 + Lock #3**: (1) `tasks/XGID_RETROFIT_PASS_2_IMPL.md` Status ACTIVE → COMPLETED v1.0 → v1.1 + Last-updated milestone-close note; (2) `tasks/XGID_RETROFIT_PASS_2_DESIGN.md` header chain entry only + §6.7 J-NNN-placeholder freeze to J-126; (3) JOURNAL.md (J-126 entry sub-sections per HANDOFF/precedent spec + header chain); (4) CLAUDE.md (PLAY block flip from "Pass 2 implementation ACTIVE — Clair pickup at runbook §4 Commit 2" to "standby for next-milestone selection" + header chain); (5) this ROADMAP.md (v1.35 → v1.36 + visual tree Pass 2 row 🟢 → ✅ with full sub-bullet detail + Present section flip + this Past entry + Near future Pass 2 line stays historical-superseded + header chain). **What unblocks**: XGID Retrofit Pass 3 (xgen-node + Appendix D — federation_session, fanout, app handlers, reconnect scheduler; six per-space HashMap key retypes from Q2.8.c deferred to this Pass; runbook authoring is next Chat Claude work on the XGID retrofit track). M6 (new) Node admin write path stays unblocked-but-not-selected — opens after Joe selects the next-active milestone at session open. Pass 3 + M6 (new) are both ready for selection; sequencing is Joe's call. **Sibling-in-shape data point for next sibling milestone runbook author**: Pass 2's runbook was authored 2026-05-27 (J-124) + executed without any audit-design re-walks + executed without any mid-implementation Joe-locks beyond the planned checkpoints. The pre-locked contingent-split posture (Commit 2a's split-trigger criterion at runbook §2.3 checkpoint #3) fired exactly as authored, validating the "pre-lock the contingent path" framing per design doc §7.3. Runbook quality + audit discipline working together; the lighter-than-trilogy Pass-internal-consistency framing did not compromise quality. **Candidate D-NNN promotion-watch**: the "small-cardinality vs large-cardinality identifier-keyed maps retype in different Passes per call-site density" sub-principle flagged at design doc §4.1 stays flagged-not-promoted per D-069 (one instance at design close + zero recurrences at implementation; three-instance threshold not met; may promote at Pass 3 milestone close if a sibling instance fires). Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 2 Commit 1 doc-pass SHIPPED

✅ **J-125 XGID Retrofit Pass 2 Commit 1 doc-pass SHIPPED 2026-05-27.** Five-file atomic commit per D-074 (twenty-second instance) + Lock #3 per-commit cadence: (1) `tasks/XGID_RETROFIT_PASS_2_DESIGN.md` Status ACTIVE → COMPLETED v1.0 → v1.1 + header chain + new §6.7 entry in Shape α (pointer-style, sibling to §6.6 forward-reference style — runbook reference + implementation kickoff at J-125 + milestone-close J-NNN placeholder to be frozen at Commit 3); (2) this ROADMAP v1.34 → v1.35 + visual tree Pass 2 row updated (Runbook ✅ at J-124 + Commit 1 ✅ at J-125 sub-rows + Pass 2 header line updated) + Present section PLAY entry flipped + Past section gains J-124 + J-125 entries + Near future Pass 2 line flipped historical-superseded + header chain; (3) `CLAUDE.md` PLAY block flip from "Pass 2 implementation ACTIVE — Clair pickup at runbook §3 Commit 1" to "Pass 2 implementation ACTIVE — Clair pickup at runbook §4 Commit 2 (Commit 1 doc-pass ✅ at J-125)" + header chain; (4) `JOURNAL.md` J-125 header chain entry only (sibling-shape to J-123 + J-124 — doc-only milestone-events use header-chain entries without separate body §-entries; the pattern is consistent with the lighter-than-trilogy framing per Pass-internal-consistency from design doc §7.7); (5) `tasks/XGID_RETROFIT_PASS_2_IMPL.md` Status stays ACTIVE v1.0 + header chain entry recording Commit 1 landed. **Pre-Clair audit at Pass 2 implementation kickoff confirmed CLEAN across six dimensions** per J-124 lock — sibling-shape to J-120's Pass 1 audit precedent (six-dimension audit pattern's second project-arc recurrence; pattern's durability at the Pass-arc layer now established): (1) **file paths** — all five runbook §8.6 source files exist (`xgen-core/src/message/exchange.rs` + `node/runtime.rs` + `dag/pending.rs` + `identity/registry.rs` + `federation/registry.rs`); (2) **type shapes at named anchors** — `ValidationOutcome::HeldPending { missing_predecessors: Vec<String>, missing_identity: Option<String> }` at exchange.rs:365 + `ExchangeError` six variants at exchange.rs:46 (`HeldPending(Vec<String>)`, `NotARoomMember(String)`, `DagError(String)`, `PermissionDenied(String)`, `AiCapabilityViolation(String)`, `AiRoleViolation(String)`) + `DispatchOutcome::Accepted { new_joiner: Option<String>, additional_persisted: Vec<Event> }` + `DispatchOutcome::HeldPending` UNIT variant per J-116 catch + `DispatchOutcome::Rejected(String)` at runtime.rs:88-93 + `NodeRuntime` struct at runtime.rs:126 with `node_id: String` + `peer_urls: HashMap<String, String>` + six per-space maps (spaces/stores/graphs/pending/dm_proposals/space_local_metadata) + `PendingBuffer` arrival hook signatures at pending.rs:151/211/241/284 (`add`/`resolve`/`resolve_identity`/`resolve_federation_relationship`) + `IdentityRegistry::get`+`contains` `&str` signatures with inline "Pass 2 widens" markers at identity/registry.rs:95+100 + `validate_steps_8_13` at exchange.rs:110 + `accept_event` at exchange.rs:330 + `accept_message` at runtime.rs:313 — all deprecation-target functions present; (3) **Pass 1 carry-overs intact** — `Borrow<str>` on `Xgid` at xgen-common/src/xgid/base.rs:68 + macro-generated `Borrow<str>` for all six flavour wrappers at xgen-common/src/xgid/flavours.rs:176 (EventXgid + NodeXgid + IdentityXgid + SpaceXgid + RoomXgid + TrustAssertionXgid) + **33 inline `// Pass 2 widens this method to take typed XGIDs` markers** distributed across pending.rs (5) + store.rs (2) + federation/registry.rs (6) + identity/registry.rs (3) + message/exchange.rs (1) + migration/state_machine.rs (1) + space/state.rs (15); (4) **contingency surfaces** — `peer_urls` insert site at runtime.rs:181 will need owned `NodeXgid` constructor per Surface #2 §4.3 step 6 (Borrow<str> handles lookup-side projection but insert requires owned typed wrapper); (5) **parallel-milestone drift since J-124** — none (HEAD at `0bdb0b8` runbook-shipping commit; no commits landed on main since); (6) **test count baseline** — `cargo test -p xgen-common -p xgen-core` reports 34 lib + 8 invariance + 447 xgen-core + 0 + 0 = **489 tests** matching J-122 baseline exactly; `cargo clippy -p xgen-common -p xgen-core --lib --all-features -- -D warnings` clean. **AUDIT VERDICT: CLEAN.** Runbook v1.0 (J-124, 2026-05-27) applies cleanly. **Shape α locked for §6.7** — pointer-style entry sibling to §6.6 forward-reference style: runbook reference + implementation kickoff at J-125 + milestone-close J-NNN placeholder to be frozen at Commit 3. Joe-locked over heavier §15-equivalent narrative shape on three grounds: (a) sibling-shape to existing §6.6's pointer-style brevity; (b) the substantive content of Pass 2 implementation entry lives in the runbook + this ROADMAP Past entry + the eventual milestone-close JOURNAL entry, not as duplication in the design doc; (c) Pass-internal-consistency framing favours lighter additions per design doc §7.7. **Six-dimension pre-Clair audit pattern recorded as second project-arc instance** of the audit-pattern-as-discipline (J-120 Pass 1 first; this J-125 second; at three sibling Pass-arc instances pattern would become durable per D-077/D-078 three-instance thresholds). **Joe-lock checkpoint #1 fires after this commit lands** per runbook §3.4: Clair pauses and asks Joe to confirm four drift-detection points (design doc Status flip ✅; ROADMAP version bump + visual tree ✅; CLAUDE PLAY flip ✅; new design-doc §6.7 entry ✅); Joe approves before Commit 2 begins. **No state transitions in this atom**: Pass 2 milestone stays PLAY (Commit 1 doc-pass is within-milestone); Pass 2 implementation has now begun (Commit 2 production code is next-active for Clair after checkpoint #1 confirmation). M6 (new) + Pass 3 + Pass 4 + Pass 5 stay PENDING. **D-074 application count: twenty-second instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — twelfth at J-122 — does NOT increment). **"Honest longer work over fast shortcuts" — Pass 2 count stays at zero** (recurrences counted at milestone-events not Commit-1-doc-pass events; sibling-shape to topo-sort J-098 + persistence-amendment J-106 + Pass 1 inherit-not-increment framing). **Honest framing per D-065 on the JOURNAL chain-only entry shape**: J-125 is a doc-pass milestone-event (no production code touched, no test count change, no behavioural change); the chain-only entry shape inherits from J-123 + J-124 sibling precedents for doc-only milestone-events; the precedent is now durable at three instances making the pattern's three-instance threshold met for header-chain-only doc-only milestone-events. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 discipline.

### XGID Retrofit Pass 2 implementation runbook SHIPPED

✅ **J-124 XGID Retrofit Pass 2 implementation runbook SHIPPED 2026-05-27** at `tasks/XGID_RETROFIT_PASS_2_IMPL.md` Status: ACTIVE v1.0 (~43 KB, eight sections + §4a contingent — §1 framing + precedent-departure self-defense + latitude / §2 sequence overview + three-commit base + contingent Commit 2a + three Joe-lock checkpoints + verification rigour / §3 Commit 1 doc-pass / §4 Commit 2 xgen-core algorithm-bearing retypes / §4a Commit 2a contingent test-fixture sweep / §5 Commit 3 milestone close / §6 verification gate + DoD / §7 discipline notes / §8 cross-references). Sibling-in-shape to topo-sort + persistence-amendment + Pass 1 runbook precedents (eight-section shape + §7 discipline-notes inclusion + precedent-departure self-defense at §7.1) but lighter at ~43 KB vs Pass 1's ~65 KB vs trilogy's ~80-100 KB per Pass-internal-consistency framing. Honest framing per D-065: Pass 2's design surface is smaller (five algorithm-bearing functions vs Pass 1's full data-structure-plus-two-appendices); Pass 1's pre-walk reconnaissance flagged every Pass 2 surface with inline code-comment markers at Pass 1 implementation; the contingent-split posture is pre-locked rather than emerging mid-implementation. **Three-commit base + contingent Commit 2a shape**: Commit 1 doc-pass (4-5 files) + Commit 2 xgen-core algorithm-bearing retypes all five surfaces atomic (6-10 files) + Commit 2a [CONTINGENT] test-fixture projection sweep firing at Joe-lock checkpoint #3 if test-fixture error count > ~50 + Commit 3 milestone close per D-074 (6-8 files). **Three Joe-lock checkpoints**: #1 post-Commit-1 doc-pass drift check; #2 pre-Commit-2 verbatim surface list (Clair extracts the five-surface Q-decision tables from design doc §2 verbatim and gets explicit Joe approval before any code lands); #3 post-Commit-2 split-trigger decision (Clair reports test-fixture error count; Joe locks single-Commit-2 or split). **Verification rigour 5 isolated + 3 workspace = 8 green runs minimum** at Commit 2 milestone-bearing boundary; package-scoped to `xgen-common` + `xgen-core` per Path A inherited from Pass 1. **§7 discipline-notes** stays narrow Clair-facing (8 sub-sections: precedent-departure self-defense + Pass-internal-consistency + contingent-split honesty + inline-lock fifth-Pass recurrence + Borrow<str> load-bearing + "honest longer work" Pass 2 count starts at zero + layered-B3 expected null + D-069 audit-vs-design boundary). **§6 DoD has 11 bullets** + explicit not-in-DoD enumeration. **Three-file atomic commit per D-074 (twenty-first instance) + Lock #3 per-commit cadence**: (1) `tasks/XGID_RETROFIT_PASS_2_IMPL.md` NEW ACTIVE v1.0; (2) JOURNAL J-124 header chain entry; (3) CLAUDE.md PLAY block flip from "XGID Retrofit Pass 2 design ✅ at J-123; runbook authoring next-active for Chat Claude + Joe" to "XGID Retrofit Pass 2 implementation ACTIVE — Clair pickup at runbook §3 Commit 1" + header chain. ROADMAP v1.34 → v1.35 deliberately NOT in this atom — that bump belongs to Clair's Commit 1 doc-pass per runbook §3.1 (sibling-shape to topo-sort runbook §6.5 + persistence-amendment runbook §6.4 ROADMAP-in-Commit-1 framing). Honest framing per D-065 caught pre-push: initial J-124 enumeration claimed four-file shape with ROADMAP included; the runbook's own §3.1 puts ROADMAP at Commit 1, so corrected to three-file shape before commit landed. **Sibling-in-shape sixth Chat-Claude-runbook-authoring recurrence**: topo-sort J-098 first; persistence-amendment J-106 second; Phase 9 Commit 3b-4 J-114 third; Pass 1 fourth (authored 2026-05-21); Pass 2 J-124 fifth; the pattern's recurrence count is now durable across both audit-design-impl-trilogy milestones and XGID-Retrofit-Pass-arc milestones. **Pre-Clair audit posture deferred to Clair's pickup time per Joe-lock** — sibling-shape to J-120's pre-Clair audit pattern at six dimensions; Clair runs the audit at Pass 2 implementation kickoff session (which became this J-125 commit). **D-074 application count: twenty-first instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — twelfth at J-122 — does NOT increment). **"Honest longer work over fast shortcuts" — Pass 2 scope count stays at zero** at this runbook-landing close (recurrences counted at milestone-events not runbook-authoring events). Next-active for Clair at J-124: pickup at `tasks/XGID_RETROFIT_PASS_2_IMPL.md` §3 Commit 1 (doc-pass) — which is what J-125 (the next commit) executed. Next-active for Chat Claude at J-124: standby until Clair's Commit 1 closes affirmatively at Joe-lock checkpoint #1. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 2 design phase CLOSED

✅ **J-123 XGID Retrofit Pass 2 design phase CLOSED 2026-05-27.** Four-file atomic commit per D-074 (twentieth instance) + Lock #3 per-commit cadence: (1) `tasks/XGID_RETROFIT_PASS_2_DESIGN.md` NEW ACTIVE v1.0 (~31 KB, seven sections — §1 framing + reading order + latitude + precedent-departure self-defense / §2 five-surface walk with Q-locks tabulated per surface / §3 design principle / §4 architectural decisions / §5 honest-broadening discipline + layered-B3 audit answer null / §6 cross-references / §7 discipline notes); (2) JOURNAL J-123 entry + header chain; (3) CLAUDE.md PLAY block flip from "standby for next-milestone selection" to "XGID Retrofit Pass 2 design ✅; runbook authoring next-active for Chat Claude + Joe" + header chain; (4) this ROADMAP v1.33 → v1.34 + Past entry + Present updated + visual tree Pass 2 row gains design ✅ sub-bullet + header chain. **Five surfaces walked in dependency order** (bottom-of-stack first): Surface #1 validate_event + ValidationOutcome at `xgen-core/src/message/exchange.rs` (Q1 ValidationOutcome payloads retype identifiers; Q2 ExchangeError payloads retype identifier variants only — descriptive-string variants stay String; Q3 internal projections bind as typed references; Q4 fed_add_via_federation bool no change; Q5 validate_steps_8_13 deprecate + retype minimally + future audit-design-impl arc per D-071, symmetric to accept_event); Surface #2 NodeRuntime::dispatch_event + DispatchOutcome at `xgen-core/src/node/runtime.rs` (Q2.1 new_joiner Option<IdentityXgid>; Q2.2 Rejected String stays; Q2.3-Q2.4 Event/EventOrigin no change; Q2.5 node_id NodeXgid; Q2.6 peer_urls HashMap<NodeXgid, String>; Q2.7 internal projections bind typed; **Q2.8 architectural lock — partial HashMap retyping**: node_id + peer_urls retype now; six per-space HashMap keys defer to Pass 3 per call-site-density heuristic); Surface #3 PendingBuffer arrival hooks at `xgen-core/src/dag/pending.rs` (Q3.1-Q3.5 method signatures take typed references; drop all Xgid::new(...) wraps inside method bodies — Pass-1-deferred bridges; no architectural surprises, all mechanical); Surface #4 FederationRegistry / IdentityRegistry APIs at `xgen-core/src/identity/registry.rs` + federation/ (Q4.1-Q4.7 get/contains take typed references; register/upsert unchanged; FederationRegistry parallels; storage layer already typed — no change needed; no architectural surprises, all mechanical); Surface #5 accept_message signature (Q5.1-Q5.3 mechanical retype propagating from Surface #1 Q5; Q5.4 flag for deprecation-audit-arc inclusion candidate alongside validate_steps_8_13 + accept_event per D-071 own-arc). **Single Pass 2 design principle locked** (design doc §3): identifier slots retype to typed XGIDs; descriptive-string slots stay String; internal variables bind as typed references; &str projection happens at the call-site boundary via Borrow<str> (Pass 1's additive API at Commit 4 implementation-kickoff lock); no Deref<Target = str> shortcuts. **One sub-principle codified at §4.1** for Pass 3-5 forward application: small-cardinality identifier-keyed maps retype in their own Pass, large-cardinality per-space maps defer to the Pass that touches their primary call-site crate — sibling-shape to D-067 no-drift-surface discipline at code-organisation layer. Flagged-not-promoted as candidate D-NNN per D-069 (one instance; three-instance threshold not met; may promote at Pass 3 milestone close if a sibling instance fires). **Three precedent departures honestly framed at design doc §1.2** vs trilogy precedent (`tasks/FEDERATION_TOPOSORT_DESIGN.md` ~33 KB; `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md` eleven sections; `tasks/FEDERATION_BIDIRECTIONAL_NODES_DESIGN.md` nine sections): (a) ~31 KB vs ~80-100 KB trilogy band — honest reflection that Pass 1's pre-walk reconnaissance did the architectural work via code-comment flags written at Pass 1 implementation time; Pass 2 design phase is principle-locking + decision-recording, not novel-question deliberation; (b) no §11 re-walk reservation — the five-surface walk closed without re-walks firing; if re-walk fires post-design-close this document gains §11 amendment in place per topo-sort precedent; (c) one sole governing principle vs trilogy's multiple Joe-locks — Pass 2 surfaces decompose to a single principle applied uniformly with two scope-bounding architectural decisions (Q2.8 + Q5) and the rest is mechanical. **Pass-internal-consistency over trilogy-internal-consistency** (codified at design doc §7.7): when the five-Pass XGID Retrofit arc and the audit-design-impl trilogy precedents conflict on shape, Pass-internal consistency wins. **Inline-lock pattern fourth Pass-arc recurrence** (codified at design doc §7.2): pattern's durability at the Pass-arc layer now matches the trilogy's three-recurrence durability at the federation-milestone layer. **Layered-B3 audit answer null** at design close (codified at design doc §5.5): no layered surfaces detected during the five-surface walk; Pass 1's milestone close (J-122) recorded the same finding; Pass 2 inherits the absence. **§5 honest-broadening discipline** enumerates deferred surfaces with their Pass-of-record: Pass 3 (xgen-node + Appendix D); Pass 4 (xgen-client + AI control docs); Pass 5 (test fixtures, integration helpers, trace field types, log formatters, Debug/Display impls); own future audit-design-impl arc per D-071. **Contingent Commit 2a split-trigger locked in advance** for runbook authoring: Pass 1's split was Joe-locked mid-implementation when ~296 fixture errors surfaced; Pass 2 design phase locks the split posture pre-runbook with explicit trigger criteria (test-fixture sweep error count > ~50 at Joe-lock checkpoint #3) per D-065 honest framing. **D-074 application count: twentieth instance** (Lock #3 per-commit cadence; not a milestone-close so the milestone-close tally — twelfth at J-122 — does NOT increment). **"Honest longer work over fast shortcuts" — count starts at zero for Pass 2** (recurrences counted at milestone-events not design-events; design close is *inside* the Pass 2 milestone scope; Pass 1 final count was one at J-121 hygiene atom). **No state transitions in this atom**: XGID Retrofit Pass 2 milestone PLAY → DESIGN ✅ within-milestone; Pass 2 implementation has not yet started; M6 (new) + Pass 3 + Pass 4 + Pass 5 stay PENDING. **Next-active for Chat Claude + Joe**: runbook authoring at `tasks/XGID_RETROFIT_PASS_2_IMPL.md` in a fresh session per trilogy + Pass-1 precedent. **Next-active for Clair**: stood down until runbook authoring closes. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 1 milestone CLOSED

✅ **J-122 XGID Retrofit Pass 1 milestone CLOSED 2026-05-26.** Seven atomic commits on `main`: Commit 1 `403ef3f` canonical-form module move (`canonical_event_bytes` + sibling helpers + `EVENT_FIELD_ORDER` const moved from xgen-core to xgen-common; thin re-export shim at `xgen-core/src/wire/canonical.rs` preserves all existing call sites and the v1 invariance test `event_xgid_roundtrip_through_event_canonical_form`); Commit 2 `8a94dee` convenience constructors on flavour wrappers (`EventXgid::from_event` / `SpaceXgid::from_space_create` / `RoomXgid::from_room_create`; `TrustAssertionXgid::from_assertion` deferred to Pass 2 with code-comment flag because `TrustAssertion` struct does not exist in Rust today — runbook §Commit 2 contingency anticipated this); Commit 3 `75e81b4` xgen-common data-structure retypes (Event struct field retypes; `SpaceLocalMetadata.space_id`; `state.rs` observability struct retypes; +5 invariance tests A–E pinned by name at runbook sub-question 4 — `event_struct_retains_wire_format` / `space_local_metadata_serde_roundtrip_with_xgid_fields` / `node_state_observability_serde_roundtrip` / `connected_client_xgid_serde_round_trip` / `federated_peer_xgid_serde_round_trip`); Commit 4 `774fe9d` xgen-core data-structure retypes (lib-clean per **Path A Joe-lock** — `cargo build --workspace` deliberately broken between this Commit and Pass 2 because xgen-node + xgen-client consume retyped xgen-core types; `Borrow<str>` additive API added on `Xgid` + flavour wrappers at implementation kickoff over per-site explicit-wrap-with-comment churn; new `sender_xgid()` private helper sibling to `sender_id()`); Commit 4a `4895446` xgen-core test-fixture projection sweep (~296 fixture errors across ~10 test modules; ~550 LOC net; **Joe-locked split mid-implementation** because the runbook's "data structures only" framing did not anticipate test-fixture sweep cost would be order-of-magnitude similar to lib retype cost); Commit 5 `096162e` Appendix C (15 XGID hits) + Appendix I (122 XGID hits) doc retypes per the Phase 2 doc-tree sweep coordination flag; this Commit 6 milestone-close commit. Plus **J-121 hygiene atom** `1dd909e` shipped immediately before Commit 6 closing a lib-level `unused import: NodeXgid` clippy regression at `xgen-core/src/message/exchange.rs:22` surfaced at the Commit 6 verification gate (sibling-shape to J-095 hygiene commit `904441b` separate-atom precedent). **Test count at close**: 489 (xgen-common 34 lib + 8 invariance + xgen-core 447). Negative delta vs J-119 baseline 627 is expected and honest per Path A — the missing ~140 tests live in xgen-node + xgen-client which don't build until Pass 3 + Pass 4. Pass 5 close should restore the workspace test count to ≥ 627 plus the +5 Pass 1 invariance tests. **Layered-B3 audit answer per runbook §6.7 DoD**: no layered surface emerged — Pass 1's scope is data-structure shape, not algorithm validation; validators (`is_dag_root_type`, `validate_dag_structure`) consume the typed fields as `&str` projections through `Borrow<str>`, naturally type-clean. Sibling-shape to the persistence-amendment milestone close (J-108) which named layered-B3 closure as a load-bearing audit dimension; J-108 found one such surface; J-122 finds zero. **"Honest longer work over fast shortcuts" first recurrence within Pass 1 scope at J-121** (hygiene atom catching the lib-level `unused import` regression rather than papering it over with `#[allow]` or shipping with the warning intact). Count NOT incremented at this Commit 6 milestone-close (close-event-not-recurrence-event sibling-shape to J-101/J-108 framing). Pass 1 milestone-scope final count: one recurrence — the **first project milestone since the original Phase 1** to ship with only one recurrence rather than the multi-recurrence shape of Phase 7.5 (one), bidirectional (one), topo-sort (three), persistence-amendment (one), Federation Phase 9 3b arc (ten). Sibling-shape data point for runbook-quality assessment: runbook authored 2026-05-21 + audited at J-120 with zero drift findings + executed with two mid-implementation Joe-locks (Borrow<str>, Commit 4 split) and one mid-implementation hygiene-atom but no audit-design re-walks. **Two flagged-not-promoted candidate D-NNNs** per D-069 (one instance each; three-instance threshold not met): "Additive `Borrow<X>` impls at the type-system level are an acceptable Pass-shape technique when the alternative is per-call-site wrap-with-comment churn at hundreds of sites" (sibling principle to D-077); "Mid-implementation scope split as honest framing technique for runbook-vs-implementation drift" (sibling to D-065 + D-074 + J-098 prose-then-batch family). **Verification at close**: `cargo test -p xgen-common -p xgen-core`: 489 tests pass (34 + 8 + 447, 0 failed, 0 ignored); `cargo clippy -p xgen-common -p xgen-core --lib --all-features -- -D warnings`: clean; `cargo clippy -p xgen-common -p xgen-core --tests -- -D warnings`: clean; `cargo build --workspace`: deliberately broken per Path A — downstream Passes 2/3/4 own the xgen-node + xgen-client retype work. **J-NNN freeze guardrail (J-108 codification)**: `grep -rn 'J-NNN' . --include='*.rs' --include='docs/*.md' --include='tasks/*.md'` returns ZERO matches post-staging. **D-074 application count: nineteenth instance** (twelfth milestone-close — sibling to J-109's tally framing where milestone-close tally was twelfth; this J-122 is the thirteenth milestone-close). **Six-file atomic commit per D-074 + Lock #3**: `tasks/XGID_RETROFIT_PASS_1_IMPL.md` Status ACTIVE → COMPLETED v2.0 → v2.1; `tasks/HANDOFF_PASS_1_COMMIT_6.md` Status ACTIVE → COMPLETED v1.1 (retained as historical record per anti-tempfile-deletion + J-100 retention precedent); JOURNAL.md (J-122 entry nine sub-sections per HANDOFF §5 spec + header chain); CLAUDE.md (PLAY block flip + header chain); this ROADMAP.md (v1.32 → v1.33 + Past + Present + visual tree Pass 1 row flip + header chain). **HANDOFF accuracy retrospective at J-121 prose**: the Pass 1 Commit 6 HANDOFF §2 verification claim that `cargo clippy --lib --all-features -- -D warnings` was clean at Commit 5 close `096162e` was inaccurate at authoring time (the `unused import: NodeXgid` warning would have been present from Commit 4a onward); honest framing per D-065 + Rule 5 — corrective record stands at J-121 + J-122 paired entries. **What unblocks**: XGID Retrofit Pass 2 (xgen-core algorithm-bearing functions; runbook authoring is the next Chat Claude work-shape on the XGID retrofit track). M6 (new) Node admin write path stays unblocked-but-not-selected — opens after Joe selects the next-active milestone at session open. Pass 2 + M6 (new) are both ready for selection; sequencing is Joe's call. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 + grep guardrail scope discipline.

### XGID Retrofit Pass 1 milestone — SELECTED

🟢 **J-120 XGID Retrofit Pass 1 milestone SELECTED** over M6 (new) per Joe-lock at session open following J-119 Federation closure. Three-file atomic commit per D-074 (seventeenth instance): JOURNAL J-120 + CLAUDE.md PLAY flip "standby" → "Pass 1 implementation ACTIVE — Clair pickup at runbook §3 Commit 1" + this ROADMAP v1.31 → v1.32. **Pre-Clair audit at session open confirmed ZERO drift** between runbook v2.0 (authored 2026-05-21) and current main (post-J-119, 627 tests). Six audit dimensions verified clean (file paths / type shapes / v1 carry-overs / named structs + TrustAssertion contingency / parallel-milestone drift / test-count baseline); all four runbook sub-questions (commit sequence / module-move ordering / bridging strategy / invariance test list, all Joe-locked 2026-05-21) still apply cleanly. **TrustAssertion struct does NOT exist in Rust** — runbook's §Commit 2 contingency confirms `TrustAssertionXgid::from_assertion` defers to Pass 2 with code-comment flag; the three from-Event constructors (`from_event`/`from_space_create`/`from_room_create`) ship at Commit 2 alone. Verbal-only audit seal per Joe-lock α (no HANDOFF artefact; audit becomes verbal record in J-120 entry; sibling-shape to J-117 framing but inverse outcome — no edits needed). Federation visual-tree drift cleanup folded into atom per Joe-lock β + D-077 backward-coherence — two-event accumulated drift (J-118 + J-119) closed atomically at lines 68/82/91/116 alongside Pass 1 row 🟡 → 🟢. **Pre-Clair audit pattern recorded for future sibling milestones** — six-dimension audit shape reusable when a runbook authored at time T is picked up by Clair at time T+N. **D-077 family third project instance** (J-107 cross-milestone B3; J-114 D-078 production-grounded test enumeration; this J-120 cross-milestone-elapsed-time check). Next-active for Clair: pickup at runbook §3 Commit 1 (canonical-form module move). Next-active for Chat Claude: parallel-eligible items M6 (new) Block 4 verb-by-verb walks; future-walk of candidate D-NNN "ingest path invariant encoding" if Joe locks it.

### Federation Event Propagation milestone CLOSED

✅ **J-119 Federation Event Propagation milestone CLOSED**: 627/627 green at close; M6 (new) + XGID Retrofit Pass 1 unblock. Per-phase shipped state in `docs/xgen_federation_propagation_design.md` §15.

### Federation Event Propagation implementation — Phase 9 Commit 3b-4

✅ **J-118 Phase 9 Commit 3b-4 SHIPPED**: 27 tests across Sc4 + C5/C7/C9/C10; 627/627 green.

### Federation Event Propagation implementation — Phase 9 Commit 3b-3

✅ **Phase 9 Commit 3b-4 implementation runbook shipped 2026-05-24 at J-114** (`tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md` Status: ACTIVE v1.0; ~57.5 KB; eight sections — §1 framing + §2 sequence overview + §3 Commit 1 single-or-split shape + §4 Scenario 4 detail + §5 compounds C5/C7/C9/C10 + §6 verification + DoD + freeze sites + §7 discipline notes + §8 cross-references; flips to COMPLETED v1.1 at Commit 3b-4 ship per the runbook's §6 ship-event DoD; flips to COMPLETED v1.2 at Phase 9 milestone close Commit 3b-5 per D-074). Sibling-in-shape to `tasks/FEDERATION_TOPOSORT_IMPL.md` (COMPLETED v1.2, ~93 KB) and `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` (COMPLETED v1.2, ~95 KB) — same eight-section shape; same five-Joe-lock-checkpoint posture; same §7 discipline-notes inclusion with precedent-departure self-defense at §7.1. **Five-file atomic commit per D-074 (fourteenth instance) + Lock #3 per-commit cadence + D-078 promotion atom**: (1) `tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md` NEW ACTIVE v1.0; (2) `DECISIONS.md` D-078 entry promoted + header chain; (3) `JOURNAL.md` J-114 entry + header chain; (4) `CLAUDE.md` PLAY block flip + header chain; (5) this ROADMAP version bump v1.28 → v1.29 + Past entry + header chain. **D-078 promoted in same atom (Path B.i)**: "Production-grounded test enumeration" — at every test enumeration (named list of test cases destined to become a regression lock), the production reject-path inventory MUST be confirmed against current code BEFORE the enumeration is Joe-locked. Three-instance threshold met (J-099 audit-doc §11 + J-109 survey §2.6 + J-113 survey §2.4); pattern durable. Sibling-shape to D-077 (bidirectional sustainability at silent-discard sites): D-077 meta-layer; D-078 protocol-test-layer sibling. Promotion path B.i locked over Path A (runbook §1.1 only, defer DECISIONS promotion) and Path B.ii (separate pre-runbook atom) on three grounds: three-instance threshold matches D-076 v1 → v1.1 promotion sibling-shape; runbook atom is the natural carrier; deferring promotion re-runs the discipline cost. Naming locked at "Production-grounded test enumeration" over alternative "Backward-coherence audit at test-enumeration time" on three grounds: product-framing matches D-067/D-070/D-075/D-076 sibling style; scope clarity at one read; D-077 relationship visible by family-shape in body, not buried in title. **Five Joe-locks at runbook-authoring session open** (full at runbook §1.1): (1) Single-commit-with-split-trigger-discipline shape — Commit 3b-4 frames as single atomic commit with three split triggers documented at runbook §2.2: Trigger (a) non-existent production contract (J-113 shape; canonical-record amendment FIRST); Trigger (b) harness extension beyond CounterLayer (J-111 shape); Trigger (c) family-boundary size split (Scenario 4 alone > ~600 lines OR single compound > ~400 lines; family-boundary, NOT arbitrary line count); sibling-shape to J-111 retrospective 3b-3-pre + 3b-3 split pattern. (2) D-078 promoted in this atom. (3) Verification rigour 5 isolated + 3 workspace = 8 green runs minimum at milestone-bearing commit (sibling to topo-sort J-101 + persistence-amendment J-108). (4) Family-uniformity + variant-uniformity structural properties as load-bearing per-family assertions per findings §2.4.1. (5) §7 discipline-notes section with seven sub-sections; precedent-departure self-defense at §7.1. **Three pre-draft code-trace findings shaped §4/§5/§6 narrow scope**: Finding A xgen-core test directory organisation (NEW `xgen-core/src/node/tests/` locked); Finding B ExchangeError Display substring matching; Finding C DispatchOutcome::Rejected destructure pattern (full at runbook §1.1 + §4.4 + §4.5). **Five Joe-lock checkpoints for Clair** (full at runbook §2.3): #1 post-runbook-read drift check; #2 pre-implementation production-code verification per D-078 (LOAD-BEARING D-078 application surface; Trigger (a) fires here, not later); #3 pre-implementation harness-extension assessment; #4 the load-bearing checkpoint — Joe-lock test enumeration BY NAME for all five families; #5 post-implementation pre-verification. **Two open questions surfaced at runbook §5 for checkpoint #2**: C7 + C10 file location (xgen-node/src/tests/ vs xgen-core/src/node/tests/ — conservative posture is xgen-node-side per their assertion targets, surface at checkpoint #2). The runbook explicitly applies D-078 to its own open questions — where it can't lock without surfacing first, records the unknown and routes through D-078's checkpoint. Runbook came in lighter than the ~80–100 KB precedent target band at ~57.5 KB — honest framing per D-065: Phase 9 Commit 3b-4 is test-only (no production code touched, unlike topo-sort's Path B at `xgen-core/src/space/state.rs` or persistence-amendment's Result-propagation through `dispatch_event` + drain helpers) AND the substantive scoping work (Scenario 4 enumeration; production reject-path inventory) was already done at J-113. **Sibling-in-shape fifth recurrence at this commit**: Phase 7.5 first (J-093/J-104); bidirectional federation_nodes second (J-096); topological-sort third (J-097–J-101); persistence-amendment sub-amendment fourth (J-104–J-108); Phase 9 Commit 3b arc fifth (J-110–J-114/upcoming J-115). Five recurrences make the audit → design → runbook → implementation → milestone-close shape robust. **D-074 application count: fourteenth instance** — Lock #3 per-commit cadence applies; not a milestone-close so the milestone-close tally (eleventh at J-108, twelfth at J-109) does NOT increment; fourteenth instance counts D-074's atomic-commit discipline across all multi-file atoms regardless of milestone-close framing. **"Honest longer work over fast shortcuts" — count inherited at eighth from J-104, NOT incremented at this runbook-authoring close** (within-milestone event, sibling-shape to topo-sort J-098 + persistence-amendment J-106 inherit-not-increment framing). **α.a cascade-close honest framing per D-065** — this Path-(c) atomic also backfills four prior ROADMAP visual-tree updates (J-110 Commit 3b-2 ✅ row + J-111 Commit 3b-3-pre ✅ row + J-112 Commit 3b-3 ✅ row + cross-cutting D-077 row missing since J-107 promotion) plus the J-114 row that this commit's prose enumerates. The cascade was the sixth recurrence of the prose-then-batch atomicity-slip family (J-094 / J-098 / J-099-eighth-file / J-100 line-1140-retroactive / J-101 line-1140-retroactive-freeze first through fifth; this J-114 cascade-as-single-event sixth). Counted as cascade-as-single per Joe-lock at session-open finding-surface: all four slips share root cause (tree updates described in prose but not written to disk in the same `edit_file` call across four consecutive atomics), surfaced together at this session-open verification, sibling-shape to D-077's three-instance threshold framing rather than four-separate-instances framing. **What this commit does NOT close.** Phase 9 milestone stays PLAY (Clair's next-active is Commit 3b-4 per the runbook; after Commit 3b-4 ships, Commit 3b-5 milestone close per D-074 remains in the §3.0a five-commit sequence — that's the close commit that flips Phase 9 milestone PLAY → DONE). Federation Event Propagation milestone stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING. Dependency chain unchanged in shape (this runbook-landing is within-milestone; depth unchanged). Clair's next-active: pickup at Joe-lock checkpoint #1 per runbook §2.3 post-runbook-read drift check. Per Rule 0 + D-065 + D-067 + D-069 + D-071 + D-074 + D-077 + D-078 (this commit's promotion) + grep guardrail scope discipline (J-108 codified).

✅ **Phase 9 Commit 3b-3 SHIPPED 2026-05-24 at J-112** (five-file atomic commit per D-074 + Lock #3 per-commit cadence; workspace test count 599 → 600 (+1); 2 of 2 consecutive `cargo test --workspace` runs PASS). Compound C2 (F-5 anti-transitivity under push queue depth) deployment-level test against the J-111 extended harness at `xgen-node/src/tests/phase9_compound_c2_anti_transitivity_at_load.rs` (~280 lines, one test `three_node_anti_transitivity_at_load_100_messages`). Three-Node setup mirrors Scenario 2 (A↔B + A↔C federated; B↔C explicitly NOT federated; Alice posts 100 MessageText events in rapid succession). **Five honesty assertions**: (1) **load-bearing per-event negative-existence F-5 regression lock** — for every Alice event_id, `node_b.push_attempt_count(event_id, &node_a.node_id) == 0` AND `node_c.push_attempt_count(event_id, &node_a.node_id) == 0` (catches partial-bypass regressions Sc2's positive `logs_contain` at-least-once cannot — Sc2's substring match would still pass even if F-5 bypassed every other event; per-event counter assertion is the contrapositive at the per-Node counter site); (2) positive G2 trace via `harness_logs_contain("federation_push_skipped_origin")` (sibling to Sc2 but via harness facility); (3) destination-side reach within tighter 60s per-event budget (vs Sc2's 120s); (4) structural anti-transitivity (B/C have no peer entry for each other; sibling-shape to Sc2); (5) source-side positive of A's pushes — `node_a.push_attempt_count(event_id, &peer_node_id) >= 1` for each event, confirming counter wiring (distinguishes "wired correctly" from "trivially zero" so assertion #1 doesn't pass for the wrong reason). **Does NOT use `#[traced_test]`** because push-attempt counter requires harness subscriber as active per-thread default; uses `install_harness_subscriber()` as first line + `harness_logs_contain` for positive G2 trace. **Runtime flavor `current_thread`** per Sc2 rationale — all tokio tasks on test thread where harness subscriber is active. Wall-clock observation: ~12s isolation, ~30s workspace (acceptable per Sc2's budget pattern; the 60s per-event budget is generous enough to absorb parallelism contention). CounterLayer routing surfaced no per-test-pollution issues across consecutive workspace runs — the global REGISTRY's Weak references correctly invalidate on prior-test InProcessNode drop. Reusable infrastructure for Commit 3b-4 if any of its tests need cross-Node push-attempt observability (most 3b-4 tests are NodeRuntime-level direct `dispatch_event` checks and likely won't need it). **D-074 Lock #3 per-commit cadence applies** — not a milestone-close. Phase 9 RESUMES at Commit 3b-4 — NodeRuntime-level Scenario 4 + C5 + C7 + C9 + C10. Federation Event Propagation stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING.

### Federation Event Propagation implementation — Phase 9 Commit 3b-3-pre

✅ **Phase 9 Commit 3b-3-pre SHIPPED 2026-05-24 at J-111** (ten-file atomic commit per D-074 + Lock #3 per-commit cadence; workspace test count 596 → 599 (+3); 2 of 2 consecutive `cargo test --workspace` runs PASS). Harness extension + G2 trace-event retrofit splitting Phase 9 Commit 3b-3 into a two-commit shape locked at Pre-Commit-3b-3 Joe-lock checkpoint #3 walk (3b-3-pre infrastructure; 3b-3 Compound C2 test). **Three components**: (1) G2 retrofit — `apply_federation_push` gains `local_node_id: &str` parameter; all four G2 federation-push trace events (`federation_push_skipped_origin`, `federation_push_sent`, `federation_push_dropped_full`, `federation_push_dropped_unregistered`) gain `local_node_id` field (production observability addition per Joe-lock Q2; type stays `String` per Joe-lock Q1; XGID Retrofit Pass 1 sweep planned). (2) `push_attempts: Arc<StdMutex<HashMap<(String, String), u64>>>` on `InProcessNode` per Joe-lock Q4 + accessors `push_attempt_count` + `push_attempts` (clone-not-reference). (3) Tracing-Layer composition — `CounterLayer` + `LogBufferLayer` over `tracing_subscriber::registry()`; per-test installation via `install_harness_subscriber() -> HarnessSubscriberGuard`. **Q3 architectural-gap surfaced and resolved at implementation time via D-077 backward-coherence audit**: original Q3 lock said "wrapper layer in phase9_harness where apply_federation_push calls are routed through InProcessNode" — grep at implementation time surfaced 4 production + 3 test call sites; only the `submit_locally` wrapper goes through InProcessNode. Instrumenting only `submit_locally` would have produced a fabricated F-5 regression lock (B's hypothetical leak happens at production sites, not through harness wrappers). Four resolution options walked with Joe; Joe-lock Option Q3-alt-a — tracing-Layer-fed counter with emitter-field G2 retrofit. **Honest finding — `tracing-test 0.2.6 set_global_default` exhaustion**: initial implementation used `set_global_default` via `OnceLock`; failed in workspace mode because `#[traced_test]` macro uses `tracing::dispatcher::set_global_default` via a shared `INITIALIZED: Once` in the tracing-test crate (one-shot exhausted once any `#[traced_test]` test fires). Resolution: switched to per-test `tracing::subscriber::set_default` with explicit guard binding (per-thread scope; coexists with tracing-test's global default on a per-thread / process-global slot dichotomy). **Three counter unit tests** at `phase9_harness::counter_unit_tests`: counter_starts_empty_on_fresh_node, counter_increments_on_federation_push_sent_for_matching_local_node (load-bearing — asserts A's counter increments + B's counter stays 0 proving F-5 short-circuit), log_buffer_captures_federation_push_skipped_origin_trace. **D-074 Lock #3 per-commit cadence applies** — not a milestone-close so the milestone-close tally (eleventh at J-108, twelfth at J-109) does NOT increment. JOURNAL inclusion satisfies D-074's per-commit JOURNAL discipline unambiguously. Phase 9 stays PLAY (RESUMES at Commit 3b-3 Compound C2 test against extended harness). Federation Event Propagation stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING.

### Federation Event Propagation implementation — Phase 9 Commit 3b-2-equivalent

✅ **Phase 9 Commit 3b-2-equivalent SHIPPED 2026-05-24 at J-110** (seven-file atomic commit per D-074 + Lock #3 per-commit cadence; workspace test count 592 → 596 (+4); 2 of 2 consecutive `cargo test --workspace` runs PASS). Four new tests added across two new modules at `xgen-node/src/tests/`: `phase9_unknown_signer_first_contact.rs` (Scenario 5 — F-10 identity-arrival hook drains buffered event end-to-end inside a 100 ms wall-clock distinguishing window — sub-100 ms proves the synchronous `handle_identity_replicate_msg` hook fired vs the 5 s sweep); `phase9_federation_relationship_rejection.rs` with three tests (Scenario 6 main F-3 defer + recovery — Alice's room_create pushed by unfederated peer X defers via HeldPending (Phase 7.5 §6 held-not-bypassed posture); X's bootstrap `state.federation_add` via federation channel fires Step 7's `drain_pending_by_federation_relationship` hook; the buffered room_create lands in B's DAG within 200 ms. Phase 7.5 §5 extended skip-set negative assertion across `StateFederationAdd` + `StateSpaceCreate` + `StateDmSpaceCreate` (skip discriminator is "creates the Space it references"). `room_create` narrowness regression — `state.room_create` is NOT in the skip set; F-3 still fires for it; structural assertions confirm the third-trigger HeldPending behavior. **Discipline note recorded for next sibling milestone runbook author — cross-crate trace-event assertion gap**: findings v1.2 §2.6 sub-item C step 3 names the G2 `event = "f3_reject"` trace event with `disposition = "held_pending"` as a load-bearing observability surface; the trace fires from `xgen-core/src/node/runtime.rs:537` at warn-level. `tracing-test 0.2`'s per-crate scope (default `xgen_node=trace`) does NOT capture cross-crate events from `xgen-core` — `logs_contain("f3_reject")` returns false from an xgen-node test even when the trace fires. Resolution at this commit: trace assertions dropped, replaced with stronger structural assertions (`DispatchOutcome::HeldPending` outcome + `PendingBuffer::pending_federation_relationship_count() == 1` on the target Space — a regression that broke F-3 buffering would fail both); documented in module doc-comment §4 of `phase9_federation_relationship_rejection.rs` per Lock #2 honesty discipline. Trace-event regression coverage falls to the Phase 7.5 NodeRuntime unit tests at `xgen-core/src/node/runtime.rs:1486+` (same crate as the emit site, in `tracing-test`'s scope). Promoting `f3_reject` capture to the deployment level would require either cross-crate test-only subscriber broadening or an xgen-node wrapper trace mirror — both structural surfaces requiring Joe-lock per HANDOFF `tasks/HANDOFF_PHASE_9_COMMIT_3B_2.md` §7 Trigger 1/3; deliberately not done at this commit. **Recovery-path setup discovery worth recording for next Phase 9 test author**: Phase 7 B3 amendment (J-088) skips F-4 step 11 (sender registration + sender membership) ONLY for federation_add via the federation channel; locally-submitted federation_add enforces full validation. First attempt at the Scenario 6 recovery used `submit_locally` for the bootstrap federation_add — Step 11 rejected because the Node's pubkey wasn't a Space member. Restructure: dispatch the bootstrap federation_add via `ReceivedViaFederation` with X as the wire-authenticated peer (X signs its own federation_add per the F-1a tip-exchange shape; D-075 vantage derivation at runtime.rs:740-756 puts the (peer=X, space=S) pair on the drain hook); pre-register X's Node-pubkey on B (modelling production F-10 identity-replication happening before/concurrent with the bootstrap so signature verification step 12 passes); B3 skip lets it through F-4 step 11; Step 7 fires the drain hook; the previously-buffered room_create lands in B's DAG. **HANDOFF `tasks/HANDOFF_PHASE_9_COMMIT_3B_2.md` Status flipped ACTIVE → COMPLETED v1.1** at this commit per the HANDOFF's own §9 DoD (the bridge's load-bearing purpose closes with this commit); body §1–§8 stays authoritative as historical record of HANDOFF-at-authoring-time per the bridge-handoff-retention discipline (anti-tempfile-deletion-of-decision-records, D-065). **D-074 Lock #3 per-commit cadence applies** — not a milestone-close, so the milestone-close tally (eleventh at J-108, twelfth at J-109) does NOT increment. JOURNAL inclusion in this commit satisfies D-074's per-commit JOURNAL discipline unambiguously per Lock #3 (locked at J-101 Phase 9 §3.0). Phase 9 RESUMES at Commit 3b-3 — Compound C2 (F-5 anti-transitivity under push queue depth) per `tasks/FEDERATION_PROPAGATION_PHASE_9.md` §3 Commit 5; setup mirrors Scenario 2's `phase9_three_node_anti_transitivity.rs` harness pattern. Phase 9 milestone stays PLAY. Federation Event Propagation milestone stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING.

### Named decisions promoted to DECISIONS.md

✅ **D-070 promoted 2026-05-18** (468 tests, same-day post-Pass-3, no code changes). "Two events of equal importance, opposite direction" — named protocol principle. The original draft in `docs/xgen_node_admin_ops_design.md` §9 framed the principle as "EventAccepted exists, symmetric to Error." The Propagation Reliability Audit (J-081 §5) found that framing was necessary but not sufficient: `TransportMessage::Error` lacked an `event_id` field at all, meaning even with both Error and a future EventAccepted, the originator couldn't correlate either signal back to a specific event. D-070's DECISIONS.md entry incorporates the corrected post-audit framing: BOTH (1) both directions of outcome exist (acceptance + rejection signals), AND (2) both directions carry the envelope-level correlation identifier (`event_id: Option<String>` on `TransportMessage`). Without (2), (1) is hollow. M6 §9 draft preserved as historical record; DECISIONS.md D-070 is the canonical authoritative form. M6 (new) Phase 2 implements both halves in coordinated work with Federation Event Propagation milestone's F-4 rejection-site changes.

✅ **D-071 promoted 2026-05-18** (468 tests, same-day post-D-070, no code changes). "Subsystem audits precede dependent milestones" — project-management principle. Every future milestone whose correctness depends on a load-bearing subsystem MUST include a subsystem audit as part of its Phase 0 (design phase). Audits produce code-grounded canonical documents and surface gaps that may need to close as preconditions of the milestone rather than as parallel work. The pattern emerged organically during the Propagation Reliability Audit (J-081), where findings consistently exceeded the audit's nominal scope (four HIGH-severity findings across five sections) and the audit became Pass 1 input for two downstream design phases (M6 Phase 0, Federation Event Propagation Phase 0). D-071 pairs with D-069: audit phase (D-071) → design phase (D-069) → implementation phase, each producing a canonical artefact. Sibling to D-065 and D-070 — D-065 and D-070 are protocol-design principles; D-071 is the project-management analogue. The shared theme across all three: don't let assumed-state substitute for verified-state.

### Deprecated tracks

⬛ **M6 (original) Multiparty baseline pass** — descoped 2026-05-17. Original M6 plan (run the full Multiparty suite S1–S5 twice through present `--batch` to fill the "A" baseline column) descoped because the binary state had shifted post-J-079, the metric-protocol applicability needed reconfirmation, and the bigger problem M6 was meant to solve had grown to span both binaries. **Replaced by M9 Multiparty Redesign** at the end of the M-series trunk; the M6 slot is reused for the Node admin write path. Affected task files (`tasks/MULTIPARTY_S1_tauri_rerun.md`, `tasks/MULTIPARTY_S2_to_S5_present_pass.md`) carry the DEPRECATED status with the M9 pointer.

⬛ **`tasks/NODE_ADMIN_PASS2_PROPOSALS.md`** — superseded by `docs/xgen_node_admin_ops_design.md`. The Pass 2 proposals file was the working document for Pass 3's lock-decisions; once the design doc shipped with the framework decisions filled in, the proposals file lost its operational role. Kept in `tasks/` as historical predecessor per D-069's canonical-document rule.

---

## Present — playing now

⚫ **M8.7 (concurrent-commit resolution, R only) CLOSED at J-302 (2026-06-07) — single commit; `mls.commit` conflict domain + `mls_commit_tip` observable; convergence repro + sensitivity witness GREEN.** The last sequenced arc before M9. Phase-0 OPENED J-299 → design Joe-LOCKED J-300 (CC-D1..CC-D4) → runbook + CC-D5 vacuity-fix J-301 → SHIPPED J-302. **The problem:** `state_key_for_event` returned `None` for `MlsCommit`, so two members committing `N→N+1` at one frontier both applied and fold-order silently decided the epoch. **The fix (three small edits, one commit):** Step 1 `RoomState.mls_commit_tip: Option<EventXgid>` (CC-D5 — the observable; rides the M8 `Eq` oracle additively, `RoomState` unserialized → no migration); Step 2 `apply_mls_commit` writes the tip beside `mls_epoch` (D3-fencing comment corrected); Step 3 the `MlsCommit` arm in `state_key_for_event` — category `"state.mls_commit"`, key `"{room}:{target_epoch}"` (CC-D2; keyed by **target epoch** not per-Room — the regression guard against a later `2→3` competing with a losing `1→2`), CC-D3 lexicographic-by-`event_id` tiebreak with no `algorithm.rs` change (Layer 1 only fires for all-membership sets). **Why CC-D5:** the `mls_epoch` counter alone is **vacuous** — two honest commits both land `N+1` under either fold order, so a counter-only proof stays green with or without the fix; `mls_commit_tip` records *which* commit is canonical, the real divergence. **Tests (+5):** state_key units (incl. the epoch-regression guard + absent-epoch `None`) · resolution unit (frontier-2 → one lexicographic winner) · headline two-`NodeRuntime` convergence repro (ingest [A,B] vs [B,A] → both converge on `(mls_epoch, mls_commit_tip)` = the lexicographic winner) · **sensitivity witness** (revert the arm → tips diverge RED while the epoch stays converged; restore GREEN — proof CC-D5 earns its place). **Checkpoint:** the live `ingest_event` SR-D1 gate applies the first-arriving commit transiently via the fast path, then the second commit's arrival triggers a `derive_resolved` rebuild that folds **only the winner** — converged tip is winner-only, identical to membership conflict resolution; not an mls-specific defect (surfaced precisely per D-065/D-084). **Honest boundary (D-065):** R proves convergence-on-winner; loser rollback-and-replay is L (production openmls client) — the Arc H C1 Finding 1 analogue. Suite **1212**/0/2 (+5 over 1207); build all-targets 0; clippy clean both feature sets. **No DECISIONS change** (CC-D# arc-local, D-069). Docs → COMPLETED (AUDIT v1.3 / DESIGN v1.2 / IMPL v1.1). **Next-active: Joe selects the next milestone** (M9 strategic multiparty test harness, the next sequenced arc). Not pushed — Joe pushes.

⚫ **M8.5 (finalization box) CLOSED at J-278 (2026-06-06) — M8.5-A (J-271) · M8.5-B (J-275) · M8.5-C (J-277 C1 + J-278 C2 + close).** The finalization box is closed → next-active = **M9** (the strategic multiparty test harness). One flagged follow-on out of M8.5-C: the "re-home notify" arc (the `home_changed` client broadcast + a `register_ok` node-id echo, CP-5). Grounds the three M8-routed findings against HEAD `cecb5ee` → `tasks/M8_5_FINALIZATION_AUDIT.md` v1.1; M8.5-A `tasks/M8_5_A_F5_COHERENCE.md`; M8.5-B design `tasks/M8_5_B_INV_BOOTSTRAP_DESIGN.md` (COMPLETED) + runbook `tasks/M8_5_B_INV_BOOTSTRAP_IMPL.md` (ACTIVE). **INV (M85-A1..A4):** member-gated sync (`collect_sync_history` skips non-member Spaces) + the Err-only `get_dag_tips` fallback (`ops.rs:770`) + the invite/join membership-key collision → `derive_resolved` Layer 4 drops the join; §4 candidate seam = invite-authorized read + invitee learns the invite `event_id`. **F-5 (M85-A5..A7) — CLOSED J-271 (M8.5-A):** not an open fork — already JOE-LOCKED May 2026 as Option 1 (pairwise, no transitive relay; federation design §8.4); the "contradicts §3.2" framing was a phantom. Real gap = doc-coherence (ch3 never absorbed F-5) → **ch3 §3.4.8 + DECISIONS D-089**. **S5 (M85-A8..A10):** `RegisterArgs` only `--name`; no `identity.home_changed` EventType/builder/applier (1 of 3 wired); identity-registry-level, **M8-free**, heaviest build shape. **Forks (D-069/D-071):** F-5 **resolved** (J-271, ch3 §3.4.8 + D-089); INV **resolved-in-design** (INV-D1..D6, J-272 — scoped fetch + invite_id-chained join + `note` + tier-graded `valid_until`, T1=14d; CLOSED J-275); S5 Q3 — Joe-locked S5-D1..D4 (re-registration per §3.13.8 + delta-shaped signed `identity.home_changed`; design+runbook J-276; 3022 dormant, no new error code). **Sub-arc roadmap:** M8.5-A ✅ → M8.5-B ✅ → M8.5-C ✅ (C1 node+core `2585e1e` J-277; C2 client `183782c` J-278 — flag-only, `home_changed` emit deferred per CP-5; doc-only close). **S5 as-built:** all 3 identity-mobility surfaces exist — `re_registration` end-to-end (flag + Step-3 bypass + re-home `upsert`/bump), `identity.home_changed` applier (version-guarded), node dispatch. Suite **1193**/0/2 (+15 over 1178: C1 +11, C2 +4). M8.5-A added **D-089**; M8.5-C no DECISIONS change (S5-D# arc-local). **Next-active: M9** (strategic multiparty test harness), plus the flagged "re-home notify" follow-on.

The track or tracks the project is actively working on right now. Detail-level here is the most granular in the document — what's in flight, what's blocking what, what the next concrete step is.

⚫ **R2-F01 fix-arc (client/node convergence) CLOSED 2026-06-05 (J-260 audit → J-261 design-lock → J-262 C1 → J-263 C2 → J-264 close); code complete and honest.** The top Round-2 finding (S2) and the only UI-correctness one — finishes the **client-side half of state-resolution convergence** Arc C scoped out (node-side only) under SR-D3. **The gap:** the client replayed each Space's DAG via timestamp-sort + plain `apply_event` (not `derive_resolved`/topo-sort), so it could diverge from the node's resolved view under concurrency/clock-skew. **Fork = Option A, A-pure (F01-D1/D2):** all three client sites re-derive through the proven Arc-C `derive_resolved` with an **empty `identity_home_nodes` map** + vantage `""` — the binding isn't in the client's drained log (it lives in the node `IdentityRegistry`), so Layers 3/5a/5b abstain cleanly and Layer 5c guarantees a deterministic, self-consistent projection; node stays authoritative; no new node surface, no wire change. **C1 (J-262):** the two `ops.rs` read paths (`members_projection`/`ai_status`) — clean `derive_resolved` swap; `ai_status` keeps its M3 DM-bail (CP-1a); caught + fixed a lazy-fixture divergence (empty-`prev_events` invite+join became a spurious concurrent conflict under the ancestry-aware engine → causal-linked the two fixtures to production faithfulness, assertions unchanged; runbook §3.3 refined). **C2 (J-263):** the `ai_service.rs` AI-inbound loop — the node's ingest gate (`conflicts_in_log`→`derive_resolved` rebuild, else incremental `apply_event`) via a pure `apply_or_rebuild` helper + a retained per-Space log (D-065 honest residue). **F01-D5 reachability probe (J-264) = positive-in-principle:** the residual divergence class — concurrent + same-event-type + same-role + cross-home-node membership/key-rotation, reachable only in a federated multi-home Space (single-home = negative) — is decided 5c-lexicographic on the client vs 3/5a/5b on the node; the named, UNBUILT **A+thin-fetch** escalation is **flagged** for a future decision (a flagged decision, not an auto-build — D-065/F01-D2). Suite **1156**/0/2 (+3 over the gate's 1153); build all-targets 0; clippy clean both feature sets. No DECISIONS change (F01-D# arc-local, D-069). **Round-2 register now Open 2/9** (R2-F06 operator terminology · R2-F09 multi-device seam, D3-gated). **Next-active: R2-F06 before M10**, then M10 → UI (UI may now proceed past the R2-F01 gate). Clair stood down.

🔷 **E2E Encryption (Arc H) CLOSED INTERFACE-LOCKED 2026-06-04 (J-254 open → C1 J-255 `ee06168` → C2 J-256 `ffda2af` → close); the last Round-1 D-071 arc.** The PG-05 gap (E2E encryption boundary — server never decrypts). **Keystone (AH-A1):** the Phase-2 epoch-key scheme (`xgen-core/src/encryption/`, ChaCha20Poly1305+SHA-256) was **built but unwired** — zero live callers. So Arc H was *wiring + interface-completion + proof*, not a from-scratch crypto build; **real RFC 9420/openmls is genuinely D3** (absent from every manifest, parallel/timing-open per D-066). **C1 — boundary + guarantee:** `SpaceState.e2e_encryption` (set-once/default-OFF, AH-D2) + `state.mls_group_init` (Node-readable genesis anchor, AH-D3) + the **`enc:` v2 envelope** (per-message random `CK` wrapped under the epoch key — a **D-088 amendment**, AH-D1, giving crypto-shred its per-message erasure granularity; the design's byte-sketch omitted the wrap nonce → CP-1 added it for nonce-uniqueness under the reused epoch key) + Node DS blind-route + the **content-blindness proof** (a real in-process Node ingests an encrypted `message.text` via the live validation path; 5 assertions incl. the **threat-defended erasability invariant** — destroy the wrapped `CK` ⇒ content unrecoverable *even with the epoch secret*) + the **content-blind ≠ blind** metadata fence; the D-088 amendment + ch3 §3.10.7 envelope extension + §3.10.8 default-ON→OFF rode C1 (D-074). **C2 — lifecycle:** Node-side `KeyPackageStore` (≥3-pool/expiry/single-use, 5001/5002 from the §3.10.11 band — CP-3 grounded) wired via an `mls.key_package` ingest hook + `request_key_package`; epoch-advance on `mls.commit` (`RoomState.mls_epoch`, no new `state_key` → no new M8 surface; **concurrent commit-race fenced to D3**). **Close (D-074 doc-only):** gap-audit §5 **PG-05 → 🔷 INTERFACE-LOCKED** (register **Open 1/13 · Done 10 · Interface-locked 1 · NO-GAP 1** — PG-02 the sole open gap, gated on D3) + the named cascade `D-088 content-erasure → PG-05 real crypto → D3` (identity-orphan half stays PG-05-independent) + Appendix C Space class / Appendix I `state.mls_group_init`+`mls_epoch` reconcile + AH-D# eval (**AH-D1 promoted into D-088 — D-088 now carries an amendment**; AH-D2…D6 arc-local, D-069). **Honest boundary (D-065, C1 Finding 1):** no production MLS client drives encryption yet, so C1 ships the substrate + the *in-process* proof and C2 ships the Node-side handlers over the real ingest path — server-blindness is real + proven on the wire; production `ops::send` live-encrypt + the client group lifecycle ride the eventual production MLS client (D3). Suite **1153**/0/2 (+22 over J-252's 1131: C1 +14, C2 +8); build all-targets 0; clippy clean both feature sets. **With PG-05 interface-locked, every Round-1 D-071 arc is closed.** **Next-active: Joe selects the next arc** — remaining: PG-02 GDPR content-erasure *implementation* (gated on D3) · M9 (build strategic multiparty test harness) · M10 (Tier-1 Auth Module reference set, a UI-gate prerequisite). Clair stood down.

⚫ **Space-Migration (Arc F) CLOSED 2026-06-04 (J-251 open → C1 → C2 → close); code complete and honest.** The Round-1 D-071 §4-F arc (PG-11) — closes the last Wave-3 self-contained gap. **Keystone (AF-A1):** built-but-unwired — the wire layer (12 msg types + `state.space_migrate`) AND the core (`xgen-core/src/migration/` — state machine + transfer + verification + a pure end-to-end test) were built, but the **node driver was wholly absent** (zero dispatch in xgen-node). So Arc F was *wiring*, not design. **C1 — core completion:** pure `transition()` sequence guard (Idle→Negotiating→Transferring→Verifying→Complete/Failed; out-of-sequence ⇒ `WrongState` 6006; `MigrationMsgKind` local enum, CP-1) + `apply_space_migrate` cutover applier (AF-D1/D2 — flips the authority anchor `home_node` source→dest; idempotent; self-protecting `sender == home_node` gate so a post-cutover competing source-signed migrate fails by construction; causally-terminal singleton, no `state_key` arm). **C2 — node driver:** `migration_driver.rs` source (`run_source_migration`, spawned by the operator verb) + destination (`handle_migration_incoming`, off the `handle_connection` first-message branch) halves; 12-msg dispatch through `transition()`; transport via `send_migration`; EventStore bridge (CP-4 — `ensure_store` fresh per-Space store + `append` + `rehydrate_space_from_store`, engine-agnostic, no separate SQLite cache-rebuild); cutover commits `state.space_migrate` (flips source) + ships it to dest (flips dest) + pushes to peers (CP-2/AF-D8a — applier reuse, `migration.federation_notify` courtesy); retention (AF-D5 — source keeps its store, teardown operator-gated only); dormant admission (AF-D6 — accept-unless-hosting, 6003/4/5 dormant); `migration initiate` operator verb (CP-3, audited). Required wiring `state.space_migrate` into `validate_event`'s Node-authored path (the C1 grounding finding) — new wire **6009 `migration_authority`** (AF-D2; **superseded the C2-guessed 6007** which collided with the spec's §3.12.11 `migration_verification_failed` — the Arc-E renumber pattern). **Close (D-074 doc-only):** ch3 §3.12 implementation note + §3.12.11 row 6009; ch4 §3.12 as-built note; gap-audit §5 PG-11 ✅ (register **10/13 done, 2 open**); Appendix K carry-in (migration sibling-drift) recorded into the Round-2 audit home; task docs COMPLETED; AF-D1–D8 all arc-local (D-069, none a cross-arc invariant — no DECISIONS change). Honest dormant-but-correct (D-065): mechanism is real + two-node-e2e-tested (`home_node` flips on both Nodes + retention + AF-D2 stale-reject + already-hosted 6002 reject); destination admission 6003/4/5 dormant, source teardown operator-gated. Suite **1131**/0/2 (+10 over J-250's 1121: C1 +8, C2 +2); build all-targets 0; clippy clean both feature sets. **Next-active: Joe selects the next arc.** Clair stood down.

⚫ **Jurisdictional-Namespacing (Arc G) CLOSED 2026-06-04 (J-249 open → C1 → C2 → J-250 close); code complete and honest.** The Round-1 D-071 §4-G arc — the actionable half of the §2.2 no-anonymity-vs-government tension (PG-04). Keystone (AG-A2): the federation containment chokepoint already existed (`policy_permits`, two-site-enforced, default-permit), so Arc G added a restrictive **dimension**, not a new gate. **C1 — protocol half:** `SpaceState.jurisdiction: Option<String>`, **set-once at create** (no mutation event, no applier, no `state_key` arm — a legal domain fixed at Space birth; rides M8 `derive_resolved` for free via `PartialEq`, AG-D3), read by all three constructors (DM ⇒ `None`, AG-D4); `build_space_create_event` gains a `jurisdiction` param (open string, not enumerated/XGID — AG-D2); ch3 §3.7.3 content schema + AppC Space class. **C2 — implementation half:** `FederationPolicy.allowed_jurisdictions: Option<Vec<String>>` (additive serde, restrictive-only, default permit-all) + pure `jurisdiction_permits` **AND-composed** with `policy_permits` at both enforcement sites (outbound `federation_session.rs`, inbound `app.rs` — guarded so the dormant default takes no extra lock); **strict undeclared-denied** semantic locked (an undeclared Space fails a restrictive set, mirroring `allowed_spaces`); `--allowed-jurisdiction` operator surface on `federation set-policy` (AG-D6); ch3 **MAY** clause authorising the hook. **Close (J-250, D-074 doc-only):** ch3 §3.6.7 central-identity-aggregation **MUST-NOT** (promotes ch1 L858's implication to normative); AppC reconciled (jurisdiction on Space; stays on AuthModule per AE-D5; identity-level jurisdiction fenced OUT, AG-D8); gap-audit §5 PG-04 ✅ (register **9/13 done, 3 open**); task docs COMPLETED; AG-D1–D8 all arc-local (D-069, none a cross-arc invariant — no DECISIONS change). Honest dormant-but-correct (D-065): the field is real + convergence-safe from C1; the federation hook is live but a **no-op** until an operator declares `allowed_jurisdictions` (PG-13 family); active data residency (geo-pinned storage, legal attestation) is **not** delivered — operator/Tier-2+ infra, fenced out. Suite **1121**/0/2 (+14 over J-248's 1107: C1 +4, C2 +10); build + clippy clean both feature sets. **Next-active: Joe selects the next arc.** Clair stood down.

⚫ **Primitive-Completion (Arc E) CLOSED 2026-06-04 (J-245 open → J-246 C1 → J-247 C2 → J-248 close); code complete and honest.** The primitive-completion cluster from gap-audit §4-E — shipped the two documented-but-unimplemented core primitives. Full first-class Role object model **spun OUT** to a later privilege-model arc; per-Room-type Thread behaviour + notifications → a client/UI milestone (AE-D10). **C1 — PG-03 `TrustAssertion` (J-246):** the third SignedPrimitive (Event/Node/TA). `xgen-common` struct + `TrustClaims` (fields per ch3 §3.8.4 — `valid_until`, not `expires_at`; no `jurisdiction`; `extra`-preserving) + canonical bytes/sign/verify (Ed25519 vs `issuer`) + `TrustAssertionXgid::from_assertion` (closed the `flavours.rs:35` Pass-2 deferral). `xgen-core` `AssertionPolicy` + the full 7-check `validate_assertion` (§3.8.5) wired into `accept_registration` — registration steps 5–7, **dead since Phase 1** (bind-and-drop; 3004/3005 never returned), now enforced; codes 3004/3005/3006/3030 reused + **3010/3011** added (the design's guessed 3006/3007/3008 collided with existing variants — honest supersession, D-065). `NodeRuntime.assertion_policy` from `[node].trusted_auth_modules` (empty default); `assertion_tier_of` now reads the validated tier (gives PG-13 real teeth at T2–4). Honest dormant-but-correct (empty trusted-list + Local-Node bypass §3.8.8; synthetic test issuer, no live Auth Module — AE-D4). **C2 — PG-08 `Thread` (J-247):** `thread.create`/`.resolved`/`.archived` EventTypes + `ThreadStatus` + `ThreadState` in `SpaceState`; appliers + `state_key_for_event` arms (shared `thread.status` key → resolved-vs-archived converges via M8 `derive_resolved`); narrow-not-widen + the AE-D9 participation tier gate (real teeth post-PG-03); resolve/archive `ChangeInfo`-gated (CP-3 reuse); `prev_events` seed the parent Room (CP-4, D-076 v1.1). **Close (J-248, D-074 doc-only):** gap-audit §5 PG-03 ✅ / PG-08 ✅ (register **8/13 done, 4 open**); ch3 §3.6.5 +rows 3010/3011 + §3.8.5 implementation-status note; AppC TrustAssertion reconciled (`valid_until`/`claims`; `jurisdiction` → Phase-3); task docs COMPLETED; AE-D1–D10 all arc-local (D-069, none a cross-arc invariant — no DECISIONS change). Suite **1107**/0/2 (+47 over J-244's 1060: C1 +33, C2 +14); build + clippy clean both feature sets. **Next-active: Joe selects the next arc.** Clair stood down.

⚫ **Privilege-Model (Arc D) CLOSED 2026-06-03 (J-242 open → J-243 C1+C2 → J-244 close); code complete and honest.** Enforcement-hardening — closed **PG-13** (tier-gate `verify_tier_assertion` never wired into the join path) + **PG-12-min** (per-Room × per-Role permission override). **PG-10 → NO-GAP** on grounding (AI-not-owner + dm_initiate already enforced in `dispatch_event` step 4; spontaneous_post spec-deferred — register reclassify at close). **Audit** (`tasks/PRIVILEGE_MODEL_AUDIT.md` v1.0): PG-13 GAP-CONFIRMED (predicate exists, zero prod callers; Space `auth_tier` present; joiner side coupled to PG-03), PG-12 NEEDS-DESIGN (as-built = fixed 4-role enum + threshold table, no Room layer; full first-class Role model → arc E), PG-10 NO-GAP. **Design** (`..._DESIGN.md` v1.0 — PM-D1…D6 LOCKED, arc-local D-069): D1 tier-gate as a `MembershipJoin`-only branch in dispatch step 4, `Err`→wire 3030 · D2 joiner tier via `assertion_tier_of` (None→1; the single PG-03 upgrade site) · D3 carry overrides on the existing `state.room_update` (already state-keyed → **M8-convergent for free**; applier currently inert, SR-F2 remainder) · D4 `RoomState.permission_overrides: HashMap<(Role,RoomPermission),Effect>`, fixed-enum-bounded · D5 room-aware layer in `check_permission` (introduces per-Room `send_messages` gating — the "Mods can't post in #announcements" case) · D6 protocol-only (applier + enforcement + test builder; user-facing surface → UI pass; M3 operator-privilege deferrals stay parked). **Runbook** (`..._IMPL.md` v1.0): C1 PG-13 wiring (honest Tier-1 no-op) → C2 PG-12-min → doc-only close. Four confirm-at-pickup (CP-1 wire-code mechanism · CP-2 map repr · CP-3 replace-vs-merge · CP-4 EventType→RoomPermission fold). **C1+C2 shipped J-243** (PG-13 gate on `MembershipJoin`, honest Tier-1 no-op; PG-12-min per-Room overrides via `state.room_update`); **close J-244** (doc-only — gap-audit §5 PG-13 ✅ / PG-12 ✅ / PG-10 NO-GAP, task docs COMPLETED, PM-D# all arc-local). Suite **1060**/0/2 (+12 over J-241). No DECISIONS change (PM-D# arc-local, D-069). Full first-class Role model + `TrustAssertion` (PG-03, gives the gate real teeth) → arc E. **Next-active: Joe selects the next arc.** Clair stood down.

⚫ **State-Resolution Convergence (Arc C) CLOSED 2026-06-03 [borrowed the M8 slot; slot vacated 2026-06-05 (R2-F05) — M8 now = A/B metrics, M9 = Multiparty Redesign, multiparty test = unnumbered] (J-237 open → J-241 close); code complete C1–C3 and honest.** The D-071 Phase-0 gate for multiparty. Grounding inverted the gap audit's premise (SR-F1): the seven-layer convergence algorithm (`xgen-core/src/resolution/`, ch3 §3.9.3) was built + unit-tested but had **zero production callers** — the apply path was Phase-1 last-write-wins. **M8 wired it onto the node apply path + proved convergence**, no algorithm redesign. **Phase 0** (J-237, doc-only): audit (SR-F1–F5) + design (SR-D1–D6 LOCKED, arc-local D-069) + runbook. **C1** (J-238) `resolution/derive.rs` — `derive_resolved` (three-phase: unconflicted-state basis → resolve frontiers → fold all-but-losers) + 6 convergence property tests; CP-A locked (unconflicted basis; SR-D7 not promoted), CP-B locked. **C2** (J-239) wired onto `runtime.rs` (`rehydrate_space_from_store` + the SR-D1 `ingest_event` gate via the ancestry-aware `conflicts_in_log`, CP-E); `identity_home_nodes` from the registry (CP-C). **C3** (J-240) two-node convergence smoke (phase9 harness) + SR-F2 **DEFER** (ch3 has no room/space-update content schema). **As-built:** `find_conflicts` does clause-1 (state-key) grouping only — the §3.9.1 causal-frontier exclusion lives in `derive_resolved` + `conflicts_in_log`; the three conflict-fns' doc-contracts reconciled at close (fix-docs: `find_conflicts` key-only / `conflicts_with` direct-parent / `conflicts_in_log` transitive). **Open edge → M9/policy:** an open-space join concurrent with an invite for the same identity is a genuine conflict — convergence holds (all Nodes agree on the winner) but *which* membership outcome is preferred is a policy question, not a convergence one; named, not M8 scope. M9 not triggered (all three proof levels clean; held as a contingency). `cargo test --workspace` **1048**/0/2 at close; build + clippy clean both feature sets. No DECISIONS.md change (SR-D# arc-local, D-069; evaluated at close, none promoted). Close records: AUDIT/DESIGN/IMPL → COMPLETED · PROTOCOL_GAP_AUDIT v1.5 (scope-fold: PG-08/10/12 NOT folded) · `conflict.rs` doc-fix · JOURNAL J-241. **Next-active: Joe selects the next arc** (gap audit §4: arc D enforcement-hardening · arc E primitives · M9). Clair stood down.

✅ **Protocol gap audit COMPLETE 2026-06-03 (J-233 → J-236).** Exhaustive spec-vs-as-built + multiparty-readiness sweep — 13 gaps catalogued (`tasks/PROTOCOL_GAP_AUDIT.md`); Wave-1 closed (Arc A doc-drift; Arc B forward-compat / PG-09, C1 `9bf57d1` + C2 `e0a1972`). 4 of 13 done; remaining open gaps grouped into Waves 2–4.

⚫ **Storage-Engine / Plugin-Framework — CLOSED at J-232 (2026-06-03); code complete (C1–C5 + S) and honest. Opened J-230; Phase 0 audit + design + runbook were doc-only.** First `system·node` slot instance of the by-trade module-framework stance, on J-228's `EventStore` trait (**D-080 LOCKED**, unchanged). Scope = the compile-time plugin/module spine + the first plugin (`xgen-store-sqlite`) through it. **Audit** (`tasks/STORAGE_ENGINE_AUDIT.md` v1.0): 4.1 trait-fit NEEDS-DESIGN (sibling `StorageEngine: EventStore`; durable-append-seq contract); 4.2 owners CONFIRMED (`Box<dyn EventStore>`, one reach, rest additive behind J-228 `&dyn`); 4.3 tier-gate STALE→Joe-lock (no singular node tier — `auth_tiers_served` set + bootstrap-scoped + module `accepted_tiers`); 4.4 deps CONFIRMED (uuid core-only, no `[storage]`). §8 banks why dynamic native loading was rejected (in-process key-theft on a no-anonymity key-holding Node) for a future Wasm/signing arc. **Two Joe-locks (2026-06-02): SE-D1** sibling `StorageEngine: EventStore` (`open`+`descriptor`), **sync v1** (forward-constraining), durable-seq + seq-stability banked, static trait type-erased to `Box<dyn EventStore>` at `register::<E>()`; **SE-D4** tier→engine gate = explicit `[node].asserts_tier` clamped, default = floor over (`auth_tiers_served` ∪ module `accepted_tiers`), never below floor (loud reject), refuse-to-start if the engine's `AssuranceClass` under-delivers; home `[node]`. **Design** (`tasks/STORAGE_ENGINE_DESIGN.md` v1.1 — SE-D1–SE-D8 LOCKED, arc-local D-069): SE-D2 descriptor types in xgen-common (+uuid, never `Xgid`); SE-D3 explicit `register::<E>()` (reject `inventory`/`linkme`, reject-unknown-loud); SE-D5 `[storage.<engine>]` opaque passthrough; SE-D6 owners→`Box<dyn EventStore>`; SE-D7 conformance spec; SE-D8 advert light/federation-deferred; `AssuranceClass` minimal (`BestEffort < Durable`). **Runbook** (`tasks/STORAGE_ENGINE_IMPL.md` v1.0): 5 code + close, all green-on-landing — C1 spine → C2 owner boxing (inert) → C3 registry+gate+`[node].asserts_tier`/`[storage]` config (vanilla-only, gate passes T1) → C4 `xgen-store-sqlite` (durable seq, settings, `Durable`) → C5 SE-D8 advert (light) → close (D-074: Appendix L + Ch4 §4.12 + SE-D7 appendix + evaluate the module-framework candidate-D at instance #1). Doc-only; suite at J-229's **999**/0/1, not re-run. No DECISIONS.md change at open (SE-D# arc-local, D-069; promotions + candidate-D at close). **Closed at J-232 (2026-06-03), doc-only D-074:** C1 `4198e46` · C2 `ef4e2ee` · C3 `5fba991` · C4 `2da7cb2` · S `f844a9c` (substitution + Scope-B durability handover, SE-SUB-D1…D6 + GUID match-by-value) · C5 `da98c9a` (active-engine advert). **1024/0/2** + 2 feature e2e; clippy clean both feature sets; vanilla byte-for-byte unchanged. A selected engine is the live per-Space store **and** durability authority (JSON bypassed; restart rehydrates from `<dir>/*.db` via `range(0)`; store-open failure = loud reject, never silent RAM fallback) — the SE-D4 gate is now honest. Promoted **D-085** (module framework) / **D-086** (module identity) / **D-087** (assurance enforcement); SE-D#/SE-SUB-D# arc-local (D-069); D-080 unchanged. Close records: Appendix L v1.1 §L.9/§L.10 · Ch4 §4.12.6 · DECISIONS +D-085/086/087 · ROADMAP/CLAUDE/JOURNAL J-232. Ch4 SQLite-per-Space prose ⬛ DEPRECATED (the SQLite-per-Space design was superseded by the as-built EventStore service — §4.12.4/§4.12.6 + Appendix L; J-282); historical record, no prose sweep pending. **Next-active: Joe selects the next milestone.** Clair stood down.

⚫ **`.events` integration test — CLOSED at J-229 (2026-06-02); the deferred client↔node `.events` end-to-end test shipped.** Closes the C5 component-test boundary flagged at J-211/J-212 (the client `subscribe`→second-WS→forward happy path and the node `apply_fanout`→observer→JSONL join were each only half-tested). Two test-only commits + a doc-only close. **C1** client seam (`xgen-client/src/tests/events_pipe_integration.rs`) — real second WS via an `xgen-core` stub (the `sync_safety_net` pattern emitting an event post-auth; no real Node / no membership — the client forwards whatever arrives, filtered by its own subscribe `Filter`); happy + filtered-out negative; `serial_test` dev-dep added + the two count-asserting unit tests serialized on `events_sessions`. **C2** node seam (`xgen-node/src/tests/events_pipe_integration.rs`) — real `apply_fanout` → observer registered via the real `.events` pipe handler → JSONL; happy + filtered-out negative; creator-only Space fixture (the catch: `apply_fanout` early-returns unless the event's Space is hosted, since `event_nodes` derive from the looked-up `SpaceState`). Both `handle_events_connection` → `pub(crate)` (EIT-D4); reused public `xgen-core` builders + `phase9_harness` helpers. `cargo test --workspace` **999**/0/1 (+4 vs J-228's 995); build all-targets 0/0; clippy `-D warnings` clean. Audit + design (EIT-D1–D5) → COMPLETED; no DECISIONS.md change (EIT-D# arc-local, D-069). **Next-active: Joe selects the next milestone** (M8 · M9 · storage-engine module · privilege-model arc · temperature-plugin arc). Clair stood down.

⚫ **Durable EventStore — CLOSED at J-228 (2026-06-02); the `EventStore` trait + vanilla file backend + minimal durability floor shipped, implementing D-080.** Two code commits + a doc-only close. **C1 `bfe5cd8`** (xgen-core, atomic) — `EventStore` trait + rename struct → `InMemoryEventStore` + append-seq `range` + `&dyn` re-route of the ~5 consumers; 987/0/1. **C2 `2eb3b0c`** (xgen-node) — durability floor: `atomic_write.rs` (temp + `sync_all` + rename; Windows `MoveFileExW`; `#[cfg(unix)]` dir-fsync), `persist_event → io::Result` loud + propagate + no ack-block (**D-084**), honest-fail/quarantine on corrupt read; read-skip deferred (load-bearing for dedup + PAL-D1, superseded-by-engine-module); 995/0/1, clippy clean. **Close (doc-only):** NEW **Appendix L** (EventStore service reference, as-built) + **Ch4 §4.12** as-built graduation of the three §8 cross-cutting notes (Tier-2–4 conformance · vanilla scale ceiling + operator contract · module-framework stance), superseding the §4.12.1 SQLite-per-Space drift; **D-084** (silent-write contract) in DECISIONS.md; **ES-D6** stale `store.rs` header fixed. ES-D1–ES-D6 arc-local (D-069); D-080 unchanged (no amendment). audit v1.1 + design v1.7 + runbook v1.2 → COMPLETED. **Storage engines (SQLite reference / redb) remain a later opt-in module behind the trait; a Node asserting Tier 2–4 must run that engine module.** **Next-active: Joe selects the next milestone.**

⚫ **M7-standalone — live config reload CLOSED at J-226 (2026-06-02); the M7 family is DONE.** Realised the `--reload-config` Node verb (an honest `NOT_IMPLEMENTED` stub through M2). Two code commits + doc-only close: **C1** `f8777ac` pure substrate (`config_reload.rs` — `reload_plan` diff/classify + `format_*` report + `listen_addr_valid` gate predicate; +11 tests) → **C2** `a63a73b` `__RELOAD_CONFIG__` handler body + CP-1 baseline (C3 client-surface folded in; +8 tests) → close (this entry). **As-built:** CP-1 home = a dedicated `Arc<Mutex<NodeConfig>>` threaded `run_node → start_pipe_server` (NOT a `NodeRuntime` field — xgen-core must not depend on the xgen-node `NodeConfig`); snapshot update rule = Design A (restart-required fields never update the snapshot, so a divergent field is honestly re-reported `PENDING_RESTART` every reload, and edit-then-revert can't produce a false report — the lie-free reading where the runbook §3 rule and §4 test-bullet were in tension). §2.6.3 corrected at close (`[node].local_mode` → Restart-required, M7S-D1; `[sync]`/`[federation]` restart-required, `[bootstrap]` N/A/seed-only, M7S-D6). `cargo test --workspace` **984**/0/1 (+19 from 965); build all-targets 0/0; clippy `-D warnings` clean. No DECISIONS.md change (M7S-D# arc-local, D-069). **M7-standalone was the last open M7 piece — the M7 family (`--aicontrol` v1 · events · completion · standalone) is now complete**, with five deferrals carried to non-M7 homes (privilege-model arc · temperature-plugin arc · pipelined-handler arc · migration subsystem · the `.events` integration test). **Next-active: Joe selects the next milestone** (M8 multiparty · Durable EventStore · M9 · privilege-model arc · temperature-plugin arc). Clair stood down.

⚫ **M7-completion cluster — CLOSED at J-223 (2026-06-01; Clair, doc-only close).** Six code commits under M7C-D1–D4 (arc-local, D-069): Block A (AC-D5 — `members`/`leave`/`create-dm-space` + node DM-init arm, J-217–J-219) → Block B (AC-D4 token J-220 · AC-D6 idempotency J-221) → Block C (`nodes`/`ordered_nodes` widening, J-222). Three Joe-lock checkpoints. Two grounding catches corrected at the impl boundary (A1 key-less DM constructor J-216; CP-1 propagation trace — invite tip-chained, DMs single-homed, membership rides the root). 965/0/1. Canonical doc v1.5; audit/design/runbook COMPLETED; no DECISIONS.md change. **Next-active: Joe selects the next milestone** (M7-standalone · M8 · Durable EventStore · M9 · privilege-model arc · temperature-plugin arc · the `.events` integration test · invitee-join-across-nodes DM flow). Clair stood down.

⚫ **(historical, superseded by the CLOSED state above) M7-completion cluster — Block A CLOSED + Block B done (2026-06-01; Clair).** B2 (J-221): AC-D6 idempotency key (M7C-D2) — opaque `Command.idempotency_key` (same shape as token) + NEW `IdempotencyStore` (bounded FIFO, `DEFAULT_IDEMPOTENCY_CAP=1024`) held per-connection; `dispatch_one` checks before dispatch (replay→cached, `absent==do-it-over`) + records after on success. Two surfaced decisions: (2) **key-binding = result-time** (only completed+successful ops dedupe; errored/crashed re-do); (1) **in-flight policy = none built** (serial handler precludes same-connection mid-flight replay; cross-connection not deduped = per-session-scope consequence; forward constraint: a pipelined handler must wait-or-reject, never do-it-over). Store lifecycle confirmed per-connection + FIFO-bounded (no unbounded growth); scope lives in placement (per-session→per-driver), wire unchanged. 964/0/1 (+7). B1 (J-220): AC-D4 per-connection token against the CP-2 lock — opaque `Command.token` (top-level, never in `args`/clap) + pure `check_token` (xgen-common) + per-command gate before dispatch; v1 ships **wired-but-inert** (`expected_token=None` at all 3 spawns; `absent==proceed`; `Some(invalid)`→`PERMISSION_DENIED`), B-subsumable (B supplies the source with no wire change). Two surfaced B1 decisions: cadence = **per-command** (not verify-once); validity source = **inert in v1** (enforcement = B/privilege-model arc, no `[aicontrol]` config invented). Coupling noted: `Command` must never gain `deny_unknown_fields`. 957/0/1 (+9). A1 (J-217): `ops::members` lift + the key-less `from_dm_space_create_node` constructor + shared `drain_space_events`. A2 (J-218): `ops::leave` pure adapter mirroring `join`. A3 (J-219): `ops::create_dm_space` (the one non-pure-adapter verb) + the node `StateDmSpaceCreate` ingest arm — **CP-1 locked** after a propagation trace. CP-1 trace findings (D-065): membership rides the **root**, not the invite; the invite is **rebuilt tip-chained** (CP-1 (iii)) so it's Accepted+persisted but a state no-op (`apply_invite` DM-reject swallowed) — the constructor's raw invite has empty `prev_events` (latent bug, flagged + witness-tested, overridden at the call site); **DMs are single-homed** (no federation push, `DmFederationNotAllowed`); room/invite are **reject-if-space-absent** (not buffered) → ordering is the correctness contract (root first over one connection). Out-of-scope: invitee-join-across-nodes discovery (A3 forms creator-home-Node state only). All three verbs routed through all four client dispatchers (D-067); node `--batch`/`pipe.rs` untouched (D-066). Verification: `cargo test --workspace` **948**/0/1 (A1 +6, A2 +1, A3 +2); build all-targets 0/0; clippy `-D warnings` clean. No DECISIONS.md change (M7C-D# arc-local, D-069; D-074 per-commit). Remaining: **CP-2 (Joe-lock) token seam** → B1 AC-D4 token → B2 AC-D6 idempotency → CP-3 → C1 `nodes`/`ordered_nodes` → close. CANNOT-close guardrail names all five deferrals. **Next-active: CP-2 (Joe-lock) — Clair pauses for Joe before B1.**

⚫ **(historical, superseded by the runbook-SHIPPED state above) M7-completion cluster — design CLOSED at J-214 (2026-06-01; M7C-D1–D4 locked, doc-only).** Audit J-213 → design J-214. **M7C-D1/D2** — the AC-D4 token + AC-D6 idempotency key bind to plane 1 (the `.aicontrol` per-connection handler), resolving the H3 connection/session overload; written B-subsumable so the better end-state (B, per-driver-identity) lands with zero rework when a future privilege-model arc activates AC-D4's inert reserved trio. **M7C-D3** — Block-A verbs `members` + `leave` are pure adapter (grounded: `ai_status` already replays `state.members`; `membership.leave` fully backed, node accepts member-initiated leave). **M7C-D4** — `create-dm-space` is the one non-pure-adapter verb: client 3-event send + a node-side key-less `StateDmSpaceCreate` ingest arm (a second catch — `runtime.rs:325` builds a SpaceState only in the `StateSpaceCreate` arm; DM falls to `_` and builds nothing); Joe folded the node-init into Block A as primitive-completion. Sequence: runbook → Block A (freezes verb set) → Block B (token → key) → Block C (`nodes` widening) → close. Doc-only — 939/0/1. **Next-active: implementation runbook** (`tasks/M7C_COMPLETION_IMPL.md`; checkpoints: token-binding seam before Block B, node DM-init arm before create-dm-space). Clair stood down.

⚫ **(historical, superseded by the design-CLOSED state above) M7-completion cluster OPENED at J-213 (2026-06-01; Phase 0 backing audit, doc-only).** Closes the `--aicontrol`-shaped remainder of M7: **Block A** client-feature (AC-D5 — `create-dm-space` · `members` · `leave`) → **Block B** hardening (AC-D4 per-connection token → AC-D6 idempotency keys) → **Block C** `nodes`-filter `ordered_nodes` widening. Audit (`tasks/M7C_COMPLETION_AUDIT.md` v1.0 ACTIVE) hunted a C2/C3-shape stale-premise across the 3 "missing" `ops::*` paths + the post-events `.aicontrol` connection layer. **H1 (`members`) STALE** — `ai_status` (`ops.rs:1031`) already replays `SpaceState` + reads `state.members`; it's a lift. **H2 (`leave`) STALE, inverted** — `membership.leave` is fully backed at xgen-core (builder `state.rs:1221` + applier `apply_leave:604` + tests); `leave` is adapter, not event-design. **H3 (AC-D4/D6 connection model) NEEDS-JOE-LOCK — the real catch**: the events arc overloaded "connection/session" across 3 layers (client `.aicontrol` `Bindings` · client `.events` process-wide count · node-side `ConnId`/`Vec<(ConnId,Sender)>`), so the AC-D4 token + AC-D6 key can't bind until the design names the layer. Block A is lighter than scoped (surface-exposure + ops-wiring); Block B is gated on H3; Block C unchanged. ROADMAP↔CLAUDE cluster-pointer drift fixed in the same commit (D-069). Doc-only — 939/0/1, not re-run. **Next-active: design phase** (`tasks/M7C_COMPLETION_DESIGN.md`, H3 first). Clair stood down.

⚫ **M7-events arc CLOSED at J-212 (2026-06-01; C6 doc-only close).** The client + node `.events` event-observation pipes (deferred from M7 `--aicontrol` v1 at J-203) shipped end-to-end across 5 code commits + 1 close, on top of the gating Node multi-connection-per-identity fan-out change. Arc: audit+design+runbook J-206 (EV-D1–EV-D6 LOCKED) → **C1** `ClientSenders`→`Vec<(ConnId,Sender)>` retype `34f9ee3` (J-207) → **C2** pure `Filter`/`parse`/`matches` substrate + **EV-D4 v1.1** (3-param `matches`, `event_nodes` caller-supplied — 2-param unimplementable for `nodes`) `a82ed21` (J-208) → **C3** node observer registry **Shape β** (process-global `OnceLock`, J-166 precedent — `apply_fanout` sig + 4 callers untouched) + node `state` count `ea4ea05` (J-209) → **C4** node `.events` pipe `events_pipe.rs` (registry writer) `188cf53` (J-210) → **C5** client `.events` pipe (second same-identity WS riding the C1 retype, closing the J-203 collision) `b57007a` (J-211) → **C6** doc-only close `fcd774e` (J-212). Single Joe-lock checkpoint (before C1) closed at J-206; C2–C5 checkpoint-free adapter work (D-065). As-built (canonical doc v1.4 §3): source-4-narrow `derive_event_nodes`; live-only no-history (Q2); pipe `…\<base>.aicontrol.events`; `event_subscriptions` now live count both binaries. C5 happy-path component-tested not e2e (flagged for a future client↔node integration test). `cargo test --workspace` **939**/0/1 (+41 across the arc). No DECISIONS.md change (EV-D# arc-local, D-069 — considered promoting EV-D4 v1.1 + Shape β, both stay arc-local). Audit/design/runbook → COMPLETED. **Next-active: Joe selects the next milestone** (M7-standalone live config reload · M8 multiparty · Durable EventStore · M9 · `--aicontrol` hardening arc [AC-D4 token + AC-D6 idempotency] · client-feature arc [AC-D5 verbs] · the C5 `.events` integration test ✅ CLOSED J-229). Clair stood down until selection. **Entry point: CLAUDE PLAY + JOURNAL J-212 per Rule 0.**

⚫ **(historical, superseded by the M7-events-CLOSED state above) M7-events arc OPENED at J-206 (2026-06-01; doc-only — audit + design + runbook, no code).** Adds the client + node `.events` pipes deferred from M7 v1, on the gating Node multi-connection-per-identity fan-out retype (`ClientSenders` → `Vec<(ConnId, Sender)>`; carries J-203 Q1/Q2/Q3). Audit `tasks/M7_EVENTS_AUDIT.md`; design `tasks/M7_EVENTS_DESIGN.md` with **EV-D1–EV-D6 all LOCKED** (`ConnId(u64)` · kind-agnostic Vec retype · client second-WS + node observer-registry-in-`apply_fanout` · reuse AC-D3b + shared `matches` · `FederationPeerSenders` out · process-wide `event_subscriptions` count); runbook `tasks/M7_EVENTS_IMPL.md` = 6 commits, 1 Joe-lock checkpoint before C1. Doc-only — suite at J-205's **898**/0/1. No DECISIONS.md change (EV-D# arc-local). **Next-active: the C1 Joe-lock checkpoint** (retype touch-set code-trace + `ConnId` mint points + prime-invariant regression); Clair stood down until it closes. **Entry point: CLAUDE PLAY + JOURNAL J-206 per Rule 0, then `tasks/M7_EVENTS_IMPL.md` Checkpoint + the design doc.**

⚫ **M7 `--aicontrol` v1 milestone CLOSED at J-205 (2026-06-01; C6 doc-only close).** Ships **command-pipes-only** — client + node `.aicontrol` command pipes over the shared `xgen-common::aicontrol` substrate; an adapter (D-065) wrapping the shipped `ops::*`/`admin_ops::*`; `--batch` untouched (D-066). Arc: audit J-198 → design J-199 (AC-D1–AC-D6) → runbook J-200 → C1 J-201 → C2 J-202 → checkpoint #2/reshape J-203 → C4 J-204 → C6 J-205. Both checkpoints closed (#1 wiring/substrate/timeout-finding; #2 event-seam → split-trigger b). As-built (canonical doc v1.3): CONCURRENT_COMMAND_NOT_ALLOWED wired-but-non-firing in v1; client reconstruct-argv vs node serde marshaling; §7.1 node surface = `admin_ops::*` + `state`; node `state` drops operator_display_name, event_subscriptions=0. Deferrals → M7-events arc (events pipes + node multi-connection fan-out) + the hardening arc (token/idempotency) + the client-feature arc (3 verbs). `cargo test --workspace` **898**/0/1 (+58 across the milestone). No DECISIONS.md change (AC-D# arc-local). canonical doc v1.3 + runbook/design/audit → COMPLETED. **Next-active: Joe selects the next milestone** (M7-events arc · M7-standalone live config reload · M8 multiparty · Durable EventStore · M9). **Entry point: CLAUDE PLAY + JOURNAL J-205 per Rule 0.**

⚫ **M7 `--aicontrol` C4 node command pipe SHIPPED at J-204 (2026-06-01; last code commit).** New `xgen-node/src/aicontrol.rs` (sister to `pipe.rs`; `--batch` untouched). Wraps the same `admin_ops::*` `dispatch_admin` calls; envelope-out (AC-D2 node: band code + `stage`); `ActorVia::AiControl`; no admin logic forked (D-065). **Marshaling = Option A:** node verbs mix positional+flag → reconstruct-argv doesn't port, so ~33 `admin_ops::*Args` derive `serde::Deserialize` (+ `#[serde(default)]` on 5 Vec fields; `ReplicaAction` too) and the arm `from_value`s into a single `cmd`-string match calling `admin_ops::*` (additive/inert to clap/CLI/`--batch`). **No separate state-file lock** — admin stores are `Arc<Mutex>`, same Arcs to both servers → serialize on the stores' mutexes. **§7.1:** surface = `admin_ops::*` + `state`, not the 7 print-only `app::cmd_*` reads. Node `state` keeps uptime/active_connections/registered_identities, drops operator_display_name, event_subscriptions=0. Asymmetry recorded (client reconstruct-argv vs node serde — surfaces differ). **Verification:** `cargo test --workspace` **898**/0/1 (+7); build all-targets 0/0; clippy `-D warnings` clean. **All M7 v1 code shipped (C1+C2+C4); C3/C5 → M7-events arc.** No DECISIONS.md change (AC-D# arc-local). **Next-active: C6 close (doc-only).** **Entry point: CLAUDE PLAY + JOURNAL J-204 per Rule 0, then `tasks/M7_AICONTROL_IMPL.md` §8.**

⚫ **M7 `--aicontrol` checkpoint #2 RESOLVED at J-203 (2026-06-01; doc-only) — events pipe DEFERRED, M7 v1 reshaped to command-pipes-only.** The event-seam code-trace found **no in-process broadcast to tap** (both resident recv loops single-consumer/discard; the tee named-but-unbuilt in `service.rs`; no `broadcast::`/event-mpsc in the client), and the dedicated-`.events`-WS alternative (reading B) **collides with the Node's one-sender-per-identity `ClientSenders` registry** (`app.rs:1238` insert-overwrite / `:1407` remove-by-identity → a second same-identity WS breaks the AI resident's fan-out). The clean fix (`HashMap<IdentityXgid, Vec<(conn_id,Sender)>>` in `fanout.rs`/`app.rs`) is a **node mechanism change, out of adapter scope** → Joe locked **option 2 STOP (split-trigger b)**. **M7 v1 = C1 + C2 + C4 + C6** (command pipes only); **C3 (client events) + C5 (node events) + the node multi-connection enhancement defer to the M7-events arc** (named follow-on, distinct from the `--aicontrol` hardening arc). Carried findings: Q1 registry collision + required Vec-sender shape; Q2 subscription = from-now-forward live (no `SyncRequest`; history via the command pipe); Q3 gaps-visible-across-reconnect + a process-wide `event_subscriptions` registry. `state.event_subscriptions` ships honest `0` both binaries in v1. Runbook folded v1.0 → **v1.2** (reshape + the C2 `CONCURRENT_COMMAND_NOT_ALLOWED` safety-net/§8 obligation; covers a dropped v1.1 edit). Doc-only — suite unchanged at **891**/0/1. No DECISIONS.md change (AC-D# arc-local, D-069). **Next-active: C4 node command pipe** (checkpoint-free, symmetric to C2). **Entry point: CLAUDE PLAY + JOURNAL J-203 per Rule 0, then `tasks/M7_AICONTROL_IMPL.md` §6.**

⚫ **M7 `--aicontrol` C2 client command pipe SHIPPED at J-202 (2026-06-01; checkpoint #1 locked).** New `xgen-client/src/aicontrol.rs` — the client `.aicontrol` command-pipe surface, a **sister** to `batch.rs` (D-066: `--batch` untouched). Wraps the same `ops::*` the batch arm calls (envelope-out vs plain-text-discard; no business logic forked, D-065). **Dispatch reuse via reconstruct-argv** (`cmd`+JSON `args` → argv → existing `Cli` clap parser → `ClientCommand` → result-capturing match → same `ops::*`). **Per-connection handler tasks** (accept→spawn→next instance; concurrent connections), each own `Bindings` + serial in-flight guard; persistent JSONL session. Second `start_aicontrol_server` spawn at all 3 resident entry points (service/desktop/ai_service). **C2-rider (locked):** `register`/`create-space`/`create-room` run under a shared `Arc<tokio::Mutex<()>>` so concurrent connections can't lose a `xgen-client_state.json` update; reads lock-free (serial-per-connection, not global). **`state` (locked):** lifecycle (on-disk-derived, also the mandatory `instance_state`) + identity_id/display_name/home_node/version/spaces[]/is_ai + control-owned bindings/event_subscriptions(=0 until C3); dropped home_node_connected/connected_since/member_count/room_count (AC-D3c no-new-instrumentation). **Honest as-built (C6):** `CONCURRENT_COMMAND_NOT_ALLOWED` defined+wired but the sequential v1 handler makes it structurally non-firing (serial-by-construction; rejection reserved for a future pipelined handler) — flagged. **Verification:** `cargo test --workspace` **891**/0/1 (+12); build `-p xgen-client --all-targets` 0/0; clippy clean. **Size:** aicontrol.rs 759 lines (~480 impl + ~280 tests) + 32 wiring — trips split-trigger (c) on totals; judged one cohesive surface (no natural seam) → one commit. No DECISIONS.md change (AC-D# arc-local, D-069). **Next-active: Joe-lock checkpoint #2 before C3** (event-observation seam code-trace — tap an existing broadcast without new instrumentation, else STOP). **Entry point: CLAUDE PLAY + JOURNAL J-202 per Rule 0, then `tasks/M7_AICONTROL_IMPL.md` §5.**

⚫ **M7 `--aicontrol` C1 shared substrate SHIPPED at J-201 (2026-06-01; first code commit).** New `xgen-common/src/aicontrol/` directory module — pure, transport-free, **no business logic** (D-065): `envelope.rs` (AC-D2 `Command`/`Reply`/`ErrorBody` + closed `Category` set + `parse_command`→`MALFORMED_COMMAND`), `codes.rs` (AC-D3d 9-code catalogue + `ControlError`; invariant control-codes-never-`protocol`), `cmd.rs` (AC-D1 `resolve_cmd` — control verbs first, then split-on-first-space; `auth-module register` two-hyphen case proven), `bindings.rs` (§5 per-connection namespace + `$name`/`$name.field` substitution, type-preserving whole-value refs, `BINDING_NOT_FOUND`), `timeout.rs` (AC-D3a 3-tier rule + `timeout_ms` honored-as-is/floor-validated→`BAD_ARGUMENT`). **Substrate-home resolved (runbook §3 confirm-at-pickup): `xgen-common`** — lowest crate, both binaries depend on it, pieces need only serde/serde_json (no unwanted dep → the runbook's surface-condition not triggered). **D-078 finding for checkpoint #1:** the three timeout constants are NOT co-located — `PENDING_TIMEOUT_SECS`(30)/`FEDERATION_RELATIONSHIP_TIMEOUT_SECS`(180) are `pub` in xgen-core, `AUTH_MODULE_PROBE_TIMEOUT_SECS`(5) is private in xgen-node, all above xgen-common → "pinned by name" can't be literal; substrate re-states the tier values with cross-ref doc-comments, the `tier ≥ verb-internal` invariant deferred to a consuming-side drift-lock test in C2/C4. **Verification:** `cargo test --workspace` **879** passed / 0 failed / 1 ignored (+38 vs J-197's 841 = the 38 new substrate tests); `cargo build --workspace --all-targets` 0/0; clippy `--workspace --lib --tests --all-features -D warnings` clean. No DECISIONS.md change (AC-D# arc-local, D-069). **Next-active: Joe-lock checkpoint #1 before C2** (pipe/codec/dispatch-arm wiring + ratify substrate home + the D-078 timeout-constant finding). **Entry point: CLAUDE.md PLAY + JOURNAL J-201 per Rule 0, then `tasks/M7_AICONTROL_IMPL.md` §4 (checkpoint #1 + C2).**

⚫ **M7 `--aicontrol` implementation runbook SHIPPED at J-200 (2026-05-31; doc-only, no code) — `tasks/M7_AICONTROL_IMPL.md` v1.0; Clair pickup at C1.** Grounded against the live tree: both binaries already expose the seam M7 plugs into — `batch.rs::dispatch_line` calls `ops::*` and discards the structured result (with an explicit "M7 will serialise as JSONL" comment); `pipe.rs::dispatch_line`→`dispatch_admin`→`start_pipe_server` + `ActorVia::AiControl` already in the enum. M7 is a **sister dispatcher** — same `ops::*`/`admin_ops::*` calls, envelope-out instead of plain-text-discard; **no new business logic**. **Shape: 6 commits, 2 Joe-lock checkpoints, client-first build** — C1 shared substrate (JSONL codec + AC-D2 envelope + AC-D1 `cmd`-resolver + binding engine + AC-D3d catalogue + AC-D3a timeout helper; pure, proceeds directly) · **#1 before C2** (pipe/codec/dispatch-arm wiring) · C2 client command pipe + arm + `state` · **#2 before C3** (event-observation seam code-trace — tap an existing broadcast without new instrumentation, else STOP) · C3 client events pipe + subscribe + filter grammar · C4 node command pipe (symmetric) + `ActorVia` attribution · C5 node events pipe (symmetric) + Node-only `nodes` filter · C6 close (D-074 atomic). **Hard rule (D-065): adapter, not feature** — split triggers stop on any verb absent from the shared layer (AC-D5), any event-seam needing new instrumentation (#2), any >600-line commit. `--batch` untouched (D-066); reserved trio inert (AC-D4); `config-reload` stays M7-standalone. **Confirm-at-pickup (D-078):** substrate home (`xgen-common` lean vs new crate); AC-D3b raw-prefix predicate; AC-D3c keep-or-drop fields; §7.1 node read-verb names; the event seam (gates C3). Doc-only — no build/test run; suite at J-197's 841/0/1. No DECISIONS.md change (AC-D# arc-local, D-069). **Next-active: Clair C1.** **Entry point: CLAUDE.md PLAY + JOURNAL J-200 per Rule 0, then `tasks/M7_AICONTROL_IMPL.md` §1–§3 + the design doc.**

⚫ **(historical, superseded by the runbook-shipped state above) M7 `--aicontrol` design phase COMPLETE at J-199 (2026-05-31; doc-only, no code) — AC-D1–AC-D6 all locked + canonical doc-sync v1.2.** Six decisions locked one-by-one into `tasks/M7_AICONTROL_DESIGN.md` (v1.0, `AC-D#` arc-local per D-069): **AC-D1** `cmd` = space-joined CLI path (split on first space; hyphen intra-token — dissolves the `auth-module` collision; chosen over nested `{cmd,sub}` on wire-stability grounds; verb list = whatever clap exposes) · **AC-D2** flat reply/error envelope (mandatory `code`/`category`/`message`/`instance_state`; optional-by-source `stage`/`hint`/band-code; `category` closed set disambiguates the `code` namespace; `stage` = the real 6 `Stage` variants; client `ops::*` message-only via `anyhow`, lossiness accepted out-of-scope) · **AC-D3a** 3-tier timeouts pinned to the shipped constants (5/30/180 s; default ≥ internal-timeout invariant; `timeout_ms` honored-as-is + floor-validated; class-derived no-table) · **AC-D3b** subscription grammar (AND-across/OR-within; empty==omitted==no-restriction; two wildcard forms raw-prefix on `EventType::as_str()`; entitlement-is-ceiling/inert-out-of-scope; `nodes` Node-only → Client `BAD_ARGUMENT`; malformed → `BAD_ARGUMENT` pre-stream) · **AC-D3c** `state` in-process live composition, no-new-instrumentation, purely-local; confirmed/control-owned core locked, uncertain fields confirm-at-pickup · **AC-D3d** catalogue = §8 eight + new `MALFORMED_COMMAND` (9, closed v1); control-codes-never-`protocol` invariant · **AC-D4** OS-ACL pipe-auth, D-082 unchanged, **no new exposure vs the already-unauthenticated `--batch` pipe**; sister pipes inherit the legacy ACL (Windows v1); `ActorVia::AiControl` attribution; reserved trio inert (activates as a unit with a privilege model); per-connection token = first-message seam, absent==proceed · **AC-D5** defer all three client verbs (`create-dm-space`/`leave`/`members`) to a future client-feature arc, v1 = exactly the 14 `ops::*` verbs (per-verb evidence recorded) · **AC-D6** defer idempotency keys, v1 = do-it-over (coherent via serial-per-connection + lifecycle errors + `state` reconciliation); `idempotency_key` optional-field seam, routed to the `--aicontrol` hardening arc (dependency-free). **Canonical doc-synced** `docs/xgen_aicontrol_implementation.md` v1.1 → v1.2 under the locks (§7 as-built verbs, §6 deferred ⏳, §4.3 envelope, §3 grammar, §8 catalogue, §9 state, §10 timeouts, §11 config-reload → M7-standalone, §12.1 superseded / §12.2 resolved). **Two named future arcs** carry deferrals: the `--aicontrol` hardening arc (token + idempotency keys) + the client-feature arc (the three verbs); config-reload → M7-standalone. **Next-active: the runbook** `tasks/M7_AICONTROL_IMPL.md` (Chat Claude + Joe) — envelope-first/Node-dominated/**client-first build**; proposed 6-commit decomposition (shared codec+bindings → client command pipe → client events pipe → node command pipe → node events pipe → close) + 2 Joe-lock checkpoints (before C2 pipe/codec infra; before C3 the event-observation seam code-trace). Clair stood down until the runbook closes. Doc-only — no build/test run; suite at J-197's 841/0/1. **Entry point: CLAUDE.md PLAY + JOURNAL J-199 per Rule 0, then `tasks/M7_AICONTROL_DESIGN.md` + the canonical M7 doc v1.2.**

⚫ **(historical, superseded by the M7-design-complete state above) M7 `--aicontrol` Phase-0 audit SHIPPED at J-198 (2026-05-31; doc-only, no code).** M7 wraps the now-shipped `admin_ops::*` (xgen-node) + `ops::*` (xgen-client) in an AI-shape JSONL protocol; the canonical doc `docs/xgen_aicontrol_implementation.md` (v1.1) predates M6 shipping, so this was **drift-reconciliation + §12 triage**, not does-it-exist (`tasks/M7_AICONTROL_AUDIT.md` v1.0). **Findings:** (1) §12.1 "M6 deliverables" effectively CLOSED by M6 (verbs enumerated, privilege model = OS-user-equals-admin/D-082, audit integration done) — mark superseded; live-reload/config-reload route to **M7-standalone**, not `--aicontrol` core (the doc's §11 is drift). (2) **Node verb drift systematic** — shipped two-token category-grouped (`federation accept`, `space set-node-policy`); §7 sketches a flat hyphenated namespace (`federate-accept`) with stale names + wrong counts ⇒ the load-bearing first design lock is the **`cmd` verb-exposure model**. (3) Client drift — three §6 verbs (`create-dm-space`/`members`/`leave`) have no `ops::*` backing; lift or defer. (4) Forward-readiness in code — `ActorVia::AiControl` exists, three-dispatcher pattern real, `AdminError{code,stage}` + bands shipped; M7 reuses. (5) Genuinely open = §12.2 (timeouts, subscription grammar, `state` schema, error catalogue, pipe-auth, replay) + verb-name model + envelope-nests-`AdminError` + client gap + config-reload boundary. **Ordering: envelope-first, Node-constraint-dominated, client-first build.** **Next-active: open `tasks/M7_AICONTROL_DESIGN.md`** (Joe-reserved) — lock order: verb-name model → reply/error envelope → §12.2 mechanicals → pipe-auth → client ops gap → replay (likely defer); `AC-D#` arc-local. Clair stood down. Doc-only — no build/test run; suite at J-197's 841/0/1. **Entry point: CLAUDE.md PLAY + JOURNAL J-198 per Rule 0, then `tasks/M7_AICONTROL_AUDIT.md` + the canonical M7 doc.**

⚫ **(historical, superseded by the M7-Phase-0-audit state above) node-policy arc CLOSED at J-197 (2026-05-31) — C1 (store + both verbs + threading + tests) + C2 (doc-only close); the fifth/final M6 deferral shipped.** No Joe-lock checkpoint (Fork X = no enforcement seam to code-trace). **C1:** xgen-core NEW `space/node_policy.rs` (`NodePolicy { auto_moderation: bool, action_threshold: Option<f64> }`, `Default={false,None}`; `NodePolicyStore { policies: HashMap<SpaceXgid, NodePolicy> }`, `FederationPolicyStore` sibling, reuses `crate::federation::registry::RegistryError`; absent==disabled in the verb/`Default`, NP-D2) + xgen-node `admin_ops` `node_set_policy` (WRITE, audited; `SPACE_8005` for `action_threshold ∉ [0.0,1.0]` pre-audit; hosted-here else `SPACE_8001` via a shared `require_hosted_space` helper) + `node_show_policy` (READ; stored or `default()` + `is_default`) + `AdminContext` threading (`node_policy_store` + builder/require/`node_policy_store_path()` = `xgen-node_node_policy.json`) + clap `SpaceCommand::{SetNodePolicy, ShowNodePolicy}` + `run_node → start_pipe_server → dispatch_line → dispatch_admin` + 2 pipe arms; store loaded inside the `#[cfg(windows)]` pipe block (Fork X, sole consumer = verbs). Four confirm-at-pickup (D-078) all resolved against the live tree. Prime invariant held (empty store + `show` → `{false,None}`, touches nothing). **C2:** §6.A4 SHIPPED banners + honest as-built deltas (2-field schema vs Block-4 sketch; `SPACE_8005` superseding the guessed `SPACE_8020`; Fork X stored-inert; propagation=none); backing-audit A4 → SHIPPED + **all M6 deferrals closed**; audit/design/runbook → COMPLETED. Verification: `cargo test --workspace` **841** / 0 failed / 1 ignored (+10 vs J-195's 831 = 6 core store + 4 node verb tests); build all-targets 0/0; clippy `-D warnings` clean. No DECISIONS.md change (NP-D# arc-local, D-069). **Every M6 admin verb across all seven categories has now shipped. Next-active: M7 `--aicontrol`** (reuses `admin_ops::*`). **Entry point: CLAUDE.md PLAY + JOURNAL J-197 per Rule 0.**

⚫ **(historical, superseded by the node-policy-arc-CLOSED state above) node-policy arc OPENED at J-196 (2026-05-31) — audit + design (NP-D1–D6 LOCKED) + impl runbook authored; doc-only, no code.** The fifth/final M6 deferral (`space set-node-policy` / `space show-node-policy`). **Audit** (`tasks/M6_NODE_POLICY_AUDIT.md` v1.0): node-policy has **no protocol-spec backing** — intentionally unspecified Node-operator territory (ch3:2404; "protocol provides mechanism, communities supply policy"); the §6.A4 "Spec refs §2.6.4" pointed at the admin-ops design's *own* numbering; store ABSENT, sibling = `FederationPolicyStore`; real moderation primitives (`active_mutes`/pacing) are Space-DAG governance not Node config. **Design** (`tasks/M6_NODE_POLICY_DESIGN.md` v1.0, NP-D1–D6 all LOCKED): NP-D1 operator-only authority distinct from owner (#3) + AI-operator (#2), non-propagating · NP-D2 smallest schema `NodePolicy { auto_moderation: bool, action_threshold: Option<f64> }`, absent==disabled, `Default={false,None}`=today (fills the 3.7.13.6 actionable-threshold gap) · NP-D3 **Fork X** — inert store, enforcement deferred to the temperature-plugin arc (A7 trait still no-op; store-before-consumer per A2/A3) · NP-D4 per-Space only, `HashMap<SpaceXgid, NodePolicy>`, no Node-wide default · NP-D5 two-modes-of-one-Node-admin-authority (`force-eject`=intervention/propagates; node-policy=standing posture/local) · NP-D6 codes reuse `SPACE_8001` + new `SPACE_8005` (drops guessed `SPACE_8020`). **Runbook** (`tasks/M6_NODE_POLICY_IMPL.md` v1.0): **2 commits, NO Joe-lock checkpoint** (the locks fully pin the data layer; Fork X = no enforcement seam to code-trace). C1 = NEW `node_policy.rs` store + both verbs + `AdminContext` threading + `SPACE_8005` + prime-invariant regression; C2 = doc-only close → PLAY flips to M7. Four confirm-at-pickup items (D-078): store location, `RegistryError` path, hosted-here accessor, clap arg style. Doc-only session — no build/test run; suite stands at J-195's **831**/0/1. No DECISIONS.md change (NP-D# arc-local, D-069). **Next-active: Clair C1** (no checkpoint gates C1). **Entry point: CLAUDE.md PLAY + JOURNAL J-196 per Rule 0, then `tasks/M6_NODE_POLICY_IMPL.md` C1 + the design doc.**

⚫ **(historical, superseded by the node-policy-arc-opened state above) bootstrap-client (A3) arc CLOSED at J-195 (2026-05-31); A3 5/5 ✅; all four D-071 verb arcs shipped.** Commit 5 (doc-only). The arc shipped end-to-end across 5 commits: design+checkpoint-#1 (J-190) · C1 config seed + combined store (J-191) · C2 `bootstrap/signing.rs` + `bootstrap_client.rs` framed send-path, checkpoint #2 CLOSED (J-192) · C3 the 5 verbs + `BOOT_71xx` + threading + prime-invariant regression (J-193) · C4 `bootstrap_keepalive.rs` scheduler + best-effort re-advertise (J-194) · C5 close (J-195). Both Joe-lock checkpoints fired + closed; three BC locks in code (BC-D1 sibling JSON store · BC-D2 config-seeds-store-is-truth · BC-D3 framed-not-HTTP). Prime invariant held (empty store = registered with nobody = today byte-for-byte). Commit 5 docs: admin-ops design §6.A3 SHIPPED banner all-5 + honest as-built deltas (`BOOT_71xx` superseding the guessed `BOOT_70xx`, framed-not-HTTP, `--url/--pubkey`, `self_info` result, `set-tiers` local-only) + §5.1/category row ✅; `tasks/M6_BACKING_AUDIT.md` v1.6 (A3 → SHIPPED ✅, arc CLOSED); audit/design/runbook → COMPLETED. Verification (doc-only): `cargo test --workspace` **831** / 0 failed / 1 ignored (unchanged); clippy clean. No DECISIONS.md change (BC-D# arc-local, D-069). **With this, all four D-071 verb arcs are shipped** (federation-admin-control · protocol-audit-log · auth-module-registry · bootstrap-client). **Next-active: node-policy** — the fifth/final M6 deferral (2 verbs: `space set-node-policy` / `space show-node-policy`; absent `NodePolicy` store + enforcement), its audit→design→impl arc opens next (it was never formally audited — only flagged in the A4-D1 force-eject session). After node-policy, M7 `--aicontrol` (reuses `admin_ops::*`). **Entry point: CLAUDE.md PLAY + JOURNAL J-195 per Rule 0, then the node-policy backing audit.**

🟢 **bootstrap-client (A3) — C4 keepalive scheduler + re-advertise SHIPPED at J-194 (2026-05-31).** Closes the two C3 scope-deferrals (no checkpoint gates C4). Scheduler (Pin 3 — separate `bootstrap_keepalive.rs`, reconnect.rs shape): `spawn_*` → `bootstrap_keepalive_tick` (extracted for tests) → per-due detached `attempt_keepalive` (drives the C2 `keepalive_bootstrap`; refreshes the ~now+7d TTL on the verified ack; failure → retry next tick). 60 s tick; due = expiry absent/malformed/within the 24 h lead of the 7-day TTL (WD-25). Spawned unconditionally (empty/nothing-due = no-op = prime invariant held); reads the shared store Arc → runtime-added registrations picked up without restart. Re-advertise (A3-D2): `readvertise_all` re-registers updated self-info with every held bootstrap node (`register`, not keepalive); `bootstrap set-info` calls it after its local write + audit when a live runtime is present (best-effort — fan-out failure never fails the verb). `set-tiers` has no re-advertise (Checkpoint #1(d)). run_node store load moved to top-level (shared Arc). Verification: `cargo test --workspace` **831** / 0 failed / 1 ignored (+8 vs J-193's 823 — 5 due-logic + 3 integration tests); build all-targets 0/0; clippy `-D warnings` clean. **Next-active: C5 doc-only close.** **Entry point: CLAUDE.md PLAY + JOURNAL J-194 per Rule 0, then `tasks/M6_BOOTSTRAP_CLIENT_IMPL.md` C5.**

🟢 **bootstrap-client (A3) — C3 the 5 verbs SHIPPED at J-193 (2026-05-31).** The consumer commit — the 5 `bootstrap` verbs wired into the admin surface (no checkpoint gates C3). Verbs in `admin_ops`: `show` (READ) · `register --url --pubkey` (WRITE, audited — drives the C2 send-path, then stores the returned registration) · `deregister <id>` (DESTRUCTIVE, audited — unknown id → BOOT_7101 before any network; send-then-remove-on-success) · `set-info` (WRITE, audited — local-write-first per A3-D2; re-advertise = C4) · `set-tiers` (WRITE, audited — **local self-info only**, Checkpoint #1(d) Option A). New `BOOT_71xx` admin block (7101 unknown · 7102 invalid-pubkey · 7103 invalid-tier · 7110 exchange-failed · 7111 ack-verify-failed). `AdminContext` gained `bootstrap_store` + threading (`run_node → start_pipe_server → dispatch_line → dispatch_admin` + clap `AdminCommand::Bootstrap` + 5 pipe arms); store loaded inside the `#[cfg(windows)]` pipe block (verbs are the sole consumer). Scope-honest deferrals (D-065): seed auto-register + keepalive scheduler + `set-info` re-advertise → C4. Prime invariant (the mandatory C3 regression): no `[bootstrap]` + empty store = registered with nobody = today byte-for-byte (`empty_store_registers_with_nobody`). Verification: `cargo test --workspace` **823** / 0 failed / 1 ignored (+5 vs J-192's 818 — 5 verb tests; network happy-path delegates to the C2-tested send-path); build all-targets 0/0; clippy `-D warnings` clean. **Next-active: C4** — keepalive scheduler (separate `bootstrap_keepalive.rs`) + best-effort `set-info` re-advertise. **Entry point: CLAUDE.md PLAY + JOURNAL J-193 per Rule 0, then `tasks/M6_BOOTSTRAP_CLIENT_IMPL.md` C4.**

🟢 **bootstrap-client (A3) — C2 framed send-path SHIPPED at J-192 (2026-05-31); checkpoint #2 CLOSED.** The load-bearing commit — the framed `BootstrapMessage` send-path over the normal transport (BC-D3, **NOT** HTTP). Checkpoint #2 pins, all code+spec-traced: Pin 1 → reuse `Connection` primitives, **no transport auth** (spec §3.14.3 verifies the message signature; no bootstrap transport handshake) · Pin 2 → verify acks against the operator-supplied / stored `bootstrap_id` (assert ack `node_id` matches + verify sig against the key derived from the expected id; not the ack's self-declared id) · Pin 3 → separate `bootstrap_keepalive.rs` (C4), reconnect.rs shape; C2's `keepalive_bootstrap` returns the refreshed TTL the C4 scheduler stores. Shipped: xgen-core `bootstrap/signing.rs` (NEW, pure — sibling of `handshake::sign_msg`/`verify_msg`: `sign_bootstrap` + `verify_bootstrap_signed` + `BootstrapSignError`) + xgen-node `bootstrap_client.rs` (NEW — `register_with_bootstrap`/`keepalive_bootstrap`/`deregister_from_bootstrap` reusing `connect_url`+`send_bootstrap`+`recv`; pure builder seams; 15 s ack timeout). No verb wiring (verbs = C3); exercised by tests. Verification: `cargo test --workspace` **818** / 0 failed / 1 ignored (+12 vs J-191's 806 — 6 signing tests + 2 builder + 4 integration tests against an in-process stub responder); build all-targets 0/0; clippy `-D warnings` clean. Prime invariant untouched (no `run_node` consumer; the mandatory regression lands at C3). **Next-active: C3** — the 5 verbs + `AdminContext` threading + the prime-invariant regression. **Entry point: CLAUDE.md PLAY + JOURNAL J-192 per Rule 0, then `tasks/M6_BOOTSTRAP_CLIENT_IMPL.md` C3.**

🟢 **bootstrap-client (A3) — design LOCKED (J-190) + C1 data layer SHIPPED at J-191 (2026-05-31).** Third of the four post-M6 D-071 arcs (A1 + A2 closed). Design (J-190): BC-D1 (runtime state in a **sibling JSON store** `xgen-node_bootstrap.json`, config seeds-only) · BC-D2 (config seeds, store is truth) · BC-D3 (register/keepalive/deregister are **framed `BootstrapMessage` exchanges over the normal transport, NOT HTTP** — the only HTTP in bootstrap is the directory-fetch, which is OUT of A3) · A3-D1 client-only · A3-D2 best-effort re-advertise. Checkpoint #1 CLOSED: names (`BootstrapRegistration`/`BootstrapRegistrationStore`/`BootstrapSelfInfo`), ONE combined file, `register --url/--pubkey`, `set-tiers` Option A (local self-info only — wire carries no tiers). **C1 (J-191, data layer, no wiring):** NEW `xgen-core/src/bootstrap/registration_store.rs` (sibling to `auth/module_registry.rs`) — `BootstrapRegistration { bootstrap_id: NodeXgid, url, directory_url, registered_at, expires_at }` + `BootstrapSelfInfo { endpoint, region, capabilities, auth_tiers_served }` + `BootstrapRegistrationStore` (registrations map keyed by `bootstrap_id` + self-info; `add`/`remove`/`get`/`get_mut`/`all`/`len`/`is_empty`/`set_info`/`set_tiers`/`self_info`/`save`/`load`, reuses `RegistryError`); `[bootstrap]` `BootstrapSection` on `NodeConfig` (`#[serde(default)]`). No run_node wiring (first consumer = C3). Prime invariant trivially held — no consumer this commit. Verification: `cargo test --workspace` **806** / 0 failed / 1 ignored (+13 vs J-189's 793 — 10 store tests in xgen-core + 3 `[bootstrap]` config tests in xgen-node); build all-targets 0/0; clippy `-D warnings` clean. **Next-active: C2** — the framed send-path (the load-bearing commit), checkpoint #2 first. **Entry point: CLAUDE.md PLAY + JOURNAL J-191 per Rule 0, then `tasks/M6_BOOTSTRAP_CLIENT_IMPL.md` C2.**

🟢 **auth-module-registry (A2) arc CLOSED at J-189 (2026-05-31).** Commit 5 (doc-only). The arc shipped end-to-end across 5 commits — **C1** `AuthModuleXgid` 7th flavour + Appendix J §J.2 six→seven + **D-083** (J-185) · **C2** `AuthModuleRecord` + `AuthModuleRegistry` store (J-186) · **C3** `list`/`register`/`revoke`/`set-tiers` + `AdminContext` threading (J-187) · **C4** `auth-module test` connectivity probe, checkpoint #2 (J-188) · **C5** close (J-189) — so **A2 is fully ✅ (5/5)**. Both Joe-lock checkpoints fired + closed (#1 data-layer names + `--pubkey`; #2 connectivity-only honest-thin probe). Three AMR locks in code: AMR-D1 standalone (store + verbs, no runtime consumer — registration/3006 consultation is a deferred future arc) · AMR-D2 (`AuthModuleXgid` → D-083) · AMR-D3 (derive-don't-store the key). Prime invariant held trivially — no consumer reads the registry this arc. Commit 5 docs: admin-ops design v1.19 (§6.A2 all-5 SHIPPED + honest as-built deltas — `AUTHMOD_61xx` superseding the guessed `AUTH_20xx`, derived-key record shape, connectivity-only `test`), backing-audit v1.5 (A2 → SHIPPED ✅, arc CLOSED), audit (COMPLETED v1.2) + design (COMPLETED v1.1) + runbook (COMPLETED v1.3). Verification (doc-only): `cargo test --workspace` **793** / 0 failed / 1 ignored (unchanged); clippy clean; D-083 present. No DECISIONS.md change at this close (D-083 landed at C1). **Next-active: the bootstrap-client (A3) D-071 arc (design phase).** **Entry point: CLAUDE.md PLAY + JOURNAL J-189 per Rule 0, then `tasks/M6_BOOTSTRAP_CLIENT_AUDIT.md` (the A3 reality map — bootstrap client send-path absent; `bootstrap/client.rs` is a placeholder) + `tasks/M6_BOOTSTRAP_CLIENT_DESIGN.md` (PENDING → ACTIVE).**

⚫ **(historical, superseded by the arc-CLOSED state above) auth-module-registry (A2) — Commit 4 SHIPPED at J-188 (2026-05-31).** The `auth-module test` ad-hoc probe (A2-D2); Joe-lock **checkpoint #2 LOCKED first** (runbook v1.2, `3e74828`): connectivity-only, honest-thin (no Auth Module wire protocol is specced yet, so don't invent a challenge/response inside a verb — AMR-D1 boundary; sibling to A7 honest-thin). `admin_ops::auth_module_test` (READ, not audited) → unknown module reuses `AUTHMOD_6101`; TCP-connects to `endpoint_url` with a 5 s fail-fast timeout (`AUTH_MODULE_PROBE_TIMEOUT_SECS`, a fresh named constant — not the 30 s/180 s federation timeouts). Result = `reachable` + `response_time_ms` + `reason` + **stored** `accepted_tiers` (display-only). Unreachable = result (`reachable: false` + reason), not an error. `AuthModuleCommand::Test` clap + pipe arm. Verification: `cargo test --workspace` **793** / 0 failed / 1 ignored (+4 vs J-187's 789 — xgen-node lib 164→**168**); build all-targets 0 errors/0 warnings; clippy `-D warnings` clean. Prime invariant (AMR-D1) held. **Next-active: Clair Commit 5** — doc-only close (§6.A2 all-5 SHIPPED; backing-audit A2 → SHIPPED + arc CLOSED; audit/design/runbook → COMPLETED; CLAUDE PLAY → bootstrap-client; A2 row ✅). **Entry point: CLAUDE.md PLAY + JOURNAL J-188 per Rule 0, then `tasks/M6_AUTH_MODULE_REGISTRY_IMPL.md` Commit 5.**

⚫ **(historical, superseded by the Commit-4-shipped state above) auth-module-registry (A2) — Commit 3 SHIPPED at J-187 (2026-05-31).** First *consumer* commit — the 4 CRUD verbs + wiring (sibling to federation-policy 2b Commit 3, J-182; no checkpoint this commit). `admin_ops` verbs: `auth_module_list` (READ) · `auth_module_register` (WRITE/audited; `--pubkey`/`--endpoint`/`--tier`, derives `module_id` via `from_pubkey`) · `auth_module_revoke` (DESTRUCTIVE/audited; block-only-retains, A2-D1) · `auth_module_set_tiers` (WRITE/audited). `--pubkey` parse reuses canonical `crypto::encoding::decode` + `VerifyingKey::from_bytes` (no drift, D-067). New admin error codes **`AUTHMOD_61xx`** (`6101` unknown-module · `6102` invalid-pubkey · `6103` invalid-tier; distinct from the deferred enforcement `AuthModuleUntrusted`/3006). `AdminContext` gained the live `auth_module_registry` Arc + builder/require/path (`xgen-node_auth_modules.json`); threaded `dispatch_line → dispatch_admin (4 arms) → start_pipe_server`; `run_node` loads it inside the `#[cfg(windows)]` pipe block (sole consumer this arc). Verification: `cargo test --workspace` **789** / 0 failed / 1 ignored (+4 vs J-186's 785 — xgen-node lib 160→**164**); build all-targets 0 errors/0 warnings; clippy `-D warnings` clean. Prime invariant (AMR-D1) held — operator-CRUD only, no enforcement consumer. **Next-active: Clair Commit 4** — `auth-module test` ad-hoc probe (READ); **checkpoint #2 fires first** (probe message shape / timeout / "reachable" / reported-tiers [lean report-only] / error-vs-result split). **Entry point: CLAUDE.md PLAY + JOURNAL J-187 per Rule 0, then `tasks/M6_AUTH_MODULE_REGISTRY_IMPL.md` Commit 4.**

⚫ **(historical, superseded by the Commit-3-shipped state above) auth-module-registry (A2) — Commit 2 SHIPPED at J-186 (2026-05-31).** Pure data-layer commit (record + store, no wiring); checkpoint #1 already LOCKED at J-185, so proceeded directly (sibling to the federation-policy 2b Commit 1, J-180). NEW `xgen-core/src/auth/module_registry.rs` (sibling to `federation/federation_policy.rs`; declared in `auth/mod.rs`): **`AuthModuleRecord`** (checkpoint-#1 field set — `module_id: AuthModuleXgid`, `endpoint_url`, `accepted_tiers: Vec<AuthTier>`, `registered_at: String`, `revoked`/`revoked_at`; no `public_key` — key via `module_id.pubkey()`, AMR-D3; RFC 3339 `String` timestamps, `pending_queue.rs` sibling — D-078 pickup confirmed) + **`AuthModuleRegistry`** (`new`/`register` insert-or-replace/`revoke` block-only-retains A2-D1/`set_tiers`/`get`/`all`/`len`/`is_empty`/`save`/`load`; reuses `RegistryError`). No run_node wiring (first consumer = C3; `xgen-node_auth_modules.json` path wired at C3). Verification: `cargo test --workspace` **785** / 0 failed / 1 ignored (+8 vs J-185's 777 — xgen-core lib 493→**501**, 8 module_registry tests); build all-targets 0 errors/0 warnings; clippy `-D warnings` clean. Prime invariant (AMR-D1) trivially held. **Next-active: Clair Commit 3** — CRUD verbs (`auth-module list`/`register`/`revoke`/`set-tiers`) in `xgen-node::admin_ops` + `AuthModuleCommand` clap/pipe arms + `AdminContext` live-Arc threading + new admin error codes; `register` takes `--pubkey`. **Entry point: CLAUDE.md PLAY + JOURNAL J-186 per Rule 0, then `tasks/M6_AUTH_MODULE_REGISTRY_IMPL.md` Commit 3.**

⚫ **(historical, superseded by the Commit-2-shipped state above) auth-module-registry (A2) — Commit 1 SHIPPED at J-185 (2026-05-31).** Second D-071 arc; design LOCKED at J-184 (AMR-D1/D2/D3), checkpoint #1 LOCKED (`642b3e6`). Commit 1 is the protocol-identity-model prerequisite — the only commit in the 5-commit arc touching the XGID flavour family + DECISIONS.md. **NEW seventh XGID flavour `AuthModuleXgid`** (third principal flavour) in `xgen-common/src/xgid/flavours.rs` — `declare_flavour!` + `from_pubkey`/`pubkey` reusing the `principal_uri`/`principal_decode` path (sibling to `NodeXgid`/`IdentityXgid`); no new URI prefix / no new wire shape (reuses `xgen://pubkey/ed25519:<key>`; inherits §J.5 invariances + D-081 by construction); re-exported from `xgid/mod.rs` + crate root. **Appendix J §J.2 six → seven** (v1.0 → v1.1; Principal-family table row, "why seven" reframed, J.2-derived counts swept). **DECISIONS.md D-083** — the first promotion to clear the D-072 / §J.2 "explicit promotion required" barrier; records the principal-not-hash distinction (design-walk D-078 catch) + AMR-D2/AMR-D3 graduation. Verification: `cargo test --workspace` **777** / 0 failed / 1 ignored (+1 vs J-184's 776 — xgen-common lib 35→**36**, `auth_module_xgid_from_pubkey_roundtrip`); build all-targets 0 errors/0 warnings; clippy `-D warnings` clean. Prime invariant (AMR-D1) trivially held — no registry consumer this commit. **Next-active: Clair Commit 2** — `AuthModuleRecord` + `AuthModuleRegistry` store in NEW `xgen-core/src/auth/module_registry.rs` (sibling to `federation/federation_policy.rs`), per checkpoint #1's pinned field set + store API + path (`xgen-node_auth_modules.json`); no wiring (first consumer = C3). **Entry point: CLAUDE.md PLAY + JOURNAL J-185 per Rule 0, then `tasks/M6_AUTH_MODULE_REGISTRY_IMPL.md` Commit 2.**

⚫ **(historical, superseded by the Commit-1-shipped state above) federation-admin-control arc CLOSED end-to-end at J-183 (2026-05-30).** The 2b (policy) sub-arc closed (Commit 4, doc-only), and with 2a (J-178) the whole arc is CLOSED — **A1 is 7/7 ✅**. 2b shipped across 4 commits: `FederationPolicy`/`PolicyMode` + sibling store (J-180) → LOAD-BEARING `policy_permits` enforcement at both sites, checkpoint #2 → Option B (J-181) → `set-policy`/`show-policy` verbs + live `AdminContext` threading (J-182) → close (J-183). Both Joe-lock checkpoints fired + closed (the #2 inbound-site trace included the honest call-site-count catch that flipped the lock A→B). Prime invariant held: absent policy = permit-all = today byte-for-byte. Commit 4 docs: admin-ops design v1.18 (§6.A1 all-7 SHIPPED + honest 2b as-built deltas), backing-audit v1.4 (A1 → SHIPPED ✅, arc CLOSED), 2b design (COMPLETED v1.1) + runbook (COMPLETED v1.3). Verification (doc-only): `cargo test --workspace` **776** / 0 failed / 1 ignored (unchanged); isolated default-permit regression ×2 green; clippy clean. No DECISIONS.md change (arc-local FAC-D# per D-069). **Next-active: the auth-module-registry D-071 arc (design phase).** **Entry point: CLAUDE.md PLAY + JOURNAL J-183 per Rule 0, then `tasks/M6_AUTH_MODULE_REGISTRY_AUDIT.md` (A2 fully ABSENT) + `tasks/M6_AUTH_MODULE_REGISTRY_DESIGN.md` (PENDING → ACTIVE).**

⚫ **(historical, superseded by the Commit-3-shipped state above) federation-admin-control 2b (policy) — Commit 2 (LOAD-BEARING `policy_permits` enforcement) SHIPPED at J-181 (2026-05-30).** The FAC-D3 enforcement layer; no policy stored = today byte-for-byte (prime invariant). **Checkpoint #2 closed (the D-078 beat):** inbound site code-traced = the F-3 gate in `dispatch_event`; the real fork was *how the store reaches it*. **Honest catch** — Option A (consult-in-`dispatch_event`) was first framed as "~6 call-site updates" but is **~6 production + ~79 test** (~85 callers); surfaced + **Joe locked Option B** = consult node-side in `process_inbound` (`dispatch_event` untouched) + a shared `space_id_of` resolver (net D-067 improvement). Shipped: pure `policy_permits(Option<&FederationPolicy>, &SpaceXgid)` (xgen-core, truth-tabled) + `space_id_of(&Event)` resolver; **outbound** consult in `apply_federation_push` (new `Option<&Arc<…>>` param; `Deny`/excluded-space → `federation_push_skipped_policy`); **inbound** consult in `process_inbound` (drops pre-dispatch, relationship intact → `federation_policy_denied_inbound`); policy store loaded at `run_node` from `xgen-node_federation_policy.json` + threaded (sibling to the 2a queue); force-eject push passes `None`; `federation_initiate` loads policy from disk (live AdminContext threading → Commit 3). `cargo test --workspace` **775** / 0 failed / 1 ignored (+9; xgen-core 489→**493**, xgen-node 154→**159**); clippy `-D warnings` clean; build all-targets 0 errors/0 warnings. Inbound test deterministic by channel-ordering (two-space `allowed_spaces` exclusion); trace-capture abandoned (worker-thread spawned-task traces uncaptured — D-065 data point). Default-permit regression: whole `federate()` suite green with empty stores + explicit `outbound_no_policy_delivers`. No DECISIONS.md change (arc-local FAC-D# per D-069). **Next-active: Clair Commit 3** — `set-policy`/`show-policy` verbs + clap/pipe arms + thread the live policy store into `AdminContext`. **Entry point: CLAUDE.md PLAY + JOURNAL J-181 per Rule 0, then `tasks/M6_FEDERATION_POLICY_IMPL.md` Commit 3.**

⚫ **(historical, superseded by the Commit-2-shipped state above) federation-admin-control 2b (policy) — Commit 1 (`FederationPolicy` sibling store + `PolicyMode` types) SHIPPED at J-180 (2026-05-30).** Pure data-layer commit; checkpoint #1 already LOCKED at J-179, so proceeded directly. NEW `xgen-core/src/federation/federation_policy.rs` (sibling to `pending_queue.rs`): `PolicyMode { Allow, Deny }`; `FederationPolicy { mode, allowed_spaces: Option<Vec<SpaceXgid>> }` (Default → permit-all; `allowed_spaces` restrictive-only; `rate_limit` DEFERRED); `FederationPolicyStore { policies: HashMap<NodeXgid,_> }`. Default-absent (no policy → permit) lives in the Commit-2 helper, not the store; no run_node wiring this commit. `cargo test --workspace` **766** / 0 failed / 1 ignored (+6; xgen-core 483→489); clippy clean; build all-targets 0 errors. No DECISIONS.md change (arc-local FAC-D# per D-069).

⚫ **(historical, superseded by the Commit-1-shipped state above) federation-admin-control 2b (policy) — design FAC-D3/D4 LOCKED + runbook ACTIVE at J-179 (2026-05-30).** Second half of the second D-071 arc; opens on 2a's `FederationState` + registry. Both design decisions Joe-locked; design doc → ACTIVE v1.0, runbook authored ACTIVE v1.0 (folded into one beat — no intervening Joe-lock, unlike 2a's J-171→J-173 split). **FAC-D4 (shape):** minimal `FederationPolicy { mode: Allow|Deny, allowed_spaces: Option<Vec> }`; `rate_limit` DEFERRED (couples to unbuilt load-measurement); **sibling store** (allows pre-deny); `allowed_spaces` **restrictive-only** (`shared_spaces ∩ allowed_spaces`, narrows never widens). **FAC-D3 (sites):** **both** — outbound `apply_federation_push` + inbound ingest gate — via one pure `policy_permits(peer, space_id)`, **default-permit** (= today byte-for-byte; sibling to `require_approval=false`). Push-path-only rejected (a Deny blocking only outbound is operator-surprising; `defederate`/`reject` cover stop-everything). **Load-bearing unknown = the inbound enforcement site, deliberately NOT named** (D-078) → code-traced at Commit-2 checkpoint #2. Runbook = 4 Clair commits (store → enforcement → verbs → close), 2 checkpoints. `cargo test --workspace` **760** (unchanged — doc-only). No DECISIONS.md change (arc-local FAC-D# per D-069). **Next-active: Clair Commit 1** (the `FederationPolicy` sibling store + types) — **checkpoint #1 LOCKED 2026-05-30** (type/store/path names pinned against the 2a precedents → runbook v1.1; `allowed_spaces: Option<Vec<SpaceXgid>>`, store inner `policies: HashMap<NodeXgid,_>`, `xgen-node_federation_policy.json`), so Clair proceeds directly. **Entry point: CLAUDE.md PLAY + JOURNAL J-179 per Rule 0, then `tasks/M6_FEDERATION_POLICY_IMPL.md` + the 2b design doc.**

⚫ **(historical, superseded by the 2b-open state above) federation-admin-control 2a sub-arc CLOSED at J-178 (2026-05-30).** Shipped end-to-end across 5 commits (state+migration J-174 · flag+queue J-175 · LOAD-BEARING pause-point J-176 · accept/reject/initiate verbs J-177 · close J-178). All 3 Joe-lock checkpoints fired + closed; prime default-off invariant held (auto-establish byte-for-byte when `require_approval = false`); 3 D-078 finds surfaced + resolved not guessed (net-new config section; gate node-side since `run_receiving` is tests-only; known-peer `initiate`). `accept`/`reject`/`initiate` SHIPPED in §6.A1 (with honest as-built deltas) + backing-audit A1 row; runbook + 2a design → COMPLETED. `cargo test --workspace` **760** / 0 failed / 1 ignored (+22 across the arc). No DECISIONS.md change (arc-local FAC-D# per D-069). **Next-active: sub-arc 2b (federation policy) — design phase** (`tasks/M6_FEDERATION_POLICY_DESIGN.md` PENDING → ACTIVE; FAC-D3/D4 = the policy store + enforcement for `set-policy`/`show-policy`). **Entry point: CLAUDE.md PLAY + JOURNAL J-178 per Rule 0, then the 2a audit (policy half) + the 2b design stub.**

⚫ **(historical, superseded by the 2a-CLOSED state above) federation-admin-control 2a — Commit 4 (`accept`/`reject`/`initiate` verbs) SHIPPED at J-177 (2026-05-30).** Checkpoint #3 locked: reject tombstone = permanent + **suppresses re-enqueue** (the gate is now 3-way `approval_gate_decision` → Proceed/Enqueue/RefuseWithoutEnqueue; Rejected/Revoked → Reject 2003 without re-queuing); `initiate` ungated + clears the tombstone on success. **D-078:** the runbook's "initiate calls run_initiating (today's path)" doesn't cleanly exist — Joe-locked **known-peer initiate** (reuse `attempt_reconnect` with stored peer_url/shared_spaces; fresh-URL bootstrap → bootstrap-client arc). 3 verbs in `admin_ops` + clap + pipe arms; `AdminContext` gained the live `federation_queue` (threaded run_node→pipe so accept/reject share the gate's queue); `federation list --state` reconciled to the real FAC-D2 field; new codes FED_3005/3006/3007. Verification: `cargo test --workspace` **760** / 0 failed / 1 ignored (+5: core 482→483, node 150→154 — accept/reject/initiate-errors + Rejected-suppression integration); clippy `-D warnings` clean; build all-targets 0 errors. **Next-active: Clair Commit 5** (close — §6.A1 SHIPPED, backing-audit row, runbook COMPLETED, PLAY → 2b) per runbook §7. **Entry point: CLAUDE.md PLAY + JOURNAL J-177 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` §7.**

⚫ **(historical, superseded by the Commit-4-shipped state above) federation-admin-control 2a — Commit 3 (LOAD-BEARING approval pause-point, FAC-D1a) SHIPPED at J-176 (2026-05-30).** Checkpoint #2 locked: reject code **2003** (`approval_pending`); pause condition `require_approval && state != Active` (Rejected → re-enqueue, option 1); gate location resolved by code-trace = **node-side in `handle_federation_incoming`** (xgen-core `run_receiving` is tests-only → untouched; no `HandshakeError::ApprovalPending` needed). Decision is a pure helper `pending_queue::should_queue_for_approval`; gate wired after negotiation (before caps) → enqueue + signed `Reject 2003` + return. run_node queue wiring (deferred from Commit 2) landed. Verification: `cargo test --workspace` **755** / 0 failed / 1 ignored (+4: 3 truth-table units incl. the explicit default-off proof [xgen-core 479→482] + 1 integration driving a real require_approval=true receiver [xgen-node 149→150], stable ×3); clippy `-D warnings` clean; build all-targets 0 errors. Default-off regression = unit `gate_off_never_queues` + the existing `federate()` suite staying green. **Next-active: Clair Commit 4** (`accept`/`reject`/`initiate` verbs) per runbook §6, after Joe-lock checkpoint #3. **Entry point: CLAUDE.md PLAY + JOURNAL J-176 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` §6.**

⚫ **(historical, superseded by the Commit-3-shipped state above) federation-admin-control 2a — Commit 2 (require_approval flag + pending-request queue store) SHIPPED at J-175 (2026-05-30).** NEW `[federation]` config section — `FederationSection { require_approval: bool }` (derive-`Default` → false), added `#[serde(default)]` to `NodeConfig`. **D-078 find:** the runbook assumed a federation config section exists — it doesn't (only `node`/`paths`/`logging`/`sync`), so it's net-new; `#[serde(default)]` keeps pre-2a configs parsing with approval off. NEW `xgen-core/src/federation/pending_queue.rs` — `PendingFederationRequest` (handshake facts, no `session_id`) + `PendingFederationQueue` (`HashMap<NodeXgid,_>`, add/remove/get/all/save/load, `add` idempotent on key; reuses `RegistryError`); sibling store, not a registry field (pre-relationship). No run_node wiring this commit (Commit 3's pause-point is the first consumer; an unused queue would trip clippy). Verification: `cargo test --workspace` **751** / 0 failed / 1 ignored (+7: 4 queue + 3 config; xgen-core 475→479, xgen-node 146→149); clippy `-D warnings` clean; build all-targets 0 errors. **Next-active: Clair Commit 3** (LOAD-BEARING pause-point in `run_receiving`) per runbook §5, after Joe-lock checkpoint #2. **Entry point: CLAUDE.md PLAY + JOURNAL J-175 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` §5.**

⚫ **(historical, superseded by the Commit-2-shipped state above) federation-admin-control 2a — Commit 1 (FederationState model + FAC-D2 migration) SHIPPED at J-174 (2026-05-30).** `FederationState` enum (`Active`/`Pending`/`Rejected`/`Revoked`, derive-`Default` → `Active`, serde lowercase) + `#[serde(default)] pub state` on `FederationRelationship` (`xgen-core/src/federation/registry.rs`). **Checkpoint #1 closed (Joe-lock):** all four variants (`Revoked` ships dormant — no 2a producer, sibling to the `key_rotation` arm); `Active`-as-default is faithful, not lossy (pre-2a the registry only held established relationships — `defederate` removes outright). **D-078 mismatch surfaced + resolved (runbook §8):** the runbook's "`mark_pending` creates-if-absent mirroring `mark_active`" doesn't hold (state lives on the relationship, needs handshake facts; `mark_active` creates a `PeerOperationalRecord`). Resolution (A), Joe-locked: state-setters `get_mut` + **no-op if absent** (mirroring `update_next_reconnect`); creation stays with `upsert`; the Rejected tombstone is built by the `reject` verb in Commit 4. Verification: `cargo test --workspace` **744** / 0 failed / 1 ignored (+6 FAC-D2 tests; xgen-core 469→475); clippy `-D warnings` clean; build all-targets 0 errors. No DECISIONS.md change (arc-local FAC-D# per D-069). **Next-active: Clair Commit 2** (require_approval flag + pending-request queue store) per runbook §4. **Entry point: CLAUDE.md PLAY + JOURNAL J-174 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` §4.**

⚫ **(historical, superseded by the Commit-1-shipped state above) federation-admin-control 2a — design COMPLETED + runbook ACTIVE (J-173, 2026-05-30).** Second D-071 arc (split 2a approval/queue + 2b policy at J-172). 2a runbook `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` v1.0, **5 Clair commits**: (1) `FederationState` enum (`Active` default) as `#[serde(default)]` field on `FederationRelationship` + `mark_pending`/`mark_rejected` + backward-compat load test — the FAC-D2 migration, a solved pattern (`registry.rs` already uses `#[serde(default)]` + has a precedent test, so old records load Active for free); (2) `require_approval` config flag (default false) + pending-request queue store; (3) **load-bearing** pause-point in `run_receiving` — on `require_approval && peer-not-Active` → enqueue + `Reject` **code 2003** (2001/2002 taken), default-off path byte-for-byte unchanged; (4) `accept`/`reject`/`initiate` in `admin_ops`; (5) close. **Prime invariant:** `require_approval = false` (default) = today's auto-establish byte-for-byte — mandatory regression test at Commit 3 (D-065; we don't quietly change federation's default posture). 3 checkpoints (#1 variant set + Active-default; #2 code 2003 + pause condition = approval-state-only not policy; #3 reject-tombstone + initiate no-self-approval). Code-traced at authoring (J-173): the `#[serde(default)]` migration pattern + reject-code 2003 free + the `run_receiving` ACTIVE-transition site. **Next-active: Clair's Commit 1** (state model + FAC-D2 migration) after checkpoint #1. **Entry point: CLAUDE.md PLAY + JOURNAL J-173 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_IMPL.md` + the 2a design doc.**

⚫ **(historical, superseded by the 2a-runbook state above) federation-admin-control arc — OPENED + SPLIT 2a/2b; 2a design ACTIVE (J-172, 2026-05-30).** Second D-071 arc. Code-traced `handshake.rs`/`registry.rs`: federation **auto-establishes** (valid handshake → ACTIVE + persist; no pending state / approval queue / admin pause point); the handshake already carries a `Reject` message (codes 2001 caps / 2002 version) but never refuses on operator *policy*; `FederationRelationship` has no state field. **Arc split into 2a (approval/queue) + 2b (policy)** — the audit's 5 verbs presuppose two separable subsystems, so the four D-071 arcs are now five. 2a = `tasks/M6_FEDERATION_ADMIN_CONTROL_DESIGN.md` v1.0 ACTIVE (`accept`/`reject`/`initiate` + queue + state model); 2b = `tasks/M6_FEDERATION_POLICY_DESIGN.md` PENDING (`set-policy`/`show-policy` + policy store, opens after 2a ships). **2a locks:** **FAC-D1** approval opt-in = configurable `federation.require_approval`, **default-OFF** (off = today's auto-establish, zero regression; on = inbound queues for accept/reject) — Option B over always-on (A, a posture reversal) and per-peer (C, needs 2b from day one); **FAC-D1a** pause-point = reject-with-retry (enqueue + `Reject` with a new "pending" code, never hold the socket); **FAC-D2** `FederationState` enum field on the relationship (Pending/Active/Rejected/Revoked), existing JSON records load Active via serde default (backward-compatible). `initiate` = operator outbound intent → no self-approval (the gate is inbound-only). **Next-active: the 2a implementation runbook** (Chat Claude + Joe) — pending-queue store, `require_approval` flag, the new reject code, the `run_receiving` pause point, the three verbs in `admin_ops`, FAC-D2 serde-default migration. Then 2a → Clair; 2b opens after 2a ships. **Entry point: CLAUDE.md PLAY + JOURNAL J-172 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_DESIGN.md` + its audit.**

⚫ **(historical, superseded by the federation-admin-control arc above) protocol-audit-log D-071 arc CLOSED at J-169 (2026-05-30).** First of the four post-M6 D-071 subsystem arcs to ship end-to-end. The §3.11.8 Node protocol audit log (which J-157→J-161 found unbuilt; **NOT** the A6 SQLite admin trail) now exists across four commits: **writer** (J-166 — `xgen-node/src/protocol_audit.rs`, single hook inside `app::persist_event`, Shape β process-global sink, 8 live EventTypes per checkpoint #1/D-078) + **reader** `space audit-events` (J-167, read-time per-Space filter + offset-cursor pagination) + **rebuild** `space audit-rebuild` PAL-D3 (J-168, checkpoint #3 — gap recovery + cold-start backfill, idempotent) + **close** (J-169). Commit 4 (doc-only): runbook ACTIVE → COMPLETED v1.1; `docs/xgen_node_admin_ops_design.md` v1.16 §6.A4 marks both verbs SHIPPED; `tasks/M6_BACKING_AUDIT.md` v1.2 A4 row updated. `cargo test --workspace` **738** passed / 0 failed / 1 ignored (+12 node-lib tests across the arc); clippy clean; build all-targets 0 errors. No DECISIONS.md change (arc-local PAL-D1/D2/D3 per D-069). **Next-active:** the 2nd D-071 arc, **federation-admin-control** (design phase — pull `tasks/M6_FEDERATION_ADMIN_CONTROL_DESIGN.md` PENDING → ACTIVE, lock FAC-D1). **Entry point: CLAUDE.md PLAY + JOURNAL J-169 per Rule 0, then `tasks/M6_FEDERATION_ADMIN_CONTROL_AUDIT.md` + its design stub.**

⚫ **(historical, superseded by arc-CLOSED state above) protocol-audit-log arc — Commit 3 (rebuild `space audit-rebuild`, PAL-D3) SHIPPED at J-168 (2026-05-30).** Operator rebuild: replays each in-scope Space's persisted DAG events and appends audit entries for the audited types whose `event_id` isn't already logged — closes PAL-D2 gaps + backfills cold-start Spaces. `admin_ops::space_audit_rebuild` + `SpaceAuditRebuildArgs { space_id: Option, dry_run }` + `SpaceAuditRebuildResult { spaces_scanned, entries_added, entries_already_present }`; clap `SpaceCommand::AuditRebuild`; pipe arm. Primitives: `ProtocolAuditSink::append_entry` made public; new `app::read_persisted_events`. Checkpoint #3 locks: scope `Some`(one)/`None`(all `runtime.spaces`, named-absent → `SPACE_8001`); dedup by `event_id` (idempotent); no startup/automatic reconcile in v1 (`--dry-run` previews). WRITE → A6 trail (dry-run not audited); disk error → `GENERIC_4000`. Verification: `cargo test --workspace` **738** passed / 0 failed / 1 ignored (713 lib: 63+35+469+**146** node; +25 integration; **+3 vs J-167's 735** — gap-recovery+idempotent, cold-start+dry-run, unknown-space→8001); clippy clean; build all-targets 0 errors. **Next-active:** Commit 4 arc close (runbook → COMPLETED; §6.A4 + backing-audit row updates; PLAY → federation-admin-control). **Entry point: CLAUDE.md PLAY + JOURNAL J-168 per Rule 0, then runbook §7.**

⚫ **(historical, superseded by Commit-3-shipped state above) protocol-audit-log arc — Commit 2 (reader `space audit-events`) SHIPPED at J-167 (2026-05-30).** Read-time-filtered view of the Node protocol audit log for one Space (A4-D3, runbook §5). `admin_ops::space_audit_events` + `SpaceAuditEventsArgs` (`space_id` + `--event-type`/`--since`/`--until`/`--limit`/`--cursor`) + `SpaceAuditEventsResult { events, returned, next_cursor }`; clap `SpaceCommand::AuditEvents`; pipe arm. Store scanning is `protocol_audit::read_all_entries` (globs present `protocol_audit_YYYY-MM.jsonl`, month-pre-filters, chronological, skips malformed lines); the reader applies precise `space_id` (entry `extra["space_id"]`, PAL-D1 read-time scope) + `event_type` + inclusive `since`/`until` (RFC-3339 string compare). Pagination = offset-as-cursor (stable on an append-only log); `limit` default 100 / cap 1000. Errors: `SPACE_8001` (not hosted/federated here — checked vs `runtime.spaces`, lock dropped before file I/O), `SPACE_8010` (bad since/until/cursor). READ → not audited. Deliberate reader-latitude call: scan **all** present month files when unbounded (not "current month only" — silent history-hiding would be a compliance footgun); documented. Verification: `cargo test --workspace` **735** passed / 0 failed / 1 ignored (710 lib: 63 client + 35 common + 469 core + **143** node; + 25 integration; **+3 vs J-166's 732** — filter-by-space/type/time + cross-month pagination + bad-filter/unknown-space); clippy clean; build all-targets 0 errors. **Next-active:** Clair **Commit 3 (rebuild `space audit-rebuild`, PAL-D3)** per runbook §6 + Joe-lock checkpoint #3; then Commit 4 close. **Entry point: CLAUDE.md PLAY + JOURNAL J-167 per Rule 0, then `xgen-node/src/protocol_audit.rs` + `app.rs::replay_spaces_from_dir`/`persist_event` + runbook §6.**

⚫ **(historical, superseded by Commit-2-shipped state above) protocol-audit-log arc — Commit 1 (writer) SHIPPED at J-166 (2026-05-30).** The load-bearing writer per `tasks/M6_PROTOCOL_AUDIT_LOG_IMPL.md` §4, after Joe-lock checkpoint #1. NEW `xgen-node/src/protocol_audit.rs` (sibling to the A6 `audit.rs` SQLite trail — "NOT the A6 trail"): `ProtocolAuditEntry` (4 universal fields + `#[serde(flatten)] extra`), `from_event` projection, monthly append-only JSONL store (`<data_dir>/audit/protocol_audit_YYYY-MM.jsonl`, month from entry `ts`), loud-failure counter (PAL-D2). The single hook is **inside `app::persist_event`** (after the dedup early-return + event write → idempotent; replay uses `ingest_event`, never re-fires). **Checkpoint #1 locks:** **1A** — audit fields use the spec §3.11.8 schema names populated from real sources (`event.sender`, content `target_identity`/`node_id`/`auth_tier`, top-level `space_id`/`room_id`); absent fields (`approving_node_id`, `reason`) omitted. **2A** — audit the **8 live** EventTypes (`membership.{join,leave,invite,kick,ban}` + `state.{space_create,room_create,federation_add}`); `system.key_rotation` kept as a forward-ready dormant arm; `state.federation_remove` + `identity.register` documented as spec-listed-but-not-emitted. **Shape β** — the sink is a process-global `OnceLock` installed once in `run_node` (one audit dir + one node_id per process), NOT a param threaded through ~11 hot async signatures; strengthens no-drift (D-067). **D-078 catch (fifth this milestone):** of the 11 §3.11.8 types only 8 are emitted; `federation_remove`/`identity.register` have no variant; `key_rotation` is dormant; `identity.register` is structurally uncapturable at this hook (not a DAG event). Surfaced + Joe-locked rather than guessed. Verification: `cargo test --workspace` **732** passed / 0 failed / 1 ignored (707 lib: 63 client + 35 common + 469 core + **140** node; + 25 integration; **+6 vs J-160's 726** — the 6 §4 writer tests); clippy `--workspace --lib --tests -D warnings` clean; build all-targets 0 errors; isolated re-runs ×3 green. **Next-active:** Joe checkpoint #2 (post-writer drift — replay-no-dup + loud-failure + 11-only-types, all confirmed at J-166) → Clair **Commit 2 (reader `space audit-events`)** per runbook §5. **Entry point: CLAUDE.md PLAY + JOURNAL J-166 per Rule 0, then `xgen-node/src/protocol_audit.rs` + runbook §5.**

⚫ **(historical, superseded by Commit-1-shipped state above) protocol-audit-log arc — design COMPLETED + runbook ACTIVE (J-165, 2026-05-30).** First of the four D-071 arcs (Joe scheduled list order: protocol-audit-log → federation-admin-control → auth-module-registry → bootstrap-client). Pulled the stub PENDING → ACTIVE; `tasks/M6_PROTOCOL_AUDIT_LOG_DESIGN.md` v1.0 with three locks: **PAL-D1** single post-accept writer hook (sibling to Option B's `process_inbound` hooks) + Node-global monthly JSONL + read-time `space_id` filter; **PAL-D2** best-effort after persist + loud failure, **never fail-closed** (the log is a recoverable DAG projection, so fail-closed buys nothing the DAG doesn't already provide); **PAL-D3** operator-invoked rebuild-from-DAG, in scope (closes gaps + cold-start backfill). Out of v1: hash-chain tamper-evidence + the separate Auth-Module audit log. The load-bearing work is the **writer side** (11-EventType hooks in the dispatch pipeline), not the reader. **Runbook shipped (J-165):** `tasks/M6_PROTOCOL_AUDIT_LOG_IMPL.md` v1.0, 4 Clair commits (writer → reader → rebuild → close); PAL-D1 hook refined by code-trace to **inside `persist_event`** (the persist chokepoint every accept path funnels through; replay-safe; no-drift D-067). **Next-active: Clair's Commit 1 (writer)** after Joe-lock checkpoint #1 (the `persist_event` signature change + the 11-EventType list approved by name, D-078). **Entry point: CLAUDE.md PLAY + JOURNAL J-165 per Rule 0, then `tasks/M6_PROTOCOL_AUDIT_LOG_IMPL.md`.**

🟢 **D-071 arc AUDIT phase done at J-161 (2026-05-30).** Opened each of the four post-M6 D-071 subsystem arcs with its backing audit (D-071: audits precede dependent milestones; audit phase only — read-only, evidence-cited, no design/code). Four canonical docs: `tasks/M6_FEDERATION_ADMIN_CONTROL_AUDIT.md`, `M6_BOOTSTRAP_CLIENT_AUDIT.md`, `M6_AUTH_MODULE_REGISTRY_AUDIT.md`, `M6_PROTOCOL_AUDIT_LOG_AUDIT.md`. All four verdicts **GAP IDENTIFIED — HIGH (whole-subsystem)**; recon fanned out to four parallel Explore subagents, then every load-bearing ABSENT claim personally grep-verified (J-081 discipline). Key findings: federation auto-establishes (no approval queue / policy store / handshake admin-gate); bootstrap gap is wider than the send-path (no `[bootstrap]` config / registrations / self-info stores); auth-module tier *verification* exists but a trusted-module *registry* doesn't; §3.11.8 protocol audit log is unbuilt and is NOT the A6 SQLite admin trail (`audit.rs`, shipped J-154). Each audit carries a per-verb backing table + a "what the design phase must build" inputs list (the design-arc handoff). node-policy (the fifth deferral) NOT audited. `tasks/M6_BACKING_AUDIT.md` cross-refs updated. **Design stubs opened at J-162 (2026-05-30):** four PENDING design docs `tasks/M6_*_DESIGN.md` — decision scaffolds carrying each audit's agenda + its named open decision framed for Joe (arc-local FAC/BC/AMR/PAL-D# IDs per D-069); the four audit cross-ref lines flipped Joe-reserved → opened-PENDING (audits bumped v1.1). **Next-active:** Joe picks the first arc to pull its stub → ACTIVE design (each audit's "what the design phase must build" is its handoff) + Joe's §5.1/§6.A4 doc work; then M7. **Entry point: CLAUDE.md PLAY + JOURNAL J-162 per Rule 0, then the four `tasks/M6_*_DESIGN.md`.**

🟢 **M6 (new) — A4 force-eject Option B (live fan-out) SHIPPED at J-160 (2026-05-30). M6 admin write-path COMPLETE — 16 verbs, all live-propagating.** The deliberate Option-A follow-up Joe-locked at J-159. `space force-eject` / `space unban` now push the Node-authored `membership.node_eject` / `node_unban` **LIVE** — `apply_fanout` to connected Space members + `apply_federation_push` to federated peers — immediately after persist, on top of Option A's sync path. **Wiring (mirrors the runtime/federation_registry threading, D-067):** `AdminContext` gained `client_senders` / `federation_peer_senders` (`Option`; `None` → sync-only, preserving the Option-A baseline + file-only verbs + unit tests); `run_node` → `start_pipe_server` → `dispatch_line` → `dispatch_admin` thread the two sender `Arc`s to the ctx; the hook is the shared `emit_node_membership_event`. Best-effort after persist (D-070 — a push failure does not roll back the eject). **Honest finding (D-065):** `apply_fanout` recipients = the Space's *current* members and dispatch already removed the target, so the ejected target is NOT in the live push (learns via sync, like a member-kick); the handoff's plan-step-3 "target's own session receives it" expectation was wrong against `apply_fanout` semantics and was corrected in code + the impl note. Verification: `cargo test --workspace` **726** passed / 0 failed (701 lib: 63 client + 35 common + 469 core + 134 node; + 25 integration; **+2 vs J-159's 724** — both node-lib live-fanout tests); clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_9_FORCE_EJECT_IMPL.md` gains the Option B section; `tasks/HANDOFF_M6_FORCE_EJECT_OPTION_B.md` COMPLETED. **Held doc work CLEARED (J-162 + J-163):** the 4 arc stubs opened PENDING (J-162); the §5.1/§6.A4 amendment + the stale A4-row fix shipped (J-163). **Next-active:** Joe's doc work → **M7 `--aicontrol`** (reuses `admin_ops::*`; same sender-map threading). **Entry point: CLAUDE.md PLAY + JOURNAL J-160 per Rule 0, then `tasks/M6_PHASE_9_FORCE_EJECT_IMPL.md` (Option B section) + `xgen-node/src/admin_ops.rs::emit_node_membership_event`.**

⚫ **(historical, superseded by M6-A4-Option-B-shipped state above) M6 (new) IMPLEMENTATION — Phase 9 (A4 force-eject) SHIPPED at J-159 (2026-05-30). M6 admin write-path COMPLETE — 16 verbs.** A4 `space force-eject` + `space unban`, the only M6 verbs emitting a Space-DAG event — ran as an A4-D1 design beat first. New EventTypes `membership.node_eject` + `membership.node_unban`, Node-signed (sender = home Node keypair), authority = signature + `sender == space.home_node` (NOT member role; reuses the B3 Node-authored pattern); forged eject → `NodeEjectAuthority` wire **3043**; federated peers validate identically (home_node is replicated). **1A eject+ban+reversible** — the 1A gate caught that no unban path existed (`banned` was insert-only), so reversibility was *built* (`node_unban`), not assumed; node_eject gets top Layer-1 resolution precedence (Node authority can't be defeated by a concurrent member action). **Option A propagation** (Joe-locked at the dispatch/fan-out fork): dispatch updates live state immediately (removed+banned) + persists; clients/peers pick up via existing sync, not live push (Option B live-fanout = future follow-up). Validation-path fix caught by the wire-gate test: node-authored events skip the F-10 sender-registration hold. Verification: `cargo test --workspace` **724** passed / 0 failed (699 lib: 63 client + 35 common + 469 core + 132 node; + 25 integration; +6 vs P10); clippy `-D warnings` clean; build all-targets 0 errors. Docs (Chat-Claude spec touches): Ch3 §3.3/§3.9 v0.5 + Appendix I v1.5. `tasks/M6_PHASE_9_FORCE_EJECT_IMPL.md` COMPLETED. **M6 verbs (16):** A6 5 + A5 4 + A1 2 + A4 3 + A7 2; ~18 deferred verbs → four D-071 arcs + node-policy. **Reserved for Joe:** §5.1/§6.A4 amendments + 4 arc stubs. **Next-active:** Joe's doc work → **M7 `--aicontrol`** (reuses `admin_ops::*`). **Entry point: CLAUDE.md PLAY + JOURNAL J-159 per Rule 0, then `tasks/M6_PHASE_9_FORCE_EJECT_IMPL.md` + `tasks/M6_BACKING_AUDIT.md`.**

⚫ **(historical, superseded by M6-Phase-9-force-eject-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 10 SHIPPED at J-158 (2026-05-29).** A7 Plugin management — the 2 READ verbs (A7-D1; WRITE verbs already deferred). **M6's backed admin write-path is now COMPLETE at 14 verbs** (A6 5 + A5 4 + A1 subset 2 + A4 subset 1 + A7 2); only `force-eject` remains M6-scoped (gated on the A4-D1 `membership.node_eject` wire sub-design). Backing verified at pickup (Rule 5): the plugin subsystem is a bare `TemperaturePlugin` trait + one compiled-in `NoOpTemperaturePlugin`, no registry/telemetry — but unlike the prior gaps the plugin genuinely exists + is loaded, so the reads ship **honest-thin** (D-065): added `crate::plugins::installed_plugins()` = the static compiled-in plugin set (one today), `version` = binary version, `events_consumed`/`last_activity` = honest `None` (no telemetry); `PLUGIN_9001` unknown. Verification: `cargo test --workspace` **693 lib** (63 client + 35 common + 465 core + 130 node) + 25 integration / 0 failed (+3 node vs P9-reads; core unchanged); clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_10_IMPL.md` COMPLETED. **Reserved for Joe**: canonical §5.1/§6 amendments + audit-doc A4-row correction + four arc stubs. **Next-active = `force-eject`** (A4-D1 wire sub-design beat, then implement) + Joe's doc work; ~19 verbs → four D-071 subsystem arcs. **Entry point: CLAUDE.md PLAY + JOURNAL J-158 per Rule 0, then design §6.A4 (A4-D1).**

⚫ **(historical, superseded by M6-Phase-10-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 9 read subset SHIPPED at J-157 (2026-05-29).** Joe ran the D-071 **backing-map audit** (`tasks/M6_BACKING_AUDIT.md`) the A3/A1 finds pointed to — systematic gap confirmed wider: **A2 Auth Module registry fully absent**, **A4 node-policy absent**. M6 real shipping write-path ≈14 backed verbs; ~19 route to **four post-M6 D-071 subsystem arcs** (federation-admin-control, bootstrap-client, auth-module-registry, node-policy). Phases 6 (A3) + 8 (A2) ship no verbs as designed. **This phase ships A4's backed read: `space list-hosted`** (READ, not audited) — reads live `runtime.spaces` filtered to `home_node == this Node` (D-082 lock #4 — hosted/originated, never federated-in) + `--name-filter`; `HostedSpaceSummary` (member/room/federated-peer counts; `created_at: None` honest); uses the existing A5 runtime handle. **`audit-events` DEFERRED (Joe-confirmed)** — reads the §3.11.8 protocol audit log which is **unimplemented** (only `event_trace`→`tracing` debug logs exist; no structured/queryable store) → protocol-audit-log arc; same spec-≠-code slip as the audit doc's A6 row (third consecutive claim-vs-reality catch). `force-eject` stays A4-D1 design-gated; node-policy → node-policy arc. Verification: `cargo test --workspace` **690 lib** (63 client + 35 common + 465 core + 127 node) + 25 integration / 0 failed (+2 node vs P7; core unchanged); clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_9_READS_IMPL.md` COMPLETED. **Reserved for Joe**: canonical §5.1/§6 amendments + audit-doc A4-row correction + four arc stubs. **Next-active = Phase 10 (A7 Plugin list+status — last backed phase; verify backing at pickup)**, then force-eject (A4-D1 gated) + Joe's doc work. **Entry point: CLAUDE.md PLAY + JOURNAL J-157 per Rule 0, then `tasks/M6_BACKING_AUDIT.md` + design §6.A7.**

⚫ **(historical, superseded by M6-Phase-9-reads-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 7 SHIPPED at J-156 (2026-05-29).** A1 Federation management, **honest-subset (2 of 7 verbs)**. Two Rule-6 checkpoints reordered the milestone (both Joe-locked): **(1) A3/Phase 6 DEFERRED** — its network half is architecturally absent (J-081 pattern): `bootstrap/client.rs` is a placeholder, nothing sends `bootstrap.register` in production, no `[bootstrap]` config/store; the gap is the bootstrap-*client send path* (server-side directory/reputation/capability are real). A3 → its own D-071 Bootstrap-client arc. Reordered to A1, whose `FederationRegistry` backing is real. **(2) A1 honest-subset** — only `federation list` (READ, paginated, `--state` active/all honest) + `federation defederate` (DESTRUCTIVE, audited; live-registry remove + persist + `cleaned_spaces`) have backing; `accept`/`reject` (no admin-approval queue — federation auto-establishes), `set-policy`/`show-policy` (no policy store/enforcement), `initiate` (would admin-gate the handshake) → deferred to a post-M6 federation-admin-control subsystem arc (D-071). Corrected the "7 verbs have backing" assumption (Rule 5). Pattern: M6 Block-4 verbs assume subsystems the milestones never built — likely recurs A2/A4. **AdminContext** extended (P5 precedent): `federation_registry` handle + `with_runtime`/`with_federation_registry` builders; pipe threads the registry `Arc` through `start_pipe_server`→`dispatch_line`→`dispatch_admin`. Verification: `cargo test --workspace` **688 lib** (63 client + 35 common + 465 core + 125 node) + 25 integration / 0 failed (+3 node vs P5; core unchanged); clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_7_IMPL.md` COMPLETED. **Reserved for Joe** (not in this commit): canonical design-doc §5.1/§6.A1/§6.A3 amendments + two D-071 arc stubs + the audit-now-fork call. **Next-active = Joe's pending decisions**, then A2/A4 (backing-check first). **Entry point: CLAUDE.md PLAY + JOURNAL J-156 per Rule 0, then `tasks/M6_PHASE_7_IMPL.md`.**

⚫ **(historical, superseded by M6-Phase-7-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 5 SHIPPED at J-155 (2026-05-29).** A5 Identity registry administration — 4 verbs (`identity show` READ · `identity revoke` DESTRUCTIVE block-only A5-D1 · `identity set-trust-expiry` WRITE · `identity manage-replica` WRITE thin-scope A5-D2; `identity list` stays in the M2 allowlist; `IDENT_6001/6002/6010/6020/6021`). **P5 decision (Joe-locked, Rule-6 checkpoint before code): `AdminContext` widens to runtime-aware.** A5's mutating verbs reach *live* Node state, not disk — A5-D1 commits revoke to "immediate, security-critical" (a disk-only write leaves the resident's in-memory registry stale → a revoked Identity authenticating until restart is a security window), and `ReplicaRegistry` is in-memory-only (no disk backing). `AdminContext` gains `runtime: Option<Arc<Mutex<NodeRuntime>>>` + `identities_path()` + `batch_with_runtime`; the pipe server threads its `Arc` through `dispatch_line`→`dispatch_admin`. Same precedent as A6-D1's `log set-level` reload handle; sets the precedent for later live-mutating categories (A1/A2/A4); M7 `--aicontrol` supplies the handle the same way. Recorded as a P5 lock in the phase file, not a D-NNN. **Immediate revoke auth gate**: `handle_connection` checks `is_revoked` on the live registry right after `server_authenticate` → revoked Identity denied the next session-open. **Core**: `IdentityRecord` gains `revoked`/`revoked_at`/`revocation_reason` (serde-default → active records byte-identical to pre-M6; pre-A5 JSON deserialises; revocation on the record because the registry persists as a bare JSON array); 22 fixture literal sites swept (guard-railed subagent off the compiler E0063 list); Ch3 §3.6.6 doc-sync (v0.3→0.4). Verification: `cargo test --workspace` **685 lib** (63 client + 35 common + 465 core + 122 node) + 25 integration / 0 failed (+13 vs P4's 672: +8 core registry, +5 node); clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_5_IMPL.md` COMPLETED. Folded commit per the M6 cadence; Joe pushes. **Next-active = Phase 6** (A3 Bootstrap config — Appendix K.2.3, 5 verbs). Phase 9 stays design-gated. **Entry point: CLAUDE.md PLAY + JOURNAL J-155 per Rule 0, then design §6.A3 + Appendix K.2.3, then `tasks/M6_PHASE_6_IMPL.md`.**

⚫ **(historical, superseded by M6-Phase-5-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 4 SHIPPED at J-154 (2026-05-29).** A6 Logging & audit — the first real verb phase and the landing-pad category (the Phase-2 `audit` skeleton is now load-bearing). 5 verbs across 4 folded commits: **C1** `audit query`/`export`/`archive` (`audit` module gained filtered query + archive prune + JSONL write; fail-safe-toward-retention `AUDIT_5001`/`5002`; fixed a `usize::MAX`→`i64::MAX` SQLite LIMIT overflow); **C2** `pipe::dispatch_line` now async, routing two-token verbs via a shared clap grouping (`AdminCli`/`AdminCommand`/`AuditCommand`/`LogCommand`, reused by M7 `--aicontrol`); read-only M2 allowlist unchanged; actor = `os-user:<name>`; **C3** reworked the Node tracing subscriber to registry+layers with a global reload handle + `LogFilterState` → `log show-level` (read) + `log set-level` (runtime apply, A6-D1, audited); **logging-regression check PASSED live** (byte-identical log format). A6-D4 audit-the-auditor honoured (WRITE/DESTRUCTIVE + export audited; pure reads not). **Phase 3 collapsed** (J-153) — Block 4/Appendix K bucketed reads into category phases. Verification: `cargo test --workspace` 672 lib + 25 integration / 0 failed; clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_4_IMPL.md` COMPLETED. **Next-active = Phase 5** (A5 Identity registry — Appendix K.2.2). Phase 9 stays design-gated. **Entry point: CLAUDE.md PLAY + JOURNAL J-154 per Rule 0, then `tasks/HANDOFF_M6_IMPL.md` + design §6.A5 + Appendix K.2.2, then `tasks/M6_PHASE_5_IMPL.md`.**

⚫ **(historical, superseded by M6-Phase-4-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 2 SHIPPED at J-153 (2026-05-29).** The foundational scaffolding (M6 design §5.2 + audit §6.5), six verified commits: **C1 wire** — `TransportMessage::EventAccepted` + optional `Error.event_id` + a single `event_id()` accessor (per-variant realisation, Joe-confirmed Option 1, over the 81-site envelope-wrapper refactor; identical wire bytes); **C2 skeletons** — `xgen-node` `admin_ops` (`AdminContext`/`AdminError`/`Stage`/`GENERIC_4000`, no verbs) + `audit` (`rusqlite` bundled, first SQLite in the workspace; `audit_entries` §2.6.4); **C3 emission** — `process_inbound` emits `EventAccepted` post-persist/pre-fan-out + **newly emits `Error` with `event_id` on rejection** (closes the J-081 §5 gap), via pure `accept_signal`/`reject_signal` helpers, gated to local submissions; **C4 client plumbing** — 7 receive loops recognise the signals (debug-log; per-verb await deferred §5.2.4); **C5 pipe seam** — documented `dispatch_line` routing seam (no write verbs yet); **C6 doc sync** — Ch3 §3.3.10 + Appendix I. Verification: `cargo test --workspace` 657 lib + 25 integration / 0 failed; clippy `-D warnings` clean; build all-targets 0 errors. `tasks/M6_PHASE_2_IMPL.md` COMPLETED. **Next-active = Phase 3** (read-only `--batch` completions), then Phases 4–10 per Appendix K (Phase 4 lands the audit-write primitive; Phase 9 design-gated on the `membership.node_eject` sub-design). **Entry point: CLAUDE.md PLAY + JOURNAL J-153 per Rule 0, then `tasks/HANDOFF_M6_IMPL.md` + design §5.1/§6 + Appendix K, then `tasks/M6_PHASE_3_IMPL.md`.**

⚫ **(historical, superseded by M6-Phase-2-shipped state above) M6 (new) IMPLEMENTATION underway — Phase 1 (R1 `rooms`) SHIPPED at J-152 (2026-05-29).** Joe locked R1 (do `rooms` + `members` first). `rooms` shipped as a zero-network local read from `KnownSpace.rooms[]`, mirroring `spaces` across the single-source `ops` layer (D-067) + all three dispatchers (CLI / batch driver / pipe): `RoomsResult` + `ops::rooms` + `RoomsArgs` + `cmd_rooms` + 2 unit tests; output matches Appendix F §F.5.6. **`members` DEFERRED** (Joe-locked) — the schema check (which Pass 1's audit deferred to implementation) found `xgen-client_state.json` persists no per-member data (`KnownSpace` = `{space_id, name, node_endpoint, role, rooms}`; only a Node-side `member_count`), so the Appendix-F `members` output cannot be produced from disk; re-enters as its own scoped piece (Node query OR `KnownSpace.members[]` schema expansion). `federate` DEFERRED to Phase 7 (R2). Verification: `cargo test -p xgen-client --lib` 63 passed / 0 failed (was 61, +2); clippy `-D warnings` clean; `cargo build --workspace --all-targets` 0 errors; live happy + error paths confirmed. Records: `tasks/M6_PHASE_1_IMPL.md` NEW COMPLETED; `tasks/HANDOFF_M6_IMPL.md` → COMPLETED v1.1 (consumed; remains the Phase 2–10 orientation map); Appendix F v1.3 → v1.4. **Next-active = Phase 2** (`admin_ops` scaffolding + `TransportMessage` envelope `event_id` + `EventAccepted` + rejection paths; M6 design §5.2). Phase 9 stays design-gated (`membership.node_eject` wire sub-design before Phase 9). **Entry point: CLAUDE.md PLAY + JOURNAL J-152 per Rule 0, then `tasks/HANDOFF_M6_IMPL.md` §Phase 2 + design §5.2, then `tasks/M6_PHASE_2_IMPL.md`.**

⚫ **(historical, superseded by M6-Phase-1-shipped state above) M6 (new) Block 4 COMPLETE (J-151) — all 7 verb categories walked + locked; §6 of the M6 admin-ops design doc fully specified (v1.9) + Appendix K (`xgen_appendix_k_en.md`, new corpus file) compiled. 33 verbs ship + 5 deferred. M6 Phase 0 design fully done.** Block 4 prep surfaced an "operator" overload (the admin-ops design used it 10× as a Node-owner/admin alias, colliding with the locked AI-operator role D-059/D-064). **D-082** locks: operator = AI-operator role only (moderator-parallel; fall-upward D-064); Node principal = **administrator** (prose) / **admin** (code, matching `admin_ops`/`AdminContext`); owner/super-admin reserved future sub-tier (M7); Node admin auto-administers originated/homed Spaces but NOT federated-in replicas (signing identity → A4 sub-design). Walk locked one Joe-checkpoint per category (A6·A5·A3·A1·A2·A4·A7). Cross-cutting: revocations never cascade in M6 (A2-D1/A5-D1); `force-eject` emits a Node-signed `membership.node_eject` (Option A; detailed wire sub-design opens Phase 9); two audit logs (SQLite admin trail vs §3.11.8 protocol log). **Gate cleared:** the Propagation Reliability Audit (the named M6 implementation gate) is COMPLETED (`docs/xgen_propagation_reliability.md`, closed 2026-05-18); its §5 rejection-signal finding is locked into Phase 2 (envelope `event_id`). **M6 implementation handed to Clair** via `tasks/HANDOFF_M6_IMPL.md` — Track 2 open; Clair starts Phase 2 (`admin_ops` scaffolding + envelope `event_id` + `EventAccepted` + rejection paths), then Phases 3–10 add one category each per Appendix K. Caveats baked into the handoff: Phase 9 is design-gated (the `membership.node_eject` wire sub-design is a Chat-Claude-+-Joe session before Phase 9); Phase 1 R1/R2/R3 scope to confirm at Phase 1 start. Chat Claude stands down to standby. **Entry point: Clair → `tasks/HANDOFF_M6_IMPL.md`; Chat Claude → CLAUDE.md PLAY + JOURNAL J-151 per Rule 0.**

⚫ **(historical, superseded by M6-Block-4-active state above) XGID Retrofit arc COMPLETE at J-148 — all five passes closed.** Pass 5 was a confirm-clean audit pass: Audit 1 (trace-field formatter, ~60 `tracing::` sites) = ONE fix (F-1: `app.rs:2288` Debug-formatted `Option<&IdentityXgid>` → `&str` projection); Audit 2 (Debug/Display impls on xgen-client public types) = clean. **Commit A** (one-line F-1 fix; `cargo build --workspace --all-targets` 0 errors, 637 lib GREEN, clippy clean) + **Commit B** (close). **D-081 promoted** — "XGID typing is wire-format and persistence-format invariant" (numbered D-081 not D-080; D-080 already taken by the Node-storage EventStore decision, collision caught at authoring). Layered-B3 null (fifth Pass-arc no-finding instance). Arc retrospective: zero serialized bytes changed across all five passes; Path A opened Pass 1, closed Pass 4; "honest longer work" by pass = 1/0/2/4/1. §7.10 consolidation SKIPPED per Joe-lock. **Track 2 (Clair): stood down** until Joe's milestone selection. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-148 entry first per Rule 0.**

⚫ **(historical, superseded by arc-COMPLETE state above) XGID Retrofit Pass 4 CLOSED J-146; Pass 5 scope-amendment SHIPPED J-147.** Pass 4 shipped in two Clair-facing commits: **Commit 1 `3869d4c`** (J-145 — consolidated xgen-client retype atomic: all seven surfaces + xgen-common §4.1.b additive-API + Surface #8 docs + T1–T15; **8/8 GREEN**, 637 tests; checkpoints #2 + #3 closed; Commit 1a did NOT fire, 0 fixture errors) + **Commit 2** close (J-146). Layered-B3 null (four-instance Pass-arc chain J-122 + J-126 + J-138 + J-146). **`cargo build --workspace --all-targets` RESTORED (0 errors) — Path A closed at Pass 4**, ahead of the §2.9/§11.3 Pass-5 expectation (honest finding per D-065); Pass 5 scope reduced 4→2 items. **"Honest longer work" final count: FOUR** (J-142 + J-143 + J-144 + J-145, all prospective catches). **Pass 5 scope-amendment Track-1 atomic SHIPPED at J-147** (design doc §2.9 → v1.7 + runbook §11.3/§11.4 → v1.5, deferred items 4→2 + §1.2 closure note + Path-A-closed reframing recorded). **Next-active**: Joe selects the next-active milestone. **Track 2 (Clair): stood down** until selection. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-147 entry first per Rule 0.**

⚫ **(historical, superseded by Pass-4-CLOSED state above) XGID Retrofit Pass 3 implementation — Commit 2a ✅ at J-137 (2026-05-28); Pass 3 milestone CLOSED at J-138; Pass 4 opened J-139 and closed J-146.** Test-fixture sweep + 11 per-surface tests T1-T11 atomic shipped (638 errors → 0; 11 tests added by name per §4.7; xgen-core 449 → 453 via T1-T4; xgen-node lib 81 → 88 via T5-T11). **8/8 GREEN runs verification rigour cleared at Commit 2a milestone-bearing boundary** (5 isolated `cargo clean` runs + 3 consecutive workspace runs); test count stable at **589 across all 8 runs**; both clippy gates clean (`--lib` + `--tests` -D warnings); `cargo build --workspace` deliberately broken at xgen-client only per Path A. Both pre-existing flakes did NOT fire. **Parallel-subagent-sweep discipline data point** recorded at runbook §9.7 for future Pass-arc Commit-2a-shape runbook authors. "Honest longer work" Pass 3 count stays at TWO inherited from J-129 + J-134. **Next-active for Clair**: pickup at runbook §6 Commit 3 milestone close — Pass 3 PLAY → DONE; runbook ACTIVE → COMPLETED v1.5 → v1.6; ROADMAP visual tree 🟢 → ✅; CLAUDE PLAY flip "standby for next-milestone selection"; grep J-NNN guardrail = ZERO post-staging. **Next-active for Chat Claude (standby)**: parallel-eligible items unchanged. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-NNN entry first per Rule 0**, then runbook §6 Commit 3.

⚫ **(historical, superseded by Commit-2a-shipped state above) XGID Retrofit Pass 3 implementation ACTIVE — Commit 2 ✅ at J-NNN (2026-05-28, this commit); Clair pickup at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` §5 Commit 2a [SPLIT] per Joe-lock checkpoint #3.** Seven-surface retype atomic shipped (xgen-core/src/node/runtime.rs Surface #1 + Surface #2; xgen-node/src/{federation_session,fanout,app,reconnect}.rs Surfaces #3+#4+#5+#6; docs/xgen_appendix_d_en.md Surface #7). xgen-{common,core,node} libs CLEAN; clippy `--lib -D warnings` CLEAN; `cargo build --workspace` deliberately broken at xgen-client consumer sites only per Path A inherited from Pass 1. **Joe-lock checkpoint #3 locked Path 2 (Commit 2a split)** against 638 test-fixture errors (xgen-core 160 + xgen-node 478) >> ~50 threshold per runbook §5.1 — sibling-shape to Pass 2 Commit 2a `58b94a5` (93 errors) + Pass 1 Commit 4a precedent. **Two discipline data points surfaced for JOURNAL J-NNN body**: (a) Pass-1-pre-walk reconnaissance Pass 3 marker sparsity (one production marker only at xgen-core/src/node/runtime.rs:588 vs 33 Pass 2 markers per J-125 audit — expected per Pass-arc framing N+1 not N+2 design); (b) Surface #4 fanout.rs "verification only" framing vs actual-lift discrepancy (9 errors at baseline before edits; substantive work landed to close Path-A-broken propagation + Q4.1-Q4.7 retypes; honest framing per D-065 — runbook scoping should distinguish "verification" from "consumer-call-site closure" when Path-A-broken counts are non-trivial). **WIP-branch lineage**: branch `wip/pass-3-commit-2-in-flight` carried checkpoint #1 `728b834` (Surfaces #1+#2+#3+#4 lib-clean + #5 partial) + checkpoint #2 `2f647bf` (Surfaces #5+#6 closed + xgen-node lib CLEAN); squashed at this Commit 2 ship per D-074 atomic discipline. **D-074 application count: thirty-third instance**; "Honest longer work" Pass 3 count stays at TWO inherited from J-129 + J-134. **Next-active for Clair**: pickup at runbook §5 Commit 2a (test-fixture projection sweep across xgen-core + xgen-node ~638 errors + 11 per-surface tests T1-T11 atomic + 8 GREEN runs verification per §5.3 + §4.9), then Commit 3 milestone close per §6. **Next-active for Chat Claude (standby)**: parallel-eligible items include M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6 (~35 verbs across 7 categories) if Joe selects parallel-track work. M6 (new) stays unblocked-but-not-selected. XGID Retrofit Passes 4 + 5 stay PENDING behind Pass 3 close. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-NNN entry first per Rule 0**, then runbook §5 Commit 2a.

⚫ **(historical, superseded by Commit-2-shipped state above) XGID Retrofit Pass 3 implementation ACTIVE — Commit 1 doc-pass ✅ at J-131 (2026-05-28, this commit); Clair pickup at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` §4 Commit 2 next (Joe-lock checkpoint #2 fires first — pre-Commit-2 verbatim surface list approval against design doc §2 Q-tables).** Commit 1 ships against amended v1.1 runbook (post-J-129 Track 1 canonical-record amendment closed the three runbook §4 drifts surfaced by Clair's pre-Clair six-dimension audit) + cleared canonical record (post-J-130 silent-gitignore-skip drift-fix atom closed the fourth drift at git-staging layer). **Two-file atomic** (ROADMAP + CLAUDE.md) — runbook §3.2's "three-file" framing was authored pre-J-129 strip-the-chain discipline; post-strip the JOURNAL.md "chain entry only" instruction maps to no-op (no chain to append to + no body per sibling-shape to J-123/J-124/J-125 chain-only doc-only milestone-event precedent + post-J-130 date already at 2026-05-28). Honest count discrepancy surfaced for resolution at Joe-lock checkpoint #1 — sibling-shape to D-NNN-η's claimed-atomic-file-count surface but at prose-vs-strip-discipline-collision layer rather than git-staging. **Joe-lock checkpoint #1 fires post-ship**: three drift-detection points to confirm (ROADMAP version bump v1.39 → v1.40 + visual tree row ✅; CLAUDE PLAY flip from "Commit 1 against amended v1.1" → "Commit 2 (Commit 1 doc-pass ✅)" ✅; honest two-file-vs-three-file count discrepancy surfaced). After checkpoint #1 approval, **Joe-lock checkpoint #2 fires next** per runbook §2.3: Clair extracts seven-surface Q-tables from design doc `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` v1.2 §2 verbatim + surfaces by name; Joe approves each surface before any production code lands. LOAD-BEARING D-078 application surface; Trigger (a) fires if any named type or method does not exist in production code — but the v1.1 amendment at J-129 already re-aligned surface ordering and file locations against design doc §2 verbatim, so checkpoint #2 should pass cleanly absent fresh drift. **Joe-lock checkpoint #3 fires post-Commit-2**: Clair runs `cargo test -p xgen-common -p xgen-core -p xgen-node --tests`, reports error count; Joe locks single-Commit-2 (absorb sweep) if ≤ ~50 or split (Commit 2 lib-clean + Commit 2a sweep atomic) if > ~50 per Pass 2 + Pass 1 precedent. **Verification rigour at Commit 2 milestone-bearing boundary**: 5 isolated runs (`cargo clean -p xgen-common -p xgen-core -p xgen-node` between each) + 3 consecutive workspace runs of `cargo test -p xgen-common -p xgen-core -p xgen-node` = 8 GREEN minimum + `cargo clippy --lib --all-features -- -D warnings` + `--tests -D warnings` clean. `cargo build --workspace` deliberately broken per Path A inherited from Pass 1 (Pass 5 close restores). **Pass 3 "Honest longer work over fast shortcuts" count: TWO** (J-129 three runbook §4 drifts + J-130 silent gitignore-skip; both prospectively caught at Clair's session-open audits before any production code touched). **Next-active for Clair**: pickup at runbook §4 Commit 2 post-checkpoint #1 + checkpoint #2 approvals. Read CLAUDE.md PLAY block + JOURNAL J-130 entry first per Rule 0, then runbook §4 in order, then design doc §2 verbatim. **Next-active for Chat Claude (standby)**: parallel-eligible items include M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6 (~35 verbs across 7 categories) if Joe selects that as parallel-track work. M6 (new) Node admin write path stays unblocked-but-not-selected. XGID Retrofit Passes 4 + 5 stay PENDING behind Pass 3 close. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-130 entry first per Rule 0**, then runbook §4 Commit 2 against amended v1.1.

⚫ **(historical, superseded by Commit-1-shipped state above) XGID Retrofit Pass 3 implementation ACTIVE — runbook ✅ at J-128 (2026-05-27); v1.0 → v1.1 amended at J-129 + silent-gitignore-skip drift-fix at J-130; Clair pickup at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` §3 Commit 1.** Runbook shipped at Status: ACTIVE v1.0 (~44 KB, nine sections — §1 framing + precedent-departure self-defense / §2 sequence overview + two-commit base + contingent Commit 2a + three Joe-lock checkpoints / §3 Commit 1 doc-pass minimal Option C hybrid / §4 Commit 2 seven-surface retype + per-surface tests / §5 Commit 2a contingent test-fixture sweep / §6 Commit 3 milestone close + DoD / §7 discipline notes nine sub-sections including §7.10 Pass 5 consolidation flag as future-walk candidate / §8 cross-references / §9 footer authoring provenance). **Two-commit base + contingent Commit 2a shape**: Commit 1 doc-pass minimal Option C hybrid (3 files: ROADMAP + CLAUDE PLAY + JOURNAL chain-only — no design-doc touch since Pass 3 design doc already at v1.2 COMPLETED at J-127) + Commit 2 seven-surface retype + per-surface tests atomic (8-12 files per §4.10 enumerating all seven surfaces from design doc §2 Q-tables: Surface #1 `dispatch_event` `Option<&NodeXgid>` borrowed boundary at xgen-core/src/node/runtime.rs; Surface #2 six per-space HashMap keys retype atomic; Surface #3 `handle_federation_incoming` async-spawned at xgen-node/src/federation_session.rs; Surface #4 fanout.rs verification (Pass 1 carry-over); Surface #5 reconnect.rs three spawned functions; Surface #6 app handlers + persistence-format boundary; Surface #7 Appendix D doc-tree sweep) + Commit 2a [CONTINGENT] test-fixture projection sweep firing at Joe-lock checkpoint #3 if `cargo test -p xgen-common -p xgen-core -p xgen-node --tests` error count > ~50 (sibling-shape to Pass 2 Commit 2a `58b94a5` + Pass 1 Commit 4a `4895446` precedent) + Commit 3 milestone close per D-074 (5-6 files). **Three Joe-lock checkpoints**: #1 post-Commit-1 doc-pass drift check (3 of 4 drift-detection points vs Pass 2's four — absent fourth is absence-of-need per D-065 since design doc already COMPLETED at J-127: ROADMAP + CLAUDE PLAY + JOURNAL chain); #2 pre-Commit-2 verbatim surface list approval (Clair extracts the seven-surface Q-tables from design doc §2 verbatim and Joe approves by name before any production code lands; LOAD-BEARING D-078 application surface; Trigger (a) fires here if any named type or method does not exist in production); #3 post-Commit-2 split-trigger decision (Clair runs `cargo test -p xgen-common -p xgen-core -p xgen-node --tests` and reports error count; Joe locks single-Commit-2 if ≤ ~50 or split if > ~50 per Pass 2 + Pass 1 precedent). **Three split triggers documented at §2.2** per Lock 1 enumeration: Trigger (a) non-existent production contract per design doc §2 (sibling-shape to J-113 + J-115 + J-116 canonical-record-staleness pattern; D-078 applies); Trigger (b) harness extension beyond design doc §4.6 fanout-already-covered scope; Trigger (c) family-boundary size split if Commit 2 alone exceeds ~600 lines OR any single surface exceeds ~400 lines. **Per-surface test target +10 enumerated by name at §4.7** (Joe-lock checkpoint #2 approval required): Surface #1 (1 test: `dispatch_event_with_borrowed_node_xgid_projects_to_str_at_callsite`); Surface #2 (3 tests: `noderuntime_per_space_map_insert_retrieve_with_typed_key` + `noderuntime_per_space_map_six_flavours_isolated` + `noderuntime_per_space_map_helper_signatures_typed_at_boundary`); Surface #3 (1 test: `handle_federation_incoming_spawned_task_owns_node_xgid_capture`); Surface #4 (1 test: `fanout_topological_sort_event_xgid_slot_pass_1_intact` sentinel regression); Surface #5 (2 tests: `reconnect_spawned_functions_each_own_typed_capture` + `reconnect_spawned_functions_arc_shared_reference_pattern_when_needed`); Surface #6 (2 tests: `app_handlers_persistence_format_round_trip_string_at_boundary` + `app_handlers_wire_message_destructure_string_at_boundary`); Surface #7 (0 tests — doc-only mechanical edit). Heavier per-surface enumeration than Pass 2's +2 reflects Pass 3's seven-surface scope vs Pass 2's five + structurally novel patterns at async-spawned forced-owned + persistence-format boundary + HashMap-key retype atomic. **Verification rigour 5 isolated + 3 workspace = 8 green runs minimum** at Commit 2 milestone-bearing boundary per §4.9; package-scoped to `xgen-common` + `xgen-core` + `xgen-node` per Path A inherited from Pass 1 (`cargo build --workspace` deliberately broken between Commits 2 and Pass 5 close; honest intermediate state, not a regression). **§7 discipline-notes nine sub-sections** (Pass 2 had eight; §7.6 format-boundary preservation unified + §7.10 Pass 5 consolidation flag are Pass-3-specific): §7.1 precedent-departure self-defense; §7.2 Pass-internal-consistency over trilogy-internal-consistency; §7.3 contingent-split honesty; §7.4 Borrow<str> load-bearing at HashMap-key retype boundary; §7.5 async-spawned task captures force owned parameters (Tokio idiom); §7.6 format-boundary preservation unified (wire + persistence); §7.7 "honest longer work" Pass 3 count starts at zero; §7.8 layered-B3 expected null per Pass-arc pattern durability; §7.9 D-069 audit-vs-design boundary; **§7.10 [FUTURE-WALK CANDIDATE] Pass 5 consolidation of runbook §7 across the five-Pass arc** (per Joe's "optimize it in some time point" guidance — proposes consolidating §7 discipline-notes across all five per-Pass runbooks into a single `docs/XGID_RETROFIT_DISCIPLINE.md` reference doc at Pass 5 milestone close; each per-Pass runbook's §7 becomes a 3-5 line pointer + per-Pass deltas only; historical record preserved via Git history; ~30-40% size reduction at Pass 6+ if XGID retrofit family extends; promotion fires at Pass 5 milestone close if Joe locks the consolidation pattern). **Honest data point on runbook size**: came in lighter at ~44 KB than the ~50-70 KB target estimated at design close — honest framing per D-065: §4.7 per-surface tests enumerated 10 tests in tighter shape than predicted; §4 + §5 narrative tighter than Pass 2 precedent. Pass-internal-consistency framing accepts the band. **Pre-Clair audit posture deferred to Clair's pickup time** per Joe-lock — sibling-shape to J-120's Pass 1 audit pattern + J-125's Pass 2 audit (six-dimension audit pattern's third project-arc instance pending at Clair's pickup); Clair runs the audit at Pass 3 implementation kickoff session and surfaces drift if any. **Next-active for Clair**: pickup at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` §3 Commit 1 (doc-pass minimal Option C hybrid). Read CLAUDE.md PLAY block + JOURNAL J-128 entry first per Rule 0, then runbook §1–§3 in order, then design doc `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` v1.2 §2 Q-tables verbatim (Joe-lock checkpoint #2 requires verbatim surface list approval before any production code touches). **Next-active for Chat Claude**: standby until Clair's Commit 1 closes affirmatively at Joe-lock checkpoint #1; parallel-eligible items include M6 (new) Block 4 verb-by-verb walks at `docs/xgen_node_admin_ops_design.md` §6 (~35 verbs across 7 categories) if Joe selects that as parallel-track work. M6 (new) Node admin write path stays unblocked-but-not-selected. XGID Retrofit Passes 4 + 5 stay PENDING behind Pass 3 close. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-128 entry first per Rule 0**, then runbook §3 Commit 1.

⚫ **(historical, superseded by runbook-shipped state above) XGID Retrofit Pass 3 design ✅ at J-127 (2026-05-27, previous commit); runbook authoring next-active for Chat Claude + Joe in a fresh session.** Design doc shipped at `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` Status: COMPLETED v1.2 (~75 KB). **Full seven-surface walk closed across two same-day design sessions**: Surfaces #1-#4 at v1.1 (morning) — NodeRuntime six per-space HashMap keys; dispatch_event peer_node_id parameter; federation_session.rs handler identifier slots; fanout.rs handler identifier slots; Surfaces #5-#7 + §4.3 consolidation + §6 + §7 fills at v1.2 (afternoon) — app.rs handler identifier slots; reconnect.rs scheduler identifiers; Appendix D doc retypes. **Single governing principle (§3) confirmed inherited from Pass 2 unchanged across the full seven-surface walk** — zero wrinkles. **Six architectural decisions locked at §4.1-§4.6**: §4.1 six per-space HashMap keys retype shape (D-NNN-γ at 2 instances) + §4.2 dispatch_event Option<&NodeXgid> borrowed boundary + sibling-shape rule table extended at v1.2 with async-spawned-task-captures sub-rule (instantiated at Surface #5 handle_federation_incoming + Surface #6 reconnect.rs three spawned functions) + §4.3 format-boundary preservation (wire OR persistence) **consolidated at v1.2** from v1.1's wire-only framing (Surface #5 walk surfaced structurally identical persistence-format boundary slots — filesystem path generation + on-disk JSON HashMap + replay_spaces_from_dir + wire-message destructure; same principle, same reasoning, both at I/O byte-serialisation boundaries; consolidated under one §4.3 rather than splitting into §4.3-wire + §4.7-persistence per sibling-shape to D-076 v1 → v1.1 amend-in-place reasoning; one decision = one drift surface) + §4.4 forced-owned return shape rule + §4.5 ClientSenders + FederationPeerSenders Pass 3 scope (v1.0 → v1.1 correction; xgen-node-internal types, never cross to xgen-client) + §4.6 topo-sort &str slot at fanout.rs:193 already covered at Pass 1 Commit 3 (J-097 anticipated retype inherent in Pass 1's Option<EventXgid> retype). **Three candidate D-NNNs flagged-not-promoted per D-069**: D-NNN-γ small-cardinality vs large-cardinality identifier-keyed maps per-Pass call-site density (2 instances); D-NNN-δ format-boundary preservation wire+persistence (2 instances; three-instance threshold opens at Pass 4 if client-side serialisation-format slot — Tauri IPC, AI control protocol over HTTP, gRPC — instantiates); D-NNN-ε async-spawned task captures force owned (3 instances at same xgen-node module-family surface; per D-065 honest framing the rule is a Rust language idiom (`'static` bound on `tokio::spawn`) not a XGen project decision, so DECISIONS.md promotion deferred; promotion-watch opens at Pass 4 surfacing structurally different fourth at xgen-client async surfaces). **Layered-B3 (§5.5) confirmed null at full seven-surface scope** — third Pass-arc instance after Pass 1 J-122 + Pass 2 J-126; pattern's durability at three instances now matches D-077/D-078 promotion-threshold framing; pattern established at Pass-arc layer that identifier-slot retype scopes don't surface layered-B3 because `Borrow<str>` projection handles type-projection at boundaries uniformly. **§6.1 historical-pointer filled in Shape α** (pointer-style sibling to Pass 2 §6.7). **§7 discipline-notes consolidated with five sub-sections** (§7.1 format-boundary preservation unified pattern + §7.2 async-spawned task captures sub-rule + §7.3 forced-owned return shape rule + §7.4 xgen-node-internal type confusion v1.0 framing data point + §7.5 doc-tree sweep classification-vs-content-shape gap at Appendix D Surface #7). **Two-session design walk shape acknowledged as Pass-internal precedent**: Pass 2 design close at J-123 fit single session; Pass 3 spanned two same-day sessions with foundational-first (#1-#4) + top-of-stack-second (#5-#7) split. Pattern flagged at §8(c) of J-127 body entry: future Pass-arc design walks with > 5 surfaces should consider two-session split as deliberate scaffolding rather than honest-longer-work recurrence. **Honest data point on design doc size**: design doc came in at ~75 KB, heavier than v1.1 §1.2's ~30-40 KB estimate. Drivers: §4.3 consolidation reasoning + §7 five sub-sections + Surfaces #5/#6/#7 Q-tables fuller than anticipated. Pass-internal-consistency framing accepts the lighter-than-trilogy ~80-100 KB band, Pass 3 lands mid-band rather than at lighter end Pass 2 hit (~31 KB). **Next-active for Chat Claude + Joe**: runbook authoring at `tasks/XGID_RETROFIT_PASS_3_IMPL.md` in a fresh session per Pass 2 + trilogy precedent. **Target runbook shape** (locked at this design close): three-commit base sibling-in-shape lighter than Pass 1's six-commit shape per Pass-internal-consistency framing (Commit 1 doc-pass; Commit 2 xgen-node + xgen-core HashMap-keys retype atomic across all seven surfaces; Commit 3 milestone close per D-074); contingent Commit 2a (test-fixture projection sweep) firing at Joe-lock checkpoint #3 if surface error count > ~50 — sibling-shape to Pass 2's pre-locked contingent posture per D-065 honest framing; three Joe-lock checkpoints (#1 post-Commit-1 doc-pass drift check; #2 pre-Commit-2 verbatim surface list — Chat Claude extracts the seven-surface Q-tables from design doc §2 verbatim and Joe approves by name before code lands; #3 post-Commit-2 split-trigger decision); runbook target ~50-70 KB (heavier than Pass 2's ~43 KB given Pass 3's seven-surface scope vs Pass 2's five). **Next-active for Clair**: stood down until runbook authoring closes. M6 (new) Node admin write path stays unblocked-but-not-selected (parallel-eligible Block 4 verb-by-verb walks remain available for Chat Claude + Joe). XGID Retrofit Passes 4 + 5 stay PENDING behind Pass 3 close. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-127 entry first per Rule 0**, then `tasks/XGID_RETROFIT_PASS_3_DESIGN.md` v1.2 (start with §2 surface enumeration + §4 architectural decisions + §6.1 + §7 discipline-notes), then runbook authoring at `tasks/XGID_RETROFIT_PASS_3_IMPL.md`.

⬛ **(historical, superseded by Pass 3 design close above) Standby for next-milestone selection at session open.** XGID Retrofit Pass 2 milestone CLOSED at J-126 (2026-05-27, this commit). Pass 3 (xgen-node + Appendix D) + M6 (new) Node admin write path are both unblocked-but-not-selected and ready for selection. Sequencing is Joe's call at session open. Pass 3's runbook authoring is the next Chat Claude work-shape on the XGID retrofit track if Joe picks Pass 3 first; M6 (new) Block 4 verb-by-verb walks (~35 verbs across 7 categories at `docs/xgen_node_admin_ops_design.md` §6) are parallel-eligible. **Entry point for the next session: CLAUDE.md PLAY block + JOURNAL J-126 entry first per Rule 0**, then whatever document Joe pointed the session at for milestone selection.

⬛ **(historical, superseded by milestone-closed state above) XGID Retrofit Pass 2 implementation ACTIVE — Commit 1 doc-pass ✅ at J-125 (2026-05-27); Clair pickup at runbook §4 Commit 2 (Joe-lock checkpoint #2 fires first — pre-Commit-2 verbatim surface list approval).** Commit 1 (this commit) shipped the doc-pass: design doc Status ACTIVE → COMPLETED v1.0 → v1.1 + new §6.7 Shape α entry (pointer-style); ROADMAP v1.34 → v1.35; CLAUDE PLAY flip; JOURNAL J-125 header chain entry; runbook header chain. **Pre-Clair audit at Pass 2 implementation kickoff confirmed CLEAN across six dimensions** (sibling-shape to J-120 Pass 1 audit pattern, sixth-dimension audit shape now durable at second project-arc recurrence): file paths resolve / type shapes at named anchors hold across all five Pass 2 surfaces / Pass 1 carry-overs intact (Borrow<str> on Xgid + flavour wrappers + 33 inline "Pass 2 widens" markers across xgen-core code) / contingency surfaces enumerated (peer_urls insert site at runtime.rs:181) / parallel-milestone drift since J-124 = none / test baseline 489 = 34 lib + 8 invariance + 447 xgen-core matches J-122 exactly + clippy `-D warnings` clean. **Joe-lock checkpoint #1 fires after this commit lands** per runbook §3.4: four drift-detection points confirmed (design doc Status flip ✅; ROADMAP version bump + visual tree ✅; CLAUDE PLAY flip ✅; new design-doc §6.7 entry ✅). **Joe-lock checkpoint #2 fires next** per runbook §4.1: before any production code touches Clair surfaces the verbatim surface list from design doc §2 + Joe approves by name. **Joe-lock checkpoint #3 fires post-Commit-2 lib retypes verify clean**: Clair runs `cargo test --tests`, reports test-fixture error count; Joe locks single-Commit-2 (absorb sweep) or split (Commit 2 lib-clean + Commit 2a sweep atomic) per the ~50 threshold heuristic. **Verification rigour at Commit 2 milestone-bearing boundary**: 5 isolated runs (cargo clean between) + 3 workspace runs of `cargo test -p xgen-common -p xgen-core` = 8 green minimum; `cargo clippy --lib --all-features -D warnings` + `--tests -D warnings` clean. `cargo build --workspace` deliberately broken per Path A inherited from Pass 1 (Pass 5 close restores). **Entry point for Clair: `tasks/XGID_RETROFIT_PASS_2_IMPL.md`** §4 Commit 2 (read CLAUDE.md PLAY block + JOURNAL J-125 + design doc §2 Q-tables first per Rule 0). **Entry point for Chat Claude (no active work)**: standby until Clair's Commit 2 closes affirmatively at Joe-lock checkpoint #3; parallel-eligible items M6 (new) Block 4 verb-by-verb walks (~35 verbs at `docs/xgen_node_admin_ops_design.md` §6).

⬛ **(historical, superseded by implementation-active state above) XGID Retrofit Pass 2 design ✅ at J-123 (2026-05-27); runbook authoring next-active for Chat Claude + Joe.** Design doc shipped at `tasks/XGID_RETROFIT_PASS_2_DESIGN.md` Status: ACTIVE v1.0 (~31 KB, seven sections). Five surfaces walked (validate_event + ValidationOutcome at xgen-core/src/message/exchange.rs; NodeRuntime::dispatch_event + DispatchOutcome at xgen-core/src/node/runtime.rs; PendingBuffer arrival hooks at xgen-core/src/dag/pending.rs; FederationRegistry / IdentityRegistry APIs at xgen-core/src/identity/registry.rs + federation/; accept_message at runtime.rs). **Single design principle locked at §3**: identifier slots retype to typed XGIDs; descriptive-string slots stay String; internal variables bind as typed references; &str projection happens at the call-site boundary via Borrow<str> (Pass 1's additive API at Commit 4 implementation-kickoff lock); no Deref<Target = str> shortcuts. **Two architectural decisions at §4**: Q2.8 partial HashMap retyping (node_id + peer_urls retype now; six per-space HashMap keys defer to Pass 3 with code-comment flag per call-site-density heuristic — small-cardinality identifier-keyed maps retype in their own Pass, large-cardinality per-space maps defer to the Pass that touches their primary call-site crate; sibling-shape to D-067 no-drift-surface discipline at code-organisation layer); Q5 validate_steps_8_13 + accept_event deprecate-and-retype-minimally per D-071 own-arc discipline (own audit-design-impl arc for removal; surface-driven application per D-071 means the arc stays flagged-not-promoted until dependent work surfaces concrete drift or Joe locks a milestone for it; candidate inclusion: accept_message if Surface #5 Q5.4 audit confirms). **§5 honest-broadening discipline enumerates deferred surfaces with their Pass-of-record**: Pass 3 (xgen-node + Appendix D — six per-space HashMaps, dispatch_event peer_node_id parameter, federation_session/fanout/app handler identifiers, reconnect scheduler); Pass 4 (xgen-client + AI control docs — ops::* layer, AiBehavior trait, AiPacingTracker, session state, AI service, Tauri commands); Pass 5 (test fixtures, integration helpers, trace field types, log formatters, Debug/Display impls); own future audit-design-impl arc per D-071 (validate_steps_8_13 + accept_event removal; candidate accept_message inclusion). **Layered-B3 audit answer null** (codified at design doc §5.5) — no layered surfaces detected during the five-surface walk; sibling-shape to Pass 1's milestone-close finding at J-122. **Three precedent departures honestly framed at design doc §1.2** vs trilogy precedent: (a) ~31 KB vs ~80-100 KB trilogy band; (b) no §11 re-walk reservation; (c) one sole governing principle vs trilogy's multiple Joe-locks. **Inline-lock pattern fourth Pass-arc recurrence** (codified at design doc §7.2). **Pass-internal-consistency over trilogy-internal-consistency** codified at §7.7. **Next-active for Chat Claude + Joe**: implementation runbook authoring at `tasks/XGID_RETROFIT_PASS_2_IMPL.md` in a fresh session per trilogy + Pass-1 split-session precedent. **Target runbook shape** (locked at design close): 4-commit base sibling-in-shape lighter than Pass 1's six-commit shape per Pass-internal-consistency framing (Commit 1 doc-pass; Commit 2 xgen-core algorithm-bearing function retypes all five surfaces in one atomic per shared Borrow<str> projection mechanic and lower internal coupling than Pass 1's lib + test split; Commit 3 milestone close per D-074); contingent Commit 2a (test-fixture projection sweep) firing at Joe-lock checkpoint #3 if surface error count > ~50 (Pass 1's split was Joe-locked mid-implementation when ~296 fixture errors surfaced; Pass 2 surfaces are smaller and Borrow<str> absorbs most projection cost, so contingent posture is the expected-shape default); three Joe-lock checkpoints (#1 post-Commit-1 doc-pass drift check; #2 pre-Commit-2 with verbatim surface-list from design walk; #3 post-Commit-2 split-trigger decision); runbook target ~40-50 KB lighter than Pass 1's ~65 KB. **Next-active for Clair**: stood down until runbook authoring closes. M6 (new) Node admin write path stays unblocked-but-not-selected (parallel-eligible Block 4 verb-by-verb walks remain available for Chat Claude + Joe). XGID Retrofit Passes 3 + 4 + 5 stay PENDING behind Pass 2 close. **Entry point for next session: CLAUDE.md PLAY block + JOURNAL J-123 entry first per Rule 0**, then `tasks/XGID_RETROFIT_PASS_2_DESIGN.md` for runbook authoring.

⚫ **(historical, superseded by Pass 2 design close above) Standby for next-milestone selection at session open.** XGID Retrofit Pass 1 milestone CLOSED at J-122 (2026-05-26, this commit). Pass 2 (xgen-core algorithm-bearing functions) + M6 (new) Node admin write path are both unblocked-but-not-selected and ready for selection. Sequencing is Joe's call at session open. Pass 2's runbook authoring is the next Chat Claude work-shape on the XGID retrofit track if Joe picks Pass 2 first; M6 (new) Block 4 verb-by-verb walks (~35 verbs across 7 categories at `docs/xgen_node_admin_ops_design.md` §6) are parallel-eligible. **Entry point for the next session: CLAUDE.md PLAY block + JOURNAL J-122 entry first per Rule 0**, then whatever document Joe pointed the session at for milestone selection.

🟢 **(historical, superseded by milestone-closed state above) XGID Retrofit Pass 1 implementation ACTIVE — SELECTED 2026-05-25 at J-120 over M6 (new); Clair pickup at runbook §3 Commit 1.** Runbook at `tasks/XGID_RETROFIT_PASS_1_IMPL.md` Status ACTIVE v2.0 (six-commit sequence: Commit 1 canonical-form module move + Commit 2 convenience constructors + Commit 3 xgen-common retypes + Commit 4 xgen-core retypes + Commit 5 Appx C + Appx I retypes + Commit 6 milestone close). Cross-crate scope spans xgen-common (Event field retypes, SpaceLocalMetadata.space_id, state.rs observability structs) + xgen-core (SpaceState/RoomState/SpaceMember/PendingInvite; FederationRegistry/PeerOperationalRecord; IdentityRegistry; PendingBuffer). Coordinated Appx C + Appx I doc retype shipped in same commit set per Phase 2 doc-tree sweep coordination flag. Five invariance tests pinned by name (Tests A–E at sub-question 4 lock). **Pre-Clair audit at session open** (verbal-only seal per Joe-lock α, recorded in JOURNAL J-120 Sub-section 2) confirmed ZERO drift between runbook v2.0 (authored 2026-05-21) and current main (post-J-119, 627 tests) across six dimensions: file paths / type shapes / v1 carry-overs / named structs + TrustAssertion contingency / parallel-milestone drift / test-count baseline. **TrustAssertion struct does NOT exist in Rust today** — runbook's §Commit 2 contingency confirms `TrustAssertionXgid::from_assertion` defers to Pass 2 with code-comment flag (three from-Event constructors `from_event`/`from_space_create`/`from_room_create` ship at Commit 2). All four sub-questions still apply cleanly. Federation visual-tree drift cleanup folded into the PLAY-flip atom per Joe-lock β + D-077 backward-coherence — two-event accumulated drift at lines 68/82/91/116 closed alongside Pass 1 row 🟡 → 🟢. **Honest-broadening discipline applies**: Pass 1 retypes data-structure fields only; method-signature widening belongs to Pass 2 (consumers project via `.as_str()` per sub-question 3 lock with code-comment discipline at non-trivial sites). **Commit 4 honest verification state**: `cargo build --workspace` will be broken between Commit 4 and Pass 2 (xgen-node + xgen-client consume retyped xgen-core types); honest intermediate state, not a regression — surface to Joe at Commit 4 implementation kickoff for a decision between holding the pure-scope discipline (workspace-broken intermediate) and folding a thin bridging-shim into Pass 1's scope (workspace-green close). Pass 2/3/4/5 ship downstream against Pass 1's typed data structures. **Entry point for Clair: `tasks/XGID_RETROFIT_PASS_1_IMPL.md`** (read CLAUDE.md PLAY block + J-120 entry first per Rule 0, then runbook §1–§3, then proceed to Commit 1). **Entry point for Chat Claude (no active Pass 1 work)**: parallel-eligible items M6 (new) Block 4 verb-by-verb walks; future-walk of candidate D-NNN "ingest path invariant encoding" if Joe locks it.

⬛ **(historical, superseded by milestone-closed state below) Federation Event Propagation Phase 9 Commit 3b-4 runbook ✅ at J-114 — Clair pickup at Joe-lock checkpoint #1 next.** Implementation runbook SHIPPED 2026-05-24 at J-114 at `tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md` Status: ACTIVE v1.0 (~57.5 KB, eight sections — §1 framing + §2 sequence overview + §3 Commit 1 single-or-split shape + §4 Scenario 4 detail + §5 compounds C5/C7/C9/C10 + §6 verification + DoD + freeze sites + §7 discipline notes + §8 cross-references). Sibling-in-shape to `tasks/FEDERATION_TOPOSORT_IMPL.md` (COMPLETED v1.2, ~93 KB) and `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` (COMPLETED v1.2, ~95 KB). Runbook came in lighter than the ~80–100 KB precedent target band at ~57.5 KB — honest framing per D-065: Phase 9 Commit 3b-4 is test-only (no production code touched, unlike topo-sort's Path B or persistence-amendment's Result-propagation) AND the substantive scoping work (Scenario 4 enumeration; production reject-path inventory) was already done at J-113. **D-078 promoted in same atom** (Path B.i — "Production-grounded test enumeration" as protocol-test-layer sibling to D-077 meta-layer; three-instance threshold met J-099 audit-doc §11 + J-109 survey §2.6 + J-113 survey §2.4). **Five-file atomic commit per D-074 (fourteenth instance) + Lock #3 per-commit cadence + D-078 promotion atom**: (1) `tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md` NEW ACTIVE v1.0; (2) `DECISIONS.md` D-078 entry promoted + header chain; (3) `JOURNAL.md` J-114 entry + header chain; (4) `CLAUDE.md` PLAY block flip + header chain; (5) this ROADMAP version bump v1.28 → v1.29 + Past entry + header chain. **Five Joe-locks at runbook-authoring session** (full at runbook §1.1): (1) Single-commit-with-split-trigger-discipline shape — Commit 3b-4 frames as single atomic commit with three split triggers documented at runbook §2.2: Trigger (a) non-existent production contract (J-113 shape; canonical-record amendment FIRST); Trigger (b) harness extension beyond CounterLayer (J-111 shape); Trigger (c) family-boundary size split (Scenario 4 alone > ~600 lines OR single compound > ~400 lines; family-boundary, NOT arbitrary line count); sibling-shape to J-111 retrospective 3b-3-pre + 3b-3 split pattern. (2) D-078 promoted in this atom. (3) Verification rigour 5 isolated + 3 workspace = 8 green runs minimum at milestone-bearing commit. (4) Family-uniformity + variant-uniformity as load-bearing structural properties per findings §2.4.1. (5) §7 discipline-notes section with seven sub-sections; precedent-departure self-defense at §7.1. **Five Joe-lock checkpoints for Clair** (full at runbook §2.3): #1 post-runbook-read drift check; #2 pre-implementation production-code verification per D-078 (LOAD-BEARING D-078 application surface; Trigger (a) fires here, not later); #3 pre-implementation harness-extension assessment; #4 the load-bearing checkpoint — Joe-lock test enumeration BY NAME for all five families; #5 post-implementation pre-verification. **Two open questions surfaced at runbook §5 for checkpoint #2**: C7 + C10 file location (xgen-node/src/tests/ vs xgen-core/src/node/tests/ — conservative posture is xgen-node-side per their assertion targets, surface at checkpoint #2). The runbook explicitly applies D-078 to its own open questions — where it can't lock without surfacing first, records the unknown and routes through D-078's checkpoint. **Sibling-in-shape fifth recurrence at this commit**: Phase 7.5 first (J-093/J-104); bidirectional federation_nodes second (J-096); topological-sort third (J-097–J-101); persistence-amendment sub-amendment fourth (J-104–J-108); Phase 9 Commit 3b arc fifth (J-110–this J-114/upcoming J-115). **"Honest longer work over fast shortcuts" — count inherited at eighth from J-104, NOT incremented** at this runbook-authoring close. **What this commit does NOT close.** Phase 9 milestone stays PLAY (Clair's next-active is Commit 3b-4 per the runbook; after Commit 3b-4 ships, Commit 3b-5 milestone close per D-074 remains in the §3.0a five-commit sequence — that's the close commit that flips Phase 9 PLAY → DONE). Federation Event Propagation milestone stays PLAY. M6 (new) + XGID Retrofit Pass 1 stay PENDING. **α.a cascade-close honest framing per D-065.** This Path-(c) atomic also backfills four prior ROADMAP visual-tree updates (J-110 Commit 3b-2 ✅ row + J-111 Commit 3b-3-pre ✅ row + J-112 Commit 3b-3 ✅ row + cross-cutting D-077 row missing since J-107 promotion) plus the J-114 row that this commit's prose enumerates. The cascade was the **sixth recurrence of the prose-then-batch atomicity-slip family** (J-094 / J-098 / J-099-eighth-file / J-100 line-1140-retroactive / J-101 line-1140-retroactive-freeze first through fifth; this J-114 cascade-as-single-event sixth). Counted as cascade-as-single per Joe-lock at session-open finding-surface: all four slips share root cause (tree updates described in prose but not written to disk in the same `edit_file` call across four consecutive atomics), surfaced together at this session-open verification, sibling-shape to D-077's three-instance threshold framing rather than four-separate-instances framing. **Entry point for Clair: `tasks/FEDERATION_PROPAGATION_PHASE_9_COMMIT_3B_4_IMPL.md`** (read CLAUDE.md PLAY block + J-114 entry first per Rule 0, then runbook §1–§3; surface at checkpoint #1 post-read drift). **Entry point for Chat Claude's next session (no active work): standby** until Clair's Commit 3b-4 arc closes. If Trigger (a) fires at checkpoint #2 surfacing canonical-record gap, Track 1 amendment session opens — would be first prospectively-caught canonical-staleness instance since D-078 promotion. Parallel-eligible items unchanged: M6 (new) Block 4 verb-by-verb walks; future-walk of candidate D-NNN "ingest path invariant encoding" if Joe locks it.

✅ **Federation Event Propagation Phase 9 Commit 3b-3 — SHIPPED at J-112; Commit 3b-3-pre SHIPPED at J-111; Commit 3b-2-equivalent SHIPPED at J-110.** (See Past section's "Phase 9 Commit 3b-3 SHIPPED" + "Phase 9 Commit 3b-3-pre SHIPPED" + "Phase 9 Commit 3b-2-equivalent SHIPPED" paragraphs.) Workspace test count trajectory: 592 → 596 (J-110 +4) → 599 (J-111 +3 counter unit tests) → 600 (J-112 +1 Compound C2 test). J-113 amendment landed v1.3 of survey findings to revise Scenario 4 enumeration from 30 → 20 tests under amended contract (timestamp-bound forgery variants dropped per non-existent production contract).

🟢 **(historical, superseded by runbook-shipped state above) Federation Event Propagation Phase 9 Commit 3b-2-equivalent — RESUMES at J-108 after persistence-amendment milestone close.** Scope: Scenarios 2 (three-Node anti-transitivity — note `phase9_three_node_anti_transitivity.rs` shipped at persistence-amendment Commit 3 may activate Scenario 2 alongside Scenario 3; Clair confirms at Commit 3b-2-equivalent open) + compound scenarios C2/C5/C7/C9/C10 per existing Q4 Lock from J-091 + Phase 9 §3.0 revised five-commit shape with C3 dropped to `tasks/FEDERATION_STRESS_FOLLOWON.md`. Expected ~4-6 atomic commits in their own sequence. Q3 escalation criterion from J-091 still applies. After Phase 9 Commit 3b-2-equivalent ships, Phase 9 milestone flips PLAY → DONE, Federation Event Propagation milestone flips PLAY → DONE, and M6 (new) + XGID Retrofit Pass 1 unblock simultaneously. **Entry point for Clair: `tasks/FEDERATION_PROPAGATION_PHASE_9.md`** (Status ACTIVE v1.1, RESUMED at J-108 — read CLAUDE.md PLAY block + J-108 entry + J-107 entry first per Rule 0, then Phase 9 task file §3.0 revised five-commit shape, then Commit 3b-2-equivalent scoping).

⬛ **Persistence-amendment sub-amendment milestone — CLOSED 2026-05-24 at J-108** (see Past section's "Persistence-amendment sub-amendment implementation milestone CLOSED" paragraph for the five-commit + Track 1 re-walk + Commit 4 milestone-close shipped state). Ten-file atomic commit per D-074 (eleventh instance — honest count after staging; runbook §6.2 "approximately twelve files" estimate counted two slots that resolved to NOT-in-this-commit per D-077 backward-coherence audit). Sentinel-tree four files at `xgen-node/src/tests/` are the activating integration-level regression lock for D-077 + Q1+Q2+Q3 locks. Grep guardrail scope discipline codified at JOURNAL J-108 sub-section 8 as fifth project instance of surfacing-gap-becomes-codified-discipline pattern.

🟢 **(historical, superseded by milestone-closed state above) Persistence-amendment sub-amendment milestone — RUNBOOK ✅ at J-106, Clair pickup at Commit 1 next.** Implementation runbook SHIPPED 2026-05-23 at J-106 at `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` Status: ACTIVE v1.0 (~95 KB, eight sections, inside ~80-100 KB target band; sibling-in-shape to `tasks/FEDERATION_TOPOSORT_IMPL.md` COMPLETED v1.2). Four-file atomic commit per D-074 ninth instance: runbook NEW ACTIVE v1.0 + JOURNAL J-106 + CLAUDE.md PLAY block flipped + this ROADMAP.md v1.20 → v1.21. Landed atomic at first attempt rather than as fix-up housekeeping atom per the J-098 + J-099 + J-100 prose-then-batch family of slip-and-correct precedents now being avoided by writing each companion file to disk + verifying via `Filesystem:get_file_info` before the next. **Six runbook-structural Joe-locks baked into runbook prose at §1.1 + §2.3**: (1) Five-commit shape (1 doc-pass / 2 Q1 ingest-path / 2a Q2+Q3 dispatch+persist / 3 sentinel+verify / 4 close); (2) Five Joe-lock checkpoints (#1 post-Commit-1 doc-pass drift / #2 pre-Commit-2 unit-test list / #3 post-Commit-2 / pre-Commit-2a primitive shape / #4 pre-Commit-2a verbatim code-comment block with rungs-list bullet / #5 post-Commit-2a / pre-Commit-3 sentinel-tree refinement scope); (3) Verification rigour 5 isolated + 3 workspace = 8 green runs minimum at Commit 3; (4) Sentinel-tree refinement folded into Commit 3 with refinement-vs-rework distinction at §5.2; (5) §15 row in Commit 1 with J-NNN placeholder freezing at Commit 4; (6) §7 discipline notes section with six sub-sections (§7.1–§7.7); §7.8 skipped per runbook-authoring §7 lock. **Two pre-draft code-trace findings shaped §4 + §4a narrow scope**: (a) Q1 covers ONLY `graph.add_event` Result-handling at runtime.rs line ~210, NOT the other four silent-discard sites in `ingest_event` (event_id-missing-return; store.insert silent; two state.apply_event silents) — those four sites belong to design doc §8 candidate D-NNN "ingest path invariant encoding" future-walk question, NOT this milestone's scope; (b) Recursive drain pattern is Shape β2 (each drain helper returns `Vec<Event>`; `dispatch_event` aggregates via concatenation; `process_inbound` persists initial event + iterates `additional_persisted` for drained events) — chosen over Shape β1 (accumulator threaded through recursion) on five grounds at §4a.4. **Seven additional pre-draft code-trace findings surfaced inline at §4 + §4a**: (1) `ingest_event` has five silent-discard sites total; (2) only one production caller in xgen-core (`dispatch_event` at line ~559); (3) xgen-node has its own caller via `replay_spaces_from_dir` at `xgen-node/src/app.rs:2628` — Commit 2 is therefore multi-crate atomic; (4) three drain helpers already silently discard at lines ~643/680/720; (5) `process_inbound` at xgen-node/src/app.rs:~1500 needs `additional_persisted` handling + sibling block in `handle_identity_replicate_msg`; (6) `GraphError` visibility check needed (likely needs `pub` widening); (7) `topological_sort` re-export from xgen-core::node::runtime needed as `pub` per D-067 + D-076 no-drift-surface family. **Verbatim code-comment block locked at Joe-lock checkpoint #4** (runbook §4.3, four structural elements + rungs-list bullet): reference to candidate D-NNN flag at JOURNAL J-105 + design doc §8; silent-discard-pattern-served-two-masters framing; rung-above-(a).iii.β three-line bullet (ValidatedEvent wrapper / sealed traits + visitor pattern / formal verification); narrow-scope note ("do not broaden without Joe-lock at future audit phase"). **One MCP tool-call discipline data point**: three-Filesystem-write sequence verified each file on disk via `Filesystem:get_file_info` between writes per J-098 + J-099 prose-then-batch discipline; second write's first attempt returned `Input validation error: path expected string, received undefined` because Chat Claude omitted `path` parameter; single-response recovery (retry with `path` parameter populated landed cleanly); distinct in shape from J-098's prose-then-batch slip family — single-tool-call schema error recovered within same response without fix-up atom commit needed; recorded per D-065 honest framing; no persistent canonical-record impact. **Sibling-in-shape fourth recurrence count**: Phase 7.5 first (J-093/J-104); bidirectional federation_nodes second (J-096); topological-sort third (J-097/J-098/J-099/J-100/J-101); this milestone fourth (J-104/J-105/J-106/upcoming milestone-close). **D-074 application count: ninth instance**. **Layered-B3 recurrence count: still second project-wide instance** (no increment at runbook-landing event). **"Honest longer work over fast shortcuts" — count inherited at eighth from J-104, NOT incremented at this runbook-authoring close** (within-milestone event, not new milestone-surface event). **Next-active for Clair: pickup at Commit 1** of the five-commit sequence per runbook §3. Sentinel working tree (four files at `xgen-node/src/tests/`: phase9_harness.rs, phase9_three_node_anti_transitivity.rs, phase9_drop_and_recover.rs, mod.rs) remains uncommitted as verification artifact for milestone close per Q4(a). Do NOT `git restore`. **Entry point for Clair: `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md`** (read CLAUDE.md PLAY block + J-106 entry first per Rule 0, then runbook §1–§3, then Commit 1 doc-pass). **Entry point for Chat Claude's next session (no active work): standby** until Clair's five-commit sequence closes. Below is preserved historical record of the prior persistence-amendment design-close PLAY state at J-105:

⬛ **(historical, superseded by runbook-shipped state above) Persistence-amendment sub-amendment milestone — DESIGN phase ✅ at J-105, implementation runbook authoring next-active for Chat Claude + Joe.** Sibling-shape to topological-sort + bidirectional + Phase 7.5 sub-amendment arcs (fourth recurrence of the audit → design → implementation pattern per D-071 within Federation Event Propagation milestone scope). Sub-amendment milestone opened at J-104 after Phase 9 Commit 3b-1 implementation surfaced the drain-without-persist gap at the `xgen-core` ↔ `xgen-node` layer boundary during Scenario 3 (three drain hooks at `xgen-core/src/node/runtime.rs:670-682` + ~750 + ~799 re-dispatch released events INSIDE `dispatch_event` and silently drop the Accepted outcome via `let _ = self.dispatch_event(...)`; `xgen-node/src/app.rs::process_inbound` only persists the explicitly-passed event, never the drained ones; on Node restart `replay_spaces_from_dir` only sees the persisted events, and `graph.add_event` silently rejects events whose predecessors aren't in store — the second silent-error gap; in-flight relationship state un-replayable). Audit doc shipped at J-104 at `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT.md` (Status: ACTIVE v1.0; COMPLETED v1.1 at design-phase close J-105). **Four Joe-locks at J-105 design-close, recorded in `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md` (NEW, ACTIVE v1.0):** **Q1 → (a).ii + (a).iii.β + candidate D-NNN flag** (sort-on-replay in `replay_spaces_from_dir` at `xgen-node/src/app.rs:2628` as defensive layer + `ingest_event` signature change at `xgen-core/src/node/runtime.rs:156` to `pub fn ingest_event(&mut self, event: Event) -> Result<(), GraphError>` for compiler-forced caller handling at all sites + candidate D-NNN "ingest path invariant encoding" flagged per D-069 audit-vs-design boundary discipline, NOT promoted to DECISIONS.md at this design close; goes through its own audit → design → impl arc in future session-arc if Joe locks the candidate as worth pursuing; sibling-shape to D-076's v1 → v1.1 progression at different scope); **Q2 → (a) return-vector** (`dispatch_event` returns `DispatchOutcome::Accepted { new_joiner, additional_persisted: Vec<Event> }`; drain helpers return their drained Accepted events to dispatch_event; aggregation into vector; `process_inbound` iterates and persists each; layer separation preserved — xgen-core stays I/O-free; pairs cleanly with Q1's Result chain); **Q3 → all three drain helpers** (`drain_pending_uniform` line ~670 Phase 4 / F-4a, `drain_pending_by_identity` line ~745 Phase 6 / F-10, `drain_pending_by_federation_relationship` line ~795 Phase 7.5 / F-3 — all three get the Q2(a) return-vector treatment; same gap pattern at all three sites; sibling-shape to topo-sort Commit 2a layered-B3 atomic close at J-101); **Q4 → (a) in-scope** (four sentinel-tree files — `phase9_harness.rs`, `phase9_three_node_anti_transitivity.rs`, `phase9_drop_and_recover.rs`, `mod.rs` — ship atomic at milestone close as activating regression lock for the persistence fix; Scenario 3 transition FAIL → PASS verifies the fix at integration level; **Commit 3b-1 collapses into the sub-amendment milestone close**; Phase 9 resumes at Commit 3b-2-equivalent with compounds C2/C3/C5/C7/C9/C10 remaining). **No re-walk fired during Q1→Q4 walk per Lock #2 discipline** — sibling-shape opportunity to topo-sort design-phase re-walk J-099/J-100 was preserved but not exercised; Q1's Result-returning ingest_event became a clean constraint on Q2 rather than a destabilising factor. **The sustainability question reframed Q1 mid-walk.** Initial recommendation (a).iii.α (log-level `tracing::error!`); user's "is this future-proof?" challenge surfaced three drift surfaces (a).iii.α does NOT catch (future caller bypasses validate_event — M6 admin write path / M8 federation-depth migration tool / exactly the shape J-104 surfaced; disk format change — Ch4 §4.12.3 SQLite drift surface; future protocol revision introducing async predecessor validation). Log-level vigilance is recurring; type-level enforcement is one-time. Revised to (a).iii.β. Second-order question "is (a).iii.β the future-proof solution?" forced honesty: more sustainable than (a).iii.α, not absolutely future-proof. Rungs above (a).iii.β named explicitly (ValidatedEvent wrapper, sealed traits + visitor pattern, formal verification). Resolution: lock (a).iii.β as the *immediate* answer + flag candidate D-NNN for future walk per D-069. **Layered-B3 recurrence count: second project-wide instance** (first instance topo-sort Commit 2a at J-101; second instance this milestone). **"Honest longer work over fast shortcuts" — eighth recurrence count inherited from J-104, NOT incremented at design close** (recurrences counted at milestone-events, not design-events). **D-074 application count: eighth instance.** Five-file atomic commit at this design-close: `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md` NEW ACTIVE v1.0 + `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT.md` Status flipped ACTIVE → COMPLETED v1.1 + JOURNAL J-105 + CLAUDE.md PLAY block flipped + ROADMAP.md v1.19 → v1.20. **Next-active work for Chat Claude + Joe: implementation runbook authoring** at `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_IMPL.md` per topo-sort precedent J-098. Runbook handoff requirements specified in design doc §9: commit sequence (candidate 4 commits; runbook-authoring locks exact shape); Joe-lock checkpoints for Clair; verification rigour (5 isolated + 3 workspace = 8 green runs minimum candidate); sentinel-tree integration shape; milestone-close file count (~12 candidate per D-074); J-NNN placeholder freeze sites; B3-shape audit answer (candidate: no gap at milestone close). Clair stays stood down through milestone close; sentinel working tree (four files at `xgen-node/src/tests/`) remains uncommitted as verification artifact for the milestone close per Q4(a) lock. **Entry point for next Chat Claude session: `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md`** (read CLAUDE.md PLAY block + J-105 entry first per Rule 0, then design doc §9 runbook handoff requirements, then author runbook).

⬛ **(historical, superseded by Federation Event Propagation milestone CLOSED at J-119) Federation Event Propagation Phase 9 Commit 3b-1 — PAUSED 2026-05-23 at J-104, COLLAPSES into persistence-amendment sub-amendment milestone close per J-105 Q4(a) lock.** Phase 9 Commit 3b-1 implementation surfaced the drain-without-persist gap during Scenario 3 implementation; J-104 opened the sub-amendment milestone per D-071 (audit → design → impl → milestone-close shape); J-105 design-close locked Q4(a) which collapses Commit 3b-1 into the sub-amendment milestone close (sentinel-tree four files ship atomic at milestone close as activating regression lock; Scenario 3 transition FAIL → PASS verifies the fix at integration level; Commit 3b-1 numbering effectively skipped). After persistence-amendment milestone closes: Phase 9 resumes at Commit 3b-2-equivalent (Scenarios 2 + 3 + compound scenarios C2/C3/C5/C7/C9/C10; ~5-7 atomic commits in their own sequence per Q4 Lock from J-091). Scenario 1 stays the activating integration-level regression lock for D-075 + D-076 v1.1 (both layers) from topological-sort milestone close per J-101. **Resume condition for Phase 9 milestone:** persistence-amendment milestone close + Phase 9 Commit 3b-2-equivalent's own atomic-commit sequence. **Entry point for Clair (post-persistence-amendment-milestone-close): `tasks/FEDERATION_PROPAGATION_PHASE_9.md`.**

⬛ **Topological-sort wire-order non-determinism phase — CLOSED 2026-05-23 at J-101 (see Past section's "Topological-sort wire-order non-determinism implementation milestone CLOSED" paragraph for the five-commit shipped state).**

---

## Near future — designed or scoped, awaiting work

Tracks that are ready to start. Each has known shape, known scope, known dependencies. Listed in roughly the order they will be picked up, though that order is not strictly locked — parallelism is possible between independent tracks.

⬛ **(historical, CLOSED 2026-06-01 at J-223 — see the Present section's "M7-completion cluster — CLOSED at J-223" entry for shipped state) M7-completion cluster.** Six code commits closed the `--aicontrol`-shaped remainder of M7: Block A (AC-D5 verbs `members`/`leave`/`create-dm-space` + node DM-init arm) → Block B (AC-D4 token + AC-D6 idempotency) → Block C (`nodes`/`ordered_nodes` widening); 965/0/1. Deferrals carried to named homes: per-driver-identity control plane → privilege-model / control-plane-identity arc · plugin-write → temperature-plugin arc · pipelined handler → own arc · `migrate-start` → migration subsystem · live config reload → M7-standalone (since shipped, J-226).

⬛ **(historical, CLOSED 2026-05-26 at J-122 — see Past section's "XGID Retrofit Pass 1 milestone CLOSED" paragraph for shipped state) XGID Retrofit Pass 1 — core data structures (spans xgen-common + xgen-core).** First retrofit pass per Shape γ + ASAP discipline (Q3 lock). Scope: retype the XGID-carrying String fields in the foundational protocol data structures, regardless of which crate they live in. This deliberately spans xgen-common (`Event` struct field retypes — `event_id`, `sender`, `room_id`, `space_id`, `prev_events`; `SpaceLocalMetadata.space_id`; the `state.rs` observability structs `NodeState`/`FederatedPeer`/`HostedSpace`/`ConnectedClient`) and xgen-core (`SpaceState` field retypes — `space_id`, `room_id` maps, `federation_nodes` peer IDs, `members` Identity keys, `ai_operator_delegations` Identity pairs, `owner_id`, `home_node`; `FederationRegistry` keys; `IdentityRegistry` keys; `PendingBuffer` map keys). Cross-crate scope is load-bearing: the Phase 2 doc-tree sweep classified Appendix C + Appendix I as Pass 1 deliverables to be shipped in one coordinated commit set, and splitting the data structures across two Passes would split Appx I documentation in a way the canonical-document rule (D-069) prohibits. Plus the canonical-form carry-over from XGID Adoption v1: move `canonical_event_bytes` from `xgen-core/src/wire/canonical.rs` to `xgen-common/src/canonical.rs` (with `xgen-core` re-exporting to preserve call sites), then add the hash-anchored `from_event` / `from_space_create` / `from_room_create` / `from_assertion` convenience constructors on the v1 flavour wrappers. Documentation: Appendix C primitive schemas and Appendix I data structures field tables retyped column-by-column from String to flavour-typed XGIDs. Test fixtures using these structures get updated in this same pass. Pass 1's runbook lives at `tasks/XGID_RETROFIT_PASS_1_IMPL.md`. Sequenced immediately after Phase 9 ships and Federation Event Propagation milestone flips to DONE; interleaves with M6 (new) work rather than waiting for M-series to settle.

⬛ **(historical, CLOSED 2026-05-27 at J-126 — see Past section's "XGID Retrofit Pass 2 milestone CLOSED" paragraph for shipped state) XGID Retrofit Pass 2 — `xgen-core`.** Second retrofit pass per ASAP discipline. Scope: retype XGID surfaces in `xgen-core` — validation core (`validate_event`, `ValidationOutcome::HeldPending` struct fields), dispatch (`NodeRuntime::dispatch_event`, `DispatchOutcome` variants), pending buffer (`PendingBuffer`'s `missing_predecessors`, `missing_identity`, `missing_federation_relationship` fields and arrival hooks), federation registry types and operational APIs (`mark_active`, `mark_lost`, `peer_records` keys), identity registry methods (`contains`, `get`, `verify_event_signature`'s parameters), `accept_message` signature. Documentation: Appendix I parts covering xgen-core-resident structures retyped per the canonical sources commit's classification. Pass 2 now PLAY at J-123 (design ✅) + J-124 (runbook ✅) + J-125 (Commit 1 doc-pass ✅); Clair active at runbook §4 Commit 2.

⬛ **(historical, CLOSED 2026-05-28 at J-138 — see Past section) XGID Retrofit Pass 3 — `xgen-node`.** Third retrofit pass. Scope: retype XGID surfaces in `xgen-node` — federation session (`stream_federation_delta`, `handle_federation_incoming`, `compute_federation_delta_for_space`), fanout (`apply_fanout`, `apply_federation_push`, `FederationPeerSenders` keys, `ClientSenders` keys), app (`process_inbound`, `handle_identity_msg`, `handle_identity_replicate_msg`, `handle_incoming_replicate`), reconnect scheduler (`spawn_reconnect_scheduler`, `attempt_reconnect`), pipe server admin verbs (post-M6). Documentation: Appendix F Node-side sections + any Ch4 §4.11 / §4.12 Node-side identifier references retyped per the canonical sources commit's classification.

⬛ **(historical, CLOSED 2026-05-29 at J-146 — see Past section's "XGID Retrofit Pass 4 milestone CLOSED" paragraph for shipped state) XGID Retrofit Pass 4 — `xgen-client`.** Fourth retrofit pass. Scope: retype XGID surfaces in `xgen-client` — ops layer (every `xgen-client-lib::ops::<verb>` function signature, `ops::*` `Result<<Verb>Result>` types), AI behaviour (`AiBehavior` trait, `EchoPlugin` reference impl, `AiPacingTracker`), session state (`SessionState`, `ClientIdentity`, `ensure_identity`, `ensure_connected`), batch dispatcher (`run_batch_file`, `batch::dispatch_line`), CLI dispatcher (`main.rs` CLI arm), AI service (`ai_service::run`, `__HEALTH__` extension), Tauri commands. Documentation: Appendix F Client-side sections + `docs/xgen_aicontrol_implementation.md` reply schemas + Ch6 §6.15 client-side spec retyped per the canonical sources commit's classification. This pass closes the AI control / batch wire-side of the Q4 invariance promise from documentation-claims-it perspective to documentation-is-it-and-code-matches perspective.

⬛ **(historical, CLOSED 2026-05-29 at J-148 — see Past section) XGID Retrofit Pass 5 — CLOSED.** Fifth and final retrofit pass; confirm-clean (Audit 1 = 1 trace fix F-1; Audit 2 clean; D-081 promoted). `cargo build --workspace` (Path A closed) and closed the xgen-client/tests fixture set (0 errors), satisfying 2 of the original 4 §2.9 deferred items. The remaining Pass 5 work is the **trace-field formatter audit** + the **Debug/Display impl audit** on xgen-client public types (projection-discipline audits, independent of compilation). Original (now-amended) scope: retype XGID surfaces in test fixture builders, integration test helpers (`build_node_with_alice_member`, federation-test setup helpers, smoke-test scaffolds), trace event field types, log line formatters, debug/Display impls, and any remaining surfaces surfaced during the earlier passes that didn't naturally fold in. After Pass 5 closes, the codebase has uniform XGID type discipline; the §5.6 principle ("field name carries the role, type carries the contract") is fully realised in code; the Q3 "mixed discipline transitionally" clause no longer applies. ROADMAP.md cross-cutting section gains the wire-format invariance promise as a named principle alongside D-065/D-070/D-071 at Pass 5 close.

⬛ **(historical, CLOSED 2026-05-29 at J-151 — M6 Phase 0 design fully done; see Past/Present) M6 Block 4 — verb-by-verb walks.** All seven verb categories (~35 verbs) walked + locked; §6 of the admin-ops design fully specified (v1.9) + Appendix K compiled (33 verbs ship + 5 deferred); D-082 terminology lock recorded J-149.

⬛ **(historical, COMPLETE — shipped J-152→J-160 + the four D-071 arcs + node-policy J-197; see Present) M6 (new) Node admin write path — implementation.** All 7 verb categories (A1–A7) shipped: 16 M6-core verbs + the four post-M6 D-071 subsystem arcs (federation-admin-control · protocol-audit-log · auth-module-registry · bootstrap-client) + node-policy. Every M6 admin verb is closed; tests 841/0/1 at M6 close (J-197).

**Durable EventStore — moved to Present (OPENED J-227, 2026-06-02); see the Present section.** The prior near-future framing here — *"engine-free, hand-rolled append-only log; amend D-080 (reversing 'backing engine never absent' + 'hand-rolled raw-file log rejected'); relocate the engine to the client; node owns crash-safety"* per an alleged "B-lock 2026-05-31" — was **drift**: it has **no JOURNAL or DECISIONS trace** and **contradicts D-080 (LOCKED)**, which keeps the backing engine *pluggable-but-never-absent* and *explicitly rejects a fully hand-rolled raw-file log*. **Struck.** As opened, the milestone implements D-080 **unchanged**: the `EventStore` **trait** + a **vanilla file backend** + a **minimal durability floor** (atomic write + honest-fail — *not* an engine); DB engines (SQLite reference / redb) stay **later modules behind the trait**.

🟡 **Production identity→home-node discovery** — routed out of MP-F1b (F1B-D5, J-333). MP-F1b shipped cross-node DM convergence *given resolved identities* (D-091); the open half is how a node resolves a **not-yet-replicated** counterparty's `home_node` — and, more broadly, discovers a stranger's verified identity — before/at DM-membership-apply. Today `dm_space_create` carries only the creator's home and `build_identity_home_nodes` reads the local registry, so a production DM to a not-yet-known counterparty does not converge until this lands. Identity-resolution sibling to M9.2's still-open *real peer discovery* question; founding-philosophy territory (no-anonymity identity verification + federation discovery), genuinely bigger than a loop-to-green verb — its own arc/milestone in the discovery space. No audit/design yet — scoped + named, unscheduled. Until it ships, MP-C-07 cross-node stays **harness-green-with-boundary** (no production witness; F1B-D4).

---

## Far future — specced, not yet scheduled

Tracks the project knows about but has not committed timing or shape to. Sketched briefly; detail accumulates if and when they approach.

⚫ **M7 — `--aicontrol` v1 — SHIPPED + CLOSED at J-205 (2026-06-01); the M7 family is DONE.** Command-pipes-only over the shared `xgen-common::aicontrol` substrate (client `ops::*` + node `admin_ops::*`, adapter per D-065, `--batch` untouched per D-066). The events pipe (J-212), completion cluster (J-223), and standalone config-reload (J-226) all subsequently closed. Canonical design at `docs/xgen_aicontrol_implementation.md`. See Present for the family close.

⚫ **M7 standalone — live config reload — SHIPPED + CLOSED at J-226 (2026-06-02).** Moved to Present/Past; the M7 family is complete. See the Present-section closure entry for the as-built.

⚫ **M8 — strong multiparty test / diagnostic. CLOSED J-269 (2026-06-05).** Diagnostic-complete (Waves 1–4): proven byte-identical convergence under exhaustive permutation (S2), migration + AF-D2 gate (S3), durability replay on vanilla store (S4), E2E content-blindness (S6), multiparty privilege (S7); suite 1167/0/2, clippy clean. Output: `tasks/M8_findings.md` (the milestone diagnostic). **Findings routed → M8.5 (finalization):** INV invitee-bootstrap [headline — invitee cannot source the invite tip, so the join is causally concurrent with the invite and derive_resolved Layer-4 drops it; candidate fix = inviter-authorized acceptance-time bootstrap, see tasks/M8_findings.md §4], F-5 anti-transitivity [Nodes never re-forward federation-received events → mesh, not chain-transitive; contradicts spec §3.2], S5 identity-rebind [build the re_registration + identity.home_changed surfaces]. **→ M9:** the AI-resident load-test harness friction list. **Fixed-in-M8:** C9 (AI resident honors --node, a51b556). **D3-fenced:** PG-05 real-crypto. No DECISIONS promotion (M8-D# arc-local). Commits 75ff383 / 4b9b7cf / 8a49ba8 / a51b556.

🟡 **Round 2 — whole-codebase audit (post-arc-pack), gates UI.** A single comprehensive audit run **after all Round-1 work completes** — the proposed §4 arcs (F migration · G jurisdictional · H E2E · I erasure) plus the full M8 pack (the J-241 convergence core + every follow-on multiparty wave). **Additive, not a replacement:** every arc still opens with its own D-071 Phase-0 audit against what it depends on (that is Round 1); Round 2 is the whole-codebase sweep on top, **semi-redundant by design** — re-checking already-audited surfaces is the accepted price of catching what per-arc audits structurally cannot: **cross-arc interactions** (e.g. erasure tombstones × thread state × trust-assertion validity — visible only once all exist) and the **client / adjacent-crate surfaces** that per-arc audits under-ground. Absorbs as in-scope subsets: the **M8 client-side impact** (SR-D3 locked node-side resolution with client apply sites untouched → does the client re-derive or trust node snapshots; stale view on node recompute, SR-D2 full rebuild; SQLite display-cache rebuildability) and the **Arc-E client/node** check (verified clean post-hoc: 0 client files; node touch = CP-2 trusted-list plumbing only). **Design principle (Joe, 2026-06-04):** prefer semi-redundant checks over gluing cross-cutting concerns into an already-locked fix program — each arc stays a clean self-consistent locked unit; cross-cutting is resolved in this dedicated pass, never by retrofitting points into a closed arc. The only non-gluing lever on foundational concerns (e.g. erasure on the append-only EventStore) is **arc ordering**, decided at selection time. Recorded 2026-06-04 (Joe). **OPENED + EXECUTED at J-258 (2026-06-05) — CONDITIONAL GO.** The sweep ran against the live tree; deliverable `tasks/ROUND_2_AUDIT.md` v1.0 (audit + findings register §5 + verdict §6). 8 findings (R2-F01..F09; F08 unallocated), all OPEN at open: **R2-F01 (S2)** client/node resolution divergence (client replays timestamp-ordered plain `apply_event`, not `derive_resolved` — the SR-D3 lock as a real seam; the one finding touching UI correctness) · **R2-F02-F04 (S3/S4)** 60xx migration code/spec drift (spec 6007/6008 vs code 6010/6011 + stale comment + numbering reuse) · **R2-F05 (S3)** the M8 number collision · **R2-F06 (S3)** operator-terminology correction (~133 code + ~194 doc) · **R2-F07 (S4)** the Arc-F/G carry-ins · **R2-F09 (S3)** multi-device seam (D3-gated). Positive: e2e/mls × migration clean; conflict domains non-overlapping + convergent; wire bands 30xx/40xx/50xx clean. **Verdict: UI may start, but R2-F01 should be a named fix-arc landing before or alongside correctness-sensitive UI views.** Suggested ordering: Round 2 → R2-F01 fix-arc → M10 → UI (F02-F05/F07 clear in one housekeeping pass). M10 confirmed OUT of the gate (unbuilt; downstream). No code (gate principle).
> **Carry-ins (record-only, to reconcile in this pass):**
> - **Appendix K federation-block reconciliation** — deferred from the Arc-G close: the §6 federation verb corpus (args/error codes) vs the as-built `admin_ops` federation surface. (The `federation show-policy` summary line is **already patched** — NOT part of the carry-in.) **Absorbed as R2-F07** in `tasks/ROUND_2_AUDIT.md` §4 (with migration sibling-drift); to expand against the close notes when a fix-arc opens.
> - **Appendix K migration sibling-drift (Arc F)** — the deferred-verb table lists `space migrate-as-source` (A4, "§3.12 migration flow heavy/unbuilt"); Arc F **shipped** migration as the `migration initiate` verb (`migration_driver.rs` + the cutover applier). Appendix K's deferred-verb + §6.A4 corpus needs reconciling to the as-built verb name/args + the new wire **6009 `migration_authority`** (ch3 §3.12.11 row already added at Arc-F close).

✅ **M8.6 — Federation stress (CLOSED J-294, 2026-06-06).** Promoted from the parallel-workstreams follow-on to a numbered milestone (J-284). Ships the **clock-injection seam** (a `Clock` trait in `xgen-common` with `now()`-style methods, threaded `Arc<dyn Clock>` through the scheduler so tests inject a controllable clock instead of waiting real wall-clock — Phase 5 never shipped it) **and** the four deferred Phase-9 compounds (C1/C4/C6/C8, which require it: the 30 s F-10 hold + the 15/30/60/120-min reconnect backoff ladder make real-clock runs impractical). Built **and** run in-milestone (one pack, Joe-locked 2026-06-06). Stub scope at `tasks/FEDERATION_STRESS_FOLLOWON.md`. Opens with its own D-071 Phase-0 audit. Side benefit: the seam is reusable by M9's harness. **Phase-0 OPENED at J-288 (2026-06-06):** seam re-grounded against live main and found surgical (six injection sites; both leaf layers already parameterise time) across three domains W/M/T; **Fork A + single-cursor MockClock Joe-locked**; audit `tasks/M8_6_FEDERATION_STRESS_AUDIT.md` v1.0 ACTIVE. Design Joe-LOCKED at J-289 (Clock/MockClock single-cursor + C4 spawn-gauge + four compound harnesses; test-quality reframes: C1 drain-consistency, C6 cross-identity isolation, C4 real leak detection, C8 strengthened; gauge test-only). Runbook authored + Joe-approved at J-290 (3 commits: seam+gauge+connect-timeout / four compounds / close; 3 checkpoints; connect_url has no timeout -> CONNECT_TIMEOUT_SECS wrap at attempt_reconnect is a C4 prerequisite). **CLOSED at J-294 (Commit 1 seam+gauge+connect-timeout `a5a8d5e` / 2a C6+C4 `0b1e5aa` / 2b C1+C8+capacity-seam `1886787` / doc-only close):** all four compounds green; checkpoint-#3 sensitivity witness recorded (C4 gauge red with the connect-timeout reverted, green restored); two below-the-lock test-scope narrowings (C1 single-Node, C8 no-hang+local-liveness) → the phase9 suite owns the uncovered two-Node halves; `Clock` trait stays a promotion-watch (promotes if M9 reuses the seam); suite 1201/0/2. Next-active: M8.7.

🟢 **M8.7 — D3 MLS operationalisation.** Promoted from the parallel-workstreams follow-on to a numbered milestone (J-284). Swaps the Phase-2 E2E scheme for real RFC 9420/openmls crypto behind the Arc-H interface (**PG-05 🔷 INTERFACE-LOCKED at J-256** — Phase-2 scheme wired + server-blindness proven) + concurrent-commit resolution. Wire shape already specced (Ch3 §3.10, Appendix I Part X.6); openmls is a ready external crate, no blocker (cascade `D-088 content-erasure → PG-05 real crypto → D3`). Independent of M9/multiparty (E2E crypto is orthogonal to convergence) — sequenced before M9 by Joe's call (2026-06-06); could float later without breaking the chain. Opens with its own D-071 Phase-0 audit. Phase-0 OPENED at J-299 (2026-06-06): audit tasks/M8_7_D3_MLS_AUDIT.md v1.1 ACTIVE; grounded against live main; headline (D-065) = M8.7 is THREE efforts not one swap: (S) primitive swap [small, behind the interface], (L) real openmls lifecycle + production client [net-new, client-milestone-adjacent], (R) concurrent-commit resolution [already self-fenced to D3 in apply_mls_commit state.rs:810-824; the center of gravity]. Joe-confirmed leans (formal lock in design): F-B = hybrid (home node serializes commits as DS + DAG state_key_for_event epoch tiebreak; replicas trust the home resolved order per the J-298 / F-5 / D-089 pairwise-trust shape; option 3 rejected); scope split = M8.7 is R+S (protocol-complete; in-process proof = two committers at one frontier converge on the same mls_epoch winner), L spun OUT to its own arc with the client/UI milestone (mirrors Arc H). Honest risk (coverage ledger): without a real openmls client the harness simulates loser-rebuild (Arc H C1 Finding 1 analogue), to be named in design. Next-active: M8.7 design phase. Design Joe-LOCKED at J-300 (2026-06-06): scope corrected to R ONLY (CC-D1) - S inseparable from L (the real MLS key schedule needs the openmls group object), so S folds into the production openmls-client arc. R = add an MlsCommit conflict domain to state_key_for_event keyed (room, target_epoch) (CC-D2; per-room would regress epochs) + the existing Layer-5c lexicographic-by-event_id tiebreak (CC-D3, no Layer-1); apply_mls_commit logic unchanged (resolved-winner path; comment corrected); home-DS serialization is L-side (CC-D4). Crypto-agnostic, no openmls dependency, provable in-process (two committers at one frontier converge on the same mls_epoch + the same winning event_id). Coverage ledger (D-065): loser rollback-and-replay deferred to L (Arc H C1 Finding 1 analogue). Deliverable tasks/M8_7_CONCURRENT_COMMIT_DESIGN.md v1.1 ACTIVE (CC-D5 added J-301). Runbook authored + Joe-approved at J-301 (2026-06-06): tasks/M8_7_CONCURRENT_COMMIT_IMPL.md v1.0 ACTIVE - single commit (RoomState.mls_commit_tip field + apply_mls_commit winner-record + MlsCommit state-key arm + comment corrections) + state_key/resolution/two-Node-convergence/sensitivity-witness tests; one checkpoint (confirm apply_mls_commit fires only for the resolved winner on the live path). CC-D5 (J-301, D-065): the counter-only proof was vacuous - honest concurrent commits share target_epoch so mls_epoch converges regardless; mls_commit_tip records the canonical winner, rides the M8 RoomState Eq oracle, RoomState unserialized so no migration. Next-active: Clair.

✅ **M9 — build strategic multiparty test harness — CLOSED J-307.** **Phase-0 OPENED J-304** (audit `tasks/M9_MULTIPARTY_HARNESS_AUDIT.md` v1.1 ACTIVE; charter = all-aspects of both binaries via --aicontrol, mass-use simulation, cooperative + adversarial; the seam = in-process phase9 → real-binary multi-process; forks M9-F-A…F-G, F-A drive-surface LOCKED to --aicontrol, F-F adversarial-injection the central new question; two-number scale model [processes x AI-resident-multiplexed participants] + a 3-round run plan recorded; **design Joe-LOCKED J-305** (forks locked M9-D1..M9-D9; design `tasks/M9_MULTIPARTY_HARNESS_DESIGN.md` v1.0 ACTIVE; raw-wire hostile injector for F-F; matrix `docs/tests/MULTIPARTY_TEST_MATRIX.md` expanded to 16 cooperative + 21 adversarial scenarios; box = 32GB/20-core); **runbook authored J-306** (`tasks/M9_MULTIPARTY_HARNESS_IMPL.md` v1.0 ACTIVE; C1 xgen-mptest orchestrator+lifecycle+batch-runner+manifest / C2 oracle+capture / C3 round-dial+micro-benchmark / C4 raw-wire injector / C5 Round-0 smokes MP-C-02+MP-A-05 / C6 close; checkpoints C1/C2/C4; Clair pickup); next-active = Clair). **CLOSED J-307** (Clair shipped C1—C5; xgen-mptest drives the real binaries black-box over .aicontrol/.events; Round-0 MP-C-02 cooperative + MP-A-05 adversarial green; suite 1262/0/8; 5 findings routed to tasks/M9_findings.md [F1 timestamp-validation gap G6, F2 fresh-peer federation-initiate surface, F3 MockClock cross-process, F4 raw-send seam], NOT patched under M9; Clock not promoted; next-active = Multiparty-tests R1→R2→R3, gated on F2/F3). The numbered operative milestone that BUILDS the harness to run the strategic multiparty scenarios (S1-S5) against a finalized binary — multi-node orchestration over the real `xgen-node`/`xgen-client` binaries, distinct from the in-process two-`NodeRuntime` tests already in the suite. The harness is the deliverable; the unnumbered **Multiparty tests** milestone then RUNS it on the finalized binary. A multiparty *redesign* is a contingent follow-on — undertaken only if those tests surface a convergence gap, NOT part of M9 scope. Inherits the intent the original (descoped) M6 multiparty pass carried. Opens with its own D-071 Phase-0 audit.

✅ **M9.1 — event timestamp-bound validation (F1 / gap G6).** Sub-branch of M9 (stays CLOSED); fixes the M9 injector finding F1 — `validate_event` (the live F-4 core) has no timestamp bound, so a skewed event is silently admitted. **Design Joe-LOCKED J-309** (design `tasks/M9_1_TIMESTAMP_VALIDATION_DESIGN.md` v1.0 ACTIVE; the audit's `validate_steps_8_13` / `xgen-node` citation corrected to `validate_event` / `xgen-core` per D-065): a protocol fix, admission-only (must not touch D-076 ordering). The crux (M9.1-F-B) is origin-gating — a local-clock-dependent bound run on every node risks divergence (the INV-EXP / F-5 / D-089 lesson), resolved at design (M9.1-D2): both-origins, convergence-safe by margin. Local-only was the INV-EXP shape, but a future ceiling is monotone under catch-up, so it is safe on every origin and closes the federated injector instead of excusing it. Locked M9.1-D1..D5 (arc-local, D-069): D1 future-skew ceiling only (causal-monotonicity considered + deferred); D2 the crux = both origins (NOT local-only); D3 in `validate_event` Step 8.5 with a threaded `now` (never feeds ordering, D-076); D4 named `MAX_FUTURE_SKEW` = 5 min; D5 two convergence repros + a sensitivity witness gate the design. Runbook authored + Joe-approved at J-310 (`tasks/M9_1_TIMESTAMP_VALIDATION_IMPL.md` v1.0 ACTIVE): single commit — Step 8.5 future-skew bound in `validate_event` (threaded `now`) + `MAX_FUTURE_SKEW_SECS`=300 + `TimestampOutOfBounds` wire 3046 (collision-checked: 3030/3040–3045 taken, 3046 free, same admission family as 3044/3045) + caller wiring (runtime.rs:1060 `self.clock.now_utc()`; one test call site) + the (a)–(d) tests + sensitivity witness; MP-A-15 confirmed far-future (2099-dated) so D1 closes it directly. Next-active = Clair. **SHIPPED + CLOSED at J-311** (Clair — single commit: code + 7 tests + `M9_findings.md` F1-resolved; Chat doc-only close): Step 8.5 future-skew bound live in `validate_event` (parse RFC3339 + `ts > now + MAX_FUTURE_SKEW_SECS`[300] → `TimestampOutOfBounds` / wire 3046; no past floor; admission-only — D-076 grep-confirmed only Step 8.5 reads `event.timestamp`); callers wired (runtime.rs `self.clock.now_utc()`; one test `Utc::now()`). +7 tests (4 unit + 3 MockClock dispatch: honest-skew same-verdict δ=2s, catch-up leniency [B via federation +2d], sensitivity witness [2099 federated event rejected + absent]). Suite 1269/0/8; build 0; clippy clean default + `--all-features`. MP-A-15 (2099-dated) closed (3046 + absent); xgen-mptest injector gap-G6 doc-comments refreshed. Honest boundary (D-065): coherently past-backdated events stay out of scope (catch-up-indistinguishable — the deferred causal-monotonicity territory, M9.1-D1). No DECISIONS change (M9.1-D# arc-local). AUDIT/DESIGN/IMPL → COMPLETED. ROADMAP v2.99→v3.00 (M9.1 🟢→✅). Next-active = M9.2 (F2/F3/F4 fenced seams; opens its own D-071 Phase-0).

✅ **M9.2 — harness-enablement seams (F2 + F3 + F4).** Sub-branch of M9; PENDING. Groups the three M9 test-tooling findings: F2 a fresh-peer federation-initiate surface (`FED_3006` is known-peers-only; gates cross-node MP-C-02/03/04/14), F3 a clock-advance control surface (MockClock inoperable across the process boundary; gates the deterministic R1 + MP-A-01), F4 a `pub` raw-send seam on `Connection` (malformed-frame injection). All three widen what an external actor can make a node do, so the central Joe-lock is **fencing** — a compile-time test/harness feature or dev-flag so they are real software for the harness but never a deployable production surface (mirrors the M10 mock-module stance). Opens its own D-071 Phase-0 after M9.1 closes. **Phase-0 OPENED J-312** (audit `tasks/M9_2_HARNESS_SEAMS_AUDIT.md` v1.0 ACTIVE): grounded against live main with two M9-finding refinements (D-065) — the `federation initiate`/`accept` aicontrol verbs DO exist (F2 gap = no peer-URL seeding surface for fresh binaries; `peer_urls` map at runtime.rs:242); `encode_frame` is `pub` (F4 gap = the private `Connection::send_bytes`, connection.rs:105); the `mock-clock` feature exists but is dev-dep-only with no cross-process advance surface (F3). The unifying crux (M9.2-A4): all three widen what an external actor can make a node do, so the central Joe-lock is the **FENCE** (M9.2-F-D) — compile-time `harness-control` cargo feature (lean: physically absent from release; composes with `mock-clock`) vs runtime dev-flag vs test-crate-only. Forks M9.2-F-A..F-E. **Design Joe-LOCKED J-313** (design `tasks/M9_2_HARNESS_SEAMS_DESIGN.md` v1.0 ACTIVE): locks M9.2-D1..D5 — D1 the fence = off-by-default `harness-control` cargo feature on xgen-node (F2/F3 seams `#[cfg(feature)]`-gated, un-buildable in release; `harness-control = ["xgen-common/mock-clock"]`); D2 fenced aicontrol `federation add-peer` (seeds `peer_urls`, no config peer-list); D3 fenced `clock advance/set` driving the binary's injected MockClock (the feature pulls mock-clock into the binary + a startup `set_clock`; default binary = RealClock); D4 F4 = test-crate-only raw `tokio-tungstenite` client in xgen-mptest — NO production surface, NO fence (the frame parser is in the normal build) → F4 drops out of the fence, only F2/F3 sit behind it; D5 one design / three commits (C1 feature+add-peer / C2 clock+plumbing / C3 raw client). Fence proof obligation: a test that the verbs are ABSENT in a default build and PRESENT only under `--features harness-control`. Honest boundary (D-065): `add-peer` is a harness seam, NOT production peer-discovery (a separate, still-open product question). Next-active = M9.2 runbook. **Runbook authored J-313** (`tasks/M9_2_HARNESS_SEAMS_IMPL.md` v1.0 ACTIVE): three work commits (C1 feature+add-peer / C2 clock+plumbing / C3 raw client) + the §6 smokes + the fence test; grounded against live main (record_peer_url runtime.rs:464, set_clock :380, app.rs:680 startup, xgen-mptest injector). The C2 trap flagged: the clock verb advances a stashed concrete `Arc<MockClock>` handle, NOT a downcast of `runtime.clock()`. Next-active = Clair (C1→C2→C3) → Chat doc-only close. **D2 re-opened + corrected at J-314** (design v1.1 §8; Clair-surfaced grounding miss, D-065): the locked F2 mechanism was wrong — `federation_initiate` reads the `FederationRegistry` relationship (FED_3006 if absent), NOT `peer_urls` (admin_ops.rs:1728); the Phase-0 audit grounded the field but never traced the consumer (my miss). D2′: `federation add-peer <node_id> <url> [space_id...]` **upserts a `FederationRelationship`** via `FederationRegistry::upsert` (peer_url + explicit shared_spaces; + `record_peer_url`), so `initiate` dials + replicates. Sharpened boundary (D-065): add-peer now fabricates a pre-established relationship — a larger fabrication that only the fence makes acceptable. F3/F4 + fence already delivered + live-passing (suite 1271/0/11); only C1′ outstanding. Classification fix: the new verbs are Write-tier, not FEDERATION_VERBS. AUDIT/IMPL → v1.1. **Next-active = Clair (C1′)** → whole-milestone close (J-315). **SHIPPED + CLOSED at J-315** (Clair across the arc + Chat doc-only close): the three seams delivered behind the `harness-control` fence; F2 bootstrap GREEN end-to-end (add-peer upserts a `FederationRelationship`, admin_ops.rs:1907, + `record_peer_url` → `initiate` dials + replicates A→B). All gates green — fence test both halves (bonus: F3 over the default binary returns UNKNOWN_COMMAND, the fence holds at the process boundary); F2/F3/F4 `#[ignore]` smokes pass live; suite 1271/0/11; build 0 + clippy clean (default + `--features harness-control`). Honest finding (D-065): the registry upsert was necessary-but-not-sufficient — the full A→B bootstrap also needs harness **ordering** (seed peer → register identity [so `push_identity_to_peers`, app.rs:2824, replicates it] → create Space → re-seed → initiate), which lives in the F2 smoke; no production code beyond the corrected add-peer. AUDIT/DESIGN/IMPL → COMPLETED. No DECISIONS change (M9.2-D# arc-local). ROADMAP v3.04→v3.05 (M9.2 🟢→✅). Next-active = Multiparty-tests (its own D-071 Phase-0; F2 unblocks MP-C-02/03/04/14, F3 unblocks R1 + MP-A-01, F4 unblocks MP-A-12).

🟢 **Multiparty tests — strategic milestone (unnumbered).** RUNS the M9 `xgen-mptest` harness on the finalized protocol core (M1–M9.2; M10/M11 layer on top without touching ordering). **Joe-locked structure (2026-06-07):** three monotonically-heavier sub-passes — **MP-R1** deterministic correctness floor (light, MockClock, fixed seeds), **MP-R2** scale + real-clock (moderate-heavy, continues where R1 ends), **MP-R3** capstone (max the box bears, ~1,562-process ceiling, chaos overlay stacked). Weight is a **continuous sweep within each round** (volume walks a sequence, oracle-checked per rung; the break-point is the deliverable) and a **monotonic climb across rounds**. Defect policy = surface-and-route, not patch (M9 D-065/D-084). **MP-R1 Phase-0 OPENED J-316** (audit `tasks/MP_R1_DETERMINISTIC_AUDIT.md` v1.0 ACTIVE): grounded against live main — six findings, the crux **G-1** = no end-to-end `run_scenario` orchestrator exists (the manifest declares `[[federation]]` links + M9.2 shipped `add-peer`/`initiate`, but nothing wires them; Round-0 hand-coded two smokes) → the general runner is MP-R1's primary build; **G-2** `dial.rs` stale (`ClockMode::Mock` declared inoperable, F3 made it operable); **G-3** no scenario clock-control step (MP-A-01 needs it); **G-4** shared `spaces_dir` state-isolation hazard; **G-5** `residents_per_process` multiplexing unbridged (R2/R3 prereq); **G-6** federation bootstrap ordering load-bearing. Forks MP-R1-F-A..F-F (F-A runner shape + F-B the sweep contract are the crux). R1 scenario set = 22 (9 cooperative + 13 adversarial; MP-C-02 + MP-A-05 already PASS at Round-0 single-node → true cross-node). **Design Joe-LOCKED J-317** (`tasks/MP_R1_DETERMINISTIC_DESIGN.md` v1.0 ACTIVE): locks MP-R1-D1..D6 (arc-local, D-069) — D1 one generic `run_scenario(scenario, dial)` runner (reuses `run_actor`; the new code is the orchestrator the Round-0 smokes hand-wired; D1a encodes the G-6 federation bootstrap once); D2 the sweep contract (`Sweep`→`SweepResult` curve+break-point; the GREEN/LOGIC-FAULT/CEILING stop condition carries the D-065 logic-vs-hardware distinction; R1 = single-rung through the same type so R2/R3 inherit); D3 manifest `[[clock]]` ordered step list (un-stales `dial.rs`, unblocks MP-A-01); D4 Space-scoped oracle as the contract (already Space-scoped, no binary change); D5 22 scenarios in 4 mechanism-grouped tranches; D6 surface-and-route defects (new `tasks/MP_findings.md`, FAIL does not block close). Change surface all in `xgen-mptest` + scenario dirs; no production crate touched. Next-active = MP-R1 runbook (`tasks/MP_R1_DETERMINISTIC_IMPL.md`) → Clair. **Runbook authored J-318** (`tasks/MP_R1_DETERMINISTIC_IMPL.md` v1.0 ACTIVE): 8-commit plan — C1 `run_scenario` runner + G-6 `establish_federation` helper + un-stale `dial.rs` / C2 `sweep.rs` (`Sweep`/`SweepResult` + the GREEN/LOGIC-FAULT/CEILING classifier) / C3 manifest `[[clock]]` + `rejection_category_verdict` / C4–C7 the four scenario tranches (T1 MP-C-02 true A↔B/-03/-07 · T2 MP-C-01/06/08/09/10/13 · T3 MP-A-02/03/04/14/16/17/20 · T4 MP-A-05/09/10/12/15 + MP-A-01) / C8 doc-only close; grounded on the `c5_mp_c_02` hand-wired template; all in `xgen-mptest` + scenario dirs. Next-active = Clair (C1). **C1–C4 SHIPPED** (Clair; latest d682345 = C4, J-319). C4 (Tranche 1 cross-node cooperative core): **MP-C-02** true A↔B ✅ · **MP-C-03** concurrent send ✅ · **MP-C-07** DM cross-node ❌→routed **MP-F1** (first finding: DM `membership.join` doesn't propagate B→A + DM messages not transcript-observable; routed to a DM-cross-node fix-arc, MP-R1-D6). **MP-R1-D7 locked J-319** (design v1.1 §7.5): federated-convergence oracle scope — exclude `state.federation_add` (registry-vs-DAG A:0/B:1 asymmetry, benign) from the per-Space event-set comparison; retain membership convergence as the positive federation-formed assertion (which caught MP-F1). Fast suite 69→72. Next-active = Clair C5 (Tranche 2 membership-lifecycle MP-C-01/06/08/09/10/13) → C6 (logic-adversarial) → C7 (wire/injector+clock) → C8 (Chat doc-only close). **C5 SHIPPED J-320 (7fdb973):** MP-C-01 ✅ + MP-C-10 ✅ + 4 BLOCKED (MP-C-06/08/09/13 — capability gaps, not defects). **C6/C7 pre-flight audit: ~10–11/13 authorable, no enabling-arc stall** (C6 BLOCKED MP-A-03 auth-tier + MP-A-14 ban-precondition; C7 6/6). **MP-R1-D8 locked J-320** (design v1.2 §9; IMPL v1.1 §5): close criterion amended — every scenario carries a recorded outcome ∈ {PASS · FAIL→routed · BLOCKED}, all-22-PASS unreachable; BLOCKED is a coverage finding (not a defect); a green MP-R1 certifies the happy-path cooperative + adversarial-logic/wire core, NOT admin/lifecycle/threads/re-home. **Test-debt ledger** (6 BLOCKED across 5 root causes — member-ban, room-override, thread, auth-tier, re-home) inherited by the UI/authoring-verb pass; the six resume as MP-R1-resumed or fold into MP-R2 (decided post-C7). MP-A-20 reframed to the role-gate path (Joe-confirmed). Next-active = Clair C6 → C7 → C8. **C6 SHIPPED J-321 (9f71327):** 4 PASS (MP-A-02/04/17/20, Option-A paired oracle) + 2 BLOCKED (MP-A-03/14) + MP-A-16 reclassified C6→C7 (batch form mis-premised — open-join model, runtime.rs:1244/J-275; the real never-issued-invite attack is injector-only). **MP-R1-D9 locked J-321** (design v1.3 §10): the rejection oracle is **path-split** — batch logic-adversarial scenarios prove *effect-absence* (offending event absent everywhere + protected state unchanged), because the rejected client ops are fire-and-forget (`send_event`+goodbye, no recv; node `Error` never reaches the reply — same mechanism as MP-F1 facet-2); category-level (`PermissionDenied`/3045/4000) is wire-only and lives in C7 (`WireActor` recvs the `Error` frame). Amends D4, supersedes the MP-A-20 category ask. Breadcrumb (design §10, not a finding): open-join + Tier-1-only auth = no join gate on the happy path today — flagged for the PG-13/auth-tier + M10 work. Next-active = Clair C7 (wire/injector + clock; opens by wiring the `kind="injector"` dispatch). **C7 SHIPPED J-322 (8dd7435):** 7/7 PASS (MP-A-05/09/10/12/15/16 + MP-A-01(i)); all 4 tranches now authored. Surfaced MP-F2 (reject path delivers generic 4000 not the specific 30xx code; observability) + MP-F3 (node re-fans-out a re-submitted duplicate; DAG dedup holds, fan-out doesn't; minor); MP-A-01(ii) PENDING (federation-replay-preserved, J-298, needs late-federation harness machinery). **STRATEGIC TURN J-322 (Joe-directed): MP-R1 does NOT close — enters a FIX-AND-RERUN phase.** The test's purpose is to fix production code; R1 stays OPEN, fixes ship, R1 re-runs, looping toward all-green. MP-R1-D8's "BLOCKED terminal / all-22-PASS unreachable" superseded in spirit (close criterion IN FLUX). Fix backlog = 3 findings (MP-F1 DM-cross-node, MP-F2 wire-codes, MP-F3 fan-out-dup — all production-surface, none security defects) + 6 BLOCKED capability gaps. **Two tracks open:** Track A = BLOCKED-sizing pass (thin-verb vs real-feature-arc — decides the BLOCKED strategy: Joe's loop-to-green if thin, Chat's hybrid if real feature work); Track B = first finding fix-arc MP-F2 (own D-071 Phase-0 — the FIRST production-crate change of the multiparty effort; order F2→F3→F1). Next-active = Clair (both tracks). **MP-R1-D10 Joe-LOCKED J-323** (design v1.4 §11): BLOCKED-sizing = 5 thin / 1 real-arc → **loop-to-green** (the 5 BLOCKED are thin client-verb gaps over already-shipped+convergence-proven core builders, not UI feature work — so the hybrid's build-twice risk evaporates; Joe's instinct vindicated). Strategy: fix 3 findings (MP-F2→F3→F1) + build 4 thin `xgen-client` verbs (ban [+A-14] / room_update / thread×3 / create-space --auth-tier, unblocking MP-C-08/09/13 + MP-A-03/14) + **defer MP-C-06 re-home** (sole real arc — unbuilt `home_changed` broadcast J-278/J-279 + harness keypair-relocation; the one surviving test-debt item → M10 era) + re-run R1. **Close criterion resolved: R1 all-green-except-MP-C-06.** Root cause (recorded): the client-verb surface silently lagged the core (each core growth stopped at builder+applier+convergence, never adding the verb) — forward rule: a core addition checks its CLI equivalent at ship time. **MP-F2 Phase-0 confirmed** (two-boundary code-drop, low-severity observability gap; D-076 discharged; forks greenlit — widen `DispatchOutcome::Rejected`, propagate-the-code-this-arc + named MP-F2-followon for the unmapped variants & the 3030-vs-3010 spec drift). Each fix/verb is now a PRODUCTION arc (xgen-common/core/node/client), unlike the xgen-mptest-only C1–C7. Next-active = Clair: author the MP-F2 design. **MP-F2 ✅ SHIPPED + CLOSED J-324** — first production-crate change of the multiparty effort: `DispatchOutcome::Rejected(String)` widened to `Rejected(RejectInfo { code, name, reason })`, 15 gates supply their computed code, `reject_signal` plumbs it to the `Error` frame (deleting the hardcoded 4000); reason strings frozen (D-077) so ~37 reason-assertion tests stayed green. Payoff proven end-to-end (MP-A-15 wire `error_code == 3046`, was 4000); boundary MP-A-05 still 4000 (unmapped variant → MP-F2-followon). Build 0 + clippy clean (default + harness-control); suites green; D-076 discharged. **MP-F2-followon named** (wire codes for the 7 unmapped signature/membership/permission variants + the 3030-vs-3010 spec drift Clair caught). Loop-to-green next: **MP-F3** (duplicate re-fan-out dedup-at-dispatch) → MP-F1 (DM cross-node; Clair runs a facet-2 grounding pass first) → 4 thin verbs → R1 rerun. Next-active = Clair (MP-F3 fix-arc). **MP-F3 design Joe-LOCKED J-325** (`tasks/MP_F3_DEDUP_REFANOUT_DESIGN.md` v1.0, F3-D1..D5): dedup check in `dispatch_event` post-`ensure_store` on `store.contains(event_id)` → a new unit variant `DispatchOutcome::Duplicate` (correct inverse of MP-F2's payload-change call — adding a variant leaves wildcards intact, only 7 exhaustive arms get `Duplicate => {}`); `process_inbound` maps it to a truthful idempotent `EventAccepted` + `FanoutRequest::none()` (suppresses local fan-out + federation push together). Proof obligation pinned: MP-A-09 flips from tolerant `n>=1` to "fanned out exactly once" (clean PASS). D-076 discharged (drop-vs-idempotent-reapply → identical log → identical convergence; the §6 side-effect-skip audit confirms the early-return loses nothing). Two out-of-scope catches recorded (drain non-fanout; re-submit-while-pending buffer-not-store, already benign). Next-active = Clair: author the MP-F3 runbook → implement → close. **MP-F3 ✅ SHIPPED + CLOSED J-326** — second production fix: new `DispatchOutcome::Duplicate` variant + a `store.contains(event_id)` dedup gate in `dispatch_event` (placed AFTER `validate_event` — an in-arc D-065 correction of the locked "before" placement, caught by `phase9_compound_c5`: a signature-forgery reusing a stored event_id must reject at step 12, not classify as Duplicate); `process_inbound` maps `Duplicate` → idempotent `EventAccepted` + `FanoutRequest::none()` (suppresses local fan-out + federation push together). MP-A-09 flipped from tolerant `n>=1` to "fanned out exactly once" (clean PASS, proven end-to-end). Build 0 + clippy clean (both); xgen-core 669/0, xgen-node 286/0, xgen-mptest 72/0; D-076 discharged. Loop-to-green next: **MP-F1** (DM cross-node convergence + DM-message observability — the biggest finding; opens with a facet-2 grounding pass: is DM `message.text` emitted to the stream at all?) → 4 thin verbs → R1 rerun. Next-active = Clair (MP-F1 fix-arc). **MP-F1 split + facet-1 (iii) Joe-LOCKED J-327; MP-F1a arc shovel-ready + C1 GREENLIT.** Facet grounding (Clair, empirical) split MP-F1 into two independent problems. **MP-F1a (facet-2, delivery):** `create_dm_space` fire-and-forgets 3 events then RST-discards events 2–3; fix = the client awaits the D-070 `EventAccepted` it already receives. Arc carried Phase-0 → design (F1A-D1..D6) → runbook this session; **C1 greenlit** (additive `Connection::send_event_confirmed` helper). Scope = (c) all 9 sending ops await their ack (durable client-send discipline; the 4 thin verbs inherit it); timeout policy op-class-split (chain → abort+`Err`; single-event → warn+proceed; discriminator = whether await-ack already disambiguated the timeout); HeldPending-positive-visibility routed-not-folded. **MP-F1b (facet-1, cross-node DM convergence):** a protocol-design tension, not a code patch — a DM Space's `federation_nodes` stays empty (`apply_federation_add` rejects under `dm_constraints_active`; confirmed empirically — every DM push logs `federation_nodes=0`, both nodes), so no DM event federates. Resolution Joe-LOCKED = **(iii) membership-driven DM federation** (over rescope-the-test / loosen-the-gate): leave `DmFederationNotAllowed` intact (no third-party node gets DM content), add a separate rule = a DM's federation set is exactly its current members' home nodes, populated at membership-apply time. Sub-locks A–E (A populate at membership-apply; **B home-node resolvability = the feasibility gate, F1b Phase-0 proves it FIRST, can kill (iii)**; C leave shrinks the set, historical local copy = standing federated-RTBF, not solved in-arc; D `dm_space_create` rides the G-6 handshake, the set carries the rest; E new invariant "a DM's federation set is exactly its members' home nodes" = DECISIONS candidate, not yet promoted). F1b queued behind F1a; Phase-0 opens gate-B-first. No production code yet (C1 not started). Next-active = Clair (MP-F1a C1). **MP-F1a ✅ SHIPPED + CLOSED J-328 (facet-2 delivery).** C2 retrofit: all 9 xgen-client sending ops route through `Connection::send_event_confirmed`; `create_dm_space` confirms-or-aborts its 3-event chain (client-state write after the confirmed block); C1 tightening folded (explicit `EventAccepted` arm + `unreachable!`); CP-1 contract change landed (node-reject → command error → batch exit 1; backward-coherence sweep clean, nothing relied on a swallowed reject). MP-C-07-LOCAL delivery witness GREEN (Option 1 — narrow-to-delivery + route the gap); federated MP-C-07 stays KNOWN-FAIL (facet-1/MP-F1b). xgen-core 674/0, xgen-client 103/0, xgen-mptest 72/0, clippy clean. C3 doc-only close: MP-F1 split (facet-2 RESOLVED, facet-1 → MP-F1b OPEN), CP-3 Appendix F §F.8 send-confirm-semantics note (D-028 lockstep, v1.5→v1.6), AUDIT/DESIGN/IMPL → COMPLETED, MP_findings v1.3→v1.4. **New finding MP-F4 (node-side, routed):** the DM invitee's room-join is dropped in node-side membership resolution (room-agnostic `state_key` + `get_invite_bootstrap` re-issue → concurrent siblings on one membership key → `derive_resolved` drops one → member-of-Space-not-room → message rejected step-11 NotARoomMember); DM-specific (MP-C-01 PASSES); own D-071 Phase-0; overlaps MP-F1b's DM-membership surface (weigh together). **Remaining loop-to-green:** MP-F1b (facet-1, iii) + MP-F4 (node-side) + 4 thin verbs → R1 rerun; next fix-arc order = Joe's call (the MP-F4 / F1b DM-membership overlap suggests weighing them together). Next-active = Clair (next fix-arc once order is picked). **MP-F4 order Joe-locked J-329 (ahead of MP-F1b) + Phase-0 grounded/pushed (71e8b72, GAP CONFIRMED moderate) + F4-A=A1 Joe-LOCKED** — room-scope the membership `state_key` (single no-drift location; all 5 production readers consume `StateKey` opaquely; zero serde/persist/wire → no migration; robust to the proximate-cause ambiguity A2 depends on; closes the latent room-agnostic conflation, D-077-forward). Mechanism: room-agnostic membership key (state_key.rs:48) + `derive_resolved` `frontier_of` (derive.rs:179) drops one of two concurrent join-siblings → member-of-Space-not-room. Honest finding (D-065): the routed bootstrap-re-issue premise isn't reproducible from the static node path, but A1 closes the finding regardless. Design greenlit — the **cross-scope-conflict proof** (space-level ban/kick/leave must dominate a room-level join across all orderings) is its spine, proven-not-assumed (A1 makes D-076 non-trivial, unlike delivery-only F2/F3). Witness flip: MP-C-07-LOCAL delivery-only → message convergence (RED on revert). Next-active = Clair (author `MP_F4_DM_MEMBERSHIP_DESIGN.md`). **MP-F4 design Joe-LOCKED J-330 (F4-D1..D5; design 861b4ab)** — A1 key transform (`membership:{space}:{id}` space-level → `membership:{space}:room:{room}:{id}` room-level; one edit site + helper). Spine (F4-D2, proven not assumed): a space-level removal (ban/kick/leave) dominates a room-level join across all orderings via guard+cascade complementarity (`apply_join` room-guard no-ops removal-before-join; removal cascade undoes join-before-removal); §5.6 pins concurrent-ban-vs-room-join to "banned, in no room" → no ban-evasion. **A1-scope sharpening Joe-locked (Clair-surfaced):** room-scope is scope-aware across join/leave/kick (all three have room-level applier branches; room-leave live via `ops::leave --room`) — scoping only join would manufacture a new room-kick-vs-room-join non-convergence; invite/ban/eject/unban stay space-level. **D-076 re-proven set = 3 pairs** (cross-scope removal×room-join + room-kick×room-join + room-leave×room-join), each RED on revert; space-level keys byte-identical. F4-D3 all 5 readers consume `StateKey` opaquely, never serialized → no migration/wire/reason change. Witness: MP-C-07-LOCAL delivery-only → message convergence (RED on revert). D-077: MP-C-01 robust. Next-active = Clair (author `MP_F4_DM_MEMBERSHIP_IMPL.md`; the 3-pair witness set is a hard deliverable). **MP-F4 ✅ SHIPPED + CLOSED J-331 (Option C: frontier anchor + A1 keying; bc057f8)** — A1-as-locked was falsified mid-arc by an empirical probe (the MP-F3/J-326 pattern): `apply_join`'s space-membership guard + single-pass lexicographic fold order mean A1 separates the keys but does not *order* the joins → room-join sorts first → guard fails → dropped (~50% by hash). Root cause re-grounded = a **multi-leaf-frontier client bug**: `get_dag_tips` returned the single topo-last event, not the DAG frontier, so the room-join anchored to alice's pre-join message (a sibling leaf) instead of the space-join → concurrent → dropped (the audit's §2.3 "off the critical path" call was wrong, on it). Joe re-locked **Option C** (anchor + A1): `get_dag_tips` now returns the true frontier (all leaves, capped at MAX_PREV_EVENTS) → room-join causally descends from its space-join → guard passes (the finding-closer; a general client-anchoring fix touching all callers); A1 retained as latent-conflation defense (D-077). `MP-C-07-LOCAL` flipped delivery-only → 2-party message convergence GREEN; MP-C-01 contrast GREEN (concurrency-robust); all-callers/D-076/D-077 sweep clean; xgen-core 683/0, node 286/0, client 103/0+integration; clippy clean (default + harness-control). Federated MP-C-07 stays KNOWN-FAIL → MP-F1b. `MP_findings` v1.5 (MP-F4 RESOLVED); matrix v1.6; arc docs AUDIT/DESIGN/IMPL → COMPLETED. Next-active = Clair: **MP-F1b** (facet-1, cross-node DM via (iii)) — opens its own D-071 Phase-0 with the empirical two-node gate-B proof as the verdict, after Chat re-supplies (iii)/A–E/gate-B fresh (J-327 guardrail). v3.17→v3.18→v3.19→v3.20→v3.21→v3.22. **MP-F1b Phase-0 + design v1.0 Joe-LOCKED J-332.** Phase-0 (Clair, `bfa0535`): **gate-B FAILS production** — no path resolves a not-yet-replicated stranger's `home_node` at DM-membership-apply (`build_identity_home_nodes` reads the local registry, runtime.rs:1895; `dm_space_create` carries the creator's home, not the invitee's); the kill-gate fired exactly as J-327 anticipated. **Gate-B fork Joe-LOCKED = Option 2** ((iii) harness-scoped + route the discovery gap) over (iii)+augment-now / re-open (ii)(i): (iii) is correct given *resolved* identities, gate-B is the gap to *discoverable* — so sub-lock A (the membership-apply population) is the half that stays. **F1B-D1..D7 + 3 honesty conditions Joe-LOCKED** (design v1.0, `bde00dc`): D1 a single idempotent NodeRuntime post-membership-apply helper (`repopulate_dm_federation_nodes`, DM-only; `DmFederationNotAllowed` intact); D2 the full members'-home set (invariant E), self-included; D3 the gate-B boundary lives **as omission** (an unresolvable member is omitted — honest-by-construction); D4 MP-C-07 cross-node recorded **with a boundary, not a bare ✅** (no production witness claimed — not expressible on G-6 rails, sibling to MP-A-01(ii)); D5 the production identity→home-node **discovery gap = its own named arc**, routed now + placed on this horizon at F1b close (F1b routes, does not build it); D6 MP-R1-D10 amended → all-green-except-MP-C-06 **with MP-C-07 harness-green-with-boundary**; D7 leave shrinks the set / `dm_space_create` rides G-6 / invariant E = DECISIONS candidate at close. The send path already consumes `federation_nodes` (populate → federates both directions, no new send code); composes with the shipped MP-F4 frontier-anchor + A1 (reads Space membership, orthogonal). `MP_findings` v1.5→v1.6 (MP-F1b section); `MP_R1_DETERMINISTIC_DESIGN` v1.4→v1.5 (§11 amendment). **MP-F1b ✅ SHIPPED + CLOSED J-333 (Design Z, `9b4ab8b`).** The live two-node witness falsified design §3.2 ("no new send code"): a DM federates *late*, so the helper alone left bob's join F-3-held on A. Design Z (Joe-locked) populates `federation_nodes` from **parties = members ∪ pending invitees** → the counterparty is in the set from create, so the bootstrap join passes F-3 with **no skip** (F-3 still blocks 3rd parties — no hole, proven by a hole-closed unit) + the creator's pre-join message pushes via the existing path; a `repopulate_dm_federation_after_identity` hook drains the F-3-held join on a late-arriving record (D-076 discharged by inheritance — reuses `drain_pending_by_federation_relationship` verbatim). The F1B-D8 spine came back **falsified** (`apply_join` open-joins, no DM 2-party gate → an unconditional skip would be a hole) — recorded as the finding that drove Z, not a skip shipped. MP-C-07 cross-node → **harness-green-with-boundary** (a3+b4 converge A↔B, stable ×3; RED-on-revert genuine; no production witness — F1B-D4). xgen-core 689/0, xgen-node 286/0, clippy clean. **Invariant E amended (members → parties) + promoted → D-091.** Production identity→home-node discovery (F1B-D5) routed — on the near-future horizon below; F1b routes, does not build. `MP_findings` v1.6→v1.7; matrix v1.6→v1.7; arc docs AUDIT/DESIGN/IMPL → COMPLETED. v3.22→v3.23. Next-active = Clair (the 4 thin verbs: `ban` [+MP-A-14] / `room_update` / `thread`×3 / `create-space --auth-tier`) → R1 rerun. **Thin-verbs arc order Joe-LOCKED J-334** (Chat doc-only planning lock, NO code, NO DECISIONS change): **(1) `create-space --auth-tier` → (2) `ban` → (3) `room_update` → (4) `thread`×3** — smallest-and-most-informative first (auth-tier surfaces the PG-13 gate-teeth question early; ban second = 2 rows + MP-A-14 precondition; supersedes the J-333 ban-first listing). Each a small `xgen-client` production arc over an existing core builder — full D-071 Phase-0 → design → Joe-lock → runbook → Clair impl → close, with **Appendix F a required close deliverable** + a **RED-on-revert matrix flip** per arc (J-323 forward rules); `thread` = one arc (3 verbs). The four unblock **5 scenarios** (MP-A-03 / MP-C-09 / MP-A-14 / MP-C-08 / MP-C-13); **MP-C-06 stays deferred** (D10). Close = all-green-except-MP-C-06 with MP-C-07 harness-green-with-boundary. v3.23→v3.24. Next-active = Clair (arc 1 Phase-0, `create-space --auth-tier`; grounds the gate-teeth / creation-cap / oracle-shape pivots). **Auth-tier verb ✅ SHIPPED + MP-F5 surfaced/routed J-335.** Arc 1 shipped its verb (`bf22aaf` — `--auth-tier <u32>` on `CreateSpaceArgs` default 1 + the ops.rs:400 literal swap; wire-neutral; pivots (a) gate-teeth-HAS-teeth + (b) creation-uncapped→M10-breadcrumb both held) but **surfaced a tranche-wide finding** instead of a green witness: building MP-A-03's batch witness, Clair re-ran the existing C6 tranche on HEAD — `mp_a_02`/`mp_a_04` (✅ PASS at J-321) **FAIL on HEAD** ("reply has no `event_id`"). Root cause: MP-F2 (reject_signal, J-324) + MP-F1a (await-confirm, J-328) made a locally-submitted reject return an `Error` frame → the offending op errors rather than returning ok+`event_id` → the C6 "fire-and-forget" oracle + MP-R1-D9's "category not batch-observable" (both J-321) are **stale**. Favorable reframe: the reject is now **batch-observable** (node sends code + `event_id`; client just flattens it to `GENERIC_4000` free text) → **MP-F5 = finish the MP-F2 surfacing into the client reply, not redesign the oracle.** Joe locked (by recomms): ship the verb (Option 2), route **MP-F5** (Phase-0 `f2866a0`), **sequence MP-F5 before ban** (ban's MP-C-09/MP-A-14 witnesses inherit the identical reject-oracle dependency), D-9 amendment at MP-F5 design-lock. **Revised order: auth-tier (shipped) → MP-F5 → ban → room_update → thread×3 → R1 rerun.** MP-A-03 recorded honestly (verb shipped; batch witness pending MP-F5; node teeth via `pg13_*`); MP-A-02/04 STALE-on-HEAD (empirically RED) + MP-A-17/20 likely-stale (inferred) annotated in the matrix. `MP_findings` v1.7→v1.8 (+MP-F5); matrix v1.7→v1.8; `HANDOFF_AUTH_TIER_VERB.md`→COMPLETED. v3.24→v3.25. Next-active = Clair (MP-F5 design — ErrorBody shape + D-9 amendment the crux). **MP-F5 ✅ SHIPPED J-336 (design `323fced` + impl `bee2ede`).** Finish-the-MP-F2-surfacing, not an oracle redesign: `EventConfirm::Rejected` carries `event_id`; `ErrorBody` gains additive `reject_code`/`event_id` (envelope + harness mirror; `GENERIC_4000`/`Protocol` retained — AC-D3d wall intact; drift-lock test); the C6 oracle rewritten to **assert-the-reject**. Re-grounded the stale C6 tranche: MP-A-02→3045 / MP-A-04→4000 / MP-A-17→4000 / MP-A-20→4000 (the three 4000s unmapped → MP-F2-followon) + **MP-A-03→3030 NEW ✅** (the auth-tier verb's deferred batch witness, RED-on-revert genuine). MP-R1-D9 amended (§10, scoped to locally-submitted single-event rejects). Category-remap sub-fork deferred (3030 is stronger + sidesteps the 3030-vs-3010 drift). C6 5/5 GREEN; fast suites green; clippy clean; Appendix F §F.8 note. (Doc-bridge for the ship was deferred + folded into the ban commit — JOURNAL J-336.) **Ban ✅ SHIPPED J-337 (thin-verb arc 2, `7eb7684` Phase-0 + `d2a7a80` impl).** Client `ban` over the existing `build_membership_event(MembershipBan)` + `apply_ban` cascade + `can_ban` gate (Admin+ only; real teeth). **MP-C-09 ✅** — banned bob's post-ban send rejected step-11 → assert-the-reject (`reject_code=4000` + `event_id`, the MP-F5 inheritance proven for ban), post absent, bob excluded; RED-on-revert genuine. **MP-A-14 ✅ green-half + M10 breadcrumb** (NOT a clean evasion-blocked pass): same-identity re-join refused at resolution (enforceable green, RED-on-revert genuine); fresh identity open-joins (pseudonymity, breadcrumb). Two honesty catches (D-065): A-14's banned re-join is **accepted-but-inert** (apply-error swallowed at runtime.rs:691, no dispatch banned pre-check) — membership-effect-absence, NOT assert-the-reject (audit pivot-(c) superseded); and the verb-add surface is **4 dispatch arms** (CLI / run-path / batch / **aicontrol**), the aicontrol arm missed in the audit + caught empirically — forward rule for arcs 3–4. Both witnesses GREEN; fast suite 0-failed; clippy clean; Appendix F ban entry. No DECISIONS change (ban arc-local; the 4-arm rule a DECISIONS candidate). `MP_findings` v1.8→v1.9 (MP-F5 RESOLVED); matrix v1.8→v1.9 (MP-A-02/03/04/17/20 + MP-C-09/MP-A-14 → ✅); `MP_R1_DETERMINISTIC_DESIGN` v1.5→v1.6 (D-9 amendment §10). v3.25→v3.26 (MP-F5)→v3.27 (ban). Next-active = Clair (**room_update**, thin-verb arc 3 — MP-C-08 / PG-12 per-room Deny override, cooperative + self-contained over `build_room_update_event`; verb-add = all 4 dispatch arms), then thread×3 (arc 4) → R1 rerun. Inherits the intent the descoped M6 multiparty pass carried; a multiparty *redesign* is a contingent follow-on, undertaken only if a run surfaces a convergence gap.

🟡 **M10: Auth Module Reference Set — Tier-1 Auth Module — reference implementation (the project's first proper module instance).** The autonomous `system`-mode module (own keypair, signs as itself) shipped as a **separate binary (`xgen-auth-module`)** running alongside a Node, per ch2 + ch4 + D-036 — **form locked (Joe, 2026-06-04):** the autonomous module is the proper form, not an in-node stub. Proves the module architecture is genuinely pluggable and tiered auth swappable end-to-end, replacing today's dormant Local-Node-bypass baseline. **Foundation already shipped (the interface it plugs into):** `AuthModuleRegistry` + the 5 `auth-module` CRUD/probe verbs + `AuthModuleXgid` (D-083); `TrustAssertion` SignedPrimitive + 7-check `validate_assertion` wired into registration, gated on `trusted_auth_modules` (PG-03 / Arc E). **Sequencing (Joe-lock 2026-06-04):** **NOT** a prerequisite for M9; **IS a prerequisite before UI start** (a UI gate, alongside the Round-2 audit). **Inbound scope — threads that converge here when it opens (collected so a future session sees them together):** (1) the **AI-D8 module-policy descriptor** on the Trust Assertion — erasability/retention as its first, forward-extensible member, plus the §8 open-doors principle (unknown future module requirements preserved verbatim), from the Arc-I design; (2) **identity-orphan GDPR erasure** — the PG-05-independent half of PG-02 (D-088), buildable *as part of* this rebuild; (3) **reconcile "baseline is built-in, not a module"** (`PRIVILEGE_MODEL_DESIGN`) against ch2/ch4's "Tier-1 reference module" — likely the module is a demonstrator layered over the hardcoded crypto-identity baseline, but unresolved on the record. No audit/design/runbook yet — specced, unscheduled. **Scope = two artifacts: a real T1 reference module + ONE parameterized mock covering T2–T4** — tier carried as a config/claim parameter, *not* four separate modules (the genuine per-tier difference is verification rigor + legal accountability, which a mock by definition skips; what varies on the wire is only claim shape + TTL + tier integer). For T2–T4 a core-team mock is the **structurally-correct** artifact, not a compromise: the core team is not an institution and cannot legally attest higher-tier identity, so the mock is the **reference template an institution forks** and replaces with its accountable version. It also unblocks testing every dormant tier path — the full 7-check, per-tier claims/TTLs, the Arc-E Thread participation gate, and the D-088 erasure tier-gate ("T4 refuses erasure" needs a T4 identity to refuse). **Mock safety (honest-by-design):** self-labels `mock`/test in manifest + assertions, honoured only via the explicit `trusted_auth_modules` gate — real *software*, never a deployable *trust* anchor. **(Milestone M10 — distinct from the Phase-9 federation bug-catalogue row also labelled M10.)**

🟡 **M11 — `self` account.** Local-only synthetic Identity, accessible from any client (D-021). Post-multiparty, identity-layer; rides the M10 / auth-module work. Promoted from a deferred open-area to a numbered milestone (J-287, Joe's call). Design not yet started.

### UI

🟡 **Clean-table UI milestone.** The live UI build — constructed fresh on a clean table after all pre-UI work completes (Round-2 GO). Replaces the deprecated visual-merge approach; no audit/design yet.

🟡 **Multi-device arc (R2-F09 — pulled from the Round-2 gate 2026-06-05).** The AH-D4 epoch-advance seam is identity-membership-shaped with no own `state_key`; device-level add/remove is a real seam a future multi-device arc breaks. D3-gated, not a UI blocker — motivated by the UI prototype when it exercises device-level add/remove.

⬛ **UI Phase 2 visual merge — DEPRECATED (J-284).** The element-modelling / visual-merge approach (chat mockups' visual treatment onto Miss Design's semantic structure, postponed at the J-033 element-modelling step) is **abandoned** — the UI will be rebuilt a different way. **Superseded by:** the clean-table UI milestone above. The Tauri scaffolding, lifecycle state machines, named-pipe IPC, `--instance` segregation, `--batch` flag, and xgen-core crate split (Sessions 14–18) stand as shipped infrastructure; only the visual-merge *approach* is deprecated.

### Parallel workstreams

⏸️ **Slovak translation pass — POSTPONED.** Suspended during active English development; a single pass after the English documentation reaches a stable end-state, or sooner if the need suddenly arises (lowest priority).

### Open areas (deferred, not scheduled)

⏸️ **Registry file encryption — POSTPONED.** Identity and federation registries at rest; deferred. Candidate **storage/security module** riding the D-080/085 module framework (encryption-at-rest as a module concern) rather than a standalone arc.

⏸️ **DPI resistance — POSTPONED.** Traffic masking / DPI resistance (D-023); Phase-3 area — investigation-only at this stage, resume when Phase 3 opens.

---

## Cross-cutting

A few items don't fit cleanly in past / present / near future / far future because they are continuous rather than milestone-shaped. Recorded here for visibility.

🟢 **Design discipline (D-069).** Every milestone Phase 0 must be Joe-locked before the implementing phase starts. Delegated technical drafts must self-flag open items. Canonical-document rule: each major surface gets one authoritative document, others point at it. M6's Phase 0 was the first milestone to follow this discipline end-to-end; Federation Event Propagation's design phase (Pass 2 just closed, Pass 3 next) is the second instance of the same pattern.

✅ **Audit-precedes-dependency discipline (D-071).** Every future milestone's Phase 0 includes a subsystem audit of whatever the milestone depends on. The Propagation Reliability Audit (J-081) established the pattern; D-071 names the discipline. Pairs with D-069: audit phase → design phase → implementation phase, each producing a canonical artefact. Sibling to D-065 and D-070. Promoted to DECISIONS.md 2026-05-18.

🟢 **Honest behaviour over polite behaviour (D-065).** Protocol-design principle. When the system can choose between a behaviour that misrepresents its state and one that honestly reflects it, XGen picks honest. Surfaces in multiple places: AI Client drop-on-throttle pacing, Node event rejection clarity, mute semantics, M6 accept-signal design, Federation Pass 2's `sync_complete` lock (F-6 chose explicit signal over silent quiet-time heuristic citing D-065), Federation Pass 2's pagination lock (F-7 chose explicit cursor over "felt incomplete" heuristic citing D-065).

✅ **Two events of equal importance, opposite direction (D-070).** Protocol-design principle. When the protocol exposes a signal from one party to another about the outcome of an action, both directions of outcome (acceptance and rejection) must be exposed with equal first-class status, AND both directions must carry the envelope-level correlation identifier so the originator can correlate the signal to the action it sent. Sibling to D-065. Promoted to DECISIONS.md 2026-05-18 with corrected post-audit framing (both halves — existence AND correlation — are load-bearing).

✅ **Bidirectional sustainability discipline (D-077).** Audit/design-phase principle. At every silent-discard, conditional-mutation, or fallible-operation-with-discard pattern, the sustainability question MUST be asked in both directions: forward-drift (what future callers could bypass this guard) AND backward-coherence (what current callers depend on this as a feature). Both answered simultaneously before locking any fix in isolation. Sits at meta-layer above the no-drift-surface discipline family (D-067 + D-070 + D-075 + D-076 v1.1 at protocol layers; D-077 + Rule 0 at meta layer). Origin: J-107 persistence-amendment re-walk — J-105 design phase asked the forward-only sustainability question and locked Q1 at (a).iii.β (`Result<(), GraphError>` ingest_event); Clair's Commit 2 implementation surfaced cross-milestone Phase 7 B3 amendment dependency (B3 federation-bootstrap path implicitly relied on the silent-discard as a feature); bidirectional discipline would have caught it at design time. Resolution Option Y locked: revert (a).iii.β → (a).iii.α, name the discipline as new principle, document broader audit as future-walk material under candidate D-NNN expanded scope. Surface-driven application per D-071 — NOT applied preemptively to all silent-discard sites in the codebase; applied at each fix site as it surfaces. **First worked instance** of D-077 value: Clair's Commit 3 prospective sweep closed three within-Commit-3 audit gaps atomically (abort-fold + identity-registry-persist + space-event-store-persist; federation-registry-persist audited and confirmed safe). Promoted to DECISIONS.md 2026-05-24 at J-107.

✅ **XGID typing is wire-format and persistence-format invariant (D-081).** Data-model / wire-format principle. Retyping a `String` identifier slot to a typed XGID flavour is a pure in-memory change: every flavour is `#[serde(transparent)]` over `Xgid(String)`, so it serialises byte-identically on every boundary (Node↔Node, Node↔Client, AI-control / batch JSONL, on-disk). No XGID Retrofit pass (1–5) changed a single serialized byte. `Display` is the canonical string form; `Debug` reveals the wrapper for diagnostics only. Sibling to D-076 in the wire-format discipline family; realises the XGID Adoption v1 Q4 invariance promise. Promoted to DECISIONS.md 2026-05-29 at J-148 (arc close). Numbered D-081 — D-080 was already taken by the Node-storage EventStore decision.

✅ **"operator" reserved for the AI-operator role; the Node administrator is a distinct infra principal (D-082).** Protocol-vocabulary / naming principle. "operator" = the AI-operator role only (moderator-parallel: operator : AI-identities :: moderator : room + members; fall-upward per D-064) — never an owner/admin alias. The Node admin principal is the **administrator** (prose) / **admin** (code, CLI, error-codes, config — matching `admin_ops`/`AdminContext`/`AdminError`); v1 has no gradation (OS-user-equals-administrator, session-scoped). owner/super-admin reserved as a future sub-tier (M7). A Node administrator auto-administers Spaces it originates/homes but NOT federated-in replicas (hosts-but-doesn't-own); the signing identity for admin-originated Space events is deferred to the A4 sub-design. Sibling to D-073 (naming discipline). Recorded at J-149; scope-refined at J-150 after a corpus audit found "operator" carries four senses — only the runtime admin principal (Sense D) is renamed; the AI-operator role, the wire field names (`operator_display_name` etc.), and the infrastructure "Node operator"/data-controller sense are all kept (an inline facet-specifier disambiguates Sense C where needed).

🟢 **Honest longer work over fast shortcuts.** Project-management principle. When project work surfaces a real gap, the default response is to close the gap properly, even if that delays downstream work. Locked during the audit's federation finding discussion; informs all milestone-sequencing calls. Pairs with the audit-precedes-dependency discipline above. Federation Pass 2 invoked it three times — to fold in F-6 (sync_complete) rather than defer, to fold in F-7 (pagination) rather than defer, and to fold in F-10 (HeldPending generalisation) rather than reject. Within Federation Event Propagation milestone scope as of J-105 the principle has eight recurrences (Phase 7.5; bidirectional; topo-sort design close J-097; runbook landing J-098; re-walk Step 2 J-099; re-walk Step 3 J-100; topo-sort implementation J-101; persistence-amendment sub-amendment milestone surfacing at J-104). Design-close events do NOT increment the count; the count belongs to the milestone-event the recurrence opened.

🟡 **Candidate D-NNN — "Ingest path invariant encoding"** (flagged at J-105, NOT promoted to DECISIONS.md; **scope expanded at J-107 re-walk** to cover five `ingest_event` silents + three drain helpers + M6 reject paths + B3 apply_event dependency). The persistence-amendment design phase locked Q1 at (a).iii.β (type-level Result-returning `ingest_event`); Y-lock revert at J-107 reverted to (a).iii.α (binary-void signature + log-level vigilance) under cross-milestone Phase 7 B3 amendment dependency surfaced at Clair's Commit 2. The candidate D-NNN names the rung-above-(a).iii.α project-level open question without pre-committing the project to a specific shape; rungs above (a).iii.β named explicitly (ValidatedEvent wrapper, sealed traits + visitor pattern, formal verification). Sibling-shape to D-076's v1 → v1.1 progression at different scope: v1 design-close didn't pre-promote the second invariant; v1.1 emerged after design walked it properly. Resolution at J-107 re-walk close: ship (a).iii.α immediately + name D-077 discipline + flag candidate D-NNN with **expanded scope** preserving optionality on the right rung per D-069 audit-vs-design boundary discipline. Future walk triggered when (a) dependent work surfaces a concrete drift instance, OR (b) Joe locks the candidate as worth pursuing on philosophical/strategic grounds independent of a surfacing gap. See JOURNAL J-105 + J-107 entries + `tasks/PHASE_7_5_PERSISTENCE_AMENDMENT_DESIGN.md` v1.2 §8 for the full reasoning trail.

---

## How to read this document

A reader landing here for the first time should be able to answer three questions in under a minute:

1. **What has the project shipped?** Past section, scanned by reading the bold milestone names.
2. **What is being worked on right now?** Present section.
3. **What's next?** Near future, top-of-list.

A reader returning after a gap can scan for state-changes by looking for symbols that have moved (a 🟢 PLAY that became ✅ DONE, a 🟡 PENDING that became 🟢 PLAY, a new 🟡 entry that wasn't there before). The roadmap is meant to be scannable, not exhaustively read.

For any item the reader wants more detail on, the canonical source is named (JOURNAL entry, DECISIONS reference, design doc, task file). ROADMAP.md is the map; the territory lives elsewhere.

---

*End of roadmap.*  
