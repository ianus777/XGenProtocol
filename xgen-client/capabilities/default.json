{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the XGen Client main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "process:default"
  ]
}
