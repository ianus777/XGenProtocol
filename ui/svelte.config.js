// Type-gate config for the ui/ library (ui/core + ui/common).
//
// Deliberately empty: no app in this project declares a preprocessor, so a
// faithful config declares none either. svelte-check requires the file to
// exist; it does not require it to do anything.
export default {};
